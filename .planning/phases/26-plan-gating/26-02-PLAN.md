---
phase: 26-plan-gating
plan: 02
type: execute
wave: 1
depends_on: ["26-01"]
files_modified:
  - src/components/inmobiliaria/UpgradePrompt.tsx
  - src/components/inmobiliaria/AgencyPricingModal.tsx
autonomous: true

must_haves:
  truths:
    - "UpgradePrompt component renders with feature name, required plan, and upgrade button"
    - "Clicking upgrade button in UpgradePrompt opens the AgencyPricingModal"
    - "UpgradePrompt shows different messaging for flex-only features vs tier-gated features"
    - "UpgradePrompt can be used as a wrapper (children shown if has access, prompt shown if not)"
  artifacts:
    - path: "src/components/inmobiliaria/UpgradePrompt.tsx"
      provides: "Reusable upgrade prompt component"
      exports: ["UpgradePrompt", "FeatureGate"]
  key_links:
    - from: "UpgradePrompt.tsx"
      to: "useAgencyPlan.ts"
      via: "calls hasFeature() and getUpgradeReason()"
      pattern: "useAgencyPlan|hasFeature"
    - from: "UpgradePrompt.tsx"
      to: "AgencyPricingModal.tsx"
      via: "opens modal on upgrade click"
      pattern: "AgencyPricingModal"
---

<objective>
Build the UpgradePrompt component and FeatureGate wrapper that shows an upgrade message when users try to access gated features.

Purpose: Every gated page/section uses FeatureGate to either show content or an upgrade prompt. This is the universal gating UI.
Output: UpgradePrompt component + FeatureGate wrapper component.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/lib/hooks/useAgencyPlan.ts
@src/lib/constants/feature-gates.ts
@src/components/inmobiliaria/AgencyPricingModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UpgradePrompt component</name>
  <files>src/components/inmobiliaria/UpgradePrompt.tsx</files>
  <action>
    Create a clean, on-brand upgrade prompt component that matches the inmobiliaria dashboard style (light, neutral colors, no flashy gradients).

    Props: { featureName: FeatureName; title?: string; description?: string }

    Visual design:
    - Centered content within a rounded-xl card with neutral-50 background and subtle border (1px solid rgba(0,0,0,0.06))
    - Icon at top (Lock from phosphor, weight duotone, neutral-400)
    - Title: "Disponible en [plan name]" or custom title — text-[15px] font-semibold text-neutral-900
    - Description: what the feature does — text-[13px] text-neutral-500
    - For flex-only features: additional line "Incluido en planes Flex" with small Sparkle icon
    - Button: "Ver planes" — neutral style (bg-neutral-900 text-white rounded-xl py-2.5 px-5 text-[13px])
    - Button opens AgencyPricingModal (manage open state internally with useState)

    Also export a FeatureGate wrapper component:
    Props: { feature: FeatureName; children: ReactNode; fallback?: ReactNode }
    - Calls useAgencyPlan().hasFeature(feature)
    - If has access: render children
    - If no access: render fallback or default UpgradePrompt

    Use useI18n() for locale-aware text. Import from @/lib/i18n.
    Use Phosphor icons (Lock, Sparkle, ArrowRight). Import from @phosphor-icons/react.
    Use cn() from @/lib/utils.
  </action>
  <verify>npx next build --no-lint succeeds</verify>
  <done>UpgradePrompt and FeatureGate exported, renders upgrade message with pricing modal integration</done>
</task>

<task type="auto">
  <name>Task 2: Add FeatureGate export to barrel file</name>
  <files>src/components/inmobiliaria/UpgradePrompt.tsx</files>
  <action>
    Verify that both UpgradePrompt and FeatureGate are properly exported as named exports.

    Test the integration by verifying the import chain works:
    - UpgradePrompt imports useAgencyPlan (which imports FEATURE_GATES)
    - UpgradePrompt imports AgencyPricingModal
    - FeatureGate wraps children conditionally

    Run build to confirm no circular dependencies or missing imports.
  </action>
  <verify>npx next build --no-lint succeeds with no new warnings</verify>
  <done>Full import chain works: FeatureGate → useAgencyPlan → FEATURE_GATES, UpgradePrompt → AgencyPricingModal</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx next build --no-lint` compiles successfully
- [ ] UpgradePrompt renders with feature name and upgrade button
- [ ] FeatureGate shows children when feature is accessible
- [ ] FeatureGate shows UpgradePrompt when feature is locked
- [ ] Upgrade button opens AgencyPricingModal
</verification>

<success_criteria>

- UpgradePrompt is a clean, reusable component matching dashboard style
- FeatureGate wrapper enables easy gating of any page/section
- Pricing modal integration works (click → modal opens)
- Both components use i18n for locale support
</success_criteria>

<output>
After completion, create `.planning/phases/26-plan-gating/26-02-SUMMARY.md`
</output>
