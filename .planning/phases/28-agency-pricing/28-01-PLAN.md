---
phase: 28-agency-pricing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/inmobiliaria/AgencyPricingModal.tsx
  - src/components/ui/plan/PlanHeader.tsx
autonomous: true

must_haves:
  truths:
    - "Clicking 'Mejorar Plan' in subscription popover opens AgencyPricingModal for agency users"
    - "Modal defaults to 'Pago por Adjudicación' tab with 'Recomendado' badge"
    - "Flex plans show AI agents features with Sparkle icon in indigo"
    - "Calculator slider shows monthly cost estimate for per-lease model"
    - "Subscription plans do NOT show AI agent features"
    - "Modal uses createPortal to avoid stacking context issues"
  artifacts:
    - path: "src/components/inmobiliaria/AgencyPricingModal.tsx"
      provides: "Agency pricing modal with Flex vs Subscription"
      exports: ["AgencyPricingModal"]
    - path: "src/components/ui/plan/PlanHeader.tsx"
      provides: "Header with agency-aware upgrade button"
  key_links:
    - from: "PlanHeader.tsx"
      to: "AgencyPricingModal.tsx"
      via: "agencyPricingOpen state opens modal"
      pattern: "AgencyPricingModal|agencyPricingOpen"
---

<objective>
Polish the agency pricing modal and verify the full upgrade flow works end-to-end.

Purpose: Ensure the pricing modal correctly differentiates Flex (per-lease) from Subscription, with AI agents as the key Flex differentiator.
Output: Polished pricing modal, verified upgrade flow.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/inmobiliaria/AgencyPricingModal.tsx
@src/components/ui/plan/PlanHeader.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish pricing modal and verify upgrade flow</name>
  <files>src/components/inmobiliaria/AgencyPricingModal.tsx, src/components/ui/plan/PlanHeader.tsx</files>
  <action>
    Read both files and verify:

    1. AgencyPricingModal.tsx:
       - Uses createPortal to render in document.body (avoids z-index issues)
       - Body scroll locked when open (position: fixed technique)
       - Close on Escape key
       - Default tab is 'per-lease' (not 'subscription')
       - Per-lease tab has 'Recomendado' badge
       - Flex plans features include AI-related items with Sparkle icon in indigo
       - Subscription plans do NOT include AI agent features
       - Calculator slider works (0-50 range)
       - Calculator shows savings comparison vs subscription
       - Value props section ("Sin riesgo", "Escala contigo", "Alineados")
       - No API REST in features (removed earlier)
       - Features are realistic: CRM, Publicación, Contratos, Pipeline, Reportes, Recordatorios, Dispersiones

    2. PlanHeader.tsx:
       - For agency users: "Mejorar Plan" button calls setAgencyPricingOpen(true) instead of Link
       - For agency users: "Mi Plan" in dropdown opens modal instead of navigating
       - AgencyPricingModal rendered at bottom of component with createPortal
       - Fragment wrapper (<>) around header + modal
       - "Gestionar suscripción" link goes to /panel/inmobiliaria/configuracion?tab=facturacion

    Fix any issues found. If the modal doesn't use createPortal yet, add it. If scroll lock is missing, add it.

    Run build to confirm compilation.
  </action>
  <verify>npx next build --no-lint | grep "Compiled" (should show "Compiled successfully")</verify>
  <done>Pricing modal polished, upgrade flow works end-to-end, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Verify features are accurate and no API REST references remain</name>
  <files>src/components/inmobiliaria/AgencyPricingModal.tsx</files>
  <action>
    Read the file and verify the feature lists are accurate:

    SUBSCRIPTION_PLANS features should NOT include:
    - "API REST básica" or "API REST completa" (we don't have this)
    - Any AI agent references (AI is Flex-only differentiator)

    SUBSCRIPTION_PLANS features SHOULD include:
    - Starter: CRM de candidatos, Publicación en portales, Contratos digitales, Pipeline de arrendamiento, Soporte por email
    - Growth: Todo en Starter, Reportes avanzados, Recordatorios automáticos, Dispersiones a propietarios, Soporte prioritario
    - Business: Todo en Growth, Multi-sucursal, Reportes ejecutivos, Webhooks, Gerente de cuenta

    PER_LEASE_PLANS features SHOULD include AI items:
    - Starter Flex: ...same as sub + "2 Agentes AI incluidos", "Scoring AI + Matching AI"
    - Growth Flex: ...same as sub + "Agentes AI avanzados"
    - Business Flex: ...same as sub + "Todos los Agentes AI"

    Fix any discrepancies. Run build.
  </action>
  <verify>npx next build --no-lint | grep "Compiled"</verify>
  <done>Feature lists are accurate, no API REST references, AI is Flex-only</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx next build --no-lint` compiles successfully
- [ ] Modal opens from "Mejorar Plan" button
- [ ] Default tab is per-lease with "Recomendado" badge
- [ ] Flex plans show AI features, subscription plans don't
- [ ] Calculator works
- [ ] No API REST in features
</verification>

<success_criteria>

- Pricing modal is polished and production-ready
- Upgrade flow works end-to-end for agency users
- Feature lists are accurate and differentiate Flex from Subscription
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/28-agency-pricing/28-01-SUMMARY.md`
</output>
