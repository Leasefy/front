---
phase: v7-02-documentos-arriendo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/inquilino/documentos/page.tsx
autonomous: true
requirements: [DOCU-01]

must_haves:
  truths:
    - "The documentos page shows lease documents (contrato firmado + recibos), not only application docs"
    - "The contrato firmado opens/downloads via the existing signed-URL path (useSignedPdfUrl / DownloadContractPdfButton), never a raw persistent URL"
    - "Recibos de pago are listed from tenant-payment-requests (the single source of truth), labeled 'comprobante interno' — never 'factura', and with NO fabricated receipt PDF"
    - "Dates render with es-CO (Colombian) locale — the es-CL bug is fixed"
  artifacts:
    - path: "src/app/inquilino/documentos/page.tsx"
      provides: "Lease-documents section (contrato firmado via signed path + recibos list) alongside application docs"
      contains: "DownloadContractPdfButton"
  key_links:
    - from: "src/app/inquilino/documentos/page.tsx"
      to: "useContracts / DownloadContractPdfButton"
      via: "signed contrato firmado download (GET /contracts/:id/pdf)"
      pattern: "DownloadContractPdfButton"
    - from: "src/app/inquilino/documentos/page.tsx"
      to: "useMyPaymentRequests"
      via: "recibos list (tenant-payment-requests, single source of truth)"
      pattern: "useMyPaymentRequests"
---

<objective>
Extend the tenant documentos hub so it surfaces the documents of the **arriendo**
(contrato firmado + recibos de pago) alongside the application documents it already
shows. The contrato firmado reuses the EXISTING signed-URL chain
(`useSignedPdfUrl` → `GET /contracts/:id/pdf` via `DownloadContractPdfButton`) — real
and IDOR-safe today. Recibos are listed from `tenant-payment-requests` (the single
source of truth) labeled **"comprobante interno"** with no fabricated PDF (the recibo
PDF is a v7-04/PAGO-03 deliverable). Also fix the `es-CL` → `es-CO` date-locale bug.

Purpose: Satisfy DOCU-01 (ve/abre docs del arriendo, no solo de la aplicación).
Output: `src/app/inquilino/documentos/page.tsx` renders a lease-docs section; contrato
opens via the signed path; recibos list honestly; dates are Colombian.

Scope note: This plan intentionally uses the *existing* contract signed hook and does
NOT depend on the new generic `documentsApi.getSignedUrl` contract built in v7-02-02.
The application-doc IDOR hardening, consent, and ARCO delete are v7-02-03.
</objective>

<execution_context>
@/Users/nicolasgarcia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolasgarcia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/v7-02-documentos-arriendo/v7-02-RESEARCH.md
@.planning/phases/v7-02-documentos-arriendo/v7-02-PATTERNS.md

# READ FIRST: docs/DESIGN.md §11 (empty/loading/EmptyState), §16 (dates es-CO / COP via
# useI18n().formatCurrency), §4 Buttons. CLAUDE.md mandates DESIGN.md before any UI edit.

# File to modify:
@src/app/inquilino/documentos/page.tsx

# THE analogs to copy (do not invent):
@src/components/contract/DownloadContractPdfButton.tsx
# Lease-detail download rows (icon + label + Download, one row per available doc):
@src/app/inquilino/arriendo/[leaseId]/page.tsx

<interfaces>
<!-- Existing signed-contract chain — REUSE, do not build a parallel one -->
// src/lib/hooks/useContracts.ts
useContracts(): { contracts: Contract[]; getActive(): Contract[]; isLoading; error; refetch }
// A "contrato firmado" = status in { 'signed', 'active', 'expired', 'cancelled' } (both parties signed).
// src/lib/types/contract.ts
Contract: { id: string; status: ContractStatus; propertyAddress: string; propertyCity: string; ... }
// src/components/contract/DownloadContractPdfButton.tsx — blob download that hides the Supabase URL
<DownloadContractPdfButton contractId={string} contractStatus={ContractStatus} variant="secondary" />

<!-- Recibos source — the SINGLE source of truth for tenant payment history -->
// src/lib/hooks/useLeases.ts
useMyPaymentRequests(): { requests: BackendTenantPaymentRequest[]; isLoading; error; refetch }
// src/lib/api/tenant-payment-requests.types.ts
BackendTenantPaymentRequest: { id; leaseId; amount /* COP int */; periodMonth; periodYear;
  paymentDate; dueDate; hasReceipt: boolean /* NO receiptUrl exists */; status;
  bankName: string|null; lease: { propertyAddress; propertyCity } }
// NOTE: there is NO receiptUrl / receipt-PDF field. Do NOT render or link a fake PDF.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the date locale + add lease-doc type config</name>
  <files>src/app/inquilino/documentos/page.tsx</files>
  <action>
    Fix the `formatDate` locale bug at line ~126: change `'es-CL'` to `'es-CO'` (DESIGN.md §16 —
    Colombian locale). The `en-US` branch is unchanged.

    Add lease-document display entries to `DOC_TYPE_CONFIG` (the map at lines 19-34) so the new
    section has labels + Phosphor icons consistent with the existing cards: add keys `CONTRATO`
    (label "Contrato firmado" / "Signed lease", icon `FileText`) and `RECIBO` (label "Comprobante
    interno" / "Internal receipt", icon `Money`). Do NOT add PAZ_Y_SALVO / CERT_RETENCION here —
    those are v7-02-04. Import any missing Phosphor icons from `@phosphor-icons/react` (FileText and
    Money are already imported).
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && test $(grep -c "es-CL" src/app/inquilino/documentos/page.tsx) -eq 0 && grep -q "es-CO" src/app/inquilino/documentos/page.tsx && echo GATE_OK</automated>
  </verify>
  <done>No `es-CL` remains; `es-CO` present; DOC_TYPE_CONFIG has CONTRATO + RECIBO entries.</done>
</task>

<task type="auto">
  <name>Task 2: Render the lease-documents section (contrato firmado + recibos)</name>
  <files>src/app/inquilino/documentos/page.tsx</files>
  <action>
    Add a "Documentos del arriendo" section ABOVE the existing application-docs grid (which stays
    unchanged). Wire two real sources:

    (a) Contrato firmado — import `useContracts` from `@/lib/hooks/useContracts` and
    `DownloadContractPdfButton` from `@/components/contract/DownloadContractPdfButton`. Compute the
    signed contracts client-side: `contracts.filter(c => ['signed','active','expired','cancelled'].includes(c.status))`.
    For each, render a lease-doc row in the DESIGN.md card/list idiom copied from
    `arriendo/[leaseId]/page.tsx:564-576` (icon + label + address subline), but instead of the raw
    `<a href={lease.contractUrl}>`, place a `<DownloadContractPdfButton contractId={c.id}
    contractStatus={c.status} variant="secondary" label="Descargar contrato" />`. This is the
    concrete DOCU-01 "opens via signed URL, not raw" behavior — the button already fetches the
    signed URL and downloads as a blob (Supabase URL never shown). Do NOT bind `lease.contractUrl`
    or any raw persistent URL into an `href`/`iframe` here.

    (b) Recibos — import `useMyPaymentRequests` from `@/lib/hooks/useLeases`. Render the payment
    requests as a list of "comprobantes internos": per row show `periodMonth`/`periodYear`,
    `formatCurrency(amount)` (COP via `useI18n`), `bankName` if present, `formatDate(paymentDate)`,
    and the status. The section heading/label MUST read "Comprobantes internos" / "Recibos
    (comprobante interno)" — NEVER "factura". Because no `receiptUrl` exists (guardrail: recibo PDF
    is v7-04), render NO download button and NO iframe for recibos — a read-only list only. Add a
    small inline note that the descargable comprobante PDF llega con Pagos (v7-04) — honest, not a
    dead button.

    Empty/loading discipline (DESIGN.md §11): if there are no signed contracts AND no recibos, show
    the section's own honest empty copy (e.g. "Aún no tienes documentos del arriendo") — reuse the
    `EmptyState` primitive already imported, or a lightweight inline empty block matching the
    existing page style. Gate the section behind the page's existing `isLoading` spinner; while
    `useContracts`/`useMyPaymentRequests` load, do not flash a fake-empty. Follow the `403/404 → []`
    tolerance already used on the page (each hook degrades to empty, not crash).

    Additive rule: do NOT remove or restructure the application-docs grid, filters, stats, or viewer
    modal — only ADD the lease section. The application-doc raw-URL hardening is v7-02-03 (leave those
    code paths untouched here).
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && grep -q "DownloadContractPdfButton" src/app/inquilino/documentos/page.tsx && grep -q "useMyPaymentRequests" src/app/inquilino/documentos/page.tsx && grep -qiE "comprobante interno" src/app/inquilino/documentos/page.tsx && test $(grep -ci "factura" src/app/inquilino/documentos/page.tsx) -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>Lease section renders; contrato downloads via DownloadContractPdfButton (signed); recibos list from useMyPaymentRequests labeled "comprobante interno"; no "factura" string; no fake receipt PDF/href; application-docs grid untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant browser → NestJS `/contracts/:id/pdf` | Signed-URL mint; server checks JWT ownership |
| tenant browser → NestJS `/tenant-payments/requests/mine` | Tenant reads own payment rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v7-02-01 | Information disclosure (IDOR) | contrato firmado download | mitigate | Use `DownloadContractPdfButton` (existing signed `GET /contracts/:id/pdf`, ownership-checked, 1h expiry) — never bind a raw persistent URL |
| T-v7-02-02 | Compliance (DIAN, Ley 1480) | recibos surface | mitigate | Label "comprobante interno", never "factura"; no client-rendered fiscal PDF (deferred to v7-04) |
| T-v7-02-03 | Tampering (supply chain) | npm installs | accept | Zero new packages this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `pnpm -C /Users/nicolasgarcia/rent/mvp-portal-inquilino build` succeeds (CI skips `next build`).
- `pnpm test` — 0 NEW failures (≈7 pre-existing unrelated failures documented in
  `.planning/phases/v7-01-fundacion-limpieza/deferred-items.md` — do not "fix" them here).
- Grep gates: `DownloadContractPdfButton` + `useMyPaymentRequests` present; zero `es-CL`; zero
  `factura`; "comprobante interno" present.
- Manual smoke as `tenant`: documentos shows a "Documentos del arriendo" section; the contrato
  downloads (no Supabase URL in the address bar); recibos list appears labeled as comprobantes
  internos with no download button; dates read Colombian.
</verification>

<success_criteria>
- DOCU-01: The tenant sees and opens the documents of their arriendo — contrato firmado (via the
  signed-URL path) and recibos de pago (list, "comprobante interno") — in addition to application
  docs, with the es-CL locale bug fixed and no fabricated receipt PDF.
</success_criteria>

<output>
After completion, create `.planning/phases/v7-02-documentos-arriendo/v7-02-01-SUMMARY.md`.
</output>
