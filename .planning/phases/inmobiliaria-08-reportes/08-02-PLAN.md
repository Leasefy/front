---
phase: inmobiliaria-08-reportes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/inmobiliaria/CarteraEdadesTable.tsx
  - src/components/inmobiliaria/OcupacionChart.tsx
  - src/components/inmobiliaria/ComisionesTable.tsx
  - src/components/inmobiliaria/VencimientosTable.tsx
  - src/components/inmobiliaria/FlujoCajaChart.tsx
  - src/components/inmobiliaria/index.ts
autonomous: true

must_haves:
  truths:
    - "User can see cartera aging table with 30/60/90+ buckets"
    - "User can see ocupacion chart with zone breakdown"
    - "User can see comisiones table with agent breakdown"
    - "User can see vencimientos table with expiry status"
    - "User can see flujo de caja chart with monthly bars"
  artifacts:
    - path: "src/components/inmobiliaria/CarteraEdadesTable.tsx"
      provides: "Aging table for receivables"
      min_lines: 200
    - path: "src/components/inmobiliaria/OcupacionChart.tsx"
      provides: "Occupancy chart by zone"
      min_lines: 150
    - path: "src/components/inmobiliaria/ComisionesTable.tsx"
      provides: "Agent commissions breakdown"
      min_lines: 180
    - path: "src/components/inmobiliaria/VencimientosTable.tsx"
      provides: "Contract expirations table"
      min_lines: 180
    - path: "src/components/inmobiliaria/FlujoCajaChart.tsx"
      provides: "Cash flow chart"
      min_lines: 150
  key_links:
    - from: "CarteraEdadesTable"
      to: "CarteraReport"
      via: "import from types/inmobiliaria"
      pattern: "CarteraReport"
    - from: "OcupacionChart"
      to: "OcupacionReport"
      via: "import from types/inmobiliaria"
      pattern: "OcupacionReport"
---

<objective>
Create specific report visualization components for each report type.

Purpose: Provide detailed visualizations for each report that can be embedded in ReporteViewer.
Output: 5 report-specific components for cartera, ocupacion, comisiones, vencimientos, and flujo de caja.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@docs/ROADMAP-INMOBILIARIA.md

# Source files with report types
@src/lib/types/inmobiliaria.ts
@src/lib/data/mock-inmobiliaria.ts

# Similar patterns to follow
@src/components/inmobiliaria/CobroTable.tsx
@src/components/inmobiliaria/AgenteLeaderboard.tsx
@src/components/inmobiliaria/AgenteWorkloadChart.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CarteraEdadesTable Component</name>
  <files>src/components/inmobiliaria/CarteraEdadesTable.tsx</files>
  <action>
Create CarteraEdadesTable for aging receivables analysis:

1. Summary Cards Row:
   - Total pendiente (large, red gradient)
   - 0-30 días (emerald)
   - 31-60 días (amber)
   - 61-90 días (orange)
   - 90+ días (red, critical)

2. Table Structure:
   - Property name/address
   - Tenant name with phone (click to call)
   - Propietario name
   - Agent assigned
   - Month owed
   - Amount pending
   - Days late (badge colored by bucket)
   - Action menu: Contact tenant, View cobro, Notify agent

3. Bucket Filter Tabs:
   - "Todos" (with count)
   - "0-30 días" (emerald badge)
   - "31-60 días" (amber badge)
   - "61-90 días" (orange badge)
   - "90+ días" (red badge)

4. Sorting:
   - Sort by days late (default, descending)
   - Sort by amount
   - Sort by property

5. Export Button:
   - "Exportar Excel" button in header
   - Uses ExportButton component

Props interface:
```typescript
interface CarteraEdadesTableProps {
  data: CarteraReport;
  onExport?: () => void;
  onContactTenant?: (item: CarteraItem) => void;
}
```

Use bucket colors: emerald (0-30), amber (31-60), orange (61-90), red (90+).
Use Table component from shadcn.
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>CarteraEdadesTable shows aging breakdown with filters</done>
</task>

<task type="auto">
  <name>Task 2: Create OcupacionChart Component</name>
  <files>src/components/inmobiliaria/OcupacionChart.tsx</files>
  <action>
Create OcupacionChart for occupancy visualization:

1. Overall Summary:
   - Large percentage circle (donut chart style with CSS)
   - Total occupied / total properties
   - Trend indicator vs previous month

2. Zone Breakdown:
   - Horizontal stacked bar per zone
   - Colors: emerald (occupied), blue (in process), gray (available)
   - Zone name and percentage label

3. Zone Cards (alternative view):
   - Toggle between chart and cards
   - Card per zone with mini progress bar
   - Occupied count / total

4. Legend:
   - Ocupado (emerald)
   - En proceso (blue)
   - Disponible (gray)

Props interface:
```typescript
interface OcupacionChartProps {
  data: OcupacionReport;
  variant?: 'chart' | 'cards';
}
```

Use CSS-only charts (no external charting library).
Follow AgenteWorkloadChart pattern for horizontal bars.
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>OcupacionChart shows zone-based occupancy</done>
</task>

<task type="auto">
  <name>Task 3: Create ComisionesTable Component</name>
  <files>src/components/inmobiliaria/ComisionesTable.tsx</files>
  <action>
Create ComisionesTable for agent commissions:

1. Summary Row:
   - Total comisiones (large, green)
   - Promedio por agente
   - Mejor agente (with medal icon)
   - Cierres totales

2. Table Structure:
   - Agent name with avatar
   - Closed deals this period
   - Total commission (formatted currency)
   - Average per deal
   - Top property (if available)
   - Trend vs previous period (up/down arrow)

3. Ranking Column:
   - Position number
   - Medal for top 3 (🥇🥈🥉)
   - Movement indicator

4. Bar Visualization:
   - Inline progress bar showing commission relative to top performer
   - Percentage of leader

5. Period Comparison:
   - Optional toggle to show vs previous period
   - Delta values in green/red

Props interface:
```typescript
interface ComisionesTableProps {
  data: ComisionesAgenteReport;
  showComparison?: boolean;
  onAgentClick?: (agenteId: string) => void;
}
```

Follow AgenteLeaderboard pattern for ranking display.
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>ComisionesTable shows agent commission breakdown</done>
</task>

<task type="auto">
  <name>Task 4: Create VencimientosTable Component</name>
  <files>src/components/inmobiliaria/VencimientosTable.tsx</files>
  <action>
Create VencimientosTable for contract expirations:

1. Urgency Summary:
   - Próximos 30 días (red, critical)
   - 31-60 días (amber, warning)
   - 61-90 días (blue, info)
   - Total vencimientos

2. Table Structure:
   - Property title/address
   - Tenant name
   - Propietario name
   - Contract end date
   - Days until expiry (badge)
   - Renewal status (badge)
   - Actions: Contact, Start renewal, View contract

3. Status Badges:
   - pending: "Pendiente" (gray)
   - negotiating: "En negociación" (blue)
   - renewed: "Renovado" (emerald)
   - terminating: "Terminando" (red)

4. Urgency Filter:
   - Filter by 30/60/90 day buckets
   - Sort by days until expiry (default)

5. Bulk Actions:
   - Select multiple
   - "Iniciar renovación masiva"
   - "Enviar recordatorios"

Props interface:
```typescript
interface VencimientosTableProps {
  data: VencimientosReport;
  onStartRenewal?: (propertyId: string) => void;
  onContactTenant?: (propertyId: string) => void;
}
```

Use countdown styling for urgency (red when <30 days).
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>VencimientosTable shows expiring contracts</done>
</task>

<task type="auto">
  <name>Task 5: Create FlujoCajaChart Component</name>
  <files>src/components/inmobiliaria/FlujoCajaChart.tsx</files>
  <action>
Create FlujoCajaChart for cash flow visualization:

1. Summary Cards:
   - Total ingresos (emerald)
   - Total dispersiones (blue)
   - Total comisiones (violet, agency income)
   - Balance neto (green or red based on sign)

2. Bar Chart:
   - Monthly grouped bars using CSS
   - Each month: Ingresos (tall, emerald), Dispersiones (blue), Comisiones (violet)
   - X-axis: month labels
   - Y-axis: amounts in millions

3. Line Overlay (optional):
   - Cumulative balance line
   - Trend indicator

4. Period Selector:
   - Último trimestre
   - Últimos 6 meses
   - Año actual
   - Custom range

5. Table Alternative:
   - Toggle between chart and table view
   - Table shows same data in rows
   - Totals row at bottom

Props interface:
```typescript
interface FlujoCajaChartProps {
  data: FlujoCajaReport;
  variant?: 'chart' | 'table';
}
```

Use CSS grid for bar chart layout.
Use formatCurrency for all amounts.
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>FlujoCajaChart shows monthly cash flow</done>
</task>

<task type="auto">
  <name>Task 6: Update Barrel Export</name>
  <files>src/components/inmobiliaria/index.ts</files>
  <action>
Add exports for report visualization components:

```typescript
// Report Visualizations (Phase 8)
export { CarteraEdadesTable } from './CarteraEdadesTable';
export { OcupacionChart } from './OcupacionChart';
export { ComisionesTable } from './ComisionesTable';
export { VencimientosTable } from './VencimientosTable';
export { FlujoCajaChart } from './FlujoCajaChart';
```
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>Report visualization components exported</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm tsc --noEmit passes
- [ ] pnpm build succeeds
- [ ] CarteraEdadesTable shows aging buckets with table
- [ ] OcupacionChart shows zone-based occupancy bars
- [ ] ComisionesTable shows agent commissions with ranking
- [ ] VencimientosTable shows expiring contracts
- [ ] FlujoCajaChart shows monthly cash flow
- [ ] All components exported from barrel
</verification>

<success_criteria>
- All tasks completed
- 5 report visualization components created
- Each component has summary + detail view
- Consistent styling with existing inmobiliaria components
- Export functionality ready for each report type
</success_criteria>

<output>
After completion, create `.planning/phases/inmobiliaria-08-reportes/08-02-SUMMARY.md`
</output>
