---
phase: v7-05-comunicacion
plan: 03
type: execute
wave: 3
depends_on: [v7-05-02]
files_modified:
  - src/lib/api/agent-contact.service.ts
  - src/lib/constants/response-sla.ts
  - src/components/messages/MessagesWidget.tsx
autonomous: true
requirements: [COMU-03]

must_haves:
  truths:
    - "The frontend contains NO outbound sender: no Twilio/SendGrid/WhatsApp/wa.me import or direct send anywhere in the messages surfaces — the only real send stays the in-app messagesApi.sendMessage (compliant)"
    - "Proactive outbound (WhatsApp/reminder) is presented as 'ruteado por tu inmobiliaria', DEFAULT-GATED/disabled 'Próximamente' via the agent contact-ledger contract (canContact → allowed:false today); the frontend never counts against the Ley 2300 cap"
    - "No 'motivo/por qué' the mora field anywhere on the chat/compose path (Ley 2300 art. 7); no fake online/typing/presence implying an instant human reply"
    - "A static, neutral expected-response hint (PQRS SLA = 15 días hábiles, Ley 1480/2011) renders near the composer — never 'responde al instante', never a live countdown"
  artifacts:
    - path: "src/lib/api/agent-contact.service.ts"
      provides: "contact-ledger CONTRACT (canContact via AGENT_SERVICE_URL) — default-gated allowed:false; no outbound sender"
      contains: "canContact"
    - path: "src/lib/constants/response-sla.ts"
      provides: "PQRS_SLA_BUSINESS_DAYS = 15 shared constant (Ley 1480/2011) for the expected-response hint"
      contains: "PQRS_SLA_BUSINESS_DAYS"
    - path: "src/components/messages/MessagesWidget.tsx"
      provides: "static expected-response hint + WhatsApp 'ruteado por el agente' disabled 'Próximamente' affordance; no motivo field, no presence"
      contains: "días hábiles"
  key_links:
    - from: "src/components/messages/MessagesWidget.tsx"
      to: "agentContactApi.canContact"
      via: "WhatsApp/proactive = default-gated disabled 'Próximamente' (never a frontend send)"
      pattern: "canContact"
    - from: "src/components/messages/MessagesWidget.tsx"
      to: "PQRS_SLA_BUSINESS_DAYS (15 días hábiles)"
      via: "static neutral expected-response hint, no instant-human / no countdown"
      pattern: "PQRS_SLA_BUSINESS_DAYS"
---

<objective>
Encode the LEGAL guardrails of this phase (COMU-03 + the PITFALLS-UX expected-response window) on the shared
`MessagesWidget.tsx`, plus a contact-ledger contract and a shared SLA constant. This is the legal crux:
the frontend must NOT become an outbound sender, must NOT ask "why the mora", and must NOT fake an instant
human reply.

Three honest deliverables:
1. **Contact-ledger contract (COMU-03).** New `agent-contact.service.ts`: a contract-only api-client for the
   `agent`'s Ley 2300/2023 gate — `canContact(...)` routed to `AGENT_SERVICE_URL`. No HTTP endpoint exists
   yet, so it DEFAULT-GATES: every call resolves `{ allowed: false, reason: 'unavailable' }` (isEndpointUnavailable
   404/403/0). The cap (máx 1/día, 1 canal/semana) is enforced ONCE, centrally, in the `agent`; the frontend
   NEVER sends and NEVER counts. NO Twilio/SendGrid/WhatsApp import in this file or the widget.
2. **WhatsApp as first-class but ruteado (COMU-03).** In the chat info panel Quick-Actions (`:655-676`), add a
   DISABLED "WhatsApp — ruteado por tu inmobiliaria · Próximamente" affordance wired to `agentContactApi.canContact`
   (returns gated today → stays disabled). WhatsApp is presented as a first-class channel but explicitly routed
   by the agent, never sent by the frontend — same honest posture as `casos/page.tsx:275-282`. No two
   independent "1 recordatorio enviado hoy" counters.
3. **Expected-response window (COMU-04 / SC4).** New `response-sla.ts` exporting
   `PQRS_SLA_BUSINESS_DAYS = 15` (Ley 1480/2011). Render a static, neutral hint near the composer (tenant
   only): "Respondemos en horario hábil. Los reclamos formales tienen respuesta en hasta {PQRS_SLA_BUSINESS_DAYS}
   días hábiles." — NEVER "responde al instante"/"respuesta inmediata", NEVER a typing/online presence, NEVER a
   live countdown (the real SLA clock is v7-06).

Plus a standing guard: assert there is NO "motivo/por qué la mora" field and NO fake presence on the chat path.

Depends on v7-05-02 (shares `MessagesWidget.tsx` → wave 3, strictly after 02).

Purpose: COMU-03 — saliente respeta el gate de contacto del `agent` (Ley 2300), el frontend no envía por su
cuenta ni pregunta "por qué" la mora; WhatsApp ruteado por el `agent`; + COMU-04 ventana de respuesta.
Output: contact-ledger contract + SLA constant + guardrailed widget.

**Guardrails (non-negotiable):** no outbound sender (no Twilio/SendGrid/WhatsApp send); proactive = default-
gated "Próximamente" via the agent; no "por qué la mora" field; no fake online/typing/presence; static SLA
hint (no "al instante", no countdown); buttons sentence case (DESIGN.md §4). Zero new npm packages.
</objective>

<execution_context>
@/Users/nicolasgarcia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolasgarcia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/v7-05-comunicacion/v7-05-RESEARCH.md
@.planning/phases/v7-05-comunicacion/v7-05-PATTERNS.md
@.planning/phases/v7-05-comunicacion/v7-05-02-SUMMARY.md

# READ FIRST: docs/DESIGN.md §4 (Buttons — SENTENCE CASE), §11 (EmptyState / neutral "Próximamente" tone),
# §7 (A11y — pair icon+text, aria-disabled). RESEARCH §4 (contact-gate do/don'ts) + §5 (expected-response).

# Files to modify / create:
@src/lib/api/agent-contact.service.ts
@src/lib/constants/response-sla.ts
@src/components/messages/MessagesWidget.tsx

# THE honest "endpoint-not-live → gated/unavailable" idiom to reuse:
@src/lib/api/lease-documents.service.ts
# THE disabled "Próximamente" affordance posture to mirror (WhatsApp routed by agent):
@src/app/inquilino/casos/page.tsx

<interfaces>
<!-- Env: the frontend calls the agent ONLY via this URL for the gate (never Twilio directly) -->
process.env.AGENT_SERVICE_URL   // CLAUDE.md: microservice URL; the agent owns the Ley 2300 contact-ledger

<!-- Honest-unavailable idiom -->
import { apiClient, ApiError } from './client'
function isEndpointUnavailable(err): boolean   // ApiError && (404 || 403 || 0)

<!-- Widget: info-panel Quick Actions (MessagesWidget.tsx :655-676) is where the WhatsApp affordance lands -->
selectedConversation: ChatConversation | undefined
isTenant = actor === 'tenant'

<!-- Disabled "Próximamente" posture to mirror (casos/page.tsx :275-282): aria-disabled span, no "activado" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: agent-contact.service.ts — contact-ledger CONTRACT (default-gated, no sender)</name>
  <files>src/lib/api/agent-contact.service.ts</files>
  <action>
    Create a contract-only api-client. Types:
      `export type ContactChannel = 'whatsapp' | 'email' | 'push';`
      `export interface CanContactResult { allowed: boolean; reason: 'unavailable' | 'gated' | 'ok'; }`
    Method:
      `async canContact(channel: ContactChannel, leaseId?: string): Promise<CanContactResult>` →
      `apiClient.get<CanContactResult>(`/agent/contact/can-contact?channel=${channel}${leaseId ? `&leaseId=${leaseId}` : ''}`)`,
      wrapped with `isEndpointUnavailable` (404/403/0) → `return { allowed: false, reason: 'unavailable' }`.
    Extensive JSDoc (this is the legal crux): the Ley 2300/2023 cap (máx 1 contacto/día, 1 canal/semana) is
    enforced ONCE, CENTRALLY, in the `Leasefy/agent` contact-ledger (`AGENT_SERVICE_URL`); the frontend NEVER
    sends and NEVER counts. There is intentionally NO `send*` method here — this module can only ASK whether
    the agent may contact, and today it always returns `allowed:false` (gate not exposed over HTTP yet →
    disabled "Próximamente"). Do NOT import Twilio/SendGrid/WhatsApp. Do NOT add any outbound-send method now
    or later in this file.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && F=src/lib/api/agent-contact.service.ts && grep -q "canContact" $F && grep -q "isEndpointUnavailable" $F && grep -q "CanContactResult" $F && test $(grep -ciE "twilio|sendgrid|wa\.me|whatsapp.*send|\.send\(|sendMessage|nodemailer" $F) -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>agent-contact.service.ts exposes only `canContact` returning `{allowed:false, reason:'unavailable'}` today (default-gated via 404/403/0); JSDoc names the agent as the single central gate; zero outbound-sender imports/methods.</done>
</task>

<task type="auto">
  <name>Task 2: response-sla.ts constant + static expected-response hint (COMU-04)</name>
  <files>src/lib/constants/response-sla.ts, src/components/messages/MessagesWidget.tsx</files>
  <action>
    Create `src/lib/constants/response-sla.ts`:
      `/** PQRS formal-claim SLA — Ley 1480/2011 art. 58: 15 business days. Shared so v7-06's real clock reuses it. */`
      `export const PQRS_SLA_BUSINESS_DAYS = 15;`

    In `MessagesWidget.tsx`, import the constant and render a static, neutral expected-response hint near the
    composer (below or beside the message input, ~:535-581), gated to `isTenant`. Copy (sentence case):
      es → "Respondemos en horario hábil. Los reclamos formales tienen respuesta en hasta {PQRS_SLA_BUSINESS_DAYS} días hábiles."
      en → "We reply during business hours. Formal claims are answered within {PQRS_SLA_BUSINESS_DAYS} business days."
    Muted, small, `Info`-icon optional (pair icon+text). It MUST NOT imply an instant human reply: no
    "responde al instante"/"respuesta inmediata"/"en línea"/"escribiendo", and NO live countdown/clock (the
    real SLA clock is v7-06). For `actor !== 'tenant'` render nothing new.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && C=src/lib/constants/response-sla.ts && W=src/components/messages/MessagesWidget.tsx && grep -q "PQRS_SLA_BUSINESS_DAYS = 15" $C && grep -q "PQRS_SLA_BUSINESS_DAYS" $W && grep -qi "días hábiles" $W && test $(grep -ciE "responde al instante|respuesta inmediata|al instante|en línea|escribiendo" $W) -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>PQRS_SLA_BUSINESS_DAYS=15 shared constant exists; a static neutral "15 días hábiles" expected-response hint renders for tenant; zero instant-human / countdown copy.</done>
</task>

<task type="auto">
  <name>Task 3: WhatsApp "ruteado por el agente" disabled affordance + contact-gate/no-motivo guard</name>
  <files>src/components/messages/MessagesWidget.tsx</files>
  <action>
    In the info-panel Quick Actions (`:655-676`), add a DISABLED affordance (tenant only), mirroring the
    `casos/page.tsx:275-282` posture: an `aria-disabled="true"` element (not a Button that sends) with copy
    es → "WhatsApp — ruteado por tu inmobiliaria · Próximamente" / en → "WhatsApp — routed by your agency ·
    Coming soon", `ChatCircle`/WhatsApp-style icon, `title`/tooltip "Aún no disponible". Wire its
    disabled/enabled state to `agentContactApi.canContact('whatsapp', selectedConversation?.leaseId)` — which
    returns `allowed:false` today, so it stays disabled. NEVER render an "activado"/"enviado"/"1 recordatorio
    enviado hoy" state, and NEVER call any send. WhatsApp is presented as first-class but explicitly routed by
    the agent.

    Standing guard (assert, do not add): confirm the widget has NO "motivo/por qué la mora" field or dropdown
    on the compose/report path (Ley 2300 art. 7 — the v7-05-02 report reason is optional free-text only), and
    NO fake online/typing/presence indicator. The only real send remains the in-app `messagesApi.sendMessage`
    (COMPLIANT — a portal message read in-app, not an outbound WhatsApp/email/push). Do NOT import
    Twilio/SendGrid/wa.me anywhere in this file.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && W=src/components/messages/MessagesWidget.tsx && grep -q "canContact" $W && grep -qi "ruteado por tu inmobiliaria\|routed by your agency" $W && grep -qi "Próximamente\|Coming soon" $W && test $(grep -ciE "twilio|sendgrid|wa\.me|whatsapp.*send|recordatorio enviado" $W) -eq 0 && test $(grep -ciE "motivo.*(mora|atraso|pago)|por qué no (ha|has) pagado" $W) -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>A disabled "WhatsApp — ruteado por tu inmobiliaria · Próximamente" affordance is gated by canContact (allowed:false today); no outbound sender / no "recordatorio enviado" counter; no "why the mora" field; no fake presence; in-app send unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant browser → Ley 2300 outbound channel | The regulated surface. The frontend MUST NOT send; it may only ASK the agent (`canContact`) |
| tenant browser → Leasefy/agent contact-ledger (AGENT_SERVICE_URL) | Single central place the máx-1/día cap is enforced; not exposed over HTTP yet → default-gated |
| tenant compose/report intake | Must never collect a "why late" reason (Ley 2300 art. 7) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v7-05-05 | Elevation / statutory (Ley 2300 art.3) | accidental frontend outbound sender | mitigate | agent-contact.service has no send method; grep-gated absence of twilio/sendgrid/wa.me/whatsapp send in service + widget; only in-app messagesApi.sendMessage remains |
| T-v7-05-06 | Repudiation / double-count | portal counting against the contact cap | mitigate | canContact routes to the agent's single ledger; no portal-side counter; grep-gated absence of "recordatorio enviado" |
| T-v7-05-07 | Compliance (Ley 2300 art.7) | "por qué la mora" field | mitigate | Guarded/grep-gated absence of "motivo…mora"/"por qué…pagado" on the chat path |
| T-v7-05-08 | Deception (PITFALLS UX) | fake instant-human / presence | mitigate | Static SLA hint only; grep-gated absence of "al instante"/"en línea"/"escribiendo"/countdown |
| T-v7-05-SC | Tampering (supply chain) | npm installs | accept | Zero new packages |
</threat_model>

<verification>
- `pnpm -C /Users/nicolasgarcia/rent/mvp-portal-inquilino build` succeeds.
- `pnpm test` — 0 NEW failures (~7 pre-existing unrelated — see v7-01 deferred-items.md).
- Grep gates (per task): agent-contact `canContact` default-gated with zero sender imports; `PQRS_SLA_BUSINESS_DAYS=15`
  + "días hábiles" hint with zero instant-human tokens; WhatsApp "ruteado" disabled affordance gated by
  canContact, zero twilio/sendgrid/wa.me/"recordatorio enviado", zero "why-late" tokens.
- Repo-wide legal sanity (manual/CI): no Twilio/SendGrid/wa.me send on any tenant messages path; the only real
  send is in-app `messagesApi.sendMessage`.
- Manual smoke as `tenant`: composer shows "…hasta 15 días hábiles" (not "al instante"); info panel shows a
  DISABLED "WhatsApp — ruteado por tu inmobiliaria · Próximamente"; no "por qué"/"motivo" prompt; no
  online/typing indicator. As `landlord`: no new tenant-only hint/affordance.
</verification>

<success_criteria>
- COMU-03: all proactive outbound is gated by the `agent` contact-ledger contract (default "Próximamente"); the
  frontend has no Twilio/WhatsApp sender; no "por qué la mora" field; WhatsApp is first-class but routed by the
  agent.
- COMU-04 (SC4 / PITFALLS UX): a static, neutral expected-response window (PQRS SLA = 15 días hábiles) renders,
  with no instant-human implication and no live countdown.
</success_criteria>

<output>
After completion, create `.planning/phases/v7-05-comunicacion/v7-05-03-SUMMARY.md`.
</output>
