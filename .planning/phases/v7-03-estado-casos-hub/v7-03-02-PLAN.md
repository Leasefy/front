---
phase: v7-03-estado-casos-hub
plan: 02
type: execute
wave: 2
depends_on: [v7-03-01]
files_modified:
  - src/app/inquilino/casos/page.tsx
  - src/app/inquilino/page.tsx
  - src/app/inquilino/layout.tsx
autonomous: true
requirements: [CASO-01, CASO-03]

must_haves:
  truths:
    - "The /inquilino/casos hub lists real cases from useTenantCases() — pago + application-journey — each with estado (neutral badge) + responsable role + link to detail"
    - "Cases 'al día'/none produce an honest neutral empty-state ('todo al día'), never a fabricated count or alarm badge"
    - "PQRS + mantenimiento + acuerdos render as honest 'Próximamente' sections (EmptyState), NOT fake rows"
    - "Case badges use neutral tone (secondary/default/warning) — NEVER destructive/alarm; no countdown, no 'Datacrédito'/urgency copy"
    - "The dashboard placeholder at page.tsx:256 is replaced by a REAL 'Casos abiertos' stat card (count from useTenantCases) linking to /inquilino/casos"
    - "The layout nav exposes a 'Mis casos' item routing to /inquilino/casos"
    - "CASO-03: the hub surfaces that state-change alerts arrive in-app (link to /inquilino/notificaciones); push/WhatsApp is an honest disabled 'Próximamente'"
  artifacts:
    - path: "src/app/inquilino/casos/page.tsx"
      provides: "Mis casos hub — real case list + Próximamente forward-ref sections + CASO-03 notifications strip"
      contains: "useTenantCases"
    - path: "src/app/inquilino/page.tsx"
      provides: "Real 'Casos abiertos' stat card replacing the v7-03 placeholder comment"
      contains: "/inquilino/casos"
    - path: "src/app/inquilino/layout.tsx"
      provides: "'Mis casos' nav item"
      contains: "/inquilino/casos"
  key_links:
    - from: "src/app/inquilino/casos/page.tsx"
      to: "useTenantCases"
      via: "aggregate case list render"
      pattern: "useTenantCases"
    - from: "src/app/inquilino/page.tsx"
      to: "/inquilino/casos"
      via: "stat card count + deep-link"
      pattern: "/inquilino/casos"
    - from: "src/app/inquilino/casos/page.tsx"
      to: "/inquilino/notificaciones"
      via: "CASO-03 in-app alert link + push/WhatsApp Próximamente"
      pattern: "/inquilino/notificaciones"
---

<objective>
Ship the tenant-facing **"Mis casos" hub** at `/inquilino/casos` — the direct P1 fix that lets the
tenant OPERATE the relationship in one place. It consumes `useTenantCases()` (from v7-03-01) and
renders real cases (pago + application-journey) as neutral status-badged rows linking to each
detail, honest **"Próximamente"** sections for the forward-ref types (PQRS/mantenimiento/acuerdos),
and a CASO-03 notifications strip. Then wire the dashboard placeholder (`page.tsx:256`) to a REAL
"Casos abiertos" stat card and add a "Mis casos" nav item so the hub is reachable.

Guardrails (CASO-04 / PITFALLS 8): "al día"/empty → neutral honest empty-state ("todo al día"),
never a fabricated count; badges capped at neutral tone (secondary/default/warning), NEVER
destructive/alarm, no countdown, no "Datacrédito"/urgency copy. Forward-refs are EmptyState
sections, never fake rows.

CASO-03: in-app notifications are ALREADY real (`useTenantNotifications` → `/notifications`,
polling, `actionUrl` deep-links, templates fire for PAYMENT/APPLICATION/CONTRACT). The hub only
surfaces this honestly — a small strip linking to `/inquilino/notificaciones` — and labels
push/WhatsApp as a disabled **"Próximamente"** (no channel emitter exists; PITFALLS 5). Do NOT
show a "push activado" state.

Purpose: Satisfy CASO-01 (hub renders the composed cases) + CASO-03 (in-app real, push/WhatsApp
honest "Próximamente").
Output: `casos/page.tsx` (new hub), a real dashboard stat card, and a nav item.
Depends on v7-03-01 (the `useTenantCases()` hook + `TenantCase` type).
</objective>

<execution_context>
@/Users/nicolasgarcia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolasgarcia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/v7-03-estado-casos-hub/v7-03-RESEARCH.md
@.planning/phases/v7-03-estado-casos-hub/v7-03-PATTERNS.md
@.planning/phases/v7-03-estado-casos-hub/v7-03-01-SUMMARY.md

# READ FIRST: docs/DESIGN.md §4 (cards/rows/buttons), §7 (badges/status — pair icon+text, never
# color alone), §11 (EmptyState / loading / "Próximamente"), §12 (Badge/PlanStatusBadge tints),
# §16 (dates es-CO / COP via useI18n().formatCurrency). CLAUDE.md mandates DESIGN.md before UI.

# Files to create / modify:
@src/app/inquilino/casos/page.tsx
@src/app/inquilino/page.tsx
@src/app/inquilino/layout.tsx

# THE analogs to copy (do not invent):
# List-hub skeleton (header + CompleteProfileFirst gate + loading Spinner + EmptyState + status-badged rows):
@src/app/inquilino/aplicaciones/page.tsx
# Dashboard stat card to mirror for "Casos abiertos" (the Applications card at :243-254; the v7-03
# placeholder comment is at :256; grid is grid-cols-2 lg:grid-cols-4):
@src/app/inquilino/page.tsx
# EmptyState primitive (honest "Próximamente"):
@src/components/ui/empty-state.tsx

<interfaces>
<!-- From v7-03-01 (this plan CONSUMES it — do not re-derive) -->
// src/lib/hooks/use-tenant-cases.ts
useTenantCases(): { cases: TenantCase[]; openCasesCount: number; primaryLease: BackendLease|null; isLoading; error; refetch }
// src/lib/types/tenant-case.ts
TenantCase: { id; type: CaseType; titulo; estadoLabel; tone: CaseTone; responsable; updatedAt; detailLink; sourceLink; events }
type CaseTone = 'neutral' | 'info' | 'attention';   // NEVER danger

<!-- Neutral badge mapping (src/components/ui/badge.tsx): variant secondary→neutral, default→primary,
     warning→amber. Map tone → variant: neutral→'secondary', info→'default', attention→'warning'.
     NEVER 'destructive' / 'risk-*'. Pair a Phosphor icon + text (DESIGN.md §7 a11y). -->

<!-- CASO-03 in-app read surface — ALREADY real, just link to it -->
// src/lib/hooks/useNotifications.ts
useTenantNotifications(): { notifications; unreadCount; isLoading; markAsRead; markAllAsRead; deleteNotification; refetch }
//   Route /inquilino/notificaciones exists (nav layout.tsx:41); notifications carry actionUrl deep-links.

<!-- Onboarding gate + primitives already used across the portal -->
// src/components/tenant/CompleteProfileFirst.tsx — <CompleteProfileFirst context="..." />
// src/components/ui/spinner.tsx — <Spinner size="lg" />
// src/components/ui/empty-state.tsx — <EmptyState icon title description action?={{label,href}} />
// src/lib/i18n — useI18n(): { t, locale, formatCurrency }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: "Mis casos" hub page (real list + Próximamente sections + CASO-03 strip)</name>
  <files>src/app/inquilino/casos/page.tsx</files>
  <action>
    Create `src/app/inquilino/casos/page.tsx` (`'use client'`). Copy the page skeleton from
    `aplicaciones/page.tsx` (header + `CompleteProfileFirst` onboarding gate + combined loading
    `Spinner size="lg"` + error `EmptyState` + `max-w-7xl` container + tenant bg
    `bg-[#f8f8f8] dark:bg-[#0e0e10]`). Swap the applications source for `useTenantCases()`.

    (a) CASE LIST (CASO-01): render `cases` as a vertical list of rows in the DESIGN.md §4 card idiom
    (icon tile + `titulo` + a subline showing `responsable` ("Responsable: Inmobiliaria") and a
    relative `updatedAt`, a neutral status Badge, and a `CaretRight` link to `case.detailLink`). Pick
    a Phosphor icon by `case.type` (e.g. pago → `CreditCard`, aplicacion → `FileText`). Map
    `case.tone` → Badge variant: neutral→`secondary`, info→`default`, attention→`warning`; pair the
    badge with a small icon + the `estadoLabel` text (never color alone, DESIGN.md §7). NEVER use
    `variant="destructive"`/risk variants, no countdown, no urgency copy.

    (b) EMPTY (CASO-04 neutral): when `cases.length === 0` (and not loading), render an honest neutral
    `EmptyState` — title "Todo al día" / "All caught up", description "No tienes casos abiertos por
    ahora." — NOT an alarm, NOT a fabricated count.

    (c) FORWARD-REF "Próximamente" (CASO-01 honesty): below the real list, render three honest
    sections for **PQRS**, **Mantenimiento**, and **Acuerdos de pago**, each a card with a short
    description and an `EmptyState` titled "Próximamente" (mirror the v7-02 paz-y-salvo /
    agency-acuerdos precedent). These render NO rows and MUST NOT fabricate cases. Suitable icons
    (e.g. `Wrench` for mantenimiento, `ChatCircle`/`Question` for PQRS, `Handshake` for acuerdos).

    (d) CASO-03 notifications strip: a small card/banner stating that Leasefy avisa **in-app** cuando
    cambia el estado de un caso, with a link to `/inquilino/notificaciones` (optionally showing
    `useTenantNotifications().unreadCount` as a subtle badge). Add an explicit **disabled**
    "Push/WhatsApp — Próximamente" affordance (a disabled control or a muted "Próximamente" label) —
    NEVER a "push activado" state (PITFALLS 5). Do not add a second poller here.

    Dates es-CO (DESIGN.md §16); COP amounts (if any shown) via `useI18n().formatCurrency`. Degrade
    per-source with the loading gate (never flash a fake-empty while `useTenantCases().isLoading`).
    Additive only — do not touch other tenant pages here.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && grep -q "useTenantCases" src/app/inquilino/casos/page.tsx && grep -c "Próximamente" src/app/inquilino/casos/page.tsx | { read n; test "$n" -ge 1; } && grep -q "/inquilino/notificaciones" src/app/inquilino/casos/page.tsx && test $(grep -c "variant=\"destructive\"" src/app/inquilino/casos/page.tsx) -eq 0 && test $(grep -ciE "datacr|última oportunidad|ultima oportunidad|MOCK" src/app/inquilino/casos/page.tsx) -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>casos/page.tsx renders the real case list with neutral badges + detail links; neutral "todo al día" empty; three "Próximamente" forward-ref sections (no fake rows); a CASO-03 in-app link + push/WhatsApp "Próximamente"; no destructive badge; no urgency/MOCK strings.</done>
</task>

<task type="auto">
  <name>Task 2: Wire dashboard "Casos abiertos" stat card + "Mis casos" nav item</name>
  <files>src/app/inquilino/page.tsx, src/app/inquilino/layout.tsx</files>
  <action>
    (a) Dashboard stat card — replace the placeholder comment at `page.tsx:256`
    (`{/* Casos abiertos: hub llega en v7-03 — no fabricar conteo */}`) with a REAL "Casos abiertos"
    stat card. Import `useTenantCases` from `@/lib/hooks/use-tenant-cases` and call it in the
    component (its `isLoading` may be folded into the existing combined loading gate at
    `page.tsx:136-148`, or handled inline — do not blank the dashboard on a single source error).
    Mirror the Applications stat card structure (`page.tsx:243-254`): icon tile (e.g. `ClipboardText`
    or `Stack` from `@phosphor-icons/react`) + label "Casos" / "Cases" + `{openCasesCount}` +
    neutral sub-label ("Sin casos abiertos" when 0 / "Abiertos" / "Al día" otherwise). Wrap the card
    in `<Link href="/inquilino/casos">` so it deep-links to the hub. Keep the count REAL from
    `openCasesCount` — never fabricate (the removed comment mandated this). The grid is
    `grid-cols-2 lg:grid-cols-4`; adding this 5th card wraps cleanly — do not disturb the existing
    Score/Arriendos/Applications/Next-Payment cards.

    (b) Nav item — in `src/app/inquilino/layout.tsx`, add a "Mis casos" entry to `useTenantNavItems()`
    (the array around lines 31-43), e.g. between the Pagos and Documentos items:
    `{ label: locale === 'es' ? 'Mis casos' : 'My cases', href: '/inquilino/casos', icon: ClipboardText }`.
    Import the icon from `@phosphor-icons/react`. Keep it additive — do not reorder/remove existing
    items.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && grep -q "useTenantCases" src/app/inquilino/page.tsx && grep -q "/inquilino/casos" src/app/inquilino/page.tsx && grep -q "openCasesCount" src/app/inquilino/page.tsx && grep -q "/inquilino/casos" src/app/inquilino/layout.tsx && test $(grep -c "no fabricar conteo" src/app/inquilino/page.tsx) -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>The dashboard shows a real "Casos abiertos" card (openCasesCount) linking to /inquilino/casos, replacing the placeholder comment; the layout nav exposes "Mis casos"; existing cards/nav untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant browser → hub render | Read-only projection of `/…/mine`-scoped sources; no new fetch/authz introduced |
| tenant browser → `/inquilino/notificaciones` | Existing tenant-scoped in-app notifications surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v7-03-04 | Consumer-law (Ley 1480, PITFALLS 8) | case badges / empty-state | mitigate | Neutral tone only (no `destructive`); "todo al día" neutral empty; no countdown/urgency/Datacrédito copy (grep-gated) |
| T-v7-03-05 | Repudiation (fake channel) | CASO-03 push/WhatsApp | mitigate | In-app link only (real); push/WhatsApp rendered as disabled "Próximamente" — no "activado" state |
| T-v7-03-06 | Integrity (fabrication) | dashboard count + forward-refs | mitigate | Count is real `openCasesCount`; PQRS/mantenimiento/acuerdos = EmptyState "Próximamente", zero rows |
| T-v7-03-SC | Tampering (supply chain) | npm installs | accept | Zero new packages (RESEARCH audit: none) |
</threat_model>

<verification>
- `pnpm -C /Users/nicolasgarcia/rent/mvp-portal-inquilino build` succeeds.
- `pnpm test` — 0 NEW failures (≈7 pre-existing unrelated — see v7-01 deferred-items.md).
- Grep gates: `useTenantCases` on hub + dashboard; ≥1 "Próximamente" + `/inquilino/notificaciones`
  on the hub; zero `variant="destructive"` / Datacrédito / urgency / MOCK on the hub; the
  `no fabricar conteo` placeholder comment is gone; `/inquilino/casos` present in layout nav.
- Manual smoke as `tenant`: `/inquilino/casos` lists real pago + aplicación cases with neutral
  badges linking to detail; PQRS/mantenimiento/acuerdos show "Próximamente"; an in-app notifications
  link plus a disabled push/WhatsApp "Próximamente"; the dashboard "Casos abiertos" card shows a real
  count and deep-links to the hub; the nav shows "Mis casos".
</verification>

<success_criteria>
- CASO-01: the tenant sees "Mis casos" — a hub aggregating the real pago + application-journey cases
  (each with estado + responsable role) plus honest "Próximamente" sections for
  PQRS/mantenimiento/acuerdos; "al día" is a neutral empty-state, no fabricated count/alarm.
- CASO-03: in-app notification on case state change is surfaced (link to the existing real
  notifications surface); push/WhatsApp is an honest disabled "Próximamente".
</success_criteria>

<output>
After completion, create `.planning/phases/v7-03-estado-casos-hub/v7-03-02-SUMMARY.md`.
</output>
