---
phase: 31-automatic-reminders
plan: 03
type: execute
wave: 2
depends_on: ["31-01", "31-02"]
files_modified:
  - src/app/panel/inmobiliaria/operaciones/page.tsx
autonomous: true

must_haves:
  truths:
    - "Operaciones page has a 'Recordatorios' tab"
    - "Recordatorios tab shows ReminderConfigPanel and ReminderLog"
    - "Recordatorios tab is wrapped with FeatureGate for 'automatic-reminders'"
    - "Non-Growth users see upgrade prompt on Recordatorios tab"
  artifacts:
    - path: "src/app/panel/inmobiliaria/operaciones/page.tsx"
      provides: "Enhanced operaciones page with Recordatorios tab"
  key_links:
    - from: "operaciones/page.tsx"
      to: "ReminderConfigPanel + ReminderLog"
      via: "renders both in Recordatorios tab"
    - from: "operaciones/page.tsx"
      to: "FeatureGate"
      via: "wraps reminder content with feature='automatic-reminders'"
---

<objective>
Wire reminder components into the operaciones page and gate to Growth+ plans.

Purpose: Integrate config + log into a Recordatorios tab on operaciones, with plan gating.
Output: Enhanced operaciones page with reminders tab.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-automatic-reminders/31-01-SUMMARY.md
@.planning/phases/31-automatic-reminders/31-02-SUMMARY.md

@src/app/panel/inmobiliaria/operaciones/page.tsx
@src/components/inmobiliaria/UpgradePrompt.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Recordatorios tab to operaciones page</name>
  <files>src/app/panel/inmobiliaria/operaciones/page.tsx</files>
  <action>
    Read the existing operaciones page. It likely has tabs for Renovaciones, Mantenimiento, and/or IPC Calculator.

    Add a new tab:
    - id: 'recordatorios'
    - Label: 'Recordatorios'
    - Icon: Bell from Phosphor

    For the recordatorios tab content:
    ```tsx
    {activeTab === 'recordatorios' && (
      <FeatureGate feature="automatic-reminders">
        <div className="space-y-6">
          <ReminderConfigPanel config={mockReminderConfig} />
          <ReminderLog entries={mockReminderLog} />
        </div>
      </FeatureGate>
    )}
    ```

    Import:
    - ReminderConfigPanel from '@/components/inmobiliaria/reminders/ReminderConfig'
    - ReminderLog from '@/components/inmobiliaria/reminders/ReminderLog'
    - mockReminderConfig, mockReminderLog from '@/lib/data/mock-reminders'
    - FeatureGate from '@/components/inmobiliaria/UpgradePrompt'
    - Bell from '@phosphor-icons/react'

    Update the tab type to include 'recordatorios'.

    Run build.
  </action>
  <verify>npx next build --no-lint | grep "Compiled"</verify>
  <done>Operaciones page has Recordatorios tab with config + log, gated to Growth+</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx next build --no-lint` compiles successfully
- [ ] Operaciones page shows Recordatorios tab
- [ ] Tab renders both config panel and log
- [ ] FeatureGate wraps reminder content
</verification>

<success_criteria>

- Reminders accessible from operaciones page
- Config + log displayed together
- Gated to Growth+ / Growth Flex+ plans
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/31-automatic-reminders/31-03-SUMMARY.md`
</output>
