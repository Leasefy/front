---
phase: v7-07-acuerdos-pago
plan: 05
type: execute
wave: 4
depends_on: [v7-07-01, v7-07-02, v7-07-03, v7-07-04]
files_modified:
  - src/components/tenant/AcuerdoAcceptPanel.tsx
  - src/app/inquilino/acuerdos/[id]/page.tsx
autonomous: true
requirements: [ACUE-01, ACUE-02]

must_haves:
  truths:
    - "The acuerdo detail resolves ONE plan by filtering the tenant's OWN useTenantAcuerdos list by the route id (find(p => p.planId === id)) — NEVER a raw fetch-by-id; an unknown/foreign id renders an honest 'no encontrado' EmptyState. It shows the cuota plan (CuotaPlanTable), the plan total verbatim, and a source-timestamp timeline (offeredAt → acceptedAt) — no synthesized milestones, no saldo math"
    - "AcuerdoAcceptPanel lets the tenant ACCEPT an already-agency-approved acuerdo by signing (reused SignaturePad + generalized OTPVerification via an injected acuerdo OtpAdapter) with explicit consent checkboxes; it shows 'Este acuerdo ya fue aprobado por tu inmobiliaria' and has NO approve button and NO terms editor — the tenant never auto-approves or sets terms (T-323, A5)"
    - "accept() is GATED: the panel calls acuerdosApi.accept(planId, {signatureData, otpVerificationToken}); on AcuerdoUnavailableError it shows an honest 'Próximamente' toast and NEVER a fake 'aceptado' — the status comes from the agent's response, never set optimistically. The accept affordance shows only when acceptedAt === null"
  artifacts:
    - path: "src/components/tenant/AcuerdoAcceptPanel.tsx"
      provides: "SignaturePad + generalized OTPVerification (acuerdo OtpAdapter) + consent checkboxes + 'lo aprueba tu inmobiliaria' banner → acuerdosApi.accept; honest 'Próximamente' on AcuerdoUnavailableError; no approve/terms affordance"
      contains: "acuerdosApi.accept"
    - path: "src/app/inquilino/acuerdos/[id]/page.tsx"
      provides: "own-list .find detail: CuotaPlanTable + total verbatim + offered→accepted timeline + accept affordance (when acceptedAt===null); honest not-found; no fetch-by-id"
      contains: "AcuerdoAcceptPanel"
  key_links:
    - from: "src/components/tenant/AcuerdoAcceptPanel.tsx"
      to: "OTPVerification adapter → acuerdosApi.accept"
      via: "inject an acuerdo OtpAdapter (send/verify); onSigned → accept(planId,{signatureData,otpVerificationToken}); AcuerdoUnavailableError → honest toast"
      pattern: "adapter"
    - from: "src/app/inquilino/acuerdos/[id]/page.tsx"
      to: "useTenantAcuerdos().items.find(planId)"
      via: "own-only resolution (anti-IDOR); unknown id → honest not-found"
      pattern: "find"
---

<objective>
Ship the ACUE-02 accept surface + the acuerdo detail (ACUE-01 read): a dedicated `/inquilino/acuerdos/[id]` page
that resolves the plan from the tenant's OWN list (anti-IDOR), renders the cuota plan + timeline, and hosts an
`AcuerdoAcceptPanel` that lets the tenant accept an already-agency-approved acuerdo by signing — never approving,
never setting terms. The accept POST + the acuerdo OTP endpoints don't exist yet, so the panel is built but the
accept is GATED (`AcuerdoUnavailableError` → honest "Próximamente"), never a fake acceptance.

Two honest deliverables:
1. **AcuerdoAcceptPanel (ACUE-02, T-323/A5).** Compose it from the shipped `SignatureForm` pattern: reuse
   `SignaturePad` AS-IS + the generalized `OTPVerification` (v7-07-02) driven by an INJECTED acuerdo
   `OtpAdapter` (send/verify hitting the agent's provisional acuerdo OTP endpoints via a tiny local transport
   that honest-degrades), + consent checkboxes ("acepto las condiciones del acuerdo de pago", "entiendo que la
   firma es vinculante", "autorizo el tratamiento de datos — Ley 1581/2012"), + a factual banner "Este acuerdo
   ya fue aprobado por tu inmobiliaria. Al firmar, confirmás que lo aceptás." On sign →
   `acuerdosApi.accept(planId, { signatureData, otpVerificationToken })`; success → `toast.success('Acuerdo
   aceptado')` (status from the agent, NEVER optimistic) + `onAccepted?.()`; `AcuerdoUnavailableError` →
   `toast.info` honest "Próximamente" (no fake "aceptado"). NO "aprobar" button, NO discount/cuota/terms editor.
   Buttons sentence case.
2. **/inquilino/acuerdos/[id] detail (ACUE-01).** Copy the `casos/[caseId]` resolution pattern: `const plan =
   useTenantAcuerdos().items.find(p => p.planId === params.id)` — NO fetch-by-id; loading gate; unknown/foreign
   id → honest "Acuerdo no encontrado" `EmptyState`. Render: the plan total `formatCurrency(totalDueCop)`
   verbatim + `CuotaPlanTable` (from v7-07-04); a `PlanActivityTimeline` built from source timestamps only
   (`offeredAt` → `acceptedAt` when present — no synthesis); the `AcuerdoAcceptPanel` shown ONLY when
   `plan.acceptedAt === null` (an already-accepted plan shows an accepted state instead). A "Pagar cuota"
   affordance is NOT added here (that's v7-07-06). BackLink → `/inquilino/acuerdos`.

Depends on v7-07-01 (service), v7-07-02 (generalized OTP), v7-07-03 (useTenantAcuerdos), v7-07-04
(CuotaPlanTable). Wave 4.

Purpose: ACUE-01/02 — the tenant reads an approved acuerdo and accepts it by signing, honestly (never approves).
Output: accept panel + acuerdo detail page.

**Guardrails:** accept-only, NEVER approve/set terms (T-323, A5 — no "aprobar" button, no terms editor); off-policy
routes through the agent's `requiresHumanReview()` (never client-side); own-only `.find` (no fetch-by-id, anti-IDOR);
`AcuerdoUnavailableError` → honest "Próximamente" (no fake "aceptado"); status from the agent (no optimistic flip);
no saldo math (render verbatim); timeline from source timestamps only; reuse SignaturePad + generalized OTP (no
fork); es-CO; sentence-case buttons; zero new npm packages.
</objective>

<execution_context>
@/Users/nicolasgarcia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolasgarcia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/v7-07-acuerdos-pago/v7-07-RESEARCH.md
@.planning/phases/v7-07-acuerdos-pago/v7-07-PATTERNS.md

# READ FIRST: docs/DESIGN.md §8/§17 (Dialog + Lenis stop/start + data-lenis-prevent), §4 (Buttons sentence case),
# §11 (EmptyState), §7 (A11y). CLAUDE.md: additive, es-CO, zero new npm packages.

# The composition template (SignaturePad + OTP + consent checkboxes + onSign payload) to MIRROR:
@src/components/contract/SignatureForm.tsx
# The generalized OTP component (v7-07-02) — inject an acuerdo adapter here:
@src/components/contract/OTPVerification.tsx
# The detail-page resolution pattern to COPY (own-list .find, honest not-found, timeline):
@src/app/inquilino/casos/[caseId]/page.tsx

<interfaces>
<!-- v7-07-01 / v7-07-02 / v7-07-03 / v7-07-04 outputs consumed here -->
acuerdosApi.accept(planId: string, body: { signatureData: string; otpVerificationToken: string }): Promise<AcuerdoAcceptResult>   // throws AcuerdoUnavailableError on 404/403/0
class AcuerdoUnavailableError extends Error
useTenantAcuerdos(): { items: AcuerdoDetail[]; isLoading; error; refetch }
type AcuerdoDetail = { planId; status: string; totalDueCop; paymentUrl: string|null; offeredAt; acceptedAt: string|null; installments: AcuerdoInstallment[] }
CuotaPlanTable({ installments, locale })   // from '@/components/tenant/CuotaPlanTable'
resolveOtpAdapter / OtpAdapter { send, verify }   // OTPVerification now accepts `adapter?: OtpAdapter`
SignaturePad({ onChange, signerName, disabled })   // generic — reuse as-is
PlanActivityTimeline({ items, emptyMessage })   // items: { id, title, timestamp, icon }[]

<!-- The acuerdo OtpAdapter transport (provisional — honest-degrade; endpoints not live):
     send: () => POST /cartera/payment-plans/:planId/otp/send   → { sentTo, cooldownSeconds }
     verify: (code) => POST /cartera/payment-plans/:planId/otp/verify { code } → { verificationToken }
     via apiClient (BFF), catch isEndpointUnavailable → surface an unavailable state (never fabricated token). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AcuerdoAcceptPanel — sign to accept (SignaturePad + generalized OTP) (ACUE-02)</name>
  <files>src/components/tenant/AcuerdoAcceptPanel.tsx</files>
  <action>
    Create `src/components/tenant/AcuerdoAcceptPanel.tsx` (`'use client'`) mirroring `SignatureForm`'s
    composition: `SignaturePad` (`onChange`) + 3 required consent checkboxes + a `canSign` gate + the
    OTP-then-accept lifecycle. Props: `{ planId: string; onAccepted?: () => void; className?: string }`.
    Build an acuerdo `OtpAdapter` (a small local const): `send` → `apiClient.post('/cartera/payment-plans/${planId}/otp/send', {})`
    mapped to `{ sentTo, cooldownSeconds }`; `verify` → `apiClient.post('/cartera/payment-plans/${planId}/otp/verify', { code })`
    mapped to `{ verificationToken }`; both catch `isEndpointUnavailable`-shaped errors and surface an unavailable
    state (NEVER a fabricated token). Pass this adapter to `<OTPVerification isOpen adapter={otpAdapter}
    onVerified={...} onCancel={...} />` (no contractId/role — the generalized component uses the adapter).
    On the sign lifecycle: when signature drawn + checkboxes checked → open OTP → on `onVerified(token)` call
    `await acuerdosApi.accept(planId, { signatureData, otpVerificationToken: token })`; success →
    `toast.success('Acuerdo aceptado')`, `onAccepted?.()` (status comes from the AGENT response — never set an
    optimistic "aceptado"); catch `AcuerdoUnavailableError` → `toast.info` honest es "La aceptación de acuerdos
    estará disponible pronto." / en analog; rethrow-toast other errors generically.
    Copy: a factual primary-soft banner "Este acuerdo ya fue aprobado por tu inmobiliaria. Al firmar, confirmás
    que lo aceptás." + the Ley 527/1999 footnote. NO "aprobar" affordance, NO discount/cuota/terms editor, NO
    reject button. Buttons sentence case. Inline `locale === 'es'` copy via `useI18n()`.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && A=src/components/tenant/AcuerdoAcceptPanel.tsx && grep -q "acuerdosApi.accept" $A && grep -q "AcuerdoUnavailableError" $A && grep -q "SignaturePad" $A && grep -q "adapter" $A && grep -q "aprobó" $A && test $(grep -vE '^\s*(\*|//|/\*)' $A | grep -ciE "aprobar acuerdo|editar t[eé]rmino|descuento|rechazar|por qu[eé]|central") -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>AcuerdoAcceptPanel signs to accept (SignaturePad + generalized OTP via acuerdo adapter + consent) → acuerdosApi.accept; honest "Próximamente" on AcuerdoUnavailableError; status from the agent (no optimistic); "lo aprueba tu inmobiliaria" banner; no approve/terms/reject affordance.</done>
</task>

<task type="auto">
  <name>Task 2: /inquilino/acuerdos/[id] detail — read + accept (ACUE-01)</name>
  <files>src/app/inquilino/acuerdos/[id]/page.tsx</files>
  <action>
    Create the detail page (`'use client'`) copying the `casos/[caseId]/page.tsx` resolution pattern: `const {
    items, isLoading, error } = useTenantAcuerdos(); const plan = items.find(p => p.planId === params.id);` —
    NO API call with the raw route id (own-only, anti-IDOR). Loading gate (Spinner). Hard aggregate error →
    `EmptyState`. `!plan` → honest "Acuerdo no encontrado" `EmptyState` (covers unknown AND foreign ids, never
    fetched) with a BackLink to `/inquilino/acuerdos`.
    Resolved detail (reuse the `casos/[caseId]` PageShell + BackLink + header + summary-card layout): header with
    a neutral tone badge (`acuerdoStatusToTone/Label`); the plan total `formatCurrency(plan.totalDueCop)`
    verbatim (font-mono tabular-nums); `<CuotaPlanTable installments={plan.installments} locale={locale} />`; a
    `PlanActivityTimeline` from source timestamps ONLY — `{ 'Propuesto' @ plan.offeredAt }` then, when
    `plan.acceptedAt`, `{ 'Aceptado' @ plan.acceptedAt }` (no synthesized/padded milestones). Render
    `<AcuerdoAcceptPanel planId={plan.planId} onAccepted={refetch} />` ONLY when `plan.acceptedAt === null`
    (offered + unaccepted); when already accepted, show a factual accepted state instead. Do NOT add a "Pagar
    cuota" button here (v7-07-06). Neutral tone; no saldo math; es-CO copy.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && P="src/app/inquilino/acuerdos/[id]/page.tsx" && grep -q "useTenantAcuerdos" "$P" && grep -q "find(" "$P" && grep -q "CuotaPlanTable" "$P" && grep -q "AcuerdoAcceptPanel" "$P" && grep -q "acceptedAt" "$P" && test $(grep -vE '^\s*(\*|//|/\*)' "$P" | grep -ciE "\.reduce\(|restante|aprobar acuerdo|apiClient.get.*\$\{params") -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>Detail resolves via own-list .find (no fetch-by-id); renders total verbatim + CuotaPlanTable + offered→accepted timeline; accept panel shown only when acceptedAt===null; honest not-found; no saldo math.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route id → detail resolution | Untrusted route param resolved own-only via `.find` on the JWT-scoped list (never a raw fetch-by-id) |
| signature PNG + OTP code → acuerdosApi.accept | Untrusted signature + code cross into the accept POST; the AGENT persists + runs offered→active + requiresHumanReview() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v7-07-14 | Information Disclosure (IDOR) | acuerdos/[id] resolution | mitigate | `items.find(p => p.planId === id)` on the own JWT-scoped list; NO `apiClient.get(.../${params.id})` fetch-by-id (grep-gated). Unknown/foreign id → honest not-found. |
| T-v7-07-15 | Elevation of Privilege (T-323, A5) | AcuerdoAcceptPanel | mitigate | Accept-only: NO "aprobar" button, NO terms/discount editor, NO reject (grep-gated). Off-policy → the agent's `requiresHumanReview()`; the tenant never decides policy client-side. Banner: "lo aprobó tu inmobiliaria". |
| T-v7-07-16 | Repudiation / Integrity | accept lifecycle | mitigate | Status comes from `CarteraPaymentPlanAcceptResponse` (the agent) — never optimistic. `AcuerdoUnavailableError` → honest "Próximamente", never a fabricated acceptance. Ley 527 OTP token binds the signature. |
| T-v7-07-17 | Spoofing | acuerdo OtpAdapter | mitigate | OTP send/verify go through the BFF (`apiClient`), tenant JWT; a not-live endpoint surfaces unavailable, never a fabricated verificationToken. |
| T-v7-07-SC | Tampering (supply chain) | npm installs | accept | N/A — zero new dependencies this phase (SignaturePad/react-signature-canvas already present). |
</threat_model>

<verification>
- `pnpm -C /Users/nicolasgarcia/rent/mvp-portal-inquilino build` succeeds (new `/inquilino/acuerdos/[id]` route registers).
- `pnpm test` — 0 NEW failures (~7 pre-existing unrelated).
- Anti-IDOR gate: `acuerdos/[id]/page.tsx` resolves via `.find`, no `apiClient.get(...${params.id}...)` fetch-by-id.
- A5/T-323 gate: AcuerdoAcceptPanel + detail (comment-stripped) have 0 `aprobar acuerdo` / `editar término` / `descuento` / `rechazar` / `por qué` / `central`.
- Manual smoke as `tenant`: open an acuerdo detail (empty today → the id resolves from an empty list → honest
  not-found); the accept panel + OTP render, and submit yields an honest "Próximamente" (no fake "aceptado").
</verification>

<success_criteria>
- ACUE-01: the acuerdo detail renders the cuota plan + total verbatim + a source-timestamp timeline, resolved
  own-only (no IDOR), honest not-found.
- ACUE-02: the tenant accepts by signing (reused SignaturePad + generalized OTP), never approving/setting terms;
  accept gated to honest "Próximamente"; status from the agent, no optimistic acceptance.
</success_criteria>

<output>
After completion, create `.planning/phases/v7-07-acuerdos-pago/v7-07-05-SUMMARY.md`.
</output>
