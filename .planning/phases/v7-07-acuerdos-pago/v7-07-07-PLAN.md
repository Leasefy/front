---
phase: v7-07-acuerdos-pago
plan: 07
type: execute
wave: 4
depends_on: [v7-07-01, v7-07-04]
files_modified:
  - src/components/tenant/SolicitarPlanPagoModal.tsx
  - src/app/inquilino/acuerdos/page.tsx
autonomous: true
requirements: [ACUE-04]

must_haves:
  truths:
    - "The tenant can REQUEST a pre-mora payment plan from the acuerdos page — a proposal that feeds the agency approval pipeline. The form PROPOSES intent only (leaseId + optional contact preference); it NEVER sets terms (no amount/cuotas/discount/consequence editor), and a T-323 notice says 'esto lo aprueba tu inmobiliaria' (requiresHumanReview stays agent-side)"
    - "The form asks NO 'por qué la mora' / 'motivo' / 'razón' field (Ley 2300/2023 art. 7) and contains NO centrales-de-riesgo / DataCrédito / TransUnion / 'reporte' copy (Ley 1266/2008 + 2157/2021) — the agency `reportar_centrales` consequence UI is NOT copied into the tenant form"
    - "requestPremoraPlan is GATED: on AcuerdoUnavailableError the modal shows an honest 'Próximamente' notice (form intact, no fabricated plan/radicado); on success it toasts + closes. A 'Solicitar un plan de pago' CTA on /inquilino/acuerdos opens the modal"
  artifacts:
    - path: "src/components/tenant/SolicitarPlanPagoModal.tsx"
      provides: "intent-only request modal (leaseId + optional contact pref) + T-323 'lo aprueba tu inmobiliaria' notice → acuerdosApi.requestPremoraPlan; honest 'Próximamente' on AcuerdoUnavailableError; NO 'por qué'/terms/bureau fields"
      contains: "requestPremoraPlan"
    - path: "src/app/inquilino/acuerdos/page.tsx"
      provides: "'Solicitar un plan de pago' CTA opening SolicitarPlanPagoModal"
      contains: "SolicitarPlanPagoModal"
  key_links:
    - from: "src/components/tenant/SolicitarPlanPagoModal.tsx"
      to: "acuerdosApi.requestPremoraPlan({ leaseId })"
      via: "proposes a plan (no terms); AcuerdoUnavailableError → honest 'Próximamente'; success → toast + close"
      pattern: "requestPremoraPlan"
    - from: "src/app/inquilino/acuerdos/page.tsx"
      to: "SolicitarPlanPagoModal"
      via: "'Solicitar un plan de pago' CTA toggles the modal open"
      pattern: "SolicitarPlanPagoModal"
---

<objective>
Ship ACUE-04: the tenant REQUESTS a pre-mora payment plan — a PROPOSAL that feeds the agency approval pipeline,
never a plan that sets terms or self-approves. The tenant-initiated propose route doesn't exist yet, so the
request is GATED (`AcuerdoUnavailableError` → honest "Próximamente", form intact) — never a fabricated plan. This
is the legal-heaviest form of the phase: no "por qué la mora", no centrales-de-riesgo.

Two honest deliverables:
1. **SolicitarPlanPagoModal (ACUE-04, T-323 + PITFALLS 4/5).** A minimal Dialog composed from the `PayRentModal`
   shell (`AnimatePresence` backdrop `fixed inset-0 z-50`, `useLenis().stop()/start()` per DESIGN §8,
   `data-lenis-prevent` scroll body, `toast`). Props: `{ open; onClose; onRequested?; leaseId?: string }`. The
   form is INTENT-ONLY: it proposes a plan for the tenant's lease (a `leaseId`, resolved from the tenant's
   primary lease when not passed) + an OPTIONAL contact-preference note — and NOTHING ELSE. It has NO amount,
   NO number-of-cuotas, NO first-date, NO discount, and CRUCIALLY NO "consecuencias"/`reportar_centrales`
   selector (that agency field is NOT copied). It asks NO "por qué"/"motivo"/"razón" for the arrears. A factual
   T-323 primary-soft notice: "Estás proponiendo un plan de pago. Los términos los define y aprueba tu
   inmobiliaria." Submit → `acuerdosApi.requestPremoraPlan({ leaseId })`; success → `toast.success` + `onRequested?.()`
   + `onClose()`; `AcuerdoUnavailableError` → honest es "Estamos habilitando las solicitudes de plan de pago.
   Vuelve a intentarlo pronto." (form intact, no fabricated plan/radicado); rethrow-toast other errors. Sentence-case
   buttons. Inline `locale === 'es'` copy.
2. **List CTA (ACUE-04).** In `/inquilino/acuerdos/page.tsx` (from v7-07-04) add a prominent "Solicitar un plan
   de pago" / "Request a payment plan" `Button` (sentence case) in the header and in the empty state, toggling
   `open` for `SolicitarPlanPagoModal`. This is the only mutation entry point on the list surface.

Depends on v7-07-01 (requestPremoraPlan) and v7-07-04 (the acuerdos list page). Wave 4 — touches
`acuerdos/page.tsx` (owned by v7-07-04), disjoint from v7-07-05's files; the executor runs it sequentially in the
shared worktree.

Purpose: ACUE-04 — the tenant proposes a pre-mora plan (never sets terms), honestly, without unlawful arrears
interrogation or bureau copy.
Output: request modal + list CTA.

**Guardrails (non-negotiable):** proposes not sets (NO amount/cuotas/discount/consequence editor — T-323, A5);
NO "por qué"/"motivo"/"razón" field (Ley 2300 art. 7, PITFALLS 4); NO centrales-de-riesgo / DataCrédito /
TransUnion / "reporte" copy — the agency `reportar_centrales` select is NOT copied (Ley 1266/2008 + 2157/2021,
PITFALLS 5); `AcuerdoUnavailableError` → honest "Próximamente" (no fabricated plan/radicado); Lenis-safe modal
(§8); es-CO; sentence-case buttons (§4); zero new npm packages.
</objective>

<execution_context>
@/Users/nicolasgarcia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolasgarcia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/v7-07-acuerdos-pago/v7-07-RESEARCH.md
@.planning/phases/v7-07-acuerdos-pago/v7-07-PATTERNS.md

# READ FIRST: docs/DESIGN.md §8/§17 (Dialog + Lenis stop/start + data-lenis-prevent), §4 (Buttons sentence case),
# §11 (EmptyState). CLAUDE.md: additive, es-CO, zero new npm packages.

# Modal shell + Lenis + submit lifecycle to COPY (the lighter tenant-modal norm — no react-hook-form/zod):
@src/components/tenant/PayRentModal.tsx
# The agency propose form to MIRROR the fail-soft lifecycle — but NOT its terms/consequence fields (do NOT copy reportar_centrales):
@src/app/panel/inmobiliaria/ai/cobranza/acuerdos/page.tsx
# The list page to extend with the CTA (from v7-07-04):
@src/app/inquilino/acuerdos/page.tsx
# Primary-lease source for the leaseId (getActive()[0]):
@src/lib/hooks/useLeases.ts

<interfaces>
<!-- v7-07-01 output consumed here -->
acuerdosApi.requestPremoraPlan(body: { leaseId: string }): Promise<{ requestId: string }>   // throws AcuerdoUnavailableError on 404/403/0
class AcuerdoUnavailableError extends Error
useLeases(): { getActive(): Lease[]; ... }   // primary lease = getActive()[0]
useLenis(): { stop(); start() }   // '@/components/providers/SmoothScroll'

<!-- FORBIDDEN to copy from the agency form (acuerdos/page.tsx): the CONSECUENCIAS select incl. `reportar_centrales`
     (:283), the discount/cuota/first-date term editors. The tenant form is intent-only. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SolicitarPlanPagoModal — intent-only propose (no terms, no "por qué", no bureau) (ACUE-04)</name>
  <files>src/components/tenant/SolicitarPlanPagoModal.tsx</files>
  <action>
    Create the Dialog copying the `PayRentModal` shell: `AnimatePresence` backdrop `fixed inset-0 z-50
    bg-black/50`, `useLenis().stop()/start()` on open (DESIGN §8), `data-lenis-prevent` on the scroll body,
    header/body/footer, `Button isLoading`, `toast`. Props: `{ open: boolean; onClose: () => void; onRequested?:
    () => void; leaseId?: string }`.
    Body: a factual T-323 primary-soft notice ("Estás proponiendo un plan de pago. Los términos los define y
    aprueba tu inmobiliaria."), a short read-only summary of which arriendo the request is for (resolve
    `leaseId` from the prop or the tenant's primary lease via `useLeases().getActive()[0]`), and ONE optional
    free-text "¿Algo que quieras contarle a tu inmobiliaria?" contact-preference note (bounded, OPTIONAL). NO
    amount, NO cuotas count, NO first-date, NO discount, NO "consecuencias"/centrales selector, NO
    "por qué"/"motivo"/"razón" field.
    Submit handler: `await acuerdosApi.requestPremoraPlan({ leaseId })`; success → `toast.success` (es
    "Enviamos tu solicitud. Tu inmobiliaria la revisará."), `onRequested?.()`, `onClose()`. Wrap in try/catch:
    `AcuerdoUnavailableError` → honest toast (es "Estamos habilitando las solicitudes de plan de pago. Vuelve a
    intentarlo pronto.") and DO NOT fabricate a plan/radicado, DO NOT close on the note being lost (keep the form
    intact); rethrow-toast other errors generically. Sentence-case buttons. Inline `locale === 'es'` copy.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && M=src/components/tenant/SolicitarPlanPagoModal.tsx && grep -q "requestPremoraPlan" $M && grep -q "AcuerdoUnavailableError" $M && grep -q "data-lenis-prevent" $M && grep -q "aprueba tu inmobiliaria" $M && test $(grep -vE '^\s*(\*|//|/\*)' $M | grep -ciE "por qu[eé]|motivo|raz[oó]n|central|datacr[eé]dito|transunion|reporte|reportar_centrales|descuento|n[uú]mero de cuotas") -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>SolicitarPlanPagoModal proposes a plan (intent-only leaseId + optional note) with a T-323 notice; honest "Próximamente" on AcuerdoUnavailableError; NO "por qué"/terms/discount/consequence/bureau fields; Lenis-safe; sentence-case buttons.</done>
</task>

<task type="auto">
  <name>Task 2: 'Solicitar un plan de pago' CTA on the acuerdos list (ACUE-04)</name>
  <files>src/app/inquilino/acuerdos/page.tsx</files>
  <action>
    In `/inquilino/acuerdos/page.tsx` (v7-07-04): import `SolicitarPlanPagoModal`, add `const [requestOpen,
    setRequestOpen] = useState(false)`, and render a prominent sentence-case `Button` "Solicitar un plan de pago"
    / "Request a payment plan" in the header (and repeat it in the empty state's `EmptyState` action) that calls
    `setRequestOpen(true)`. Render `<SolicitarPlanPagoModal open={requestOpen} onClose={() =>
    setRequestOpen(false)} onRequested={refetch} />` (pass the primary `leaseId` if the page already has it).
    Do NOT add any terms/consequence UI to the list; do NOT touch the row rendering or the detail wiring. Keep
    the neutral, no-bureau posture.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && P=src/app/inquilino/acuerdos/page.tsx && grep -q "SolicitarPlanPagoModal" $P && grep -qE "Solicitar un plan|Request a payment plan" $P && test $(grep -vE '^\s*(\*|//|/\*)' $P | grep -ciE "por qu[eé]|central|reportar_centrales") -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>A "Solicitar un plan de pago" CTA (header + empty state) opens SolicitarPlanPagoModal on /inquilino/acuerdos; no terms/consequence/bureau UI added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant form input → acuerdosApi.requestPremoraPlan | Untrusted intent (leaseId + optional note) crosses; the agency + agent compute + approve terms |
| request → agency approval pipeline | The tenant proposes; the agent's requiresHumanReview() + the agency operator decide (never the tenant) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v7-07-23 | Elevation of Privilege (T-323, A5) | SolicitarPlanPagoModal | mitigate | Intent-only: NO amount/cuotas/discount/first-date/consequence editor (grep-gated). The tenant proposes; the agent's `requiresHumanReview()` + the agency approve. T-323 notice: "lo aprueba tu inmobiliaria". |
| T-v7-07-24 | Compliance (Ley 2300 art. 7) | request form fields | mitigate | NO "por qué"/"motivo"/"razón" field for the arrears (grep-gated to 0). Only a leaseId + an OPTIONAL neutral note. |
| T-v7-07-25 | Compliance (Ley 1266/2008 + 2157/2021) | request form copy | mitigate | NO centrales-de-riesgo / DataCrédito / TransUnion / "reporte" / `reportar_centrales` copy (grep-gated to 0); the agency consequence select is NOT copied. Any bureau reference requires the agent's 3-party gate (out of scope). |
| T-v7-07-26 | Tampering | requestPremoraPlan lifecycle | mitigate | `AcuerdoUnavailableError` → honest "Próximamente" (form intact); NEVER a fabricated plan/radicado on a real-tenant path (PITFALLS 5). |
| T-v7-07-SC | Tampering (supply chain) | npm installs | accept | N/A — zero new dependencies this phase. |
</threat_model>

<verification>
- `pnpm -C /Users/nicolasgarcia/rent/mvp-portal-inquilino build` succeeds.
- `pnpm test` — 0 NEW failures (~7 pre-existing unrelated).
- Ley-2300/1266 gate: `SolicitarPlanPagoModal.tsx` + `acuerdos/page.tsx` (comment-stripped) have 0
  `por qué|motivo|razón|central|datacrédito|transunion|reporte|reportar_centrales|descuento|número de cuotas`.
- Manual smoke as `tenant`: "Solicitar un plan de pago" opens the modal; submit yields an honest "Próximamente"
  (no fabricated plan); the T-323 "lo aprueba tu inmobiliaria" notice is visible; no arrears-reason / bureau copy.
</verification>

<success_criteria>
- ACUE-04: the tenant proposes a pre-mora plan (intent-only) that feeds the agency approval pipeline — never
  setting terms, never asking "por qué" the mora, never mentioning centrales de riesgo — gated to honest
  "Próximamente" until the tenant-initiated propose route ships.
</success_criteria>

<output>
After completion, create `.planning/phases/v7-07-acuerdos-pago/v7-07-07-SUMMARY.md`.
</output>
