---
phase: v7-02-documentos-arriendo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/api/documents.types.ts
  - src/lib/api/documents.service.ts
  - src/lib/hooks/useDocuments.ts
autonomous: true
requirements: [DOCU-04]

must_haves:
  truths:
    - "A signed/expiring URL contract exists for generic documents: documentsApi.getSignedUrl(docId) → { url, expiresAt }, modeled 1:1 on ContractSignedPdf"
    - "A useSignedDocUrl hook mirrors useSignedPdfUrl (enabled gate, {url,isLoading,error,refetch})"
    - "The old getDownloadUrl raw-URL anti-pattern is disclosed as a backend gap (JSDoc/TODO), not silently kept as the download path"
    - "A per-purpose consent contract exists (separate booleans, unchecked-default factory), plus a recordConsent api-client contract — no persistence fabricated"
  artifacts:
    - path: "src/lib/api/documents.service.ts"
      provides: "getSignedUrl signed-URL contract + recordConsent stub; getDownloadUrl deprecated with disclosure"
      contains: "getSignedUrl"
    - path: "src/lib/hooks/useDocuments.ts"
      provides: "useSignedDocUrl hook cloned from useSignedPdfUrl"
      contains: "useSignedDocUrl"
    - path: "src/lib/api/documents.types.ts"
      provides: "DocumentSignedUrl + DocumentConsent types + createEmptyDocumentConsent factory"
      contains: "DocumentSignedUrl"
  key_links:
    - from: "src/lib/hooks/useDocuments.ts"
      to: "documentsApi.getSignedUrl"
      via: "GET /documents/:id/signed-url"
      pattern: "getSignedUrl"
    - from: "src/lib/api/documents.service.ts"
      to: "DocumentSignedUrl"
      via: "{ url, expiresAt } contract (anti-IDOR)"
      pattern: "expiresAt"
---

<objective>
Build the DOCU-04 **api-client backbone** — the signed/expiring URL contract, its consumer
hook, and the per-purpose consent contract — as pure service/type/hook files with NO page
edit, so this runs in parallel with v7-02-01 (zero file overlap). The page that consumes
these (IDOR hardening, consent gate, ARCO delete) is v7-02-03.

Model everything 1:1 on the already-real contracts chain: `ContractSignedPdf { url, expiresAt }`
(`contracts.types.ts:176`), `getSignedPdfUrl` (`contracts.service.ts:214`), `useSignedPdfUrl`
(`useContracts.ts:182`). The consent contract mirrors the avalúo 3-boolean unchecked-default
model (`avaluo.ts:65-116`).

Purpose: Satisfy the DOCU-04 backbone — a signed-URL contract (anti-IDOR), a consent-by-purpose
contract, and honest disclosure of the raw-URL `/documents` gap.
Output: `documents.types.ts`, `documents.service.ts`, `src/lib/hooks/useDocuments.ts`.

**Honesty guardrail (non-negotiable):** the frontend cannot mint signed URLs. Define + consume
the `{ url, expiresAt }` contract, but where the backend still returns a raw persistent Supabase
URL (today's `/documents` behavior), DISCLOSE it as a backend dependency in JSDoc — do NOT claim
"sin IDOR" is satisfied by the frontend alone.
</objective>

<execution_context>
@/Users/nicolasgarcia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolasgarcia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/v7-02-documentos-arriendo/v7-02-RESEARCH.md
@.planning/phases/v7-02-documentos-arriendo/v7-02-PATTERNS.md

# Files to modify / create:
@src/lib/api/documents.service.ts
@src/lib/api/documents.types.ts

# THE analogs to clone (copy shape verbatim):
@src/lib/api/contracts.service.ts
@src/lib/hooks/useContracts.ts
# api-client contract + 403/404 → [] tolerance:
@src/lib/api/client.ts
# Consent-by-purpose model (3 separate booleans, all default false, policyVersion):
@src/lib/types/avaluo.ts

<interfaces>
<!-- Clone target 1 — the signed/expiring URL contract (contracts.types.ts:176) -->
export interface ContractSignedPdf { url: string; expiresAt: string; }
// contracts.service.ts:214
async getSignedPdfUrl(id: string): Promise<ContractSignedPdf> { return apiClient.get(`/contracts/${id}/pdf`); }

<!-- Clone target 2 — the hook (useContracts.ts:182): -->
export function useSignedPdfUrl(id, { enabled }): { url; isLoading; error; refetch }

<!-- Clone target 3 — consent model (avaluo.ts): 3 separate booleans, all default false; policyVersion string -->
// createEmptyAvaluoFormData() sets purposeAvaluo/purposeDataset/purposeContacto = false — never pre-tick.

<!-- Anti-pattern to deprecate (documents.service.ts:106) -->
getDownloadUrl(doc): string { return doc.url; }  // raw persistent Supabase URL, no expiry — the IDOR-shaped gap
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DocumentSignedUrl + DocumentConsent types</name>
  <files>src/lib/api/documents.types.ts</files>
  <action>
    Add an exported `DocumentSignedUrl` interface with exactly `{ url: string; expiresAt: string }`
    — the anti-IDOR shape, modeled 1:1 on `ContractSignedPdf` (the `expiresAt` is the expiry
    guarantee the UI surfaces). Add a JSDoc noting it mirrors `ContractSignedPdf` and that
    `expiresAt` is the short-lived expiry.

    Add the per-purpose consent contract for document access (Ley 1581, mirroring the avalúo model):
    an exported `DocumentConsent` interface with **separate** booleans, one purpose each, each
    documented with its Ley 1581 purpose string:
      - `purposeDocAccess: boolean` — MANDATORY: process/serve the tenant's lease documents so they
        can consult/download them.
      - `purposeThirdPartyShare: boolean` — OPTIONAL: share generated certificates (paz y salvo /
        retención) with third parties the tenant designates (e.g. a new arrendador).
      - `policyVersion: string` — the accepted privacy-policy version, stored server-side for SIC audit.
    Export a `createEmptyDocumentConsent(policyVersion: string): DocumentConsent` factory that
    defaults **both** booleans to `false` (never pre-tick) — the exact DOCU-04 requirement.
    Keep the existing `BackendDocumentFull` / `UploadDocumentDto` types intact.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && grep -q "DocumentSignedUrl" src/lib/api/documents.types.ts && grep -q "expiresAt" src/lib/api/documents.types.ts && grep -q "createEmptyDocumentConsent" src/lib/api/documents.types.ts && echo GATE_OK</automated>
  </verify>
  <done>DocumentSignedUrl {url,expiresAt} + DocumentConsent (2 separate booleans + policyVersion) + createEmptyDocumentConsent factory (both false) exported; existing types intact.</done>
</task>

<task type="auto">
  <name>Task 2: Add getSignedUrl + recordConsent; deprecate raw getDownloadUrl</name>
  <files>src/lib/api/documents.service.ts</files>
  <action>
    Import `DocumentSignedUrl`, `DocumentConsent` from `./documents.types`.

    Add `async getSignedUrl(docId: string): Promise<DocumentSignedUrl>` → `apiClient.get(
    `/documents/${docId}/signed-url`)`, modeled 1:1 on `contractsApi.getSignedPdfUrl`. Wrap in the
    canonical `403/404 → throw` handling consistent with the codebase; do NOT invent a fake signed
    URL if the endpoint is missing. JSDoc it: returns a short-lived `{ url, expiresAt }` — the
    anti-IDOR contract; the backend mints and ownership-checks it.

    DISCLOSE the gap on the existing `getDownloadUrl(doc)` (line 106): keep it for back-compat but
    add a `@deprecated` JSDoc stating it returns a **raw persistent Supabase URL with no expiry and
    no ownership check** — an IDOR-shaped exposure — and that callers on tenant-reachable paths MUST
    migrate to `getSignedUrl`. This is the honest "backend gap" disclosure required by DOCU-04; do
    not delete it (other non-tenant callers may exist) and do not claim it is signed.

    Add `async recordConsent(docId: string, consent: DocumentConsent): Promise<void>` →
    `apiClient.post(`/documents/${docId}/consent`, consent)`, wrapped so a missing endpoint
    (403/404) degrades to a resolved no-op (frontend-first: the UI still enforces unchecked-default;
    server persistence is a disclosed backend dependency). JSDoc that the SIC-audit persistence is
    backend-owned. Keep `getById`, `getByApplication`, `upload`, `delete` unchanged.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && grep -q "getSignedUrl" src/lib/api/documents.service.ts && grep -q "signed-url" src/lib/api/documents.service.ts && grep -q "recordConsent" src/lib/api/documents.service.ts && grep -qi "deprecated" src/lib/api/documents.service.ts && echo GATE_OK</automated>
  </verify>
  <done>getSignedUrl → GET /documents/:id/signed-url returns {url,expiresAt}; recordConsent contract stub; getDownloadUrl marked @deprecated with raw-URL/IDOR disclosure; no fake signed URL invented.</done>
</task>

<task type="auto">
  <name>Task 3: useSignedDocUrl hook (clone of useSignedPdfUrl)</name>
  <files>src/lib/hooks/useDocuments.ts</files>
  <action>
    Create `src/lib/hooks/useDocuments.ts`. Export `useSignedDocUrl(docId: string | null | undefined,
    { enabled }: { enabled: boolean })` cloned line-for-line in shape from `useSignedPdfUrl`
    (`useContracts.ts:182-208`): `useState` for `url`/`isLoading`/`error`; a `load` callback that
    early-returns (clearing url) when `!docId || !enabled`; sets loading, calls
    `documentsApi.getSignedUrl(docId)`, sets `url` from `result.url`, catches into `error`; a
    `useEffect(() => { load(); }, [load])`; returns `{ url, isLoading, error, refetch: load }`. Import
    `documentsApi` from `@/lib/api/documents.service`. The `enabled` gate exists so the page only
    fetches a signed URL at open/download time (not for every card on mount).
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && grep -q "export function useSignedDocUrl" src/lib/hooks/useDocuments.ts && grep -q "getSignedUrl" src/lib/hooks/useDocuments.ts && grep -q "refetch" src/lib/hooks/useDocuments.ts && echo GATE_OK</automated>
  </verify>
  <done>useSignedDocUrl exported, mirrors useSignedPdfUrl (enabled gate, {url,isLoading,error,refetch}), consumes documentsApi.getSignedUrl.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant browser → NestJS `/documents/:id/signed-url` | Short-lived signed URL mint; server ownership check (backend dependency) |
| tenant browser → NestJS `/documents/:id/consent` | Consent record persistence for SIC audit (backend dependency) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v7-02-04 | Information disclosure (IDOR) | `getSignedUrl` contract vs raw `getDownloadUrl` | mitigate (partial) + disclose | Define `{url,expiresAt}` contract consumed by the page (v7-02-03); mark `getDownloadUrl` @deprecated with explicit raw-URL disclosure. FULL IDOR closure needs the backend to sign `/documents/:id` — disclosed as external dependency, not faked |
| T-v7-02-05 | Repudiation / compliance | `recordConsent` | mitigate | Consent contract mirrors avalúo (per-purpose, unchecked default); persistence is backend-owned SIC-audit store, disclosed as dependency |
| T-v7-02-06 | Tampering (supply chain) | npm installs | accept | Zero new packages (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `pnpm -C /Users/nicolasgarcia/rent/mvp-portal-inquilino build` succeeds.
- `pnpm test` — 0 NEW failures (≈7 pre-existing unrelated — see v7-01 deferred-items.md).
- Grep gates: `getSignedUrl` + `signed-url` + `recordConsent` + `deprecated` in documents.service.ts;
  `DocumentSignedUrl` + `createEmptyDocumentConsent` in documents.types.ts; `useSignedDocUrl` in
  the new hook file.
- Type check: the new contract types compile and are importable (verified by `pnpm build`).
</verification>

<success_criteria>
- DOCU-04 (backbone): a signed/expiring `{ url, expiresAt }` documents contract + consumer hook +
  per-purpose consent contract exist, modeled on the real contracts/avalúo chains; the raw-URL
  `getDownloadUrl` anti-pattern is honestly disclosed as a backend gap — no fabricated signing.
</success_criteria>

<output>
After completion, create `.planning/phases/v7-02-documentos-arriendo/v7-02-02-SUMMARY.md`.
</output>
