# v1 — front (rent/mvp)

**Estado:** ABIERTA
**Tag de freeze:** _(pendiente — `git tag v1 <commit>` cuando Victor confirme el corte)_
**Rama de integración:** `Main`
**Emparejado con:** `@leasefy/cadence` v1 (commit `212354e`)

## PRs — orden de merge (bottom‑up, no saltarse el orden)

| # | PR / commit | Qué entra |
|---|-----|-----------|
| 1 | commit `1d43fa42` (checkpoint directo sobre `feat/leasefy-ds-redesign`, sin PR) | Chat: backend `snapshot`→`ChatDataCard` (SSE `onSnapshot` + one-shot) + §17G @-mención en ambos composers (hero+docked, nav teclado) + fix shimmer (`StepAIReview`). Base: migración `@leasefy/ui`→`@leasefy/cadence` + brand redesign de toda la plataforma. Bump `@leasefy/cadence` → v1.0.52. |
| 2 | commit `41e6e2d9` (checkpoint directo sobre `feat/leasefy-ds-redesign`, sin PR) | UI/DS: `PlanSidebar` reconstruida a la anatomía completa de cadence §Navigation (workspace switcher + search→⌘K + invite/upgrade); shell con bg unificado body+header+sidebar (`#FBFAF9` light / `#141310` dark); fix indicador de tabs con icono (`inline-flex`); fix dark-mode del botón primary (`--primary-fg` ink + barrido de 29 `text-white`→`text-primary-fg` en 22 archivos app/panel). |
| 3 | commits `2ce29d7d` · `c3a9e511` · `eaf2c0aa` · `390f66af` (checkpoints directos, sin PR) | Dark polish + pre-integración: shell dark unificado a `#141310`; header/footer de tabla con gris adaptativo (shim); item activo del sidebar con icono duotone en primary. **fix(security):** revierte el `DEV-LOCAL-OVERRIDE` (panel inmobiliaria vuelve a solo `agency`). **chore:** elimina 6 rutas de preview de dev. Requiere `cd mvp && pnpm install` (cadence → v1.0.54). |
| 4 | **PR #45** `feat/landing-v2-port` → `feat/leasefy-ds-redesign` (5 commits) | **Landing v2:** port de la landing standalone (Cohere-style, diseño aprobado) a **código real** React/Next en ruta dedicada `/landing-v2` (noindex, **aditivo** — no toca `/` ni `landing-nueva`). Home (shader WebGL del hero, secciones scroll-driven, header + mega-menú + menú móvil, footer), **8 páginas de producto SSG** (`/landing-v2/p/[slug]`), blog (filtro por tabs) y contacto (form→mailto). Fuentes fieles Inter Tight + Inter + IBM Plex Mono (`next/font`); azul `#1a40ff` ya coincide con cadence. Assets en `public/landing-v2/assets/` (~12MB, incl. webp de cierre ~8.4MB lazy). `tsc` 0 + `next build` ✓ (home/blog/contacto estáticas, 8 slugs SSG). **Pendiente:** swap de `/` (PR aparte, cuando se apruebe) + optimizar/`<video>` el webp del cierre. |

## Ledger de Victor — front

- **Migraciones:** _(si aplica)_
- **Secrets / env:** _(setear antes de deploy)_
- **Flags:** _(qué prender / dejar OFF)_
- **Notas:** redesign commiteado en `feat/leasefy-ds-redesign`. Pre-integración YA hecho (commit `390f66af`): `DEV-LOCAL-OVERRIDE` revertido (panel inmobiliaria solo `agency`) + 6 rutas de preview de dev eliminadas. **Quedan sin tocar** `agentes-preview`, `landing-nueva`, `avaluo-ia` (posibles features reales — confirmar si entran o se quitan). `public/fonts/Satoshi-*.woff2` **eliminadas** (sin referencias en `src`/`public` → cleanup seguro). Requiere `cd mvp && pnpm install` (cadence → v1.0.54).

## Smoke (antes de marcar INTEGRADA)

- [ ] `next lint` + build OK en cada merge
- [ ] Smoke de browser en las vistas tocadas
