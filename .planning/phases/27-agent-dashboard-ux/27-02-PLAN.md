---
phase: 27-agent-dashboard-ux
plan: 02
type: execute
wave: 1
depends_on: ["27-01"]
files_modified:
  - src/components/inmobiliaria/ai/AIAgentExecutionPanel.tsx
  - src/components/inmobiliaria/ai/AIAgentDetailSidebar.tsx
  - src/app/panel/inmobiliaria/page.tsx
autonomous: true

must_haves:
  truths:
    - "Clicking an activity item with executionTrace opens full-screen execution panel"
    - "Execution panel shows split view: timeline left, computer view right"
    - "Clicking '¿Cómo funciona?' on agent card opens detail sidebar"
    - "Detail sidebar shows step-by-step pipeline, triggers, escalation, impact"
    - "Agent section on dashboard is wrapped with FeatureGate for ai-agents feature"
  artifacts:
    - path: "src/components/inmobiliaria/ai/AIAgentExecutionPanel.tsx"
      provides: "Full-screen Manus-style execution viewer"
    - path: "src/components/inmobiliaria/ai/AIAgentDetailSidebar.tsx"
      provides: "Agent explanation sidebar with pipeline steps"
  key_links:
    - from: "AIAgentActivityFeed.tsx"
      to: "AIAgentExecutionPanel.tsx"
      via: "selectedActivity state opens panel"
    - from: "AIAgentCard.tsx"
      to: "AIAgentDetailSidebar.tsx"
      via: "showDetail state opens sidebar"
    - from: "page.tsx (dashboard)"
      to: "FeatureGate"
      via: "wraps AgentSection with feature='ai-agents'"
---

<objective>
Polish execution panel and detail sidebar, then wrap the agent dashboard section with FeatureGate so AI agents are only visible on Flex plans.

Purpose: Complete the agentic UX and gate it to Flex plans — the key differentiator.
Output: Polished execution panel + detail sidebar + plan-gated agent section.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-plan-gating/26-01-SUMMARY.md

@src/components/inmobiliaria/ai/AIAgentExecutionPanel.tsx
@src/components/inmobiliaria/ai/AIAgentDetailSidebar.tsx
@src/components/inmobiliaria/UpgradePrompt.tsx
@src/app/panel/inmobiliaria/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish execution panel and detail sidebar</name>
  <files>src/components/inmobiliaria/ai/AIAgentExecutionPanel.tsx, src/components/inmobiliaria/ai/AIAgentDetailSidebar.tsx</files>
  <action>
    Read both files and verify they're production-ready:

    1. AIAgentExecutionPanel.tsx:
       - Full-screen layout with split view (timeline left, computer view right)
       - Progress bar in header
       - Steps are clickable to show computer view
       - Uses neutral/sobrio colors (no bright colors except for status indicators)
       - Close button works
       - Uses createPortal to render in document.body (avoids stacking context issues)

    2. AIAgentDetailSidebar.tsx:
       - Uses createPortal to render in document.body
       - Body scroll is locked when open (position: fixed technique)
       - Scroll works inside sidebar (overscroll-none)
       - Pipeline steps use neutral icon boxes
       - Triggers and escalation sections present
       - Impact card at bottom
       - Uses neutral color scheme (the accentColor/accentBg variables should all be neutral)
       - Close on Escape key works

    Fix any remaining color issues (any leftover emerald/blue/indigo in these panels should be neutral, except active dot which stays emerald).

    Run build.
  </action>
  <verify>npx next build --no-lint | grep "Compiled"</verify>
  <done>Both panels are polished, use neutral colors, scroll correctly, portal correctly</done>
</task>

<task type="auto">
  <name>Task 2: Wrap agent section with FeatureGate</name>
  <files>src/app/panel/inmobiliaria/page.tsx</files>
  <action>
    Import FeatureGate from '@/components/inmobiliaria/UpgradePrompt'.

    Wrap the AgentSection component call in the dashboard with:
    ```tsx
    <FeatureGate feature="ai-agents">
      <AgentSection agents={aiAgents} activities={aiActivities} metricsMap={agentMetricsMap} />
    </FeatureGate>
    ```

    This means:
    - Flex plan users see the full agent dashboard
    - Subscription plan users see an upgrade prompt encouraging them to switch to Flex
    - The upgrade prompt will say something like "Agentes AI disponibles en planes Flex"

    Since the default plan type is 'flex' (from Phase 26 decision), the agent section will show by default in demo mode.

    To test gating: in browser console, run `localStorage.setItem('leasefy_agency_plan_type', 'subscription')` and refresh — should see upgrade prompt instead of agents.

    Run build.
  </action>
  <verify>npx next build --no-lint | grep "Compiled"</verify>
  <done>Agent section gated with FeatureGate, shows for flex plans, shows upgrade prompt for subscription plans</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx next build --no-lint` compiles successfully
- [ ] Execution panel opens on activity click
- [ ] Detail sidebar opens on "¿Cómo funciona?" click
- [ ] Agent section wrapped with FeatureGate
- [ ] Default (flex) shows agents, subscription shows upgrade prompt
</verification>

<success_criteria>

- Execution panel and detail sidebar are polished and production-ready
- Agent section is gated to flex plans via FeatureGate
- All portals work correctly (no stacking context issues)
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/27-agent-dashboard-ux/27-02-SUMMARY.md`
</output>
