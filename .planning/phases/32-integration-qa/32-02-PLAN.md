---
phase: 32-integration-qa
plan: 02
type: execute
wave: 1
depends_on: ["32-01"]
files_modified:
  - src/app/panel/inmobiliaria/ai/page.tsx
  - src/app/panel/inmobiliaria/layout.tsx
autonomous: true

must_haves:
  truths:
    - "Agent Hub page (/panel/inmobiliaria/ai) renders without errors"
    - "All sidebar nav items link to valid pages"
    - "No broken imports across all inmobiliaria pages"
    - "Build compiles successfully with no new warnings"
  artifacts:
    - path: "src/app/panel/inmobiliaria/ai/page.tsx"
      provides: "Agent Hub page works correctly"
    - path: "src/app/panel/inmobiliaria/layout.tsx"
      provides: "Sidebar with all nav items including Agentes AI"
  key_links:
    - from: "layout.tsx"
      to: "ai/page.tsx"
      via: "nav item href='/panel/inmobiliaria/ai'"
---

<objective>
Final QA — verify all pages compile, no broken links, edge cases handled.

Purpose: Ensure the entire v5.0 milestone is production-ready.
Output: Clean build, all pages verified.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-integration-qa/32-01-SUMMARY.md

@src/app/panel/inmobiliaria/layout.tsx
@src/app/panel/inmobiliaria/ai/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full build verification and link audit</name>
  <files>src/app/panel/inmobiliaria/ai/page.tsx, src/app/panel/inmobiliaria/layout.tsx</files>
  <action>
    1. Run `npx next build --no-lint` and capture output. Verify "Compiled successfully".

    2. Check that all inmobiliaria routes have valid page.tsx files:
       - /panel/inmobiliaria (dashboard)
       - /panel/inmobiliaria/ai (agent hub)
       - /panel/inmobiliaria/propietarios
       - /panel/inmobiliaria/portafolio
       - /panel/inmobiliaria/pipeline
       - /panel/inmobiliaria/agentes
       - /panel/inmobiliaria/cobros
       - /panel/inmobiliaria/dispersiones
       - /panel/inmobiliaria/operaciones
       - /panel/inmobiliaria/documentos
       - /panel/inmobiliaria/reportes
       - /panel/inmobiliaria/analytics
       - /panel/inmobiliaria/mensajes
       - /panel/inmobiliaria/configuracion
       - /panel/inmobiliaria/perfil

    3. Check sidebar layout.tsx has all nav items pointing to valid routes.

    4. Verify no TypeScript import errors by checking build output for "Cannot find module" patterns.

    5. If any issues found, fix them. If all clean, document the clean state.
  </action>
  <verify>npx next build --no-lint | grep "Compiled" shows "Compiled successfully"</verify>
  <done>Full build passes, all routes exist, no broken imports</done>
</task>

<task type="auto">
  <name>Task 2: Verify agent hub and pricing modal edge cases</name>
  <files>src/app/panel/inmobiliaria/ai/page.tsx</files>
  <action>
    Read the agent hub page and verify:

    1. Active agents section renders with correct data
    2. Coming soon agents section renders
    3. Activity feed renders
    4. No hardcoded indigo/blue/green colors (should all be neutral per decision)
    5. Page imports compile correctly

    Also verify the pricing modal import in PlanHeader doesn't cause issues when AgencyPricingModal is not needed (non-agency users). The modal should only render when user?.role === 'agency'.

    Run final build to confirm everything is clean.
  </action>
  <verify>npx next build --no-lint | grep "Compiled"</verify>
  <done>All edge cases handled, final build clean</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx next build --no-lint` compiles successfully
- [ ] All inmobiliaria routes have valid page files
- [ ] No broken imports
- [ ] Agent hub page renders correctly
- [ ] Pricing modal doesn't break non-agency users
</verification>

<success_criteria>

- Clean build with no new errors
- All pages accessible via sidebar navigation
- No edge case crashes
- v5.0 milestone is production-ready
</success_criteria>

<output>
After completion, create `.planning/phases/32-integration-qa/32-02-SUMMARY.md`
</output>
