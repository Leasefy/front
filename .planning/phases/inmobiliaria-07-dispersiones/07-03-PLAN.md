---
phase: inmobiliaria-07-dispersiones
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - src/components/inmobiliaria/DispersionWizard.tsx
  - src/app/panel/inmobiliaria/dispersiones/page.tsx
  - src/app/panel/inmobiliaria/dispersiones/generar/page.tsx
  - src/components/inmobiliaria/index.ts
autonomous: true

must_haves:
  truths:
    - "User can see full dispersiones page with tabs and filters"
    - "User can view dispersion detail in modal"
    - "User can generate dispersiones for a month"
    - "User can process individual or all dispersions"
    - "User can view and download extracto from page"
  artifacts:
    - path: "src/components/inmobiliaria/DispersionWizard.tsx"
      provides: "Wizard to generate monthly dispersions"
      min_lines: 350
    - path: "src/app/panel/inmobiliaria/dispersiones/page.tsx"
      provides: "Dispersiones management page"
      min_lines: 300
    - path: "src/app/panel/inmobiliaria/dispersiones/generar/page.tsx"
      provides: "Generate dispersiones page with wizard"
      min_lines: 100
  key_links:
    - from: "dispersiones/page.tsx"
      to: "MOCK_DISPERSIONES"
      via: "import from mock-inmobiliaria"
      pattern: "MOCK_DISPERSIONES"
    - from: "DispersionWizard"
      to: "calculateDispersionSummary"
      via: "import from mock-inmobiliaria"
      pattern: "calculateDispersionSummary"
---

<objective>
Create the Dispersiones page with DispersionWizard component for generating monthly disbursements.

Purpose: Provide a complete dispersiones management interface with generation, filtering, and processing.
Output: Full dispersiones page at /panel/inmobiliaria/dispersiones with wizard at /generar.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@docs/ROADMAP-INMOBILIARIA.md

# Prior plan context
@.planning/phases/inmobiliaria-07-dispersiones/07-01-PLAN.md
@.planning/phases/inmobiliaria-07-dispersiones/07-02-PLAN.md

# Source files
@src/lib/types/inmobiliaria.ts
@src/lib/data/mock-inmobiliaria.ts
@src/components/inmobiliaria/index.ts

# Existing page patterns
@src/app/panel/inmobiliaria/cobros/page.tsx
@src/app/panel/inmobiliaria/portafolio/nuevo/page.tsx (for wizard pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DispersionWizard Component</name>
  <files>src/components/inmobiliaria/DispersionWizard.tsx</files>
  <action>
Create DispersionWizard component for generating monthly dispersions (6 steps):

**Step 1: Seleccionar Mes**
- Month/year picker
- Show if dispersiones already exist for month
- Warning if trying to regenerate
- "Continuar" button

**Step 2: Revisar Cobros Recibidos**
- List cobros received and paid for the month
- Group by propietario
- Show totals per propietario
- Expandable to see individual properties
- "Continuar" button

**Step 3: Calcular Comisiones**
- Show commission per property
- Use ComisionDesglose component
- Allow override of commission % (future feature - disabled for now)
- Show agency total commission
- "Continuar" button

**Step 4: Generar Netos**
- Summary of net amounts per propietario
- Large cards with propietario name, net amount, property count
- Total to disburse
- "Generar Dispersiones" button

**Step 5: Aprobar Dispersiones**
- List generated dispersiones (pending status)
- Checkbox to select for approval
- "Seleccionar todas" option
- Approve button (changes status to 'processing')

**Step 6: Confirmación**
- Success message with count of dispersiones created
- Summary of total disbursed
- Links to view dispersiones or process payments
- "Volver a Dispersiones" button

Props interface:
```typescript
interface DispersionWizardProps {
  initialMonth?: string;
  onComplete?: (dispersiones: Dispersion[]) => void;
  onCancel?: () => void;
}
```

Use ConsignacionWizard pattern for step navigation.
Use WizardProgress component for step indicator.
Store wizard state in component state (not context - simpler for mock).
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>DispersionWizard guides through 6-step generation process</done>
</task>

<task type="auto">
  <name>Task 2: Create Dispersiones Page</name>
  <files>src/app/panel/inmobiliaria/dispersiones/page.tsx</files>
  <action>
Create dispersiones/page.tsx following cobros/page.tsx pattern:

1. Header Section:
   - Title: "Dispersiones a Propietarios"
   - Breadcrumb: Inmobiliaria > Dispersiones
   - Month selector (prominent)
   - "Generar Dispersiones" button (primary, links to /generar)

2. Summary Section:
   - DispersionResumen component with month summary
   - Stats: Total a dispersar, Comisiones, Pendientes, Completadas

3. Filter Section:
   - DispersionFilters component
   - Status tabs: Todas, Pendientes, Procesando, Completadas, Fallidas
   - Propietario dropdown
   - Search input

4. Table/Grid Section:
   - DispersionTable with filtered dispersiones
   - Toggle between table and card grid view
   - Pagination if > 20 items

5. Modals/Drawers:
   - DispersionDetail (controlled by selectedDispersion state)
   - ExtractoPropietario modal (for viewing extracto)

6. State Management:
   - dispersiones: filtered MOCK_DISPERSIONES for selected month
   - filters: DispersionFiltersState
   - selectedDispersion: for detail modal
   - extractoDispersion: for extracto modal
   - viewMode: 'table' | 'cards'

7. Handlers:
   - handleDispersionClick: open detail modal
   - handleProcessDispersion: update status to processing, then completed
   - handleViewExtracto: open extracto modal
   - handleFilterChange: update filter state
   - handleDownloadPDF: trigger PDF download

8. Process Flow (mock):
   - "Procesar" changes status to 'processing'
   - After 1s delay, changes to 'completed' with mock transfer reference
   - Toast shows success message

Use MOCK_DISPERSIONES, MOCK_PROPIETARIOS from mock-inmobiliaria.
Use calculateDispersionSummary helper for summary data.
  </action>
  <verify>pnpm build</verify>
  <done>Dispersiones page loads with filters, table, and modals</done>
</task>

<task type="auto">
  <name>Task 3: Create Generate Dispersiones Page</name>
  <files>src/app/panel/inmobiliaria/dispersiones/generar/page.tsx</files>
  <action>
Create generar/page.tsx as wrapper for DispersionWizard:

1. Page Structure:
   - Header with back button to /dispersiones
   - Title: "Generar Dispersiones del Mes"
   - DispersionWizard component

2. Navigation:
   - Back button in header
   - onComplete navigates to /dispersiones with success toast
   - onCancel navigates back to /dispersiones

3. Layout:
   - Centered content (max-w-4xl)
   - Card wrapper for wizard
   - Clean, focused layout

```typescript
'use client';

import { useRouter } from 'next/navigation';
import { DispersionWizard } from '@/components/inmobiliaria';
import { toast } from 'sonner';

export default function GenerarDispersionesPage() {
  const router = useRouter();

  const handleComplete = () => {
    toast.success('Dispersiones generadas correctamente');
    router.push('/panel/inmobiliaria/dispersiones');
  };

  const handleCancel = () => {
    router.push('/panel/inmobiliaria/dispersiones');
  };

  return (
    <div className="container max-w-4xl py-8">
      <DispersionWizard
        onComplete={handleComplete}
        onCancel={handleCancel}
      />
    </div>
  );
}
```
  </action>
  <verify>pnpm build</verify>
  <done>Generate page wraps wizard with navigation</done>
</task>

<task type="auto">
  <name>Task 4: Update Barrel Export and Navigation</name>
  <files>src/components/inmobiliaria/index.ts</files>
  <action>
1. Add export for DispersionWizard:
```typescript
export { DispersionWizard } from './DispersionWizard';
```

2. Check if sidebar link exists for /panel/inmobiliaria/dispersiones:
   - If not present, add to navigation
   - Icon: BankNote or ArrowsLeftRight from Phosphor
   - Label: "Dispersiones"
   - Position after Cobros

3. Verify all Dispersion components are exported:
   - DispersionCard (+ Compact)
   - DispersionTable
   - DispersionFilters (+ State type)
   - DispersionDetail
   - ComisionDesglose
   - ExtractoPropietario
   - DispersionResumen
   - DispersionWizard
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>All components exported, navigation updated</done>
</task>

<task type="auto">
  <name>Task 5: Final Verification</name>
  <files>N/A - verification only</files>
  <action>
Run final verification:

1. Build check:
   ```bash
   pnpm build
   ```

2. Navigate to /panel/inmobiliaria/dispersiones:
   - Page should load with summary and filters
   - Month selector should work
   - Status tabs should filter correctly

3. Click "Generar Dispersiones":
   - Should navigate to /generar
   - Wizard should show 6 steps
   - Each step should have proper content

4. Test dispersion detail:
   - Click on a dispersion
   - Detail modal should open
   - ComisionDesglose should show
   - Actions should be available

5. Test extracto:
   - View extracto button should work
   - PDF download should generate file
  </action>
  <verify>pnpm build</verify>
  <done>All features verified and working</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm tsc --noEmit passes
- [ ] pnpm build succeeds
- [ ] Navigate to /panel/inmobiliaria/dispersiones shows page
- [ ] Month selector changes displayed dispersiones
- [ ] Status tabs filter correctly
- [ ] Click dispersion opens detail modal
- [ ] "Generar Dispersiones" opens wizard
- [ ] Wizard completes all 6 steps
- [ ] Extracto PDF downloads correctly
- [ ] Process dispersion updates status with toast
</verification>

<success_criteria>
- All tasks completed
- Dispersiones page shows summary and filterable list
- Month selection works correctly
- Status tabs show counts
- Detail modal opens with full info
- Wizard generates dispersiones for month
- Extracto PDF downloads correctly
- Process flow works with status updates
</success_criteria>

<output>
After completion, create `.planning/phases/inmobiliaria-07-dispersiones/07-03-SUMMARY.md`
</output>
