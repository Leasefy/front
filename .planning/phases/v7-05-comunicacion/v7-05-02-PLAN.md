---
phase: v7-05-comunicacion
plan: 02
type: execute
wave: 2
depends_on: [v7-05-01]
files_modified:
  - src/lib/api/messages.types.ts
  - src/lib/api/messages.service.ts
  - src/components/messages/MessagesWidget.tsx
autonomous: true
requirements: [COMU-02]

must_haves:
  truths:
    - "The Paperclip/Image buttons open a REAL file picker (accept image/pdf, size cap); on select the UI honestly discloses 'el envío de adjuntos estará disponible pronto' — it NEVER shows a fake 'enviado' and NEVER stages an orphaned upload to /documents"
    - "archive/mute/report no longer use alert(): each calls a typed api-client method that degrades to an honest 'Próximamente' toast on 404/403/0 (isEndpointUnavailable) — report opens an AlertDialog confirm first (safety action)"
    - "No 'motivo/por qué' the mora field: the optional report reason is voluntary free-text only, never a required or suggested reason-for-nonpayment (Ley 2300 art. 7)"
    - "landlord/agency actors keep working — the shared handlers now honest-toast instead of alert(); no actor-specific regression; any future attachment retrieval uses getSignedUrl (no IDOR)"
  artifacts:
    - path: "src/lib/api/messages.service.ts"
      provides: "archiveConversation/muteConversation/reportConversation + sendAttachment CONTRACTS with isEndpointUnavailable honest-degrade"
      contains: "reportConversation"
    - path: "src/components/messages/MessagesWidget.tsx"
      provides: "real file-picker wired to inert buttons (send=Próximamente); alert() replaced by service + sonner toast + AlertDialog report confirm"
      contains: "AlertDialog"
  key_links:
    - from: "src/components/messages/MessagesWidget.tsx"
      to: "hidden <input type=file> → honest 'Próximamente' toast"
      via: "file-picker UI real, send gated (no fake enviado, no orphaned /documents upload)"
      pattern: "type=\"file\""
    - from: "src/components/messages/MessagesWidget.tsx"
      to: "messagesApi.reportConversation / archiveConversation / muteConversation"
      via: "sonner toast + AlertDialog confirm; 404/403/0 → honest Próximamente (no alert)"
      pattern: "reportConversation"
---

<objective>
Make chat attachments and conversation actions REAL-or-honest (COMU-02) on the shared `MessagesWidget.tsx`
and shared `messages.service.ts`/`messages.types.ts` — replacing the inert Paperclip/Image buttons and the
three `alert()` placeholders, without breaking the landlord/agency actors.

Three honest deliverables:
1. **Attachment contract + real picker (send = Próximamente).** The file-picker UI is REAL; the SEND is a
   contract + honest "Próximamente" because there is no chat-attachment endpoint AND no attachment field on
   `BackendChatMessage` (two backend gaps — RESEARCH §2). Wire the inert Paperclip/Image `IconButton`s
   (`:539-550`) to a hidden `<input type="file">` (accept image/pdf, size cap); on select, show an honest
   disclosure toast — NEVER a fake "enviado", and NEVER stage the file to `/documents` (that creates an
   orphaned doc invisible to the thread — a "looks done but isn't" trap). Model the eventual real send on
   `documents.service.ts:78 upload(dto)` (multipart, `entityType:'conversation'|'lease'`, `entityId`).
2. **archive/mute/report — real, never alert().** Add typed api-client methods; replace the three `alert()`
   handlers (`:198-219`) with a service call + optimistic + `sonner` toast where the endpoint answers, else an
   honest "Próximamente" toast via the `isEndpointUnavailable` (404/403/0) idiom — NEVER a bare `alert`, never
   a fake success. **report** is a safety/compliance action → open an `AlertDialog` confirm first, then POST.
3. **No "why late" field.** If report collects a reason, it is a single OPTIONAL free-text field — never a
   required/suggested "motivo del atraso"/"por qué no ha pagado" (Ley 2300 art. 7, PITFALLS 5).

Depends on v7-05-01 (shares `MessagesWidget.tsx` + `messages.service.ts` + `messages.types.ts` → wave 2,
strictly after 01). Keep the shared handlers behavior-preserving for all actors (no more `alert`, honest
toasts everywhere).

Purpose: COMU-02 — adjuntar archivos/fotos y acciones (archivar/reportar) funcionan de verdad, no
`alert()`/inertes.
Output: attachment + conversation-action contracts + real picker UI + alert()-free actions.

**Guardrails:** no fake "enviado"; no orphaned `/documents` upload from chat; zero `alert(`; report behind
`AlertDialog` confirm; no required "motivo/por qué" reason; retrieval (future) via `getSignedUrl` (no IDOR);
landlord/agency intact; buttons sentence case (DESIGN.md §4). Zero new npm packages.
</objective>

<execution_context>
@/Users/nicolasgarcia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolasgarcia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/v7-05-comunicacion/v7-05-RESEARCH.md
@.planning/phases/v7-05-comunicacion/v7-05-PATTERNS.md
@.planning/phases/v7-05-comunicacion/v7-05-01-SUMMARY.md

# READ FIRST: docs/DESIGN.md §4 (Buttons — SENTENCE CASE), §11 (EmptyState / "Próximamente"), §17 (Dialog /
# AlertDialog for the report confirm), §4 Toaster (Sonner). CLAUDE.md: additive only, es-CO.

# Files to modify:
@src/lib/api/messages.types.ts
@src/lib/api/messages.service.ts
@src/components/messages/MessagesWidget.tsx

# THE multipart upload analog for the eventual attachment send (do NOT call it for chat this phase):
@src/lib/api/documents.service.ts
# THE isEndpointUnavailable (404/403/0) honest-degrade idiom to reuse:
@src/lib/api/lease-documents.service.ts
# THE ARCO delete + confirm + toast reference (real destructive action, NOT alert):
@src/app/inquilino/documentos/page.tsx

<interfaces>
<!-- Existing widget alert() placeholders to REPLACE (MessagesWidget.tsx) -->
handleArchive() → alert(...)   // :198   REMOVE alert, call archiveConversation
handleMute()    → alert(...)   // :207   REMOVE alert, call muteConversation
handleReport()  → alert(...)   // :216   REMOVE alert, AlertDialog confirm → reportConversation
selectedConversation?.id       // conversation id to pass to the action methods

<!-- Inert attachment buttons to WIRE (MessagesWidget.tsx :539-550) -->
<IconButton icon={<Paperclip/>} aria-label="Adjuntar archivo" />   // add onClick → hidden file input
<IconButton icon={<Image/>}     aria-label="Enviar imagen" />       // add onClick → hidden file input (image/*)

<!-- Upload analog (documents.service.ts) — the shape the REAL send will use later -->
documentsApi.upload({ file, type, entityType, entityId }): Promise<DocumentItem>   // multipart FormData, Bearer
documentsApi.getSignedUrl(docId): Promise<{ url; expiresAt }>                       // anti-IDOR retrieval

<!-- Honest-unavailable idiom -->
import { apiClient, ApiError } from './client'
function isEndpointUnavailable(err): boolean   // ApiError && (404 || 403 || 0)

<!-- AlertDialog primitive (report confirm) -->
import { AlertDialog, AlertDialogContent, AlertDialogHeader, AlertDialogTitle, AlertDialogDescription, AlertDialogFooter, AlertDialogAction, AlertDialogCancel } from '@/components/ui/alert-dialog'
import { toast } from 'sonner'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Conversation-action + attachment CONTRACTS (honest-degrade, no fake success)</name>
  <files>src/lib/api/messages.types.ts, src/lib/api/messages.service.ts</files>
  <action>
    In `messages.types.ts`: add a contract-only attachment shape —
    `export interface ChatAttachmentDraft { file: File }` and an optional `attachment?: { id: string; fileName: string; mimeType: string; size: number }` note-type
    (declared for the future in-thread attachment; `BackendChatMessage` has no attachment field today — leave
    it untouched, add a JSDoc naming the backend seam). Add
    `export type ConversationActionResult = 'ok' | 'unavailable'`.

    In `messages.service.ts` reuse the local `isEndpointUnavailable` (from v7-05-01) and add typed methods that
    degrade honestly (return `'unavailable'` / `null` on 404/403/0, NEVER a fake success):
      - `async archiveConversation(conversationId: string): Promise<ConversationActionResult>` →
        `apiClient.patch(`/messages/conversations/${conversationId}/archive`)`; unavailable → `'unavailable'`.
      - `async muteConversation(conversationId: string): Promise<ConversationActionResult>` →
        `apiClient.patch(`/messages/conversations/${conversationId}/mute`)`; unavailable → `'unavailable'`.
      - `async reportConversation(conversationId: string, reason?: string): Promise<ConversationActionResult>` →
        `apiClient.post(`/messages/conversations/${conversationId}/report`, { reason })`; unavailable →
        `'unavailable'`. JSDoc: `reason` is VOLUNTARY free-text; the frontend must never require or suggest a
        "why late"/"motivo del atraso" reason (Ley 2300 art. 7).
      - `async sendAttachment(conversationId: string, file: File): Promise<null>` → contract stub modeled on
        `documents.service.ts:78 upload` (multipart FormData, entityType:'conversation', entityId:conversationId,
        Bearer). It ALWAYS resolves `null` today (no endpoint + no message attachment field) so the UI stays on
        an honest "Próximamente"; JSDoc names both backend gaps and forbids staging an orphaned `/documents`
        upload. Do NOT actually POST this phase.
    JSDoc each method: no NestJS route exists yet — `'unavailable'`/`null` is the expected result; the UI shows
    an honest "Próximamente" and NEVER a fabricated success.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && S=src/lib/api/messages.service.ts && T=src/lib/api/messages.types.ts && grep -q "archiveConversation" $S && grep -q "muteConversation" $S && grep -q "reportConversation" $S && grep -q "sendAttachment" $S && grep -q "isEndpointUnavailable" $S && grep -q "ConversationActionResult" $T && test $(grep -ciE "motivo.*(mora|atraso)|por qué no (ha|has) pagado" $S) -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>archive/mute/report + sendAttachment contracts exist; all degrade to 'unavailable'/null on 404/403/0 (no fake success); report reason JSDoc'd as voluntary; no "why late" prompt token.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the inert attachment buttons to a real picker (send = honest "Próximamente")</name>
  <files>src/components/messages/MessagesWidget.tsx</files>
  <action>
    Add a hidden `<input ref={fileInputRef} type="file" className="hidden">` in the input row (near the
    Paperclip/Image buttons, ~:537-551). Give the Paperclip button `onClick` → open the picker with
    `accept="image/*,application/pdf"`; give the Image button `onClick` → open it with `accept="image/*"`
    (reuse one hidden input, set its `accept` before `.click()`). On `onChange`, take the first `File`,
    enforce a size cap (e.g. 10 MB → `toast.error` if exceeded), then call
    `messagesApi.sendAttachment(selectedConversation.id, file)`; since it resolves `null` (no backend), show
    `toast.info('El envío de adjuntos estará disponible pronto.')` — an honest "Próximamente". Reset the input
    value so re-selecting the same file fires again.

    Non-negotiable: the picker UI is REAL, the SEND is honestly pending. Do NOT render a fake attachment
    bubble, do NOT print "enviado"/"adjunto enviado", and do NOT call `documentsApi.upload`/`/documents` from
    the chat path (no orphaned doc). Keep the buttons' existing `aria-label`s; sentence-case any new copy.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && W=src/components/messages/MessagesWidget.tsx && grep -q "type=\"file\"" $W && grep -q "sendAttachment" $W && grep -qi "disponible pronto\|Próximamente" $W && test $(grep -ciE "adjunto enviado|archivo enviado|documentsApi\.upload|'/documents'" $W) -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>Paperclip/Image open a real file picker with accept + size cap; on select the UI honestly discloses attachments are coming; no fake "enviado", no orphaned /documents upload.</done>
</task>

<task type="auto">
  <name>Task 3: Replace alert() archive/mute/report with real service + sonner + AlertDialog confirm</name>
  <files>src/components/messages/MessagesWidget.tsx</files>
  <action>
    Import `toast` from `sonner` and the `AlertDialog*` primitives. Delete the three `alert()` bodies
    (`:198-219`) and the "mantenemos alert" comment. Rewire:
      - `handleArchive` → `await messagesApi.archiveConversation(selectedConversation.id)`; on `'ok'`,
        `toast.success('Conversación archivada')` and optimistically drop it from the list / clear selection; on
        `'unavailable'`, `toast.info('Archivar conversaciones estará disponible próximamente.')`.
      - `handleMute` → `muteConversation`; `'ok'` → `toast.success('Notificaciones silenciadas')`; `'unavailable'`
        → `toast.info('Silenciar estará disponible próximamente.')`.
      - `handleReport` → open an `AlertDialog` confirm ("¿Reportar esta conversación?" + neutral description).
        On confirm → `await messagesApi.reportConversation(selectedConversation.id)`; `'ok'` →
        `toast.success('Conversación reportada')`; `'unavailable'` → `toast.info('Reportar estará disponible
        próximamente.')`. If the confirm includes an optional reason, it is a single OPTIONAL free-text
        `Textarea` labeled neutrally (e.g. "Cuéntanos qué pasó (opcional)") — NEVER a required field and NEVER
        a "motivo del atraso"/"por qué no pagaste" prompt or dropdown.
    These handlers are SHARED by tenant + landlord/agency (options menu `:443-462` and quick-actions
    `:662-673`); the replacement is behavior-preserving for every actor (honest toast, no alert). Keep the
    dialog behind Radix `AlertDialog` (focus-trapped, no outside-dismiss). No `window.alert` anywhere.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && W=src/components/messages/MessagesWidget.tsx && test $(grep -c "alert(" $W) -eq 0 && grep -q "AlertDialog" $W && grep -q "reportConversation" $W && grep -q "from 'sonner'" $W && grep -qi "Próximamente" $W && test $(grep -ciE "motivo.*(mora|atraso|pago)|por qué no (ha|has) pagado" $W) -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>archive/mute/report call the typed service with sonner toasts; report is behind an AlertDialog confirm; zero `alert(`; any report reason is optional free-text with no "why late" prompt; landlord/agency handlers still work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant browser → file picker (attachment) | Untrusted file selected client-side; must NOT reach the thread or /documents as an orphan this phase |
| tenant browser → NestJS /messages/conversations/:id/{archive,mute,report} | Conversation-action endpoints (not live); report must reach the agency, never a client-side no-op |
| (future) attachment bytes retrieval | Must use backend-signed getSignedUrl — never a raw persistent URL (IDOR) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v7-05-01 | Spoofing / integrity | fake "adjunto enviado" | mitigate | Picker real, send resolves null → honest "Próximamente"; grep-gated absence of "enviado"/documentsApi.upload on chat path |
| T-v7-05-02 | Information disclosure (IDOR) | orphaned /documents upload + raw retrieval | mitigate | No /documents upload from chat this phase; future retrieval documented to use getSignedUrl (v7-02) |
| T-v7-05-03 | Repudiation / compliance (Ley 2300 art.7) | report reason field | mitigate | Reason is optional free-text; grep-gated absence of "motivo…mora"/"por qué…pagado" |
| T-v7-05-04 | Tampering | client-only "archive" hides a legally-relevant thread | mitigate | archive is a backend contract; 404/403/0 → honest "Próximamente", never a silent local hide implying persistence |
| T-v7-05-SC | Tampering (supply chain) | npm installs | accept | Zero new packages |
</threat_model>

<verification>
- `pnpm -C /Users/nicolasgarcia/rent/mvp-portal-inquilino build` succeeds.
- `pnpm test` — 0 NEW failures (~7 pre-existing unrelated — see v7-01 deferred-items.md).
- Grep gates (per task): archive/mute/report + sendAttachment contracts with honest-degrade, no "why late"
  token; real `type="file"` picker + `sendAttachment` + honest "Próximamente", no fake "enviado" / no
  `documentsApi.upload` on chat path; zero `alert(`, `AlertDialog` report confirm, `sonner` toasts.
- Manual smoke as `tenant`: Paperclip/Image open a picker → toast says attachments coming soon (no fake
  bubble); options menu → Archivar/Silenciar toast honestly, Reportar opens a confirm dialog. As `landlord`:
  the same actions honest-toast (no alert), no regression.
</verification>

<success_criteria>
- COMU-02: attachments have a real file-picker UI with an honest pending send (no fake "enviado", no orphaned
  upload); archive/mute/report are typed api-client actions with sonner feedback and an AlertDialog report
  confirm — never `alert()`, never a fake success; no "why late" field; landlord/agency unaffected.
</success_criteria>

<output>
After completion, create `.planning/phases/v7-05-comunicacion/v7-05-02-SUMMARY.md`.
</output>
