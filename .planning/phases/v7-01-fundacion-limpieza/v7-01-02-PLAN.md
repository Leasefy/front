---
phase: v7-01-fundacion-limpieza
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/inquilino/perfil/page.tsx
autonomous: true
requirements: [BASE-02]

must_haves:
  truths:
    - "The tenant profile form seeds from useAuth().user, not from Chilean literals"
    - "Saving personal info calls updateProfile() (PATCH /users/me), not a setTimeout"
    - "Deleting the account calls settingsApi.deleteAccount() + supabase signOut, not a setTimeout"
    - "The ID label reads 'Cédula' and sample data is Colombian (+57), locale es-CO, currency COP — no RUT/+56/CLP"
    - "Save errors surface a real error toast (no unconditional success)"
  artifacts:
    - path: "src/app/inquilino/perfil/page.tsx"
      provides: "Real profile read/write bound to auth context + settingsApi"
      contains: "updateProfile"
  key_links:
    - from: "src/app/inquilino/perfil/page.tsx"
      to: "updateProfile"
      via: "useAuth() PATCH /users/me"
      pattern: "updateProfile\\("
    - from: "src/app/inquilino/perfil/page.tsx"
      to: "settingsApi.deleteAccount"
      via: "real ARCO delete"
      pattern: "settingsApi\\.deleteAccount"
---

<objective>
Convert the tenant profile page from a Chilean `setTimeout` mock into a real get/save
surface, mirroring the already-wired landlord profile. Seed the form from
`useAuth().user`, save via `updateProfile()`, delete via `settingsApi.deleteAccount()`,
and Colombianize labels/locale/currency (cédula, `+57`, `es-CO`, COP).

Purpose: Satisfy BASE-02 (profile via real get/update API with Colombia data; remove the
Chilean RUT/+56/CLP mock) with Habeas Data discipline (delete = real ARCO action).
Output: `src/app/inquilino/perfil/page.tsx` reads/writes real profile data — no theater.
</objective>

<execution_context>
@/Users/nicolasgarcia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolasgarcia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/v7-01-fundacion-limpieza/v7-01-RESEARCH.md
@.planning/phases/v7-01-fundacion-limpieza/v7-01-PATTERNS.md

# THE analog — a near-identical layout that is FULLY WIRED. The tenant page is a stale
# fork of this. Copy its seeding, handleSave, and handleDeleteAccount patterns 1:1.
@src/app/panel/(landlord)/perfil/page.tsx

# File to modify:
@src/app/inquilino/perfil/page.tsx

# IMPORTANT — read docs/DESIGN.md §11 (empty/loading) + §16 (money COP / date es-CO) first.

<interfaces>
<!-- From src/lib/auth/types.ts — User (identity store; `rut` legacy key HOLDS the cédula) -->
user: { name: string; email: string; firstName?: string; lastName?: string;
        phone?: string; rut?: string; address?: string; birthDate?: string;
        emergencyContactName?: string; emergencyContactPhone?: string }

// From useAuth() (auth-context.tsx):
updateProfile(data: { firstName?; lastName?; phone?; rut?; address?; birthDate?;
                      emergencyContactName?; emergencyContactPhone? }): Promise<void>   // PATCH /users/me

// From src/lib/api/settings.service.ts:
settingsApi.deleteAccount(): Promise<{ success; message }>   // DELETE /users/me/account
settingsApi.uploadAvatar(file: File): Promise<{ url }>       // POST /users/me/avatar

// getSupabase from '@/lib/supabase/client' → supabase.auth.signOut()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed the form from user + wire real save/delete (mirror landlord)</name>
  <files>src/app/inquilino/perfil/page.tsx</files>
  <action>
    Follow `landlord/perfil/page.tsx` almost exactly — it is the correct, wired version.

    Imports: add `useAuth` (already imported for `user`; also pull `updateProfile`),
    `settingsApi` from `@/lib/api/settings.service`, `getSupabase` from `@/lib/supabase/client`,
    and `useRouter` from `next/navigation`.
    `const { user, updateProfile } = useAuth();`

    Replace the hardcoded Chilean `formData` (current lines 46-54) with a seed from `user`
    plus Colombian fallbacks (copy landlord:47-57):
      firstName: user?.firstName || '', lastName: user?.lastName || '',
      email: user?.email || '', phone: user?.phone || '+57 300 123 4567',
      rut: user?.rut || '1.020.345.678', address: user?.address || 'Cra. 7 #71-21, Bogotá',
      birthDate: user?.birthDate || '', emergencyContactName: user?.emergencyContactName || '',
      emergencyContactPhone: user?.emergencyContactPhone || ''.
    Add the derived helpers `fullName` and `emergencyContactDisplay` and the
    `handleNameChange` / `handleEmergencyContactChange` splitters (copy landlord:60-126).
    R2: when a `user.*` field is undefined, the input shows empty/placeholder — honest,
    never the old Chilean value.

    Replace `handleSave` (current lines 107-120, the `setTimeout(800)`) with the real
    landlord version (landlord:130-158): avatar → `settingsApi.uploadAvatar`; personal →
    `updateProfile({ firstName, lastName, phone, address, birthDate })`; emergency →
    `updateProfile({ emergencyContactName, emergencyContactPhone })`; wrap in try/catch with
    `toast.success` ONLY on success and `toast.error` on failure (PITFALLS 2 — no
    unconditional success). Remove the `avatarPreview`-only fake; keep an `avatarFile` state
    like landlord (:42) so the real multipart upload has the file.

    Replace `handleDeleteAccount` (current lines 202-217, the `setTimeout(2000)`) with the
    real landlord version (landlord:208-229): `await settingsApi.deleteAccount()` then
    `getSupabase()?.auth.signOut()`, advance to the goodbye step, then `router.push('/')`.
    Keep the type-"ELIMINAR" confirm gate (R5 / safety.always_confirm_destructive). Never
    show the success step without the persisted backend effect (Habeas Data / ARCO).

    DO NOT rename the `rut` field key — it is the identity contract used by auth + backend.
    Only the visible LABEL changes (Task 2).
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && grep -q 'updateProfile(' src/app/inquilino/perfil/page.tsx && grep -q 'settingsApi.deleteAccount' src/app/inquilino/perfil/page.tsx && test $(grep -c 'setTimeout(resolve' src/app/inquilino/perfil/page.tsx) -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>Form seeds from user; save calls updateProfile; delete calls settingsApi.deleteAccount + signOut; no setTimeout theater; errors toast honestly.</done>
</task>

<task type="auto">
  <name>Task 2: Colombianize labels, locale, currency + purge fabricated stats</name>
  <files>src/app/inquilino/perfil/page.tsx</files>
  <action>
    Colombia data hygiene (PITFALLS 3 — more than a string swap):
    - The ID field label must read "Cédula" (not "RUT"/"ID number"). If the existing
      `t('profile.idNumber')` string still says RUT, use the landlord's label key
      `t('landlordProfile.fields.cedula')` (confirmed present) or a literal "Cédula".
    - Date locale `es-CL` → `es-CO` in the birthDate display (current line ~749).
    - The employment-verify modal asks "Ingreso mensual (CLP)" (line ~911) — change CLP → COP,
      and any `$1.500.000`/Chilean placeholder to a Colombian example. (This modal submits
      nothing real; relabel to COP — do not add a new fake backend call.)
    - Any remaining `+56` placeholder → `+57`; any Chilean address example → Colombian.

    Purge fabricated identity claims (honest surface):
    - The Quick Stats block (lines ~552-576) hardcodes "1 Arriendo activo · Departamento
      Providencia" and "12 Pagos realizados · 100% a tiempo". Replace with real derived
      values (use `useLeases().getActive().length` for arriendos and
      `useMyPaymentRequests()` APPROVED count for pagos) OR remove the block. Do NOT ship a
      fabricated count (R1 / PITFALLS 1). Prefer removal if wiring both hooks bloats this file.
    - "Inquilino desde Enero 2024" (line ~521) is a fabricated date — remove it or derive
      from a real field; do not keep the literal.

    Habeas Data (BASE-02): the delete + (future) data-export are the ARCO surface — they are
    wired real in Task 1. Do not add any consent checkbox that is pre-checked; if any consent
    UI is added it must default unchecked with an inline purpose. Do not add a "motivo"/"por
    qué" field anywhere (Ley 2300 art.7).
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && test $(grep -c 'es-CL' src/app/inquilino/perfil/page.tsx) -eq 0 && test $(grep -c 'CLP' src/app/inquilino/perfil/page.tsx) -eq 0 && test $(grep -c '+56' src/app/inquilino/perfil/page.tsx) -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>Label reads Cédula; no es-CL/CLP/+56 remain; fabricated quick-stats removed or derived from real hooks; no motivo field.</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds (run locally — CI skips `next build`).
- `pnpm test` green.
- Grep gates: `updateProfile(` and `settingsApi.deleteAccount` present; zero `setTimeout(resolve`
  (fake-await theater — a legit redirect `setTimeout(() => router.push`, mirrored from landlord, is fine),
  `es-CL`, `CLP`, `+56` in `perfil/page.tsx`.
- Manual smoke as `tenant`: edit personal info → Save persists via network (not instant fake);
  fields seed from the logged-in user; cédula label + COP + es-CO visible.
</verification>

<success_criteria>
- BASE-02: profile loads from and saves to the real `/users/me` API with Colombia data
  (cédula label, `+57`, `es-CO`, COP); the Chilean RUT/+56/CLP mock and all `setTimeout`
  theater are gone; the `rut` contract key is preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/v7-01-fundacion-limpieza/v7-01-02-SUMMARY.md`.
</output>
