---
phase: v7-01-fundacion-limpieza
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/inquilino/page.tsx
  - src/app/inquilino/arriendo/page.tsx
autonomous: true
requirements: [BASE-01, PAGO-01]

must_haves:
  truths:
    - "The tenant dashboard shows the real active lease count from useLeases (not a hardcoded [])"
    - "The dashboard next-payment card shows the real amount from paymentInfo.monthlyRent AND the due date derived from paymentInfo.paymentDay (or the explore CTA), never a literal"
    - "The dashboard applications count comes from a real hook, not a hardcoded []"
    - "The arriendo 'Estado general' card reflects the real currentPeriodStatus, not the constant string 'Al día'"
    - "No fabricated '% match' badge remains on the dashboard property cards"
  artifacts:
    - path: "src/app/inquilino/page.tsx"
      provides: "Dashboard wired to real lease + payment-info + applications hooks"
      contains: "useLeasePaymentInfo"
    - path: "src/app/inquilino/arriendo/page.tsx"
      provides: "Overall-status card driven by currentPeriodStatus"
      contains: "useLeasePaymentInfo"
  key_links:
    - from: "src/app/inquilino/page.tsx"
      to: "useLeasePaymentInfo"
      via: "same single source of truth as pagos/page.tsx"
      pattern: "useLeasePaymentInfo"
    - from: "src/app/inquilino/arriendo/page.tsx"
      to: "currentPeriodStatus"
      via: "payment-info period status"
      pattern: "currentPeriodStatus"
---

<objective>
Wire the tenant dashboard (`/inquilino`) and the "Mi Arriendo" overview to the real
lease + payment data that already exists in this repo, deleting the hardcoded empty
arrays / `TODO(Backend)` block and the fabricated status/match strings. This is the
foundation of truth every other v7 pillar reads.

Purpose: Satisfy BASE-01 (dashboard reflects real active lease + next payment, no
hardcoded empties) and PAGO-01 (estado de cuenta traces to a single source of truth,
no self-computed number, no dark-pattern / fake status).
Output: `src/app/inquilino/page.tsx` and `src/app/inquilino/arriendo/page.tsx` render
real state via `useLeases` / `useLeasePaymentInfo` / applications hook — no fake data.
</objective>

<execution_context>
@/Users/nicolasgarcia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolasgarcia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/v7-01-fundacion-limpieza/v7-01-RESEARCH.md
@.planning/phases/v7-01-fundacion-limpieza/v7-01-PATTERNS.md

# Reference implementation — the estado-de-cuenta single source of truth. Read it;
# it already does exactly the derivation the dashboard must reuse. DO NOT rewrite it.
@src/app/inquilino/pagos/page.tsx

# Files to modify:
@src/app/inquilino/page.tsx
@src/app/inquilino/arriendo/page.tsx

# IMPORTANT — read docs/DESIGN.md §4 (cards/banners), §11 (empty/loading), §16
# (money/date es-CO) before touching any UI.

<interfaces>
<!-- Hooks the executor uses directly — no exploration needed. From src/lib/hooks/useLeases.ts -->
useLeases(): { leases, isLoading, getActive(): Lease[] /* status active|ending_soon */, ... }
useMyPaymentRequests(): { requests, isLoading, refetch }   // "fuente única del historial"
useLeasePaymentInfo(leaseId: string | null): { info: BackendPaymentInfo | null, isLoading, refetch }

// From src/lib/api/leases.types.ts — BackendPaymentInfo:
//   { monthlyRent: number; paymentDay: number;
//     currentPeriod: { month: number; year: number };
//     currentPeriodStatus: 'NONE'|'PENDING_VALIDATION'|'APPROVED'|'REJECTED';
//     currentPeriodRejectionReason: string | null }

// From src/lib/hooks/useApplications.ts — pick ONE:
useMyApplications(): { applications: Application[], isLoading, refetch }
useTenantApplications(): { applications, active: Application[], completed, isLoading, refetch }

// The exact no-fabrication derivation to copy (pagos/page.tsx:90-92):
//   const showNextPaymentCta =
//     paymentInfo?.currentPeriodStatus === 'NONE' || paymentInfo?.currentPeriodStatus === 'REJECTED';
//   const nextAmount = showNextPaymentCta ? paymentInfo!.monthlyRent : 0;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the dashboard to real lease + payment + applications data</name>
  <files>src/app/inquilino/page.tsx</files>
  <action>
    Delete the hardcoded block at lines ~97-105 (the `TODO (Backend)` comment plus
    `activeLeases: any[] = []`, `activeApplications: any[] = []`, `nextPayment = null`,
    `primaryLease = null`). Replace with real hook consumption, mirroring
    `pagos/page.tsx:46-58,90-92`:

    - Add `useLeases`, `useMyPaymentRequests`, `useLeasePaymentInfo` from
      `@/lib/hooks/useLeases`, and `useTenantApplications` from
      `@/lib/hooks/useApplications`.
    - `const { getActive, isLoading: leasesLoading } = useLeases();`
    - Gate leases on onboarding like pagos does:
      `const activeLeases = isOnboardingComplete ? getActive() : [];`
      `const primaryLease = activeLeases[0];`
    - `const { info: paymentInfo, isLoading: payInfoLoading } = useLeasePaymentInfo(primaryLease?.id ?? null);`
    - `const { requests, isLoading: requestsLoading } = useMyPaymentRequests();`
      (kept so the loading gate is honest even if not directly rendered).
    - `const { active: activeApplications, isLoading: appsLoading } = useTenantApplications();`
    - Compute next payment WITHOUT fabricating (copy pagos idiom):
      `const showNextPaymentCta = paymentInfo?.currentPeriodStatus === 'NONE' || paymentInfo?.currentPeriodStatus === 'REJECTED';`
      `const nextAmount = showNextPaymentCta ? (paymentInfo?.monthlyRent ?? 0) : 0;`
      Derive the due DATE too (SC #1 requires fecha + monto), reusing the pagos idiom
      (`pagos/page.tsx:110-111`):
      `const today = new Date(); let due = new Date(today.getFullYear(), today.getMonth(), paymentInfo?.paymentDay ?? 1);`
      `if (due < today) due = new Date(today.getFullYear(), today.getMonth() + 1, paymentInfo?.paymentDay ?? 1);`
      Build `nextPayment` as `paymentInfo && nextAmount > 0 ? { amount: nextAmount, dueDate: due } : null`.

    Extend the loading gate: the page currently only gates on
    `isOnboardingComplete === null` (:111). Add `|| leasesLoading || payInfoLoading || appsLoading || requestsLoading`
    to that spinner condition so `.length` is never read on in-flight data.

    Keep the existing onboarding gate (`isOnboardingComplete`) and the
    `TenantDashboardEmpty` branch untouched (R6) — only the post-onboarding branch reads real data.

    The stat cards at :203 (activeLeases.length), :217 (activeApplications.length) and
    the next-payment card at :226 already read these variables — they now render real values.
    Render the due date in the next-payment card (SC #1 = fecha + monto) next to the amount,
    e.g. `nextPayment.dueDate.toLocaleDateString('es-CO', { day: 'numeric', month: 'long' })`;
    if the card has no date slot, add one. Format money with the existing `i18nFormatCurrency`
    (COP); use `es-CO` for dates (NOT `es-CL`).

    GUARDRAILS (encode as you go):
    - PAGO-01 single source: the amount MUST come from `paymentInfo.monthlyRent`. Do NOT
      sum payment requests or compute a "saldo total" / "deuda" number anywhere.
    - Neutral framing (PITFALLS 8): do NOT add a red "EN MORA" badge, countdown, or guilt
      copy. If you surface period status text, keep it factual/neutral.
    - Casos abiertos (R1 / PITFALLS 1): the cases hub is v7-03. Do NOT add a fabricated
      cases count. Leave a comment `{/* Casos abiertos: hub llega en v7-03 — no fabricar conteo */}`.
      A neutral, NON-numeric "Próximamente" placeholder is acceptable but a number is not.
    - Remove the fabricated `92 - index * 5` "% match" badge at :301 on the property cards
      (delete the badge element or replace with nothing — it is invented data).
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && grep -vc '^\s*//' src/app/inquilino/page.tsx >/dev/null; test $(grep -c 'any\[\] = \[\]' src/app/inquilino/page.tsx) -eq 0 && test $(grep -c 'TODO (Backend)' src/app/inquilino/page.tsx) -eq 0 && grep -q 'useLeasePaymentInfo' src/app/inquilino/page.tsx && test $(grep -c '92 - index' src/app/inquilino/page.tsx) -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>Dashboard reads useLeases + useLeasePaymentInfo + applications hook; no `any[] = []`, no `TODO (Backend)`, no `92 - index` badge; loading gate covers all fetches.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the arriendo hardcoded "Al día" status with real period status</name>
  <files>src/app/inquilino/arriendo/page.tsx</files>
  <action>
    The "Estado general" card (~lines 152-163) hardcodes "Al día / Todos los pagos al día"
    regardless of real state — a PAGO-01 / PITFALLS 8 violation on a live page. Drive it
    from the real `currentPeriodStatus` instead.

    - Import `useLeasePaymentInfo` from `@/lib/hooks/useLeases` (the file already imports
      `useLeases, useMyPayments`).
    - `const primaryLease = activeLeases[0];`
    - `const { info: paymentInfo } = useLeasePaymentInfo(primaryLease?.id ?? null);`
    - Map `paymentInfo?.currentPeriodStatus` to a neutral, factual label + neutral feedback
      token (per DESIGN.md §4 banners), NOT a constant:
        - `APPROVED` → "Al día" (success token) — this is now TRUE only when the backend says so.
        - `PENDING_VALIDATION` → "En verificación" (warning/info token).
        - `NONE` → "Pago del período pendiente" (neutral / info token) — factual, no alarm color, no countdown.
        - `REJECTED` → "Pago rechazado" (error token); if you show detail, use the backend
          `currentPeriodRejectionReason` verbatim, no added guilt copy.
        - `paymentInfo == null` (no active lease / not loaded) → neutral "—" or hide the card.
    - Keep the icon/card shell; only the label + token become dynamic.

    GUARDRAILS: neutral factual only — no red "EN MORA", no "¡Última oportunidad!", no
    countdown, no credit-bureau ("Datacrédito"/"riesgo de reporte") strings. Do not compute
    a second saldo number; this card is a status label, not a balance.

    Also fix the `es-CL` date locale to `es-CO` in this file's `formatDate`/`formatShortDate`
    (lines ~175, ~183) — Colombia locale.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && grep -q 'currentPeriodStatus' src/app/inquilino/arriendo/page.tsx && test $(grep -c "es-CL" src/app/inquilino/arriendo/page.tsx) -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>Estado general card reflects currentPeriodStatus (not a constant); no `es-CL` locale remains; no dark-pattern strings added.</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds (CI does NOT run `next build` — run it locally; MEMORY `project-mvp-ci-build-gap`).
- `pnpm test` green (no dashboard/arriendo tests exist today; build + tsc are the gate).
- Grep gates: no `any[] = []` and no `TODO (Backend)` in `page.tsx`; `useLeasePaymentInfo`
  present in both files; no `92 - index` match badge; no `es-CL` in `arriendo/page.tsx`.
- Manual smoke as a `tenant` (agency demo login is agency@example.com; tenant demo per repo):
  dashboard shows real lease/next-payment or the explore CTA; arriendo status reflects real period.
</verification>

<success_criteria>
- BASE-01: dashboard shows the real active lease + next payment (fecha/monto or CTA) with no hardcoded empty arrays / `TODO(Backend)`.
- PAGO-01: next payment + status trace to `useLeasePaymentInfo` (same source as pagos) — no self-computed saldo, no fabricated "Al día", no dark patterns.
- No fabricated "% match" badge remains.
</success_criteria>

<output>
After completion, create `.planning/phases/v7-01-fundacion-limpieza/v7-01-01-SUMMARY.md`.
</output>
