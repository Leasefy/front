---
phase: v7-07-acuerdos-pago
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/contract/OTPVerification.tsx
  - src/components/contract/OTPVerification.test.ts
autonomous: true
requirements: [ACUE-02]

must_haves:
  truths:
    - "OTPVerification is generalized ADDITIVELY: a new optional `adapter?: OtpAdapter` (send/verify) prop lets any entity drive the OTP flow, and a pure exported `resolveOtpAdapter({adapter, contractId, role})` selects the transport — when no adapter is passed it builds the EXISTING contractsApi transport from contractId+role, so the shipped contract-signing flow (SignatureForm passes contractId+role) is byte-for-byte unchanged"
    - "The component calls the resolved adapter's send()/verify(code) instead of contractsApi directly; contractId/role become OPTIONAL props (required only when no adapter is given). No second OTP component is forked — one Ley 527/1999 flow serves both contract and acuerdo"
    - "resolveOtpAdapter is a pure, total function unit-tested WITHOUT React: no-adapter+contractId+role → an adapter whose send() calls contractsApi.sendOtp(contractId,{role}) and verify(code) calls contractsApi.verifyOtp(contractId,{role,code}); an injected adapter → returned verbatim; neither adapter nor contractId+role → throws"
  artifacts:
    - path: "src/components/contract/OTPVerification.tsx"
      provides: "OtpAdapter interface + resolveOtpAdapter() + optional `adapter?` prop (contractId/role now optional); component drives send/verify through the resolved adapter; contract path preserved"
      contains: "resolveOtpAdapter"
    - path: "src/components/contract/OTPVerification.test.ts"
      provides: "ACUE-02 enabler proof: contract transport built from contractId+role (contractsApi called with the right args); injected adapter returned verbatim; missing-both throws"
      contains: "resolveOtpAdapter"
  key_links:
    - from: "src/components/contract/OTPVerification.tsx"
      to: "contractsApi.sendOtp / verifyOtp (default transport)"
      via: "resolveOtpAdapter builds the contract adapter when no `adapter` prop is passed — backward compatible"
      pattern: "resolveOtpAdapter"
    - from: "src/components/contract/SignatureForm.tsx"
      to: "OTPVerification (contractId + role, no adapter)"
      via: "existing call site keeps working unchanged (contract-signing flow preserved)"
      pattern: "contractId"
---

<objective>
Generalize `OTPVerification` so an acuerdo acceptance (v7-07-05) can reuse the SAME Ley-527 OTP flow WITHOUT
forking a second component and WITHOUT breaking the shipped contract-signing flow. This is the one genuine build
of the phase (GAP-ANALYSIS:58); it is itself gated (the acuerdo OTP send/verify endpoints don't exist yet), so
the acuerdo accept path stays honest "Próximamente" — but the component seam lands now.

Two honest deliverables:
1. **Additive OTP transport (ACUE-02 enabler).** Add an `OtpAdapter { send, verify }` interface + a pure
   exported `resolveOtpAdapter({ adapter, contractId, role })` that returns the injected `adapter` when present,
   else builds the EXISTING contract transport from `contractId`+`role` (calling
   `contractsApi.sendOtp/verifyOtp`), else throws. Make `contractId`/`role` OPTIONAL props and add an optional
   `adapter?: OtpAdapter` prop. The component computes `const otp = useMemo(() => resolveOtpAdapter({adapter,
   contractId, role}), ...)` and calls `otp.send()` / `otp.verify(code)` where it currently calls
   `contractsApi.sendOtp/verifyOtp`. Everything else (6-digit inputs, cooldown, paste, Ley 527 note, verified
   state) stays intact. `SignatureForm` (passes `contractId`+`role`, no adapter) is UNCHANGED — the contract
   flow keeps working exactly as before.
2. **Pure unit test.** `OTPVerification.test.ts` unit-tests `resolveOtpAdapter` (no RTL, no DOM): no-adapter +
   contractId + role → send/verify call `contractsApi` with the right args (mock `contractsApi`); an injected
   adapter → returned verbatim; neither → throws.

Depends on nothing. Wave 1, disjoint from v7-07-01 (different files) — the executor runs it in the shared
worktree; it does not touch the api layer.

Purpose: ACUE-02 — one compliant OTP flow reused for acuerdo acceptance, additive, contract flow preserved.
Output: generalized OTPVerification + pure resolver test.

**Guardrails:** ADDITIVE only — do NOT change the contract-signing behavior, do NOT fork a second OTP component;
`contractId`/`role` stay valid (now optional); keep the Ley 527/1999 note; no new npm packages; es-CO/keep
existing copy (register cleanup is a milestone-wide pass, out of scope — RESEARCH Open Question 1).
</objective>

<execution_context>
@/Users/nicolasgarcia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolasgarcia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/v7-07-acuerdos-pago/v7-07-RESEARCH.md
@.planning/phases/v7-07-acuerdos-pago/v7-07-PATTERNS.md

# READ FIRST: CLAUDE.md (additive only, PR workflow, zero new npm packages). docs/DESIGN.md §17 (Dialog).

# The component to GENERALIZE (contract-hardcoded today: contractsApi.sendOtp/verifyOtp at :73,:123):
@src/components/contract/OTPVerification.tsx
# The consumer that MUST keep working unchanged (passes contractId + otpRole, no adapter):
@src/components/contract/SignatureForm.tsx

<interfaces>
<!-- contractsApi (src/lib/api/contracts.service.ts) + ContractOtpRole (contracts.types.ts:136) -->
type ContractOtpRole = 'tenant' | 'landlord'
contractsApi.sendOtp(id: string, dto: { role: ContractOtpRole }): Promise<{ sentTo: string; cooldownSeconds?: number }>
contractsApi.verifyOtp(id: string, dto: { role: ContractOtpRole; code: string }): Promise<{ verificationToken: string }>

<!-- The target seam (additive): -->
export interface OtpAdapter {
  send: () => Promise<{ sentTo: string; cooldownSeconds: number }>;
  verify: (code: string) => Promise<{ verificationToken: string }>;
}
export function resolveOtpAdapter(opts: { adapter?: OtpAdapter; contractId?: string; role?: ContractOtpRole }): OtpAdapter

<!-- SignatureForm call site (SignatureForm.tsx:306-312) — must stay valid:
     <OTPVerification isOpen contractId={contractId} role={otpRole} onVerified onCancel /> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize OTPVerification with an injected OtpAdapter (additive)</name>
  <files>src/components/contract/OTPVerification.tsx</files>
  <action>
    Edit `src/components/contract/OTPVerification.tsx` (do NOT rewrite the UI/state machine — only re-route the
    transport):
      - Export `interface OtpAdapter { send: () => Promise<{ sentTo: string; cooldownSeconds: number }>; verify:
        (code: string) => Promise<{ verificationToken: string }>; }`.
      - Export a PURE `function resolveOtpAdapter(opts: { adapter?: OtpAdapter; contractId?: string; role?:
        ContractOtpRole }): OtpAdapter`: if `opts.adapter` → return it; else if `opts.contractId && opts.role` →
        return `{ send: async () => { const r = await contractsApi.sendOtp(opts.contractId!, { role: opts.role! });
        return { sentTo: r.sentTo, cooldownSeconds: r.cooldownSeconds ?? 60 }; }, verify: async (code) => { const r
        = await contractsApi.verifyOtp(opts.contractId!, { role: opts.role!, code }); return { verificationToken:
        r.verificationToken }; } }`; else `throw new Error('OTPVerification requires an adapter or contractId+role')`.
      - In `OTPVerificationProps`: make `contractId?` and `role?` OPTIONAL and add `adapter?: OtpAdapter;` (JSDoc:
        inject a send/verify transport to reuse the flow for a non-contract entity, e.g. an acuerdo; when omitted,
        contractId+role drive the default contract transport).
      - In the component body, build `const otp = useMemo(() => resolveOtpAdapter({ adapter, contractId, role }),
        [adapter, contractId, role]);`. Replace the `contractsApi.sendOtp(...)` call in `sendOtp` with
        `await otp.send()` (destructure `{ sentTo, cooldownSeconds }`), and the `contractsApi.verifyOtp(...)` call
        in `verifyCode` with `await otp.verify(code)` (destructure `{ verificationToken }`). Update those two
        `useCallback` dependency arrays (`[otp]` in place of `[contractId, role]`, keeping `onVerified` where it is).
      - Keep the Dialog, inputs, cooldown, paste, verified/error states, and the Ley 527/1999 note UNCHANGED.
    Do NOT touch `SignatureForm.tsx` — it passes `contractId`+`role` (no adapter), so `resolveOtpAdapter` selects
    the contract transport and behavior is identical.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && O=src/components/contract/OTPVerification.tsx && grep -q "export interface OtpAdapter" $O && grep -q "export function resolveOtpAdapter" $O && grep -q "adapter?: OtpAdapter" $O && grep -q "otp.send()" $O && grep -q "otp.verify(" $O && grep -q "Ley 527" $O && pnpm exec tsc --noEmit 2>&1 | grep -E "OTPVerification|SignatureForm" | head -5 ; echo DONE</automated>
  </verify>
  <done>OtpAdapter + resolveOtpAdapter exported; `adapter?` prop added; contractId/role optional; component drives send/verify via the resolved adapter; contract-signing flow (SignatureForm) compiles + behaves unchanged; Ley 527 note kept.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure unit test for resolveOtpAdapter (ACUE-02 enabler)</name>
  <files>src/components/contract/OTPVerification.test.ts</files>
  <behavior>
    - resolveOtpAdapter({ contractId: 'c1', role: 'tenant' }).send() → calls contractsApi.sendOtp('c1', { role: 'tenant' }); returns { sentTo, cooldownSeconds } (defaults cooldownSeconds to 60 when the API omits it).
    - resolveOtpAdapter({ contractId: 'c1', role: 'tenant' }).verify('123456') → calls contractsApi.verifyOtp('c1', { role: 'tenant', code: '123456' }); returns { verificationToken }.
    - resolveOtpAdapter({ adapter }) where adapter is a stub → returns the SAME adapter object (identity); contractsApi is NOT called.
    - resolveOtpAdapter({}) → throws (neither adapter nor contractId+role).
  </behavior>
  <action>
    Create `src/components/contract/OTPVerification.test.ts`. `vi.mock('@/lib/api/contracts.service', ...)` (or
    the relative `./` path the component imports) with `sendOtp`/`verifyOtp` as `vi.fn()` returning fixed
    objects (`sendOtp` → `{ sentTo: 'a***@b.com', cooldownSeconds: 60 }`; also add one assertion with
    `cooldownSeconds` omitted to prove the `?? 60` default; `verifyOtp` → `{ verificationToken: 'tok-1' }`).
    Import `resolveOtpAdapter, type OtpAdapter` from `./OTPVerification`. Cover every <behavior> bullet: assert
    the exact args passed to `contractsApi.sendOtp/verifyOtp`, the identity return for an injected adapter (and
    that `contractsApi` was NOT called in that case), and that `resolveOtpAdapter({})` throws. Reset mocks in
    `beforeEach`. No React Testing Library — this is a pure function test.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && grep -q "resolveOtpAdapter" src/components/contract/OTPVerification.test.ts && pnpm test -- src/components/contract/OTPVerification.test.ts 2>&1 | tail -6</automated>
  </verify>
  <done>resolveOtpAdapter unit-tested (contract transport args, injected-adapter identity + contractsApi-not-called, missing-both throws); tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OTP code input → resolved adapter.verify | Untrusted 6-digit code crosses into the transport (contract OR acuerdo) here |
| resolved adapter → agent/backend OTP send/verify | JWT-scoped; the backend issues the one-use verificationToken (acuerdo OTP endpoints not live today) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v7-07-05 | Spoofing / Repudiation | OTPVerification generalization | mitigate | The Ley 527/1999 one-use `verificationToken` flow is PRESERVED (not reimplemented); the injected adapter only swaps the transport endpoint, not the verification semantics. Contract flow byte-unchanged (regression-gated by tsc + the preserved SignatureForm call site). |
| T-v7-07-06 | Tampering | resolveOtpAdapter | mitigate | Pure + total: no adapter and no contractId+role → throws (never a silent no-op that could bypass verification). Unit-tested. |
| T-v7-07-SC | Tampering (supply chain) | npm installs | accept | N/A — zero new dependencies this phase. |
</threat_model>

<verification>
- `pnpm -C /Users/nicolasgarcia/rent/mvp-portal-inquilino build` succeeds (contract-signing flow still compiles).
- `pnpm test -- src/components/contract/OTPVerification.test.ts` green.
- `pnpm test` — 0 NEW failures (~7 pre-existing unrelated); any existing OTP/contract test still passes.
- Additive gate: `SignatureForm.tsx` is UNCHANGED (still passes `contractId`+`role`, no adapter).
</verification>

<success_criteria>
- ACUE-02 enabler: OTPVerification is generalized via an injected `OtpAdapter` (+ pure `resolveOtpAdapter`) so
  an acuerdo acceptance can reuse the exact Ley-527 flow, with the shipped contract-signing flow unchanged —
  unit-tested; no forked component; zero new npm packages.
</success_criteria>

<output>
After completion, create `.planning/phases/v7-07-acuerdos-pago/v7-07-02-SUMMARY.md`.
</output>
