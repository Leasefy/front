---
phase: inmobiliaria-06-cobros
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - src/components/inmobiliaria/RecordatorioConfig.tsx
  - src/components/inmobiliaria/CobroDetail.tsx
  - src/components/inmobiliaria/index.ts
  - src/app/panel/inmobiliaria/cobros/page.tsx
autonomous: true

must_haves:
  truths:
    - "User can see full cobros page with tabs and filters"
    - "User can view cobro detail in modal"
    - "User can configure reminder settings"
    - "User can register payment from page"
    - "User can see monthly summary stats"
  artifacts:
    - path: "src/components/inmobiliaria/RecordatorioConfig.tsx"
      provides: "Reminder configuration panel"
      min_lines: 150
    - path: "src/components/inmobiliaria/CobroDetail.tsx"
      provides: "Cobro detail modal"
      min_lines: 200
    - path: "src/app/panel/inmobiliaria/cobros/page.tsx"
      provides: "Cobros management page"
      min_lines: 250
  key_links:
    - from: "cobros/page.tsx"
      to: "MOCK_COBROS"
      via: "import from mock-inmobiliaria"
      pattern: "MOCK_COBROS"
    - from: "CobroDetail"
      to: "Sheet"
      via: "import from ui"
      pattern: "Sheet"
---

<objective>
Create the Cobros page with RecordatorioConfig and CobroDetail components.

Purpose: Provide a complete cobros management interface with filtering, payment registration, and reminder configuration.
Output: Full cobros page at /panel/inmobiliaria/cobros with all components integrated.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@docs/ROADMAP-INMOBILIARIA.md

# Prior plan context
@.planning/phases/inmobiliaria-06-cobros/06-01-PLAN.md
@.planning/phases/inmobiliaria-06-cobros/06-02-PLAN.md

# Source files
@src/lib/types/inmobiliaria.ts
@src/lib/data/mock-inmobiliaria.ts
@src/components/inmobiliaria/index.ts

# Existing page patterns
@src/app/panel/inmobiliaria/pipeline/page.tsx
@src/app/panel/inmobiliaria/agentes/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RecordatorioConfig Component</name>
  <files>src/components/inmobiliaria/RecordatorioConfig.tsx</files>
  <action>
Create RecordatorioConfig component for reminder settings:

1. Panel Structure:
   - Collapsible section or card
   - Title: "Configurar Recordatorios"
   - Settings icon in header

2. Pre-vencimiento Settings:
   - "Días antes del vencimiento" multi-select
   - Options: 1, 3, 5, 7 días
   - Default from MOCK_INMOBILIARIA_CONFIG.reminderDaysBefore

3. Post-vencimiento Settings:
   - "Días después del vencimiento" multi-select
   - Options: 1, 3, 7, 15, 30 días
   - Default from MOCK_INMOBILIARIA_CONFIG.reminderDaysAfter

4. Notification Channels:
   - Email checkbox (default on)
   - SMS checkbox
   - WhatsApp checkbox

5. Message Templates:
   - "Recordatorio pre-vencimiento" textarea preview
   - "Recordatorio de mora" textarea preview
   - Variables shown: {inquilino}, {propiedad}, {monto}, {fecha}

6. Actions:
   - "Guardar configuración" button
   - Success toast on save

Props interface:
```typescript
interface RecordatorioConfigProps {
  config: {
    daysBefore: number[];
    daysAfter: number[];
    channels: ('email' | 'sms' | 'whatsapp')[];
  };
  onSave: (config: RecordatorioConfigProps['config']) => void;
}
```

Use Switch components for toggles.
Use Badge components for selected days.
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>RecordatorioConfig allows setting reminder schedule</done>
</task>

<task type="auto">
  <name>Task 2: Create CobroDetail Component</name>
  <files>src/components/inmobiliaria/CobroDetail.tsx</files>
  <action>
Create CobroDetail as a Sheet drawer showing full cobro info:

1. Header:
   - Property title as main heading
   - Status badge with current status color
   - Close button

2. Property Section:
   - Property thumbnail and address
   - Zone and type
   - Link to consignacion detail

3. Tenant Section:
   - Tenant name with avatar
   - Email and phone with action buttons
   - WhatsApp link

4. Propietario Section:
   - Propietario name (linked)
   - Quick context on owner

5. Amount Breakdown:
   - Rent amount
   - Admin fee
   - Late fee (if applicable)
   - Total amount
   - Paid amount (with date if paid)
   - Pending amount

6. Payment History (if partial/paid):
   - List of payments made
   - Date, amount, method, reference

7. Reminder History:
   - Reminders sent count
   - Last reminder date
   - Log of sent reminders

8. Actions Footer:
   - "Registrar Pago" button (primary)
   - "Enviar Recordatorio" button (secondary)
   - "Marcar como incobrable" button (destructive, if late)

Props interface:
```typescript
interface CobroDetailProps {
  isOpen: boolean;
  onClose: () => void;
  cobro: Cobro | null;
  onRegisterPayment?: (cobro: Cobro) => void;
  onSendReminder?: (cobro: Cobro) => void;
}
```

Use Sheet component from ui.
Follow PipelineDetail pattern for layout.
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>CobroDetail shows complete cobro info with actions</done>
</task>

<task type="auto">
  <name>Task 3: Create Cobros Page</name>
  <files>src/app/panel/inmobiliaria/cobros/page.tsx, src/components/inmobiliaria/index.ts</files>
  <action>
1. Create cobros/page.tsx:

   Header Section:
   - Title: "Gestión de Cobros"
   - Breadcrumb: Inmobiliaria > Cobros
   - Month selector (prominent)
   - "Configurar recordatorios" button (opens settings)

   Summary Section:
   - CobroResumen component with month summary
   - Stats: Por cobrar, Cobrado, Pendiente, En mora

   Filter Section:
   - CobroFilters component
   - Status tabs: Todos, Pendientes, Pagados, En mora
   - Property and propietario dropdowns
   - Search input

   Table/Grid Section:
   - CobroTable with filtered cobros
   - Toggle between table and card grid view
   - Pagination if > 20 items

   Modals:
   - CobroDetail (controlled by selectedCobro state)
   - RegistrarPagoModal (controlled by paymentCobro state)
   - RecordatorioConfig in Sheet/Dialog

   State Management:
   - cobros: filtered MOCK_COBROS for selected month
   - filters: CobroFiltersState
   - selectedCobro: for detail modal
   - paymentCobro: for payment modal
   - showConfig: for reminder config

   Handlers:
   - handleCobroClick: open detail modal
   - handleRegisterPayment: open payment modal
   - handlePaymentSubmit: process payment, update state, toast
   - handleFilterChange: update filter state

2. Update barrel export with RecordatorioConfig and CobroDetail:
```typescript
export { RecordatorioConfig } from './RecordatorioConfig';
export { CobroDetail } from './CobroDetail';
```

3. Add sidebar link (if not present):
   - Check if /panel/inmobiliaria/cobros is in navigation
   - Add if missing with icon Money or CurrencyDollar

Use MOCK_COBROS, MOCK_CONSIGNACIONES, MOCK_PROPIETARIOS from mock-inmobiliaria.
Use getCobroSummary helper for summary data.
Follow pipeline/page.tsx pattern for layout and animations.
  </action>
  <verify>pnpm build</verify>
  <done>Cobros page loads with filters, table, and modals</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm tsc --noEmit passes
- [ ] pnpm build succeeds
- [ ] Navigate to /panel/inmobiliaria/cobros shows page
- [ ] Month selector changes displayed cobros
- [ ] Status tabs filter correctly
- [ ] Click cobro opens detail modal
- [ ] Register payment opens payment modal
- [ ] Payment registration updates list
- [ ] Reminder config saves settings
</verification>

<success_criteria>
- All tasks completed
- Cobros page shows summary and filterable list
- Month selection works correctly
- Status tabs show counts
- Detail modal opens with full info
- Payment modal processes payments
- Reminder config saves preferences
</success_criteria>

<output>
After completion, create `.planning/phases/inmobiliaria-06-cobros/06-03-SUMMARY.md`
</output>
