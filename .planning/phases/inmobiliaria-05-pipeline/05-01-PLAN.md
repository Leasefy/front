---
phase: inmobiliaria-05-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/inmobiliaria/PipelineCard.tsx
  - src/components/inmobiliaria/PipelineColumn.tsx
  - src/components/inmobiliaria/index.ts
autonomous: true

must_haves:
  truths:
    - "User can see pipeline card with property, candidate, and agent info"
    - "User can see days in current stage on each card"
    - "User can see next action and date on each card"
    - "Cards have stage-colored header based on PIPELINE_STAGES"
    - "Column shows count of items in that stage"
  artifacts:
    - path: "src/components/inmobiliaria/PipelineCard.tsx"
      provides: "Draggable card showing pipeline item info"
      min_lines: 150
    - path: "src/components/inmobiliaria/PipelineColumn.tsx"
      provides: "Kanban column with header, count, and drop zone"
      min_lines: 100
  key_links:
    - from: "PipelineCard"
      to: "PIPELINE_STAGES"
      via: "import from types/inmobiliaria"
      pattern: "PIPELINE_STAGES|getPipelineStageInfo"
    - from: "PipelineColumn"
      to: "PipelineCard"
      via: "component import"
      pattern: "PipelineCard"
---

<objective>
Create PipelineCard and PipelineColumn components for the Kanban board.

Purpose: Display pipeline items as draggable cards within stage columns, showing property, candidate, and progress info.
Output: PipelineCard and PipelineColumn components ready for Kanban board integration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@docs/ROADMAP-INMOBILIARIA.md

# Prior phase context
@.planning/phases/inmobiliaria-04-agentes/04-03-SUMMARY.md

# Source files
@src/lib/types/inmobiliaria.ts
@src/lib/data/mock-inmobiliaria.ts
@src/components/inmobiliaria/index.ts

# Existing patterns to follow
@src/components/inmobiliaria/ConsignacionCard.tsx
@src/components/inmobiliaria/AgenteCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PipelineCard Component</name>
  <files>src/components/inmobiliaria/PipelineCard.tsx</files>
  <action>
Create PipelineCard component displaying pipeline item info:

1. Card Structure:
   - Colored top border based on stage color from PIPELINE_STAGES
   - Compact layout suitable for Kanban (narrower than ConsignacionCard)
   - Subtle shadow, rounded corners, hover lift effect
   - onClick handler for opening detail modal

2. Property Section:
   - Small thumbnail (48x48 or 64x64)
   - Property title (truncate if long)
   - Monthly rent formatted as currency

3. Candidate Section:
   - Candidate name with small avatar/initials
   - Risk score badge if present (A/B/C/D/E with colors)
   - Phone number for quick contact

4. Progress Section:
   - "Días en etapa" with count (warn color if >7 days)
   - Next action text (truncated)
   - Next action date (highlight if overdue)

5. Footer:
   - Assigned agente name (small text)
   - Stage badge with PIPELINE_STAGES color

Props interface:
```typescript
interface PipelineCardProps {
  item: PipelineItem;
  onClick?: (item: PipelineItem) => void;
  isDragging?: boolean;
  dragHandleProps?: any; // For drag-and-drop integration
}
```

Use Phosphor icons: HouseLine, User, Clock, CalendarCheck, Phone.
Animation: Use framer-motion for hover and drag states.
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>PipelineCard renders with all info sections and stage colors</done>
</task>

<task type="auto">
  <name>Task 2: Create PipelineColumn Component</name>
  <files>src/components/inmobiliaria/PipelineColumn.tsx</files>
  <action>
Create PipelineColumn component for Kanban board columns:

1. Column Header:
   - Stage label from PIPELINE_STAGES (labelEs)
   - Item count badge (e.g., "4 items")
   - Stage color as left border or header background
   - Optional collapse/expand toggle (collapsed = hidden cards)

2. Column Body:
   - Scrollable container for cards
   - Fixed max-height with overflow-y-auto
   - Vertical gap between cards (8px or 12px)
   - Drop zone styling for drag-and-drop (dashed border on drag over)

3. Empty State:
   - When no items in column, show subtle "Sin leads" message
   - Light gray dashed border placeholder

4. Sizing:
   - Fixed width (240-280px) for horizontal scroll
   - Flexible height based on container

Props interface:
```typescript
interface PipelineColumnProps {
  stage: PipelineStage;
  items: PipelineItem[];
  onCardClick?: (item: PipelineItem) => void;
  isDropTarget?: boolean; // Highlight when dragging over
  onDrop?: (item: PipelineItem, newStage: PipelineStage) => void;
}
```

Use PIPELINE_STAGES constant for stage info and colors.
Prepare for drag-and-drop in 05-03 by accepting isDropTarget prop.
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>PipelineColumn renders with header, cards, and empty state</done>
</task>

<task type="auto">
  <name>Task 3: Update Barrel Export</name>
  <files>src/components/inmobiliaria/index.ts</files>
  <action>
Add exports for new components:

```typescript
export { PipelineCard } from './PipelineCard';
export { PipelineColumn } from './PipelineColumn';
```
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>Components exported from barrel</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm tsc --noEmit passes
- [ ] pnpm build succeeds
- [ ] PipelineCard can be rendered with mock data
- [ ] PipelineColumn shows correct count
- [ ] Stage colors match PIPELINE_STAGES
- [ ] Empty column shows placeholder
</verification>

<success_criteria>
- All tasks completed
- PipelineCard shows property, candidate, agente info
- Days in stage displays with warning for overdue
- PipelineColumn has header with count
- Components ready for board integration in 05-03
</success_criteria>

<output>
After completion, create `.planning/phases/inmobiliaria-05-pipeline/05-01-SUMMARY.md`
</output>
