---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [components.json, tailwind.config.ts, src/app/globals.css, src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/input.tsx, src/components/ui/badge.tsx, src/lib/utils.ts]
autonomous: true

must_haves:
  truths:
    - "shadcn/ui components render correctly"
    - "Tailwind CSS utilities work"
    - "cn() utility function available"
  artifacts:
    - path: "components.json"
      provides: "shadcn/ui configuration"
      contains: "shadcn"
    - path: "src/lib/utils.ts"
      provides: "cn() class merge utility"
      contains: "clsx"
    - path: "src/components/ui/button.tsx"
      provides: "Button component"
    - path: "src/components/ui/card.tsx"
      provides: "Card component for property listings"
    - path: "src/components/ui/badge.tsx"
      provides: "Badge component for risk levels"
  key_links:
    - from: "tailwind.config.ts"
      to: "src/app/globals.css"
      via: "@tailwind directives"
    - from: "src/components/ui/*"
      to: "src/lib/utils.ts"
      via: "cn import"
      pattern: "import.*cn.*from"
---

<objective>
Configure Tailwind CSS and install essential shadcn/ui components.

Purpose: Establish the UI foundation with accessible, Airbnb-quality components for property cards and forms.
Output: Working shadcn/ui setup with core components for property catalog and scoring display.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-01-SUMMARY.md (after Plan 01)
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize shadcn/ui</name>
  <files>components.json, tailwind.config.ts, src/app/globals.css, src/lib/utils.ts</files>
  <action>
    Run `npx shadcn@latest init` with the following configuration:
    - Style: Default
    - Base color: Slate (neutral, professional for PropTech)
    - CSS variables: Yes
    - React Server Components: Yes
    - Import alias: @/components, @/lib/utils

    This creates:
    - components.json (shadcn configuration)
    - src/lib/utils.ts (cn() utility)
    - Updates tailwind.config.ts with shadcn paths
    - Updates globals.css with CSS variables

    Customize the color scheme for Arriendo Fácil brand:
    - Primary: Blue tones (trust, professional)
    - Accent colors for risk levels:
      - A/B (green): success
      - C (yellow): warning
      - D (red): destructive
  </action>
  <verify>components.json exists, cn() utility works</verify>
  <done>shadcn/ui initialized with brand-appropriate color scheme</done>
</task>

<task type="auto">
  <name>Task 2: Install essential UI components</name>
  <files>src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/input.tsx, src/components/ui/badge.tsx, src/components/ui/label.tsx, src/components/ui/skeleton.tsx</files>
  <action>
    Install core shadcn/ui components needed for MVP:

    ```bash
    npx shadcn@latest add button card input badge label skeleton
    ```

    Components selected for:
    - **button**: CTAs (Apply, Save, Filter)
    - **card**: Property listing cards (Airbnb-style)
    - **input**: Form fields
    - **badge**: Risk level indicators (A/B/C/D), verification badges
    - **label**: Form labels
    - **skeleton**: Loading states (UXPL-01 requirement)

    After installation, verify each component imports cn() from @/lib/utils.
  </action>
  <verify>All component files exist in src/components/ui/, npm run build succeeds</verify>
  <done>Essential UI components installed for property catalog and scoring display</done>
</task>

<task type="auto">
  <name>Task 3: Create demo page with components</name>
  <files>src/app/page.tsx</files>
  <action>
    Update the landing page to demonstrate working UI components:

    Create a simple layout showcasing:
    - Card component (property card preview)
    - Badge component showing risk levels (A, B, C, D with appropriate colors)
    - Button variants (primary, secondary, outline)
    - Skeleton loader

    This serves as:
    1. Verification that shadcn/ui works
    2. Quick visual reference for UI patterns
    3. Placeholder landing until proper pages are built

    Keep it simple - just a centered demo of components, not a full design.
    Text should be in Spanish for Colombia locale.
  </action>
  <verify>npm run dev shows styled components, badge colors differentiate risk levels</verify>
  <done>Demo page confirms all UI components render correctly with proper styling</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] components.json exists with correct config
- [ ] cn() utility works (test import)
- [ ] Badge variants show correct colors for A/B/C/D levels
- [ ] Skeleton component available for loading states
- [ ] Demo page renders without errors
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- UI foundation ready for property catalog (Phase 3)
- Risk level badge colors established (Phase 6/8)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
