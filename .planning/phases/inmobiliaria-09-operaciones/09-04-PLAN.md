---
phase: inmobiliaria-09-operaciones
plan: 04
type: execute
wave: 2
depends_on: ["09-01", "09-02", "09-03"]
files_modified:
  - src/components/inmobiliaria/CotizacionComparator.tsx
  - src/components/inmobiliaria/MantenimientoViewer.tsx
  - src/app/panel/inmobiliaria/operaciones/page.tsx
  - src/app/panel/inmobiliaria/layout.tsx
  - src/components/inmobiliaria/index.ts
autonomous: true

must_haves:
  truths:
    - "User can see operaciones dashboard with tabs for renovaciones and mantenimiento"
    - "User can compare multiple quotations side by side"
    - "User can view full maintenance request details with timeline"
    - "User can navigate to operaciones from sidebar"
  artifacts:
    - path: "src/components/inmobiliaria/CotizacionComparator.tsx"
      provides: "Side-by-side quote comparison component"
      min_lines: 300
    - path: "src/components/inmobiliaria/MantenimientoViewer.tsx"
      provides: "Detailed view of maintenance request with timeline"
      min_lines: 400
    - path: "src/app/panel/inmobiliaria/operaciones/page.tsx"
      provides: "Operaciones dashboard page"
      min_lines: 450
  key_links:
    - from: "operaciones/page.tsx"
      to: "RenovacionesTable"
      via: "import from components"
      pattern: "RenovacionesTable"
    - from: "operaciones/page.tsx"
      to: "MantenimientoList"
      via: "import from components"
      pattern: "MantenimientoList"
    - from: "CotizacionComparator"
      to: "MantenimientoQuote"
      via: "import from types/inmobiliaria"
      pattern: "MantenimientoQuote"
---

<objective>
Create CotizacionComparator, MantenimientoViewer, and integrate into operaciones page.

Purpose: Complete the operaciones module with quote comparison and full page integration.
Output: Operaciones page accessible from sidebar with all components integrated.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@docs/ROADMAP-INMOBILIARIA.md

# Prior plan context
@.planning/phases/inmobiliaria-09-operaciones/09-01-PLAN.md
@.planning/phases/inmobiliaria-09-operaciones/09-02-PLAN.md
@.planning/phases/inmobiliaria-09-operaciones/09-03-PLAN.md

# Source files
@src/lib/types/inmobiliaria.ts
@src/lib/data/mock-inmobiliaria.ts
@src/components/inmobiliaria/index.ts

# Existing page patterns
@src/app/panel/inmobiliaria/reportes/page.tsx
@src/app/panel/inmobiliaria/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CotizacionComparator Component</name>
  <files>src/components/inmobiliaria/CotizacionComparator.tsx</files>
  <action>
Create CotizacionComparator for comparing maintenance quotes:

1. Header:
   - Request title and type
   - Property info
   - "Comparar cotizaciones" title

2. Side-by-Side Cards:
   - Card per quote (horizontal scroll on mobile)
   - Provider name and phone
   - Amount (large, prominent)
   - Description
   - Estimated days
   - Date received
   - Difference from lowest (percentage)
   - "Seleccionar" button

3. Comparison Summary:
   - Lowest price highlighted
   - Fastest delivery highlighted
   - Best value indicator (price/time ratio)
   - Recommendation badge

4. Visual Indicators:
   - Green highlight for selected quote
   - Price bar visualization
   - Days bar visualization

5. Actions:
   - Select quote → triggers approval flow
   - Request another quote
   - Contact provider (click to call)

Props interface:
```typescript
interface CotizacionComparatorProps {
  solicitud: SolicitudMantenimiento;
  onSelectQuote: (quoteId: string) => void;
  onRequestNewQuote?: () => void;
  selectedQuoteId?: string;
}
```

Use horizontal scroll layout for quotes.
Highlight best options visually.
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>CotizacionComparator enables quote comparison</done>
</task>

<task type="auto">
  <name>Task 2: Create MantenimientoViewer Component</name>
  <files>src/components/inmobiliaria/MantenimientoViewer.tsx</files>
  <action>
Create MantenimientoViewer as a detailed view drawer:

1. Header:
   - Type icon and title
   - Status badge
   - Priority badge
   - Created date
   - Close button

2. Property & People Section:
   - Property card (thumbnail, title, address)
   - Tenant info (name, phone, email)
   - Owner info
   - Agent assigned

3. Request Details:
   - Description
   - Before photos (gallery)
   - After photos (if completed)

4. Timeline:
   - Visual timeline of all actions
   - Reported → Quoted → Approved → In Progress → Completed
   - Each step with date and actor
   - Current step highlighted

5. Quotations Section:
   - List of quotes received
   - Selected quote highlighted
   - CotizacionComparator embedded or link

6. Approval Section (if quoted):
   - Owner approval status
   - Approve/reject buttons
   - Amount confirmation

7. Completion Section (if in_progress or completed):
   - Provider contact
   - Completion notes
   - After photos upload
   - "Marcar completado" button

8. Actions:
   - Add note
   - Upload photo
   - Change status
   - Cancel request

Props interface:
```typescript
interface MantenimientoViewerProps {
  solicitud: SolicitudMantenimiento;
  onStatusChange?: (newStatus: MantenimientoStatus) => void;
  onApproveQuote?: (quoteId: string) => void;
  onAddNote?: (note: string) => void;
  onUploadPhoto?: (type: 'before' | 'after', files: File[]) => void;
  onClose: () => void;
}
```

Use Sheet from shadcn.
Include photo gallery component.
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>MantenimientoViewer shows full request details</done>
</task>

<task type="auto">
  <name>Task 3: Create Operaciones Page</name>
  <files>src/app/panel/inmobiliaria/operaciones/page.tsx</files>
  <action>
Create operaciones/page.tsx as the main operations dashboard:

1. Header Section:
   - Title: "Centro de Operaciones"
   - Breadcrumb: Inmobiliaria > Operaciones
   - Quick stats row

2. Quick Stats:
   - Renovaciones pendientes (with urgency breakdown)
   - Mantenimientos activos
   - Cotizaciones pendientes aprobación
   - IPC actual (from DANE)

3. Tab Navigation:
   - Renovaciones
   - Mantenimiento
   - IPC (Calculadora)

4. Renovaciones Tab:
   - RenovacionesTable component
   - "Nuevo" button (disabled - auto-generated from contracts)
   - Filter by urgency
   - RenovacionWorkflow sheet for workflow

5. Mantenimiento Tab:
   - MantenimientoList component
   - "Nueva solicitud" button
   - MantenimientoForm sheet for creation
   - MantenimientoViewer sheet for details

6. IPC Tab:
   - IPCCalculator component
   - Bulk apply section
   - Historical chart

7. State Management:
   - activeTab: 'renovaciones' | 'mantenimiento' | 'ipc'
   - selectedRenovacion: for workflow sheet
   - selectedMantenimiento: for viewer sheet
   - showNewMantenimiento: for form sheet

8. Actions:
   - handleStartRenewal: opens RenovacionWorkflow
   - handleViewMantenimiento: opens MantenimientoViewer
   - handleNewMantenimiento: opens MantenimientoForm
   - handleApproveQuote: updates quote selection

Props interface:
```typescript
// Page component, no props needed
// Uses 'use client' for interactivity
```

Follow reportes/page.tsx pattern for layout.
Use Tabs from shadcn.
  </action>
  <verify>pnpm build</verify>
  <done>Operaciones page integrates all components</done>
</task>

<task type="auto">
  <name>Task 4: Update Navigation</name>
  <files>src/app/panel/inmobiliaria/layout.tsx</files>
  <action>
Add Operaciones to sidebar navigation:

Check if navigation item exists, if not add:
- Icon: Wrench or Gear from Phosphor
- Label: "Operaciones"
- href: /panel/inmobiliaria/operaciones
- Position: After Reportes (or before if it makes more sense)

Verify full navigation list:
1. Dashboard
2. Propietarios
3. Portafolio
4. Agentes
5. Pipeline
6. Cobros
7. Dispersiones
8. Reportes
9. Operaciones (NEW)
  </action>
  <verify>pnpm build</verify>
  <done>Navigation includes Operaciones link</done>
</task>

<task type="auto">
  <name>Task 5: Update Barrel Export</name>
  <files>src/components/inmobiliaria/index.ts</files>
  <action>
Add exports for remaining components:

```typescript
// Operaciones - Quote Comparison & Viewer (Phase 9)
export { CotizacionComparator } from './CotizacionComparator';
export { MantenimientoViewer } from './MantenimientoViewer';
```
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>All Phase 9 components exported</done>
</task>

<task type="auto">
  <name>Task 6: Final Verification</name>
  <files>N/A - verification only</files>
  <action>
Run final verification:

1. Build check:
   ```bash
   pnpm build
   ```

2. Navigate to /panel/inmobiliaria/operaciones:
   - Page should load with tabs
   - Default to Renovaciones tab

3. Test Renovaciones:
   - RenovacionesTable shows contracts
   - Urgency filters work
   - Click opens RenovacionWorkflow

4. Test Mantenimiento:
   - MantenimientoList shows requests
   - Filters work
   - Click opens MantenimientoViewer
   - "Nueva solicitud" opens MantenimientoForm

5. Test IPC:
   - IPCCalculator shows current rate
   - Calculation works
   - Historical chart displays

6. Test components:
   - CotizacionComparator shows in viewer
   - Quote selection works
  </action>
  <verify>pnpm build</verify>
  <done>All features verified and working</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm tsc --noEmit passes
- [ ] pnpm build succeeds
- [ ] Navigate to /panel/inmobiliaria/operaciones shows page
- [ ] Renovaciones tab shows RenovacionesTable
- [ ] Mantenimiento tab shows MantenimientoList
- [ ] IPC tab shows IPCCalculator
- [ ] Sheets open correctly for workflows
- [ ] Navigation shows Operaciones link
</verification>

<success_criteria>
- All tasks completed
- Operaciones page shows 3 tabs working
- Renovaciones workflow accessible
- Mantenimiento creation and viewing works
- IPC calculator functional
- Quote comparison works
- Navigation integrated
</success_criteria>

<output>
After completion, create `.planning/phases/inmobiliaria-09-operaciones/09-04-SUMMARY.md`
</output>
