---
phase: v7-01-fundacion-limpieza
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/inquilino/configuracion/page.tsx
autonomous: true
requirements: [BASE-03]

must_haves:
  truths:
    - "Password change calls supabase.auth.updateUser({ password }), not a setTimeout"
    - "Download-my-data calls settingsApi.requestDataExport() and downloads the real blob"
    - "Delete account calls settingsApi.deleteAccount() + supabase signOut, not a setTimeout"
    - "Notification toggles persist via useNotificationSettings().updateSetting (real), not local useState only"
    - "The active-sessions surface is honest — the real global 'cerrar todas las sesiones' or an honest empty-state, NOT a fabricated mockSessions device list"
  artifacts:
    - path: "src/app/inquilino/configuracion/page.tsx"
      provides: "Config actions wired to real supabase + settingsApi + notification hook"
      contains: "settingsApi.requestDataExport"
  key_links:
    - from: "src/app/inquilino/configuracion/page.tsx"
      to: "getSupabase().auth"
      via: "real password change + global signOut"
      pattern: "auth.updateUser|signOut\\(\\{ scope: 'global' \\}\\)"
    - from: "src/app/inquilino/configuracion/page.tsx"
      to: "useNotificationSettings"
      via: "persisted notification prefs"
      pattern: "useNotificationSettings"
---

<objective>
Replace every `setTimeout` "theater" handler and the fabricated `mockSessions` device list
in the tenant configuration page with the real actions that already exist and are wired on
the landlord config page. Where no backend exists (per-device sessions), use an honest
empty-state, not fake devices.

Purpose: Satisfy BASE-03 (config runs real actions where backend exists, honest empty-state
otherwise; remove `setTimeout` theater for password / sessions / download / delete + persist
notification prefs).
Output: `src/app/inquilino/configuracion/page.tsx` performs real password change, real data
export, real account delete, real notification persistence, and an honest sessions surface.
</objective>

<execution_context>
@/Users/nicolasgarcia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolasgarcia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/v7-01-fundacion-limpieza/v7-01-RESEARCH.md
@.planning/phases/v7-01-fundacion-limpieza/v7-01-PATTERNS.md

# THE analog — every one of these actions is ALREADY REAL here. Copy its handlers 1:1.
@src/app/panel/(landlord)/configuracion/page.tsx

# File to modify:
@src/app/inquilino/configuracion/page.tsx

# IMPORTANT — read docs/DESIGN.md §11 (empty-state) + §4 (banners) first. Use the
# <EmptyState> primitive (src/components/ui/empty-state.tsx) for any no-backend surface.

<interfaces>
<!-- Real handlers to copy — all from landlord/configuracion/page.tsx -->
// password (landlord:101-124):  getSupabase().auth.updateUser({ password: passwordForm.new })
// sessions (landlord:126-138):  getSupabase().auth.signOut({ scope: 'global' })  → router.push('/auth')
// export  (landlord:140-161):   settingsApi.requestDataExport() → Blob(JSON) → a.download
// delete  (landlord:163-181):   settingsApi.deleteAccount() → getSupabase().auth.signOut() → router.push('/')
// notif   (landlord:53,84-93):  const { settings, updateSetting } = useNotificationSettings();
//                               await updateSetting(backendKey, value)

// From src/lib/api/settings.service.ts — NotificationSettings backend keys:
//   emailApplications, emailVisits, emailContracts, emailPayments, emailMessages,
//   emailMarketing, pushAll, pushUrgent
// From src/lib/hooks/useSettings.ts:
//   useNotificationSettings(): { settings, isLoading, error, updateSetting(key, value) } // optimistic + revert
// Imports to add: getSupabase from '@/lib/supabase/client'; useNotificationSettings from
//   '@/lib/hooks/useSettings'; settingsApi from '@/lib/api/settings.service'.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire password / data-export / delete to real backend</name>
  <files>src/app/inquilino/configuracion/page.tsx</files>
  <action>
    Add imports: `getSupabase` from `@/lib/supabase/client`, `settingsApi` from
    `@/lib/api/settings.service`.

    Replace `handlePasswordChange` (current lines 128-143, `setTimeout(1500)`) with the
    landlord version (landlord:101-124): keep the local match/length validation, then
    `const supabase = getSupabase(); ... await supabase.auth.updateUser({ password: passwordForm.new })`;
    on `error` throw; `toast.success` only on success, `toast.error(err.message)` on failure.

    Replace `handleDownloadData` (current lines 150-156, `setTimeout(2000)`) with the landlord
    version (landlord:140-161): `const data = await settingsApi.requestDataExport();` → build a
    `Blob([JSON.stringify(data, null, 2)], { type: 'application/json' })` → object URL → an
    `<a download="leasefy-datos-YYYY-MM-DD.json">` click → revoke. Real ARCO/Habeas Data export.

    Replace `handleDeleteAccount` (current lines 158-168, `setTimeout(2000)`) with the landlord
    version (landlord:163-181): `await settingsApi.deleteAccount();` then
    `getSupabase()?.auth.signOut();` then `router.push('/')`. Keep the type-"ELIMINAR" confirm
    gate (R5 / safety.always_confirm_destructive). Never toast success without the persisted
    delete (PITFALLS 2 — no theater masking failure).

    Keep `handleResetOnboarding`, theme, language, and `<MfaSetupSection />` exactly as-is
    (already real).
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && grep -q 'auth.updateUser' src/app/inquilino/configuracion/page.tsx && grep -q 'settingsApi.requestDataExport' src/app/inquilino/configuracion/page.tsx && grep -q 'settingsApi.deleteAccount' src/app/inquilino/configuracion/page.tsx && echo GATE_OK</automated>
  </verify>
  <done>Password change hits supabase.auth.updateUser; download hits requestDataExport (real blob); delete hits deleteAccount + signOut; all surface real errors.</done>
</task>

<task type="auto">
  <name>Task 2: Persist notification prefs + honest sessions surface</name>
  <files>src/app/inquilino/configuracion/page.tsx</files>
  <action>
    Notifications (persist for real, mirror landlord:53,84-93):
    - Add `const { settings: notifSettings, updateSetting } = useNotificationSettings();`
    - Delete the local `const [settings, setGear] = useState({...})` (lines 92-98) and the
      local `handleToggle` (lines 112-115).
    - Add a `handleNotifToggle(uiKey)` that maps each tenant toggle to a REAL backend key and
      calls `await updateSetting(backendKey, !notifSettings[backendKey])` with a success/err
      toast. Recommended map (confirm keys against `NotificationSettings`):
        emailNotifications → emailMessages,  paymentReminders → emailPayments,
        marketingEmails → emailMarketing,    pushNotifications → pushAll.
      For `smsNotifications` there is NO backend field → do NOT keep a local-only toggle that
      pretends to persist. Either remove that row, or render it disabled with an honest
      "No disponible" note. Each rendered toggle's `enabled` reads `notifSettings[backendKey]`.

    Sessions (honest — no `/users/me/sessions` endpoint exists, R4):
    - Delete `mockSessions` (lines 74-78) and the `sessions` state + per-device
      `handleCloseSession` (lines 109, 145-148).
    - Replace the Sessions modal body with the landlord pattern (landlord:567-592): show the
      current session as read-only (derive a device label from `navigator.userAgent`, guarded
      for SSR) and a single real "Cerrar todas las sesiones" button calling
      `getSupabase().auth.signOut({ scope: 'global' })` then `router.push('/auth')`. Do NOT
      fabricate a multi-device list. (Alternatively, if you prefer, render a DESIGN.md §11
      `<EmptyState>` explaining per-device management isn't available — but the landlord's
      real global-signout surface is preferred since it is a real action.)
    - Fix the Security section's sessions description (line ~284) that says
      `${sessions.length} dispositivos` — it must not reference a fabricated count; use a
      neutral label like "Cerrar sesión en todos los dispositivos".
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && grep -q 'useNotificationSettings' src/app/inquilino/configuracion/page.tsx && test $(grep -c 'mockSessions' src/app/inquilino/configuracion/page.tsx) -eq 0 && grep -q "signOut({ scope: 'global' })" src/app/inquilino/configuracion/page.tsx && test $(grep -c 'setTimeout(resolve' src/app/inquilino/configuracion/page.tsx) -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>Notif toggles persist via updateSetting; mockSessions gone; sessions surface is the real global signOut (or honest EmptyState); zero setTimeout theater remains.</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds (run locally — CI skips `next build`).
- `pnpm test` green.
- Grep gates: `auth.updateUser`, `settingsApi.requestDataExport`, `settingsApi.deleteAccount`,
  `useNotificationSettings`, `signOut({ scope: 'global' })` all present; zero `mockSessions`
  and zero `setTimeout(resolve` (fake-await theater; a legit redirect `setTimeout(() => router.push` is allowed) in `configuracion/page.tsx`.
- Manual smoke as `tenant`: toggle a notification → persists across reload; "Descargar datos"
  downloads a real JSON; sessions modal shows the real global-signout action (no fake devices).
</verification>

<success_criteria>
- BASE-03: password / data-export / delete run against the real backend; notification prefs
  persist via `useNotificationSettings`; the sessions surface is honest (real global signOut
  or empty-state); all `setTimeout` theater and `mockSessions` are removed.
</success_criteria>

<output>
After completion, create `.planning/phases/v7-01-fundacion-limpieza/v7-01-03-SUMMARY.md`.
</output>
