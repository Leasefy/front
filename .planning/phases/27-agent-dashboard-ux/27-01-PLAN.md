---
phase: 27-agent-dashboard-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/inmobiliaria/ai/AIAgentCard.tsx
  - src/components/inmobiliaria/ai/AIAgentActivityFeed.tsx
  - src/app/panel/inmobiliaria/page.tsx
  - src/app/panel/inmobiliaria/ai/page.tsx
  - src/app/panel/inmobiliaria/layout.tsx
autonomous: true

must_haves:
  truths:
    - "Dashboard shows agent cards + activity feed as first section after header"
    - "Activity feed height matches left column (agent cards) height"
    - "Agent cards use neutral/sobrio color scheme (no bright green/blue)"
    - "Active dot on agent cards is emerald green"
    - "Sidebar shows Agentes AI nav item with badge 2"
    - "Agent Hub page at /panel/inmobiliaria/ai renders with active + coming soon agents"
  artifacts:
    - path: "src/components/inmobiliaria/ai/AIAgentCard.tsx"
      provides: "Agent card with Agente AI badge, neutral colors, emerald active dot"
    - path: "src/components/inmobiliaria/ai/AIAgentActivityFeed.tsx"
      provides: "Activity feed with scroll, neutral status indicators"
    - path: "src/app/panel/inmobiliaria/page.tsx"
      provides: "Dashboard with agent section at top, height-synced"
    - path: "src/app/panel/inmobiliaria/ai/page.tsx"
      provides: "Agent Hub page"
  key_links:
    - from: "page.tsx (dashboard)"
      to: "AIAgentCard + AIAgentActivityFeed"
      via: "imports and renders in AgentSection component"
---

<objective>
Polish the agent dashboard UX components that were built earlier in this session. Fix height sync, verify colors are sobrio/neutral, ensure activity feed scrolls properly.

Purpose: These components already exist but need cleanup for production quality.
Output: Polished agent cards, activity feed, hub page, sidebar nav.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/inmobiliaria/ai/AIAgentCard.tsx
@src/components/inmobiliaria/ai/AIAgentActivityFeed.tsx
@src/app/panel/inmobiliaria/page.tsx
@src/app/panel/inmobiliaria/ai/page.tsx
@src/app/panel/inmobiliaria/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and fix agent card + feed polish</name>
  <files>src/components/inmobiliaria/ai/AIAgentCard.tsx, src/components/inmobiliaria/ai/AIAgentActivityFeed.tsx, src/app/panel/inmobiliaria/page.tsx</files>
  <action>
    Read the current state of all three files. Verify:

    1. AIAgentCard.tsx:
       - Badge "AGENTE AI" uses neutral-100/neutral-500 (NOT indigo)
       - Active dot uses emerald-400/emerald-500 (green)
       - "Activo" text uses emerald-600
       - Card border/bg are neutral (no colored borders)
       - "¿Cómo funciona?" link at bottom right

    2. AIAgentActivityFeed.tsx:
       - Header icon uses neutral-100/neutral-500 (NOT indigo)
       - "Ver todo" link uses neutral-500 (NOT indigo)
       - Status indicators: completed = neutral-400 checkmark, in_progress = neutral-400 spinner
       - In-progress items have bg-neutral-50 + border-l-neutral-400 (NOT blue)
       - Feed accepts className prop and has overflow-y-auto on list div
       - Has h-full + flex flex-col structure

    3. Dashboard page.tsx:
       - AgentSection component uses ResizeObserver to sync feed height to cards
       - Feed gets maxHeight from cards column height
       - Grid is 1/3 + 2/3 layout

    Fix any issues found. If everything is already correct, just verify and move on.
    Run build to confirm compilation.
  </action>
  <verify>npx next build --no-lint | grep "Compiled" (should show "Compiled successfully")</verify>
  <done>All agent card/feed components use neutral colors, feed height syncs with cards, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Verify Agent Hub page and sidebar nav</name>
  <files>src/app/panel/inmobiliaria/ai/page.tsx, src/app/panel/inmobiliaria/layout.tsx</files>
  <action>
    Read and verify:

    1. Agent Hub page (ai/page.tsx):
       - Shows summary stats (active agents, actions this week, hours saved)
       - Active agents section with cards + metrics
       - Activity feed with more items
       - Coming soon agents section with info banner
       - Uses neutral color scheme consistent with cards

    2. Layout (layout.tsx):
       - "Agentes AI" nav item with Robot icon and badge: 2
       - Positioned after Dashboard in nav order

    Fix any inconsistencies with the neutral color scheme. If Agent Hub still uses indigo/emerald for stat cards or badges, switch to neutral.

    Run build to confirm.
  </action>
  <verify>npx next build --no-lint | grep "Compiled"</verify>
  <done>Agent Hub page and sidebar nav are consistent with neutral color scheme, build passes</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx next build --no-lint` compiles successfully
- [ ] Agent cards use neutral colors with emerald active dot only
- [ ] Activity feed height syncs with cards column
- [ ] Agent Hub page renders correctly
- [ ] Sidebar shows "Agentes AI" with badge
</verification>

<success_criteria>

- All agent dashboard components are polished and production-ready
- Colors are sobrio/neutral throughout (no flashy greens/blues except active dot)
- Height sync works between cards and feed
- Build passes without new errors
</success_criteria>

<output>
After completion, create `.planning/phases/27-agent-dashboard-ux/27-01-SUMMARY.md`
</output>
