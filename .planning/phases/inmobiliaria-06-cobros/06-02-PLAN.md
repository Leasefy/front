---
phase: inmobiliaria-06-cobros
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/inmobiliaria/RegistrarPagoModal.tsx
  - src/components/inmobiliaria/MoraAlert.tsx
  - src/components/inmobiliaria/CobroResumen.tsx
  - src/components/inmobiliaria/index.ts
autonomous: true

must_haves:
  truths:
    - "User can register a payment with amount and method"
    - "User can register partial payments"
    - "User can see mora alert with days late and late fee"
    - "User can see monthly summary with totals"
    - "User can see collection rate percentage"
  artifacts:
    - path: "src/components/inmobiliaria/RegistrarPagoModal.tsx"
      provides: "Modal to register payment"
      min_lines: 200
    - path: "src/components/inmobiliaria/MoraAlert.tsx"
      provides: "Alert component for late payments"
      min_lines: 80
    - path: "src/components/inmobiliaria/CobroResumen.tsx"
      provides: "Monthly summary card"
      min_lines: 120
  key_links:
    - from: "RegistrarPagoModal"
      to: "Dialog"
      via: "import from ui"
      pattern: "Dialog"
    - from: "CobroResumen"
      to: "CobroSummary"
      via: "import from types/inmobiliaria"
      pattern: "CobroSummary"
---

<objective>
Create RegistrarPagoModal, MoraAlert, and CobroResumen components for payment registration and summaries.

Purpose: Enable payment registration and provide visual feedback for late payments and monthly totals.
Output: Payment modal, mora alert, and summary components ready for page integration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@docs/ROADMAP-INMOBILIARIA.md

# Source files
@src/lib/types/inmobiliaria.ts (lines 244-303 for Cobro, CobroSummary)
@src/lib/data/mock-inmobiliaria.ts
@src/components/inmobiliaria/index.ts

# Existing patterns to follow
@src/components/inmobiliaria/AsignacionModal.tsx
@src/components/ui/dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RegistrarPagoModal Component</name>
  <files>src/components/inmobiliaria/RegistrarPagoModal.tsx</files>
  <action>
Create RegistrarPagoModal component for registering payments:

1. Modal Structure:
   - Use Dialog component from ui
   - Title: "Registrar Pago"
   - Subtitle: Property title and tenant name

2. Cobro Summary Section:
   - Property and tenant info (read-only)
   - Month and due date
   - Total amount with late fee if applicable
   - Already paid amount (if partial)
   - Pending amount (highlighted)

3. Payment Form:
   - Amount input (default to pending amount)
   - "Pago total" quick button
   - Payment method dropdown:
     * Transferencia
     * Efectivo
     * Tarjeta
     * Cheque
     * PSE
     * Otro
   - Payment date input (default to today)
   - Reference/comprobante input (optional)
   - Notes textarea (optional)

4. Validation:
   - Amount must be > 0
   - Amount cannot exceed pending amount
   - Payment date required
   - Method required

5. Actions:
   - Cancel button
   - Submit button: "Registrar Pago"
   - Loading state during submission
   - Success toast on completion

Props interface:
```typescript
interface RegistrarPagoModalProps {
  isOpen: boolean;
  onClose: () => void;
  cobro: Cobro | null;
  onSubmit: (data: {
    amount: number;
    method: string;
    date: string;
    reference?: string;
    notes?: string;
  }) => Promise<void> | void;
}
```

Use react-hook-form for form handling.
Show confirmation if amount is less than pending (partial payment).
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>RegistrarPagoModal handles full and partial payments</done>
</task>

<task type="auto">
  <name>Task 2: Create MoraAlert Component</name>
  <files>src/components/inmobiliaria/MoraAlert.tsx</files>
  <action>
Create MoraAlert component for late payment indicators:

1. Alert Variants:
   - Warning (1-15 days late): Orange/amber
   - Critical (16-30 days late): Red
   - Severe (30+ days late): Dark red with urgent styling

2. Content:
   - Warning icon (Warning, WarningCircle)
   - Days late: "X días de mora"
   - Late fee amount: "+$XX,XXX de interés"
   - Total with fees

3. Actions (optional):
   - "Enviar recordatorio" button
   - "Ver historial" link

4. Compact Mode:
   - Just icon and days count
   - For use in tables/cards

Props interface:
```typescript
interface MoraAlertProps {
  daysLate: number;
  lateFee: number;
  totalWithFees: number;
  onSendReminder?: () => void;
  compact?: boolean;
}
```

Use motion for subtle pulse animation on critical/severe alerts.
Calculate severity based on daysLate thresholds.
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>MoraAlert shows severity-based styling with late fee info</done>
</task>

<task type="auto">
  <name>Task 3: Create CobroResumen Component</name>
  <files>src/components/inmobiliaria/CobroResumen.tsx</files>
  <action>
Create CobroResumen component for monthly summary:

1. Card Structure:
   - Large summary card with stats grid
   - Month title prominent
   - Refresh button (optional)

2. Stats Grid (2x3 or 4 columns):
   - Por cobrar: Total expected amount
   - Cobrado: Total collected (green)
   - Pendiente: Total pending (amber)
   - En mora: Total late (red)

3. Collection Rate:
   - Large percentage display
   - Progress bar visualization
   - Color based on rate (green >90%, amber 70-90%, red <70%)

4. Counts Row:
   - Cobros pagados: X
   - Cobros pendientes: X
   - Cobros en mora: X

5. Quick Actions:
   - "Ver todos los pendientes" link
   - "Ver morosos" link

Props interface:
```typescript
interface CobroResumenProps {
  summary: CobroSummary;
  onViewPending?: () => void;
  onViewLate?: () => void;
}
```

Use formatCurrency for all amounts.
Use motion for counter animations on load.
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>CobroResumen shows monthly totals with collection rate</done>
</task>

<task type="auto">
  <name>Task 4: Update Barrel Export</name>
  <files>src/components/inmobiliaria/index.ts</files>
  <action>
Add exports for new components (append to Cobro section):

```typescript
export { RegistrarPagoModal } from './RegistrarPagoModal';
export { MoraAlert } from './MoraAlert';
export { CobroResumen } from './CobroResumen';
```
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>Components exported from barrel</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm tsc --noEmit passes
- [ ] pnpm build succeeds
- [ ] RegistrarPagoModal opens with cobro info
- [ ] Partial payment shows confirmation
- [ ] MoraAlert shows severity-based colors
- [ ] CobroResumen shows all summary stats
- [ ] Collection rate displays with progress bar
</verification>

<success_criteria>
- All tasks completed
- Payment modal handles full and partial payments
- Mora alert has warning/critical/severe variants
- Summary card shows totals and collection rate
- All amounts formatted correctly
- Forms validate properly
</success_criteria>

<output>
After completion, create `.planning/phases/inmobiliaria-06-cobros/06-02-SUMMARY.md`
</output>
