---
phase: 31-automatic-reminders
plan: 02
type: execute
wave: 1
depends_on: ["31-01"]
files_modified:
  - src/components/inmobiliaria/reminders/ReminderLog.tsx
autonomous: true

must_haves:
  truths:
    - "ReminderLog shows history of sent reminders in a table"
    - "Each entry shows: type, recipient, property, date, status, channel"
    - "Status badges: sent (green), scheduled (blue), failed (red), cancelled (gray)"
    - "Entries are sortable by date (newest first)"
  artifacts:
    - path: "src/components/inmobiliaria/reminders/ReminderLog.tsx"
      provides: "Reminder log table with status badges and filtering"
      exports: ["ReminderLog"]
  key_links:
    - from: "ReminderLog.tsx"
      to: "mock-reminders.ts"
      via: "uses ReminderLogEntry type and mockReminderLog data"
---

<objective>
Build the reminder log component showing history of sent/scheduled reminders.

Purpose: Users can see what reminders were sent, to whom, and their delivery status.
Output: ReminderLog table component.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-automatic-reminders/31-01-SUMMARY.md

@src/lib/types/reminders.ts
@src/lib/data/mock-reminders.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ReminderLog component</name>
  <files>src/components/inmobiliaria/reminders/ReminderLog.tsx</files>
  <action>
    Create a reminder log component that shows the history of reminders.

    ReminderLog:
    - Props: { entries: ReminderLogEntry[] }
    - Header: "Historial de Recordatorios" with ClockCounterClockwise icon
    - Filter row: Filter by type (all, pre-payment, overdue, escalation, contract) + filter by status (all, sent, scheduled, failed)
    - Table/list showing each entry:
      - Type icon (same icons as config: Clock, Warning, ShieldWarning, FileText)
      - Type label badge
      - Recipient name + type badge (tenant/landlord)
      - Property title
      - Date (formatted as "25 mar 2026, 14:30")
      - Status badge: sent=neutral-700 bg, scheduled=neutral-200 bg, failed=red, cancelled=neutral-300
      - Channel icon (email=Envelope, whatsapp=WhatsAppLogo, push=Bell, sms=ChatText)
      - Amount (if payment reminder, formatted COP)

    Sort by scheduledAt descending (newest first).
    Use useState for filters.
    Follow existing table patterns (like agent table or cobros table).
    Use useI18n() for labels. Phosphor icons.
    Responsive: on mobile, stack key info vertically instead of table.
  </action>
  <verify>npx next build --no-lint | grep "Compiled"</verify>
  <done>ReminderLog renders filterable list of reminders with status badges</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx next build --no-lint` compiles successfully
- [ ] ReminderLog renders with mock data
- [ ] Filters work for type and status
- [ ] Status badges show correct colors
</verification>

<success_criteria>

- Reminder log shows comprehensive history
- Filters allow narrowing by type and status
- Responsive design works on mobile
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/31-automatic-reminders/31-02-SUMMARY.md`
</output>
