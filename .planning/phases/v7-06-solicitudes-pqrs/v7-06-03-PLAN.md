---
phase: v7-06-solicitudes-pqrs
plan: 03
type: execute
wave: 3
depends_on: [v7-06-01, v7-06-02]
files_modified:
  - src/components/tenant/NuevaSolicitudModal.tsx
  - src/app/inquilino/solicitudes/page.tsx
  - src/app/inquilino/layout.tsx
  - src/app/inquilino/casos/page.tsx
autonomous: true
requirements: [SOLI-01, SOLI-02, SOLI-03]

must_haves:
  truths:
    - "The tenant can OPEN a maintenance request (tipo 'reparacion') with a description + REAL photos (native picker, image/pdf, 10MB cap) and a formal PQRS (tipo peticion/queja/reclamo/sugerencia/solicitud) from one Dialog — the same shared entity, solicitanteTipo set server-side"
    - "On submit the modal calls pqrsApi.create; on success it uploads photos via documentsApi.upload(entityType:'pqrs', entityId=created.id); when create is not live (PqrsUnavailableError) it shows an honest 'Próximamente' toast and NEVER fabricates a radicado or uploads orphan photos"
    - "/inquilino/solicitudes lists the tenant's OWN requests (useTenantPqrs) with a per-row SLA estimate (resolveExpectedResponse, labeled 'estimado', never blank) and a neutral tone badge; empty/not-live → honest empty-state; each row deep-links to the unified /inquilino/casos/[id] detail (no duplicated timeline)"
    - "A 'Solicitudes' nav entry + a real 'Nueva solicitud' entry point on the caso hub replace the PQRS/mantenimiento 'Próximamente' placeholders; Acuerdos stays 'Próximamente' (v7-07)"
  artifacts:
    - path: "src/components/tenant/NuevaSolicitudModal.tsx"
      provides: "Dialog: tipo selector (6 PqrsTipo) + asunto + descripcion + real photo picker (image/pdf, 10MB) → pqrsApi.create → documentsApi.upload; honest 'Próximamente' on PqrsUnavailableError"
      contains: "pqrsApi.create"
    - path: "src/app/inquilino/solicitudes/page.tsx"
      provides: "own-requests list (useTenantPqrs) with SLA estimate per row + tone badge + 'Nueva solicitud' CTA; honest empty; rows link to /inquilino/casos/[id]"
      contains: "resolveExpectedResponse"
    - path: "src/app/inquilino/layout.tsx"
      provides: "'Solicitudes' nav entry → /inquilino/solicitudes"
      contains: "/inquilino/solicitudes"
    - path: "src/app/inquilino/casos/page.tsx"
      provides: "real 'Nueva solicitud' entry point replacing the PQRS/mantenimiento 'Próximamente' sections; Acuerdos stays 'Próximamente'"
      contains: "/inquilino/solicitudes"
  key_links:
    - from: "src/components/tenant/NuevaSolicitudModal.tsx"
      to: "pqrsApi.create → documentsApi.upload"
      via: "create returns id → upload photos with entityId; PqrsUnavailableError → honest toast, no fake radicado"
      pattern: "documentsApi.upload"
    - from: "src/app/inquilino/solicitudes/page.tsx"
      to: "resolveExpectedResponse (business-days) + useTenantPqrs"
      via: "per-row SLA estimate 'estimado', never blank"
      pattern: "resolveExpectedResponse"
---

<objective>
Ship the tenant CREATE + LIST surface (SOLI-01/02/03): a dedicated `/inquilino/solicitudes` page that hosts
the "Nueva solicitud" Dialog (maintenance + formal PQRS, real photo picker) and lists the tenant's own
requests with the per-row SLA estimate — all honest-degrading, with the caso hub + nav wired to it.

Three honest deliverables:
1. **NuevaSolicitudModal (SOLI-01/02).** A Dialog composed from the `PayRentModal` shell (AnimatePresence
   backdrop + `useLenis().stop()/start()` per DESIGN §8, `data-lenis-prevent` scroll body) with: a `tipo`
   selector over the 6 `PqrsTipo` values, `asunto`, `descripcion` textarea, and a REAL photo picker (hidden
   `<input type="file">` via ref, `accept='image/*,application/pdf'`, 10 MB cap + `toast.error` on oversize,
   reset `e.target.value=''`). Submit: `pqrsApi.create(input)` → on success, upload each photo via
   `documentsApi.upload({file, type:'solicitud_evidencia', entityType:'pqrs', entityId: created.id})`, toast
   success, close. On `PqrsUnavailableError` → honest "Próximamente" toast, no fake radicado, no orphan upload.
2. **/inquilino/solicitudes list (SOLI-03).** Tenant page shell + gates (Spinner → onboarding
   `CompleteProfileFirst` → error `EmptyState`, `min-h-screen bg-[#f8f8f8] dark:bg-[#0e0e10]` + `max-w-7xl`).
   List via `useTenantPqrs`; each row shows a neutral tone badge (`pqrsStatusToTone/Label`) + the SLA estimate
   from `resolveExpectedResponse(s.createdAt, s.slaVenceAt)` — soft "Respuesta estimada hacia el {es-CO date}"
   + a `· estimado` chip when `estimated`, "Respuesta a más tardar el {date}" when authoritative; NEVER blank,
   NEVER a red countdown. Empty / not-live → honest empty-state. Each row links to `/inquilino/casos/${id}`
   (the unified detail — do NOT re-render the timeline here). A prominent "Nueva solicitud" CTA opens the modal.
3. **Nav + hub entry points.** Add a "Solicitudes" nav item (`Lifebuoy` icon) between "Mis casos" and
   "Documentos" in `layout.tsx`. On the caso hub `casos/page.tsx`, REPLACE the two `ProximamenteSection` calls
   for PQRS + Mantenimiento with a single real "Abrir una solicitud" entry (link/CTA → `/inquilino/solicitudes`);
   keep the Acuerdos "Próximamente" section (that's v7-07). Real PQRS rows already fold into the hub list via
   v7-06-02 — no row-rendering change needed here.

Depends on v7-06-01 (pqrsApi + business-days) and v7-06-02 (useTenantPqrs + pqrs mappers). Wave 3.

Purpose: SOLI-01/02/03 — the tenant opens + tracks maintenance/PQRS with photos and a visible SLA, honestly.
Output: create Dialog + solicitudes list + nav/hub wiring.

**Guardrails:** no fabricated radicado (PqrsUnavailableError → honest toast); photos upload ONLY after a real
create id (never orphan); any future photo RETRIEVAL must use `getSignedUrl` (no raw URL / no `getDownloadUrl`);
own-requests-only (`useTenantPqrs`/`listMine`, no fetch-by-id); SLA never blank + "estimado" label, neutral
tone (no red countdown); no "por qué la mora" / no credit-bureau field (Ley 2300); es-CO copy (not es-CL);
buttons sentence case (DESIGN §4); tenant never assigns providers / sets cost / self-closes; zero new npm packages.
</objective>

<execution_context>
@/Users/nicolasgarcia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolasgarcia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/v7-06-solicitudes-pqrs/v7-06-RESEARCH.md
@.planning/phases/v7-06-solicitudes-pqrs/v7-06-PATTERNS.md

# READ FIRST: docs/DESIGN.md §4 (Buttons — SENTENCE CASE), §8 (Lenis stop/start + data-lenis-prevent for
# modals), §11 (EmptyState / honest "Próximamente"), §7 (A11y — pair icon+text). CLAUDE.md: additive, es-CO,
# PR workflow, zero new npm packages.

# Modal shell + Lenis + submit lifecycle to COPY:
@src/components/tenant/PayRentModal.tsx
# Photo picker idiom (hidden input via ref, accept image/pdf, size cap, reset value):
@src/components/messages/MessagesWidget.tsx
# The REAL multipart upload path (FormData + Bearer + entityType/entityId):
@src/lib/api/documents.service.ts
# Tenant page shell + gates + CaseRow/TONE_BADGE + formatRelative to reuse:
@src/app/inquilino/casos/page.tsx
# Files to modify:
@src/app/inquilino/layout.tsx

<interfaces>
<!-- v7-06-01 / v7-06-02 outputs consumed here -->
pqrsApi.create(input: NuevaSolicitudInput): Promise<SolicitudPqrs>   // throws PqrsUnavailableError on 404/403/0
interface NuevaSolicitudInput { tipo: PqrsTipo; asunto: string; descripcion: string; contratoId?: string; propiedadId?: string }
class PqrsUnavailableError extends Error
type PqrsTipo = 'peticion'|'queja'|'reclamo'|'sugerencia'|'solicitud'|'reparacion'
useTenantPqrs(): { items: SolicitudPqrs[]; isLoading; error; refetch }
pqrsStatusToTone(e): CaseTone   // from '@/lib/types/tenant-case'
pqrsStatusToLabel(e): string
resolveExpectedResponse(createdAtIso, slaVenceAtIso?): { date: Date; estimated: boolean }   // from '@/lib/date/business-days'

<!-- REAL upload path -->
documentsApi.upload({ file: File; type: string; entityType?: string; entityId?: string }): Promise<DocumentItem>
documentsApi.getSignedUrl(docId): Promise<{url;expiresAt}>   // the ONLY sanctioned retrieval path (anti-IDOR)

<!-- layout.tsx nav array (:30-45): each item is { label, href, icon }. Insert Solicitudes after 'Mis casos'. -->
<!-- casos/page.tsx: ProximamenteSection is called for PQRS (:333), Mantenimiento (:344), Acuerdos (:355). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NuevaSolicitudModal — tipo + descripcion + real photos → create → upload (SOLI-01/02)</name>
  <files>src/components/tenant/NuevaSolicitudModal.tsx</files>
  <action>
    Create the Dialog copying the `PayRentModal` shell: `AnimatePresence` backdrop `fixed inset-0 z-50
    bg-black/50`, `useLenis().stop()/start()` on open (DESIGN §8), `data-lenis-prevent` on the scroll body,
    header/body/footer, `Button isLoading`, `toast` on success/error. Props: `{ open: boolean; onClose: () =>
    void; onCreated?: () => void; prefill?: { contratoId?: string; propiedadId?: string } }`.
    Fields (simple `useState` + inline validation — the lighter tenant-modal norm; do NOT pull in
    react-hook-form/zod here): `tipo` select over the 6 `PqrsTipo` (es-CO labels: Petición/Queja/Reclamo/
    Sugerencia/Solicitud/Reparación), `asunto` (bounded, required), `descripcion` textarea (bounded, required).
    Photo picker (SOLI-01): hidden `<input type="file" multiple>` reached via a ref, `accept='image/*,
    application/pdf'`, a 10 MB `MAX_BYTES` guard with `toast.error` on oversize, and `e.target.value=''` reset
    so re-selecting the same file re-fires; hold selected `File[]` in state + show removable chips.
    Submit handler: build `NuevaSolicitudInput` (never set `solicitanteTipo` — server assigns it), call
    `await pqrsApi.create(input)`; on success upload each `File` via `documentsApi.upload({ file, type:
    'solicitud_evidencia', entityType: 'pqrs', entityId: created.id })`, then `toast.success`, `onCreated?.()`,
    `onClose()`. Wrap in try/catch: on `PqrsUnavailableError` (or `instanceof PqrsUnavailableError`) show an
    honest toast — es: "Estamos habilitando las solicitudes. Vuelve a intentarlo pronto." / en: "We're enabling
    requests. Please try again soon." — and DO NOT fabricate a radicado, DO NOT upload photos (no entityId).
    Rethrow-toast any other error generically. No provider assignment, no cost input, no estado field, no "por
    qué" field. Buttons sentence case.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && M=src/components/tenant/NuevaSolicitudModal.tsx && grep -q "pqrsApi.create" $M && grep -q "PqrsUnavailableError" $M && grep -q "documentsApi.upload" $M && grep -q "data-lenis-prevent" $M && grep -qE "image/\*.*pdf|accept=" $M && test $(grep -ciE "getDownloadUrl|por qu[eé]|motivo de mora|asignar proveedor|solicitanteTipo" $M) -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>Modal creates a maintenance/PQRS request with real photos; honest "Próximamente" + no fake radicado on unavailable; no provider/cost/estado/"por qué" fields; Lenis-safe; sentence-case buttons.</done>
</task>

<task type="auto">
  <name>Task 2: /inquilino/solicitudes list with per-row SLA estimate (SOLI-03)</name>
  <files>src/app/inquilino/solicitudes/page.tsx</files>
  <action>
    Create the page (`'use client'`) reusing the tenant page shell + gates from `casos/page.tsx`
    (Spinner loading gate → `useOnboardingStatus` + `CompleteProfileFirst` onboarding gate → error
    `EmptyState`; `min-h-screen bg-[#f8f8f8] dark:bg-[#0e0e10]` + `max-w-7xl mx-auto` + framer `motion.header`).
    Header: es "Solicitudes" / en "Requests" + a subtitle. A prominent "Nueva solicitud" / "New request"
    `Button` (sentence case) toggles `open` for `NuevaSolicitudModal` (pass `onCreated={refetch}`).
    List from `useTenantPqrs()`. For each `SolicitudPqrs` render a row (reuse the `CaseRow`/`TONE_BADGE`
    idiom): leading icon (`Wrench` for `reparacion`, `ChatCircle` otherwise), `asunto`, a neutral badge from
    `pqrsStatusToTone/Label`, and an SLA line computed with `resolveExpectedResponse(s.createdAt, s.slaVenceAt)`:
      · `estimated` → es "Respuesta estimada hacia el {date} · estimado" (small muted chip) with a tooltip
        "Fecha estimada; tu inmobiliaria confirma la definitiva." / en analog.
      · authoritative → es "Respuesta a más tardar el {date}".
      Format the date with `new Intl.DateTimeFormat('es-CO' | 'en-US', { day:'numeric', month:'long',
      year:'numeric' })`. NEVER render a blank/dashed SLA; NEVER the word "vence" next to an estimate; neutral
      styling only (no red countdown). Each row is a `Link href={`/inquilino/casos/${encodeURIComponent(s.id)}`}`
      (the unified detail — do NOT render a timeline here).
    Empty list (incl. not-live `[]`) → honest `EmptyState` (`Lifebuoy` icon, es "Aún no tienes solicitudes" +
    "Abre una solicitud de mantenimiento o una PQRS." with the "Nueva solicitud" action). Inline `locale === 'es'`
    copy (tenant convention). No fabricated rows.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && P=src/app/inquilino/solicitudes/page.tsx && grep -q "useTenantPqrs" $P && grep -q "resolveExpectedResponse" $P && grep -q "NuevaSolicitudModal" $P && grep -q "/inquilino/casos/" $P && grep -q "estimado" $P && grep -q "es-CO" $P && test $(grep -ciE "vence el|getDownloadUrl|destructive|countdown" $P) -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>Solicitudes list renders own requests with a never-blank SLA (authoritative vs "estimado"), neutral tone, "Nueva solicitud" CTA, honest empty-state; rows link to the unified caso detail.</done>
</task>

<task type="auto">
  <name>Task 3: Nav entry + caso-hub "Nueva solicitud" wiring</name>
  <files>src/app/inquilino/layout.tsx, src/app/inquilino/casos/page.tsx</files>
  <action>
    In `layout.tsx`: import `Lifebuoy` from `@phosphor-icons/react` and insert a nav item
    `{ label: locale === 'es' ? 'Solicitudes' : 'Requests', href: '/inquilino/solicitudes', icon: Lifebuoy }`
    into the `useTenantNavItems` array BETWEEN the "Mis casos" entry (:37) and the "Documentos" entry (:38).
    Follow the exact object shape of the neighboring items.
    In `casos/page.tsx`: REPLACE the two `ProximamenteSection` calls for PQRS (:333-343) and Mantenimiento
    (:344-354) with ONE real entry-point card — a `Link href="/inquilino/solicitudes"` (reuse the section
    card styling) titled es "Solicitudes" with copy "Abre y sigue tus solicitudes de mantenimiento y PQRS."
    and a sentence-case "Nueva solicitud"/"Abrir solicitud" affordance (an `ArrowUpRight`/`CaretRight`).
    KEEP the Acuerdos `ProximamenteSection` unchanged (v7-07). Real PQRS rows already fold into the hub list
    via v7-06-02 — do NOT touch the `cases.map` rendering. Do not remove `ProximamenteSection` itself (still
    used by Acuerdos). Adjust the grid so the layout still reads well with 1 real card + 1 "Próximamente" card.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && L=src/app/inquilino/layout.tsx && C=src/app/inquilino/casos/page.tsx && grep -q "/inquilino/solicitudes" $L && grep -q "Lifebuoy" $L && grep -q "/inquilino/solicitudes" $C && grep -q "Handshake" $C && echo GATE_OK</automated>
  </verify>
  <done>"Solicitudes" nav entry added; caso hub links to /inquilino/solicitudes (PQRS/mant placeholders replaced by a real entry point); Acuerdos stays "Próximamente".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant form input → pqrsApi.create | Untrusted tipo/asunto/descripcion + files cross here |
| photo bytes → documents store / retrieval | Files carry personal data (Habeas Data); retrieval must be signed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v7-06-07 | Information Disclosure (IDOR) | photo retrieval | mitigate | This phase only UPLOADS; any retrieval/display uses `documentsApi.getSignedUrl` (short-lived, ownership-checked). `getDownloadUrl`/raw URL forbidden (grep-gated). |
| T-v7-06-08 | Tampering | NuevaSolicitudModal submit | mitigate | `PqrsUnavailableError` → honest "Próximamente" toast; never fabricate a radicado; photos upload ONLY after a real create id (no orphan upload). |
| T-v7-06-09 | Input Validation / Compliance | modal fields | mitigate | Bounded `asunto`/`descripcion`; `tipo` from the shared enum; photo MIME (`image/*,application/pdf`) + 10 MB cap; NO "por qué"/motivo-de-mora field, NO credit-bureau copy (Ley 2300, grep-gated). |
| T-v7-06-10 | Elevation of Privilege | modal / list | mitigate | No provider-assignment, no cost input, no estado field, no self-close — the tenant only opens/tracks. `solicitanteTipo` set server-side. |
| T-v7-06-SC | Tampering (supply chain) | npm installs | accept | N/A — zero new dependencies this phase. |
</threat_model>

<verification>
- `pnpm -C /Users/nicolasgarcia/rent/mvp-portal-inquilino build` succeeds (new route registers).
- `pnpm test` — 0 NEW failures (~7 pre-existing unrelated).
- Grep gates (per task): create → PqrsUnavailableError honest toast + `documentsApi.upload`, no `getDownloadUrl`/
  "por qué"/"solicitanteTipo"; list uses `resolveExpectedResponse` + `es-CO`, no "vence el"/countdown; nav +
  hub link to `/inquilino/solicitudes`.
- Manual smoke as `tenant`: open `/inquilino/solicitudes` → "Nueva solicitud" opens the Dialog; pick a tipo +
  add a photo + submit → with the backend not live, an honest "Próximamente" toast (no radicado); the list
  shows an honest empty-state; the "Solicitudes" nav item + the caso-hub entry point both route here.
</verification>

<success_criteria>
- SOLI-01: the tenant opens a maintenance/reparación request with a description + real photos.
- SOLI-02: the tenant opens a formal PQRS reusing the shared entity (tipo selector), `solicitanteTipo` server-side.
- SOLI-03: the solicitudes list shows a never-blank SLA (authoritative vs "estimado") per request; the caso hub
  and nav route to it — all honest-degrading, no fabricated radicado/rows.
</success_criteria>

<output>
After completion, create `.planning/phases/v7-06-solicitudes-pqrs/v7-06-03-SUMMARY.md`.
</output>
