---
phase: v7-02-documentos-arriendo
plan: 04
type: execute
wave: 3
depends_on: [v7-02-03]
files_modified:
  - src/lib/api/lease-documents.service.ts
  - src/app/inquilino/documentos/page.tsx
autonomous: true
requirements: [DOCU-02, DOCU-03]

must_haves:
  truths:
    - "The documentos page has a paz y salvo section and a cert. retención (3.5%) section"
    - "Both sections show an honest 'Próximamente' empty-state — NO fabricated/generated document on any tenant-reachable path"
    - "An api-client contract exists for both (request-generation → poll status → signed downloadUrl), modeled on the avalúo async flow, but no fake downloadUrl is invented"
    - "The frontend never computes the 3.5% withholding number — it is a backend/fiscal assertion"
  artifacts:
    - path: "src/lib/api/lease-documents.service.ts"
      provides: "Contract-only api-client for paz y salvo + cert. retención (request + status poll)"
      contains: "pazYSalvo"
    - path: "src/app/inquilino/documentos/page.tsx"
      provides: "Paz y salvo + cert. retención sections with honest 'Próximamente' empty-states"
      contains: "Próximamente"
  key_links:
    - from: "src/app/inquilino/documentos/page.tsx"
      to: "lease-documents.service.ts"
      via: "request/status contract (empty-state until backend)"
      pattern: "leaseDocumentsApi"
    - from: "src/lib/api/lease-documents.service.ts"
      to: "avaluo async flow"
      via: "request → poll status → signed downloadUrl (modeled 1:1)"
      pattern: "downloadUrl"
---

<objective>
Ship **paz y salvo** (DOCU-02) and **cert. retención en la fuente 3.5%** (DOCU-03) as
frontend-first: UI sections + an api-client CONTRACT + an honest **"Próximamente"**
empty-state. Neither feature exists anywhere in the codebase (grep-verified) and both are
legal/fiscal assertions the backend must generate/certify — so this plan builds the contract
and the honest UI, and fabricates NOTHING. No client-rendered "paz y salvo", no client-computed
3.5% number, no fake `downloadUrl` on any path a real tenant can reach.

Model the api-client contract on the avalúo async pattern (`avaluo.ts:159-174`): a POST to
request generation returning an id, then a status poll returning `{ status, downloadUrl? }`
where the presigned `downloadUrl` appears only when the backend has it ready. Until the backend
exists, the UI degrades to the "Próximamente" empty-state (DESIGN.md §11) — same discipline as
the agency acuerdos page and v7-01.

Depends on v7-02-03 (the documentos page already hardened) — this plan only ADDS two sections.

Purpose: Satisfy DOCU-02 + DOCU-03 as honest frontend-first (contract + "Próximamente"), never
fabricating a legal/fiscal document.
Output: `src/lib/api/lease-documents.service.ts` (new) + two sections on the documentos page.
</objective>

<execution_context>
@/Users/nicolasgarcia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolasgarcia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/v7-02-documentos-arriendo/v7-02-RESEARCH.md
@.planning/phases/v7-02-documentos-arriendo/v7-02-PATTERNS.md
@.planning/phases/v7-02-documentos-arriendo/v7-02-03-SUMMARY.md

# READ FIRST: docs/DESIGN.md §11 (EmptyState — honest "Próximamente"), §4 Buttons/cards.

# Files to create / modify:
@src/app/inquilino/documentos/page.tsx

# THE analogs:
# Avalúo async generation contract (request id → poll status → presigned downloadUrl):
@src/lib/types/avaluo.ts
# api-client + 403/404 → [] tolerance:
@src/lib/api/client.ts
# EmptyState primitive (honest "Próximamente"):
@src/components/ui/empty-state.tsx

<interfaces>
<!-- Model target — avaluo async flow (avaluo.ts) -->
IntakeResponse { id: string }                                   // POST → start generation
AvaluoStatusResponse { status: string; downloadUrl?: string }   // GET /:id/status — downloadUrl only when ready

<!-- EmptyState primitive (src/components/ui/empty-state.tsx) -->
<EmptyState icon={Icon} title="Próximamente" description="..." action?={{ label; href }} />

<!-- api-client (src/lib/api/client.ts): apiClient.get/post<T>; ApiError(status,message); 403/404 → [] idiom -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: leaseDocumentsApi contract (paz y salvo + cert. retención)</name>
  <files>src/lib/api/lease-documents.service.ts</files>
  <action>
    Create `src/lib/api/lease-documents.service.ts`. Export `leaseDocumentsApi` with a contract for
    both documents, modeled 1:1 on the avalúo async flow — request-generation returns an id, a status
    poll returns `{ status, downloadUrl? }` (presigned, appears only when the backend has it ready):
      - Types: `LeaseDocRequestResponse { id: string }` and `LeaseDocStatusResponse { status:
        'pending' | 'processing' | 'ready' | 'unavailable'; downloadUrl?: string; expiresAt?: string }`.
      - `async requestPazYSalvo(leaseId: string): Promise<LeaseDocRequestResponse>` →
        `apiClient.post('/lease-documents/paz-y-salvo', { leaseId })`.
      - `async requestCertRetencion(leaseId: string, year: number): Promise<LeaseDocRequestResponse>` →
        `apiClient.post('/lease-documents/cert-retencion', { leaseId, year })`.
      - `async getStatus(id: string): Promise<LeaseDocStatusResponse>` →
        `apiClient.get('/lease-documents/${id}/status')`.
    Every method wraps 403/404 (endpoint not live) to a `{ status: 'unavailable' }` / rethrow-as-
    unavailable posture so the UI degrades to "Próximamente" — do NOT invent a fake id or fake
    `downloadUrl`. JSDoc the file: these are legal (paz y salvo) / fiscal (retención 3.5%) documents
    generated + certified SERVER-SIDE; the frontend never computes the withholding number and never
    renders the document. Contract-only until the backend exists.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && grep -q "leaseDocumentsApi" src/lib/api/lease-documents.service.ts && grep -q "requestPazYSalvo" src/lib/api/lease-documents.service.ts && grep -q "requestCertRetencion" src/lib/api/lease-documents.service.ts && grep -q "downloadUrl" src/lib/api/lease-documents.service.ts && echo GATE_OK</automated>
  </verify>
  <done>leaseDocumentsApi exports requestPazYSalvo/requestCertRetencion/getStatus modeled on the avalúo async flow; 403/404 → unavailable; no fabricated id/downloadUrl; JSDoc marks both as backend-owned legal/fiscal docs.</done>
</task>

<task type="auto">
  <name>Task 2: Paz y salvo + cert. retención sections with honest "Próximamente"</name>
  <files>src/app/inquilino/documentos/page.tsx</files>
  <action>
    Add two sections to the documentos page (below the lease-docs section from v7-02-01): "Paz y
    salvo" and "Certificado de retención en la fuente (3.5%)". Each is a card (DESIGN.md §4) with a
    short description and, since no backend exists today, an honest `EmptyState` (already imported;
    DESIGN.md §11) with title "Próximamente" and a description explaining the document will be
    available self-service / auto-generated once habilitado. Use a suitable Phosphor icon
    (e.g. `FileText` / `Certificate`).

    Wire the contract WITHOUT fabricating output: import `leaseDocumentsApi` from
    `@/lib/api/lease-documents.service`. A "Generar / Descargar" action MAY call
    `requestPazYSalvo(leaseId)` / `requestCertRetencion(leaseId, year)` then poll `getStatus`, but
    while status is `unavailable`/absent (no backend), the UI stays on "Próximamente" — the button is
    either disabled with a "Próximamente" label or triggers the honest empty-state. Under NO
    circumstance render a generated paz y salvo, a client-side "sin deuda" assertion, or a computed
    3.5% number. The `leaseId` comes from the tenant's active lease (`useLeases().getActive()[0]?.id`,
    already available in the portal); if none, the sections show the same honest empty-state.

    Guardrails: no "factura" string; no fabricated fiscal/legal content; keep both sections additive
    (do not disturb the lease/app-doc sections or the DOCU-04 hardening from v7-02-01/03).
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && grep -q "leaseDocumentsApi" src/app/inquilino/documentos/page.tsx && grep -qi "paz y salvo" src/app/inquilino/documentos/page.tsx && grep -qiE "retención|retencion" src/app/inquilino/documentos/page.tsx && grep -q "Próximamente" src/app/inquilino/documentos/page.tsx && test $(grep -ci "factura" src/app/inquilino/documentos/page.tsx) -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>Paz y salvo + cert. retención sections render with honest "Próximamente" empty-states; wired to leaseDocumentsApi contract without fabricating any document/number; no "factura"; other sections untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant browser → NestJS `/lease-documents/*` | Backend generates/certifies paz y salvo (legal) + retención (fiscal); does not exist yet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v7-02-11 | Repudiation / consumer-law | paz y salvo | mitigate | Never client-render a "sin deuda" legal assertion; contract + "Próximamente" only; backend certifies against the single source of truth |
| T-v7-02-12 | Compliance (fiscal/DIAN) | cert. retención 3.5% | mitigate | Frontend never computes the withholding number; backend auto-generates; "Próximamente" until live |
| T-v7-02-13 | Tampering (supply chain) | npm installs | accept | Zero new packages (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `pnpm -C /Users/nicolasgarcia/rent/mvp-portal-inquilino build` succeeds.
- `pnpm test` — 0 NEW failures (≈7 pre-existing unrelated — see v7-01 deferred-items.md).
- Grep gates: `leaseDocumentsApi` + `requestPazYSalvo` + `requestCertRetencion` + `downloadUrl` in
  the service; `leaseDocumentsApi` + "paz y salvo" + "retención" + "Próximamente" on the page, zero
  "factura".
- Manual smoke as `tenant`: both sections show "Próximamente"; no generated document, no "sin deuda"
  text, no computed 3.5% number appears anywhere.
</verification>

<success_criteria>
- DOCU-02: paz y salvo self-service surface exists (UI + contract + honest "Próximamente"), no
  fabricated legal document.
- DOCU-03: cert. retención en la fuente (3.5%) surface exists (UI + contract + honest "Próximamente"),
  no client-computed fiscal number, no fabricated document.
</success_criteria>

<output>
After completion, create `.planning/phases/v7-02-documentos-arriendo/v7-02-04-SUMMARY.md`.
</output>
