---
phase: 15-qa-responsive-accessibility
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [src/app/globals.css]
autonomous: true

must_haves:
  truths:
    - "Color contrast meets WCAG AA (4.5:1 text, 3:1 large text)"
    - "Screen reader announces page changes and dynamic content"
  artifacts:
    - path: "src/app/globals.css"
      provides: "WCAG AA compliant color tokens"
  key_links: []
---

<objective>
Audit color contrast for WCAG AA compliance and add screen reader support for dynamic content.

Purpose: Ensure text is readable for all users and screen readers can announce page changes.
Output: All text meets WCAG AA contrast ratios, dynamic content properly announced to screen readers.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix color contrast for WCAG AA</name>
  <files>src/app/globals.css, components with custom text colors</files>
  <action>
    1. Read globals.css and extract all color token HSL values
    2. Calculate contrast ratios for common combinations:
       - --foreground on --background (should be >= 4.5:1)
       - --muted-foreground on --background (should be >= 4.5:1)
       - --muted-foreground on --muted (should be >= 4.5:1)
       - --primary-foreground on --primary (should be >= 4.5:1)
       - --destructive-foreground on --destructive (should be >= 4.5:1)
       - White text on --primary (buttons) (should be >= 4.5:1)
       - Risk badge text on risk badge backgrounds (should be >= 4.5:1 or >= 3:1 for large text)
    3. For any failing combinations, adjust the CSS custom property values to meet minimums
    4. Check for any low-opacity text (text-foreground/50, text-muted-foreground/60) that might fail contrast
    5. Fix by increasing opacity or adjusting base color

    Phase 7 already adjusted --muted-foreground from 45% to 40% lightness.
    Verify this is sufficient and check all other token combinations.
  </action>
  <verify>All token combinations meet 4.5:1 for normal text, 3:1 for large text</verify>
  <done>All color combinations meet WCAG AA contrast requirements</done>
</task>

<task type="auto">
  <name>Task 2: Add screen reader support for dynamic content</name>
  <files>src/app/layout.tsx, src/components/layout/Navbar.tsx, dynamic content components</files>
  <action>
    1. Add aria-live="polite" region for dynamic content announcements:
       - Toast/notification area
       - Application status changes
       - Form submission feedback
       - Loading state transitions
    2. Add role="status" to loading indicators
    3. Add aria-live="assertive" for error messages
    4. Verify route changes are announced:
       - Next.js App Router should handle this via document.title changes
       - Ensure each page.tsx has appropriate metadata/title
    5. Add sr-only text for icon-only buttons that lack aria-label
    6. Check for any visually-hidden content that screen readers need:
       - "Skip to main content" link (should already exist from Phase 7)
       - Section headings for navigation landmarks

    Focus on:
    - Wizard step changes
    - Decision button feedback (approve/reject)
    - Score loading animation
    - Filter result count changes
  </action>
  <verify>grep -rn "aria-live" src/ to confirm live regions exist</verify>
  <done>Screen readers announce page changes, form feedback, and dynamic content</done>
</task>

</tasks>

<verification>
- [ ] npm run build passes
- [ ] All color token combinations meet WCAG AA contrast ratios
- [ ] aria-live regions exist for dynamic content
- [ ] Page titles set for each route
</verification>

<success_criteria>
- Color contrast meets WCAG AA across all token combinations
- Screen readers properly announce dynamic content changes
</success_criteria>

<output>
After completion, create `.planning/phases/15-qa-responsive-accessibility/15-03-SUMMARY.md`
</output>
