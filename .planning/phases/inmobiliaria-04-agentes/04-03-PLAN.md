---
phase: inmobiliaria-04-agentes
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/inmobiliaria/AgenteLeaderboard.tsx
  - src/components/inmobiliaria/AsignacionModal.tsx
  - src/components/inmobiliaria/AgenteWorkloadChart.tsx
  - src/components/inmobiliaria/index.ts
  - src/app/panel/inmobiliaria/agentes/page.tsx
autonomous: true

must_haves:
  truths:
    - "User can see ranking of agentes by performance"
    - "User can reassign property to different agente"
    - "User can see workload distribution across agentes"
    - "Reassignment shows confirmation before applying"
  artifacts:
    - path: "src/components/inmobiliaria/AgenteLeaderboard.tsx"
      provides: "Ranking table of agentes by closes/commissions"
      min_lines: 120
    - path: "src/components/inmobiliaria/AsignacionModal.tsx"
      provides: "Modal to reassign property to another agente"
      min_lines: 150
    - path: "src/components/inmobiliaria/AgenteWorkloadChart.tsx"
      provides: "Visual representation of workload distribution"
      min_lines: 80
  key_links:
    - from: "AsignacionModal"
      to: "AgenteSelector"
      via: "component import"
      pattern: "AgenteSelector"
    - from: "AgenteLeaderboard"
      to: "MOCK_AGENTES"
      via: "import and sort"
      pattern: "sort.*closedThisMonth|commissionsThisMonth"
---

<objective>
Create the Agente Leaderboard, Workload visualization, and Property Assignment Modal.

Purpose: Allow inmobiliaria admins to track top performers and efficiently redistribute workload.
Output: AgenteLeaderboard, AgenteWorkloadChart, AsignacionModal components integrated into agentes page.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@docs/ROADMAP-INMOBILIARIA.md

# Source files
@src/lib/types/inmobiliaria.ts
@src/lib/data/mock-inmobiliaria.ts
@src/components/inmobiliaria/index.ts
@src/components/inmobiliaria/AgenteSelector.tsx

# Existing patterns
@src/components/inmobiliaria/ConsignacionWizard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AgenteLeaderboard Component</name>
  <files>src/components/inmobiliaria/AgenteLeaderboard.tsx</files>
  <action>
Create AgenteLeaderboard component showing ranked table of agentes:
- Rank column with medal icons (🥇, 🥈, 🥉) for top 3
- Avatar + Name column
- Cierres Mes (primary ranking metric)
- Comisiones Mes (secondary display)
- Tasa Conversión (percentage)
- Trend indicator (up/down arrow based on comparison to last month - mock as random)

Features:
- Toggle between "Este Mes" and "Este Año" views
- Only show active agentes
- Sorted by closedThisMonth or closedThisYear
- Highlight top performer with subtle gold background
- Click row navigates to agente detail

Use existing table styling patterns.
Animation: stagger rows on initial render.
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>Leaderboard shows ranked agentes with medals and trends</done>
</task>

<task type="auto">
  <name>Task 2: Create AgenteWorkloadChart and AsignacionModal</name>
  <files>src/components/inmobiliaria/AgenteWorkloadChart.tsx, src/components/inmobiliaria/AsignacionModal.tsx</files>
  <action>
1. AgenteWorkloadChart component:
   - Horizontal bar chart showing properties per agente
   - Each bar: agente name, bar width proportional to assignedPropertyIds.length
   - Color gradient: green (low load) to amber (medium) to red (overloaded, >10 props)
   - Show capacity line at 8 properties (recommended max)
   - "Promedio: X propiedades" label
   - Pure CSS implementation (no charting library)

2. AsignacionModal component (Sheet drawer):
   - Props: isOpen, onClose, consignacion (the property to reassign)
   - Header: "Reasignar Propiedad" with property title
   - Current agente section with avatar and name
   - Use AgenteSelector to pick new agente (from ConsignacionWizard)
   - Reason for reassignment (optional textarea)
   - Preview section showing:
     - From: [Current Agente Name]
     - To: [New Agente Name]
     - Property: [Title]
   - Confirm button: "Confirmar Reasignación" (shows toast, closes modal)
   - Cancel button
   - Prevent selecting same agente as current

Integration:
- Add "Reasignar" action to AgentePropertyList rows (04-02)
- Click opens AsignacionModal with that consignacion
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>Workload chart renders and assignment modal opens/closes correctly</done>
</task>

<task type="auto">
  <name>Task 3: Integrate into Agentes Page</name>
  <files>src/app/panel/inmobiliaria/agentes/page.tsx, src/components/inmobiliaria/AgentePropertyList.tsx, src/components/inmobiliaria/index.ts</files>
  <action>
1. Update agentes/page.tsx:
   - Add tabs: "Equipo" | "Ranking" | "Carga de Trabajo"
   - "Equipo" tab: existing card/table view
   - "Ranking" tab: AgenteLeaderboard component
   - "Carga de Trabajo" tab: AgenteWorkloadChart component
   - Default to "Equipo" tab
   - Stats row stays visible across all tabs

2. Update AgentePropertyList.tsx (from 04-02):
   - Add "Reasignar" button to each property row
   - Track selected property for reassignment
   - Include AsignacionModal in component
   - OnSuccess: show toast with success message

3. Update barrel export in index.ts with new components.

4. Add navigation link in inmobiliaria sidebar if not already present.
  </action>
  <verify>pnpm build</verify>
  <done>Agentes page has all three tabs working and reassignment modal functions</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm tsc --noEmit passes
- [ ] pnpm build succeeds
- [ ] Navigate to /panel/inmobiliaria/agentes
- [ ] "Equipo" tab shows card/table view
- [ ] "Ranking" tab shows leaderboard with medals
- [ ] "Carga de Trabajo" tab shows workload bars
- [ ] Toggle between tabs works smoothly
- [ ] From agente detail, click "Reasignar" opens modal
- [ ] Select different agente in modal, confirm shows toast
</verification>

<success_criteria>
- All tasks completed
- Three tabs work on agentes page
- Leaderboard shows ranked agentes
- Workload chart shows distribution
- Assignment modal allows property reassignment
- Integration with AgentePropertyList works
</success_criteria>

<output>
After completion, create `.planning/phases/inmobiliaria-04-agentes/04-03-SUMMARY.md`
</output>
