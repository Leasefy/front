---
phase: v7-04-pagos-wompi
plan: 04
type: execute
wave: 4
depends_on: [v7-04-03]
files_modified:
  - src/lib/api/autopago.service.ts
  - src/components/tenant/AutopagoSection.tsx
  - src/app/inquilino/pagos/page.tsx
autonomous: true
requirements: [PAGO-04]

must_haves:
  truths:
    - "A tenant-facing autopago section lets the tenant see/configure/change/cancel tokenized autopago"
    - "Tokenization is fully backend-gated, so the section ships as UI + an api-client contract + an honest 'Próximamente' — NO fake 'autopago activado' on any real-tenant path"
    - "The autopago section is mounted on the pagos page and reads the tenant's active lease"
  artifacts:
    - path: "src/lib/api/autopago.service.ts"
      provides: "Contract-only api-client: get/enable/cancel autopago (tokenized), 403/404 → unavailable"
      contains: "autopagoApi"
    - path: "src/components/tenant/AutopagoSection.tsx"
      provides: "Config/change/cancel UI with honest 'Próximamente' empty-state"
      contains: "Próximamente"
  key_links:
    - from: "src/app/inquilino/pagos/page.tsx"
      to: "AutopagoSection"
      via: "mounted in the payments sidebar for the active lease"
      pattern: "AutopagoSection"
    - from: "src/components/tenant/AutopagoSection.tsx"
      to: "autopagoApi"
      via: "get/enable/cancel contract (empty-state until backend + Wompi token support)"
      pattern: "autopagoApi"
---

<objective>
Ship **autopago / domiciliación tokenizada** (PAGO-04) as frontend-first: a config/change/cancel
**UI** + an api-client **contract** + an honest **"Próximamente"** empty-state. Verified: no
tenant-facing tokenization/subscription surface exists (the only recurring-payment code is
`subscriptions.service.ts`, which is the **agency Flex-plan** subscription — a shape reference only,
NOT tenant autopago; do not repurpose it). Real autopago depends on Wompi tokenized payment-source
support + a backend token store + a recurring scheduler (ROADMAP `v7-04 External deps`) — all backend.

So this plan builds the honest surface and fabricates NOTHING: no fake "autopago activado", no fake
tokenized card, no charge on any path a real tenant can reach. Same discipline as the v7-02 paz-y-salvo /
cert-retención "Próximamente" plan and the agency acuerdos page.

Depends on v7-04-03 (the pagos page is settled; this plan only ADDS a section + its mount).

Purpose: Satisfy PAGO-04 (configure/change/cancel autopago) as honest frontend-first.
Output: `autopago.service.ts` (contract) + `AutopagoSection.tsx` (UI + "Próximamente") + one mount
line in the pagos page.

**Guardrails:** no fake activation/tokenization on a real-tenant path; contract degrades to
"Próximamente" when the backend is absent; additive (do not disturb the history / return / receipt work
from v7-04-02/03). Zero new npm packages.
</objective>

<execution_context>
@/Users/nicolasgarcia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolasgarcia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/v7-04-pagos-wompi/v7-04-RESEARCH.md
@.planning/phases/v7-04-pagos-wompi/v7-04-PATTERNS.md
@.planning/phases/v7-04-pagos-wompi/v7-04-03-SUMMARY.md

# READ FIRST: docs/DESIGN.md §11 (EmptyState — honest "Próximamente"), §4 Buttons/cards, §17 (Dialog
# for the cancel confirm if used).

# Files to create / modify:
@src/app/inquilino/pagos/page.tsx
# THE contract-first + "Próximamente" analog (from v7-02 — copy the tolerant service + empty-state shape):
@src/lib/api/lease-documents.service.ts
# api-client + 403/404 tolerance idiom:
@src/lib/api/client.ts
# EmptyState primitive (honest "Próximamente"):
@src/components/ui/empty-state.tsx

<interfaces>
<!-- api-client (client.ts): apiClient.get/post/delete<T>; ApiError(status,message); 403/404 → tolerant -->
<!-- EmptyState (src/components/ui/empty-state.tsx) -->
<EmptyState icon={Icon} title="Próximamente" description="..." action?={{ label; href }} />
<!-- Active lease id already available on the page: useLeases().getActive()[0]?.id -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: autopagoApi contract (get/enable/cancel, tolerant → unavailable)</name>
  <files>src/lib/api/autopago.service.ts</files>
  <action>
    Create `src/lib/api/autopago.service.ts`. Types:
      - `interface AutopagoStatus { enabled: boolean; maskedMethod?: string; nextChargeDate?: string; available: boolean }`
        (`available: false` means the backend/tokenization is not live → UI shows "Próximamente").
      - `interface EnableAutopagoPayload { leaseId: string; paymentSourceToken?: string }` (the Wompi
        payment-source token is created on Wompi's tokenization flow — placeholder until that exists).
    Export `autopagoApi` with:
      - `async get(leaseId: string): Promise<AutopagoStatus>` → `apiClient.get(`/tenant-payments/autopago/${leaseId}`)`,
        wrapped so `ApiError` 403/404 → `{ enabled: false, available: false }`.
      - `async enable(payload: EnableAutopagoPayload): Promise<AutopagoStatus>` →
        `apiClient.post('/tenant-payments/autopago', payload)`, 403/404 → `{ enabled: false, available: false }`.
      - `async cancel(leaseId: string): Promise<AutopagoStatus>` →
        `apiClient.delete(`/tenant-payments/autopago/${leaseId}`)`, 403/404 → `{ enabled: false, available: false }`.
    JSDoc the file: autopago is a **tokenized recurring charge** owned by the backend (Wompi payment-source
    token + scheduler); this is a contract-only client and MUST NOT fabricate an enabled state or a token.
    Mirror the tolerant idiom from `lease-documents.service.ts` / `getMine()`.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && S=src/lib/api/autopago.service.ts && grep -q "autopagoApi" $S && grep -qE "enable" $S && grep -qE "cancel" $S && grep -q "available" $S && grep -qE "403|404" $S && echo GATE_OK</automated>
  </verify>
  <done>autopagoApi exports get/enable/cancel with AutopagoStatus/EnableAutopagoPayload; 403/404 → { enabled:false, available:false }; JSDoc marks it backend-owned/tokenized; no fabricated enabled state or token.</done>
</task>

<task type="auto">
  <name>Task 2: AutopagoSection UI (config/change/cancel + honest "Próximamente")</name>
  <files>src/components/tenant/AutopagoSection.tsx</files>
  <action>
    Create `src/components/tenant/AutopagoSection.tsx` (`'use client'`). Props: `{ leaseId: string | null }`.
    On mount (if `leaseId`), `autopagoApi.get(leaseId)` into local state. Render a card (DESIGN.md §4) titled
    "Autopago" describing domiciliación automática del arriendo. Behavior:
      - When `status.available === false` (today's reality — no backend): show an honest `EmptyState`
        (DESIGN.md §11) title **"Próximamente"** describing that autopago (débito automático tokenizado)
        estará disponible pronto; the "Activar autopago" button is disabled or triggers the same
        "Próximamente" state. **Never** show "autopago activado"/"autopago activo" on this path.
      - When `status.available === true && !status.enabled`: show an "Activar autopago" CTA that calls
        `autopagoApi.enable({ leaseId })` (the real Wompi tokenization flow is wired later; until then the
        contract returns `available:false` and the UI stays on "Próximamente").
      - When `status.available === true && status.enabled`: show `maskedMethod` + `nextChargeDate` (from the
        contract, never fabricated) and a "Cancelar autopago" action (`autopagoApi.cancel(leaseId)`) behind a
        confirm; on success `toast.success` + refresh state.
    Use a suitable Phosphor icon (e.g. `ArrowsClockwise` / `CreditCard`). Keep it additive and self-contained.
    Guardrail: under NO branch does the component assert an active autopago without the contract returning
    `enabled:true` from the backend — no optimistic/local "activado" flag.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && C=src/components/tenant/AutopagoSection.tsx && grep -q "autopagoApi" $C && grep -qi "autopago" $C && grep -qi "Próximamente" $C && grep -qE "cancel|Cancelar" $C && test $(grep -ciE "autopago activado|autopago activo" $C) -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>AutopagoSection renders config/change/cancel states driven by autopagoApi; the no-backend path shows honest "Próximamente"; enabled state uses only contract data; no fake "autopago activado/activo".</done>
</task>

<task type="auto">
  <name>Task 3: Mount AutopagoSection on the pagos page</name>
  <files>src/app/inquilino/pagos/page.tsx</files>
  <action>
    Import `AutopagoSection` from `@/components/tenant/AutopagoSection` and mount it in the sidebar column
    (the `<motion.div className="space-y-6">` that holds `PeriodStatusCard` + Quick Links, ~lines 411-460),
    below the Quick Links block, passing `leaseId={primaryLease?.id ?? null}`. Additive only — do NOT alter
    the history, return handling, or comprobante work from v7-04-02/03.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && grep -q "AutopagoSection" src/app/inquilino/pagos/page.tsx && echo GATE_OK</automated>
  </verify>
  <done>AutopagoSection is imported and rendered in the pagos sidebar with the active leaseId; no regressions to existing sections.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant browser → NestJS `/tenant-payments/autopago/*` | Tokenized recurring config; backend owns token store + scheduler; does not exist yet |
| tenant → Wompi tokenization (future) | Payment-source token created on Wompi; never fabricated client-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v7-04-12 | Spoofing / false state | fake "autopago activado" | mitigate | UI shows "Próximamente" until the contract returns `available:true && enabled:true`; grep-gated absence of "autopago activado/activo" |
| T-v7-04-13 | Tampering (PCI / token) | payment-source token | mitigate | Token is created + stored backend/Wompi-side; client never fabricates or persists a token |
| T-v7-04-SC | Tampering (supply chain) | npm installs | accept | Zero new packages |
</threat_model>

<verification>
- `pnpm -C /Users/nicolasgarcia/rent/mvp-portal-inquilino build` succeeds.
- `pnpm test` — 0 NEW failures (≈7 pre-existing unrelated — see v7-01 deferred-items.md).
- Grep gates (Task verifies): `autopagoApi` + enable/cancel + `available` + 403/404 in the service;
  `AutopagoSection` uses autopagoApi + "Próximamente" + cancel, zero "autopago activado/activo"; the page
  mounts `AutopagoSection`.
- Manual smoke as `tenant`: the pagos sidebar shows an "Autopago" card in the honest "Próximamente" state;
  no active-autopago claim, no tokenized card, no charge.
</verification>

<success_criteria>
- PAGO-04: the tenant can reach an autopago configure/change/cancel surface backed by an api-client
  contract; because tokenization is backend-gated it degrades to an honest "Próximamente" with no
  fabricated activation, token, or charge on any real-tenant path.
</success_criteria>

<output>
After completion, create `.planning/phases/v7-04-pagos-wompi/v7-04-04-SUMMARY.md`.
</output>
</content>
