# v1 — front (rent/mvp)

**Estado:** ABIERTA
**Tag de freeze:** _(pendiente — `git tag v1 <commit>` cuando Victor confirme el corte)_
**Rama de integración:** `Main`
**Emparejado con:** `@leasefy/cadence` v1 (commit `212354e`)

## PRs — orden de merge (bottom‑up, no saltarse el orden)

| # | PR / commit | Qué entra |
|---|-----|-----------|
| 1 | commit `1d43fa42` (checkpoint directo sobre `feat/leasefy-ds-redesign`, sin PR) | Chat: backend `snapshot`→`ChatDataCard` (SSE `onSnapshot` + one-shot) + §17G @-mención en ambos composers (hero+docked, nav teclado) + fix shimmer (`StepAIReview`). Base: migración `@leasefy/ui`→`@leasefy/cadence` + brand redesign de toda la plataforma. Bump `@leasefy/cadence` → v1.0.52. |

## Ledger de Victor — front

- **Migraciones:** _(si aplica)_
- **Secrets / env:** _(setear antes de deploy)_
- **Flags:** _(qué prender / dejar OFF)_
- **Notas:** redesign YA commiteado en `feat/leasefy-ds-redesign` @ `1d43fa42` (ya no hay riesgo de `git checkout`). El commit incluye rutas de preview **removibles** (`cadence-audit-preview`, `beta-welcome-preview`, `cadence-sidebar-preview`, `cadence-announcement-preview`, `chat-preview`, `chat-screen`) — borrar antes del corte si no entran. `public/fonts/Satoshi-*.woff2` quedaron **eliminadas** (verificar intención antes del freeze).

## Smoke (antes de marcar INTEGRADA)

- [ ] `next lint` + build OK en cada merge
- [ ] Smoke de browser en las vistas tocadas
