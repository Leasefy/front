---
phase: inmobiliaria-05-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/inmobiliaria/PipelineFilters.tsx
  - src/components/inmobiliaria/PipelineDetail.tsx
  - src/components/inmobiliaria/index.ts
autonomous: true

must_haves:
  truths:
    - "User can filter pipeline by agente"
    - "User can filter pipeline by property"
    - "User can filter pipeline by date range"
    - "User can view full pipeline item details in modal"
    - "User can see timeline of stage changes"
  artifacts:
    - path: "src/components/inmobiliaria/PipelineFilters.tsx"
      provides: "Filters for agente, property, and date"
      min_lines: 120
    - path: "src/components/inmobiliaria/PipelineDetail.tsx"
      provides: "Modal showing full pipeline item details"
      min_lines: 200
  key_links:
    - from: "PipelineFilters"
      to: "MOCK_AGENTES"
      via: "import from mock-inmobiliaria"
      pattern: "MOCK_AGENTES"
    - from: "PipelineDetail"
      to: "Sheet"
      via: "import from ui"
      pattern: "Sheet"
---

<objective>
Create PipelineFilters and PipelineDetail components for filtering and viewing pipeline items.

Purpose: Allow users to filter the Kanban board by agente/property/date and view detailed info for each pipeline item.
Output: PipelineFilters component and PipelineDetail modal ready for integration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@docs/ROADMAP-INMOBILIARIA.md

# Prior plan context
@.planning/phases/inmobiliaria-05-pipeline/05-01-PLAN.md

# Source files
@src/lib/types/inmobiliaria.ts
@src/lib/data/mock-inmobiliaria.ts
@src/components/inmobiliaria/index.ts

# Existing patterns to follow
@src/components/inmobiliaria/ConsignacionFilters.tsx
@src/components/inmobiliaria/ConsignacionDetailSections.tsx
@src/components/inmobiliaria/AsignacionModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PipelineFilters Component</name>
  <files>src/components/inmobiliaria/PipelineFilters.tsx</files>
  <action>
Create PipelineFilters component with filter controls:

1. Agente Filter:
   - Dropdown with all agentes from MOCK_AGENTES
   - "Todos los agentes" as default option
   - Show agente name and role badge in options

2. Property Filter:
   - Dropdown with unique properties from MOCK_CONSIGNACIONES
   - "Todas las propiedades" as default
   - Show property title and zone

3. Date Range Filter:
   - Two date inputs: "Desde" and "Hasta"
   - Filter by createdAt or enteredStageAt
   - Quick presets: "Hoy", "Esta semana", "Este mes"

4. Search Filter (optional):
   - Search input for candidate name
   - Debounced search (300ms)

5. Actions:
   - "Limpiar filtros" button
   - Active filter count badge

Props interface:
```typescript
interface PipelineFiltersProps {
  agentes: Agente[];
  consignaciones: Consignacion[];
  filters: {
    agenteId?: string;
    consignacionId?: string;
    dateFrom?: string;
    dateTo?: string;
    search?: string;
  };
  onFilterChange: (filters: PipelineFiltersProps['filters']) => void;
}
```

Layout: Horizontal row with responsive wrap on mobile.
Follow ConsignacionFilters styling pattern.
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>PipelineFilters renders with all filter controls</done>
</task>

<task type="auto">
  <name>Task 2: Create PipelineDetail Component</name>
  <files>src/components/inmobiliaria/PipelineDetail.tsx</files>
  <action>
Create PipelineDetail as a Sheet drawer showing full pipeline item info:

1. Header:
   - Property title as main heading
   - Stage badge with current stage color
   - Close button

2. Property Section:
   - Large thumbnail
   - Full address
   - Monthly rent
   - Link to consignacion detail

3. Candidate Section:
   - Avatar with name
   - Email and phone with action buttons (copy, call, WhatsApp)
   - Risk score visualization (colored badge + score if available)

4. Agente Section:
   - Assigned agente info
   - Option to reassign (opens AsignacionModal - placeholder for now)

5. Progress Section:
   - Current stage with days in stage
   - Next action with date
   - Warning indicator if overdue

6. Timeline Section (Mock):
   - List of stage transitions with dates
   - Show: stage name, date entered, days spent
   - Most recent at top
   - Use vertical timeline styling

7. Notes Section:
   - Display existing notes
   - Textarea for adding new notes (placeholder)

8. Actions Footer:
   - "Mover a siguiente etapa" button
   - "Marcar como perdido" button (secondary/destructive)
   - Both show toast for now

Props interface:
```typescript
interface PipelineDetailProps {
  isOpen: boolean;
  onClose: () => void;
  item: PipelineItem | null;
  onStageChange?: (itemId: string, newStage: PipelineStage) => void;
}
```

Use Sheet component from ui.
Follow AsignacionModal pattern for drawer behavior.
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>PipelineDetail modal shows complete item info with timeline</done>
</task>

<task type="auto">
  <name>Task 3: Update Barrel Export</name>
  <files>src/components/inmobiliaria/index.ts</files>
  <action>
Add exports for new components:

```typescript
export { PipelineFilters } from './PipelineFilters';
export { PipelineDetail } from './PipelineDetail';
```
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>Components exported from barrel</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm tsc --noEmit passes
- [ ] pnpm build succeeds
- [ ] PipelineFilters renders with all dropdowns
- [ ] Filter changes call onFilterChange
- [ ] PipelineDetail opens as Sheet drawer
- [ ] Timeline section shows mock stage history
- [ ] Action buttons show toasts
</verification>

<success_criteria>
- All tasks completed
- Filters include agente, property, date range
- Detail modal shows full pipeline item info
- Timeline section visualizes stage progression
- Quick actions available for stage changes
</success_criteria>

<output>
After completion, create `.planning/phases/inmobiliaria-05-pipeline/05-02-SUMMARY.md`
</output>
