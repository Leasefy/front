---
phase: inmobiliaria-04-agentes
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/inmobiliaria/AgenteProfile.tsx
  - src/components/inmobiliaria/AgenteMetrics.tsx
  - src/components/inmobiliaria/AgentePropertyList.tsx
  - src/components/inmobiliaria/AgentePipeline.tsx
  - src/components/inmobiliaria/index.ts
  - src/app/panel/inmobiliaria/agentes/[id]/page.tsx
autonomous: true

must_haves:
  truths:
    - "User can view full agente profile with contact info"
    - "User can see detailed performance metrics"
    - "User can see list of assigned properties"
    - "User can see agente's active pipeline items"
    - "User can see commission history"
  artifacts:
    - path: "src/components/inmobiliaria/AgenteProfile.tsx"
      provides: "Profile header with contact and role info"
      min_lines: 80
    - path: "src/components/inmobiliaria/AgenteMetrics.tsx"
      provides: "Detailed KPI cards for agente performance"
      min_lines: 100
    - path: "src/app/panel/inmobiliaria/agentes/[id]/page.tsx"
      provides: "Agente detail page"
      exports: ["default"]
  key_links:
    - from: "agentes/[id]/page.tsx"
      to: "getAgenteById"
      via: "import from mock-inmobiliaria"
      pattern: "getAgenteById"
    - from: "AgentePropertyList"
      to: "getConsignacionesForAgente"
      via: "function call"
      pattern: "getConsignacionesForAgente"
---

<objective>
Create the Agente detail page with profile, metrics, properties, and pipeline sections.

Purpose: Allow inmobiliaria admins to see full agente performance and manage their workload.
Output: AgenteProfile, AgenteMetrics, AgentePropertyList, AgentePipeline components and detail page.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@docs/ROADMAP-INMOBILIARIA.md

# Prior plan context (if exists after execution)
# @.planning/phases/inmobiliaria-04-agentes/04-01-SUMMARY.md

# Source files
@src/lib/types/inmobiliaria.ts
@src/lib/data/mock-inmobiliaria.ts
@src/components/inmobiliaria/index.ts

# Existing patterns to follow
@src/components/inmobiliaria/ConsignacionHeader.tsx
@src/components/inmobiliaria/ConsignacionDetailSections.tsx
@src/app/panel/inmobiliaria/portafolio/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AgenteProfile and AgenteMetrics</name>
  <files>src/components/inmobiliaria/AgenteProfile.tsx, src/components/inmobiliaria/AgenteMetrics.tsx</files>
  <action>
1. AgenteProfile component:
   - Large avatar (or initials) with status indicator dot
   - Name, role badge, email, phone
   - Zone and specialization tags
   - Hire date formatted
   - Commission split visualization (progress bar like ConsignacionDetailSections)
   - Contact buttons: Email, Call, WhatsApp
   - Edit profile button (disabled placeholder)

2. AgenteMetrics component (8 KPI cards in 2x4 grid):
   - Propiedades Asignadas (count, icon Buildings)
   - Arriendos Activos (count, icon Handshake)
   - Cierres Este Mes (count, icon CheckCircle)
   - Cierres Este Año (count, icon Calendar)
   - Comisiones Mes (currency, icon CurrencyDollar)
   - Comisiones Totales (currency, icon Wallet)
   - Días Promedio Cierre (number, icon Clock)
   - Tasa de Conversión (percentage, icon ChartLineUp)

   Use color-coded backgrounds based on performance:
   - Green for above average
   - Amber for average
   - Red for below average (only for conversion rate <30%)

   Cards should have subtle hover effect.
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>AgenteProfile and AgenteMetrics render correctly with all data</done>
</task>

<task type="auto">
  <name>Task 2: Create AgentePropertyList and AgentePipeline</name>
  <files>src/components/inmobiliaria/AgentePropertyList.tsx, src/components/inmobiliaria/AgentePipeline.tsx</files>
  <action>
1. AgentePropertyList component:
   - List of properties assigned to this agente
   - Use getConsignacionesForAgente(agenteId)
   - Each row: thumbnail, title, zone, availability status, monthly rent
   - Click navigates to /panel/inmobiliaria/portafolio/[id]
   - Empty state if no properties
   - "Asignar propiedad" button (placeholder for 04-03)

2. AgentePipeline component:
   - List of active pipeline items for this agente
   - Use getPipelineItemsForAgente(agenteId)
   - Filter to exclude 'completed' and 'lost' stages
   - Each row: property thumbnail, candidate name, stage badge, days in stage
   - Stage badges use PIPELINE_STAGES colors
   - Click navigates to property/candidate (toast for now)
   - Empty state with "Sin leads activos" message
   - Show total count in header

Both components should be collapsible sections (like ConsignacionDetailSections pattern).
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>Property list and pipeline sections render with correct data</done>
</task>

<task type="auto">
  <name>Task 3: Create Agente Detail Page</name>
  <files>src/app/panel/inmobiliaria/agentes/[id]/page.tsx, src/components/inmobiliaria/index.ts</files>
  <action>
1. Create agentes/[id]/page.tsx:
   - Breadcrumb: Inmobiliaria > Agentes > [Agente Name]
   - Use getAgenteById to fetch agente
   - 404 NotFound if invalid ID
   - Layout: Two columns (2/3 + 1/3)

   Left column (2/3):
   - AgenteProfile at top
   - AgenteMetrics below
   - AgentePropertyList section

   Right column (1/3):
   - AgentePipeline section
   - "Historial de Comisiones" section (placeholder card with "Próximamente")

   Use staggered animations like ConsignacionDetail.

2. Update barrel export in index.ts with new components.

3. Update agentes/page.tsx to navigate to detail page on card/row click (instead of toast).
  </action>
  <verify>pnpm build</verify>
  <done>Agente detail page loads with all sections and navigation works</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm tsc --noEmit passes
- [ ] pnpm build succeeds
- [ ] Navigate to /panel/inmobiliaria/agentes/agent-001 shows detail
- [ ] Profile section shows all agente info
- [ ] Metrics section shows 8 KPIs
- [ ] Property list shows assigned properties
- [ ] Pipeline section shows active leads
- [ ] Back navigation to agentes list works
- [ ] Invalid ID shows 404
</verification>

<success_criteria>
- All tasks completed
- Detail page has two-column layout
- All sections render correctly
- Navigation between list and detail works
- Pipeline and properties show correct data for agente
</success_criteria>

<output>
After completion, create `.planning/phases/inmobiliaria-04-agentes/04-02-SUMMARY.md`
</output>
