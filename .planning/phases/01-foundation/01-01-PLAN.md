---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, tsconfig.json, next.config.ts, src/app/layout.tsx, src/app/page.tsx, .gitignore, .env.example]
autonomous: true

must_haves:
  truths:
    - "npm run dev starts development server without errors"
    - "npm run build completes successfully"
    - "TypeScript strict mode is enabled"
  artifacts:
    - path: "package.json"
      provides: "Next.js 14 dependencies"
      contains: "next"
    - path: "tsconfig.json"
      provides: "TypeScript strict configuration"
      contains: "strict"
    - path: "src/app/layout.tsx"
      provides: "Root layout with Spanish lang"
    - path: "src/app/page.tsx"
      provides: "Landing page placeholder"
  key_links:
    - from: "package.json"
      to: "node_modules"
      via: "npm install"
      pattern: "dependencies"
---

<objective>
Scaffold Next.js 14 App Router project with TypeScript strict mode.

Purpose: Create the foundation for Arriendo Fácil - a Colombian rental marketplace with AI scoring.
Output: Working Next.js project that can start dev server and build successfully.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/CONTEXT.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Next.js 14 project</name>
  <files>package.json, tsconfig.json, next.config.ts, src/app/layout.tsx, src/app/page.tsx, .gitignore</files>
  <action>
    Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm` with the following answers:
    - Would you like to use Turbopack? No (stability for MVP)

    After creation, verify package.json has Next.js 14.x.

    Update tsconfig.json to enable strict mode and path aliases:
    - "strict": true
    - "paths": { "@/*": ["./src/*"] }

    Update next.config.ts for future image remotePatterns (UploadThing).

    Update src/app/layout.tsx:
    - Set html lang="es" for Spanish/Colombia
    - Set metadata title: "Arriendo Fácil - Marketplace de Arriendos"
    - Set metadata description for SEO
  </action>
  <verify>npm run dev starts without errors, npm run build completes</verify>
  <done>Next.js 14 project created with TypeScript strict mode, Spanish lang, correct metadata</done>
</task>

<task type="auto">
  <name>Task 2: Create environment configuration</name>
  <files>.env.example, .env.local</files>
  <action>
    Create .env.example with all required environment variables (no secrets):

    ```
    # Database (Neon)
    DATABASE_URL="postgresql://..."

    # Auth (Clerk) - Phase 2
    NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=""
    CLERK_SECRET_KEY=""
    CLERK_WEBHOOK_SECRET=""

    # File Upload (UploadThing) - Phase 4
    UPLOADTHING_SECRET=""
    UPLOADTHING_APP_ID=""

    # Background Jobs (Inngest) - Phase 6
    INNGEST_EVENT_KEY=""
    INNGEST_SIGNING_KEY=""
    ```

    Create .env.local with placeholder DATABASE_URL (will be replaced when Neon is configured).

    Ensure .env.local is in .gitignore (should be by default).
  </action>
  <verify>.env.example exists, .env.local exists, .gitignore includes .env.local</verify>
  <done>Environment configuration template created for all planned integrations</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run dev` starts on localhost:3000
- [ ] `npm run build` completes without errors
- [ ] `npm run lint` passes
- [ ] tsconfig.json has "strict": true
- [ ] layout.tsx has lang="es"
- [ ] .env.example documents all planned env vars
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Project structure matches Next.js 14 App Router conventions
- Ready for UI configuration (Plan 02) and database setup (Plan 03)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
