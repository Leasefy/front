---
phase: 02-property-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types/property.ts
  - src/lib/data/mock-properties.ts
  - src/components/property/PropertyCard.tsx
autonomous: true

must_haves:
  truths:
    - "Property types are defined and exported"
    - "Mock data contains 15+ Colombian properties"
    - "PropertyCard displays image, price, location, features"
  artifacts:
    - path: "src/lib/types/property.ts"
      provides: "Property TypeScript interface"
      exports: ["Property", "PropertyAmenity", "PropertyStatus"]
    - path: "src/lib/data/mock-properties.ts"
      provides: "Realistic Colombian mock data"
      exports: ["mockProperties"]
      min_lines: 100
    - path: "src/components/property/PropertyCard.tsx"
      provides: "Property card component"
      exports: ["PropertyCard"]
  key_links:
    - from: "src/components/property/PropertyCard.tsx"
      to: "src/lib/types/property.ts"
      via: "type import"
      pattern: "import.*Property.*from"
---

<objective>
Create data foundation for Property Catalog: TypeScript types, mock data, and PropertyCard component.

Purpose: Establish the data layer and core visual component that all other catalog features depend on.
Output: Reusable Property types, realistic Colombian mock data, and PropertyCard component.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/FRONTEND-VISION.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

@src/components/ui/card.tsx
@src/components/ui/badge.tsx
@src/lib/utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Property TypeScript types</name>
  <files>src/lib/types/property.ts</files>
  <action>
Create comprehensive Property interface with all fields needed for catalog:

```typescript
export type PropertyStatus = 'available' | 'rented' | 'pending';
export type PropertyType = 'apartment' | 'house' | 'studio' | 'room';

export interface PropertyAmenity {
  id: string;
  name: string;
  icon?: string;
}

export interface Property {
  id: string;
  title: string;
  description: string;
  type: PropertyType;
  status: PropertyStatus;

  // Location
  city: string;
  neighborhood: string;
  address: string;

  // Pricing (COP as integers)
  monthlyRent: number;
  adminFee: number;
  deposit: number;

  // Features
  bedrooms: number;
  bathrooms: number;
  area: number; // m²
  floor?: number;

  // Amenities
  amenities: PropertyAmenity[];

  // Images
  images: string[];
  thumbnailUrl: string;

  // Metadata
  landlordId: string;
  createdAt: string;
  updatedAt: string;
}
```

Also create helper constants:
- COLOMBIAN_CITIES: string[] with major cities
- PROPERTY_AMENITIES: PropertyAmenity[] with common amenities (parking, gym, pool, etc.)
  </action>
  <verify>tsc --noEmit passes with no type errors</verify>
  <done>Property interface and related types exported, usable throughout app</done>
</task>

<task type="auto">
  <name>Task 2: Create mock properties data</name>
  <files>src/lib/data/mock-properties.ts</files>
  <action>
Create realistic Colombian property data for development. Requirements:

- 15+ properties across 5 cities (Bogotá, Medellín, Cali, Barranquilla, Cartagena)
- Varied types: apartments, houses, studios
- Realistic prices in COP (800K - 5M range for monthly rent)
- Real Colombian neighborhood names
- Mix of statuses (mostly available, some pending)
- 3-5 images per property (use placeholder URLs like https://placehold.co/800x600/png?text=Property+1)
- Varied amenities per property

Example property:
```typescript
{
  id: 'prop-001',
  title: 'Apartamento moderno en Chapinero',
  description: 'Hermoso apartamento de 2 habitaciones...',
  type: 'apartment',
  status: 'available',
  city: 'Bogotá',
  neighborhood: 'Chapinero Alto',
  address: 'Cra 7 #67-23',
  monthlyRent: 2500000,
  adminFee: 350000,
  deposit: 5000000,
  bedrooms: 2,
  bathrooms: 2,
  area: 75,
  floor: 8,
  amenities: [...],
  images: [...],
  thumbnailUrl: '...',
  landlordId: 'landlord-001',
  createdAt: '2025-01-15T10:00:00Z',
  updatedAt: '2025-01-15T10:00:00Z'
}
```

Export: mockProperties array
  </action>
  <verify>Import mockProperties and check length >= 15</verify>
  <done>15+ realistic Colombian properties ready for catalog display</done>
</task>

<task type="auto">
  <name>Task 3: Create PropertyCard component</name>
  <files>src/components/property/PropertyCard.tsx</files>
  <action>
Create PropertyCard component using shadcn Card as base:

Features:
- Large image with aspect ratio (16:9 or 4:3)
- Heart icon button for wishlist (top-right overlay)
- Price prominently displayed (formatted as COP)
- Location (neighborhood, city)
- Key features row: bedrooms, bathrooms, area
- Status badge if not available
- Hover state with subtle shadow lift

Use existing components:
- Card, CardContent from @/components/ui/card
- Badge from @/components/ui/badge
- cn() from @/lib/utils

Props interface:
```typescript
interface PropertyCardProps {
  property: Property;
  isWishlisted?: boolean;
  onWishlistToggle?: (propertyId: string) => void;
}
```

Format helpers needed:
- formatCurrency(amount: number): string - formats as "$ 2.500.000" (Colombian format)
- Create in src/lib/format.ts

Styling:
- Mobile-first (full width on mobile)
- Responsive image
- Truncate long titles
- Use Lucide icons: Heart, Bed, Bath, Maximize (for area)
  </action>
  <verify>npm run build succeeds, component renders in isolation</verify>
  <done>PropertyCard displays property with image, price, location, features, and wishlist button</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] Property types compile without errors
- [ ] mockProperties.length >= 15
- [ ] PropertyCard imports and uses Property type
- [ ] formatCurrency formats numbers with Colombian locale
</verification>

<success_criteria>
- All tasks completed
- No TypeScript errors
- Types, mock data, and PropertyCard are importable and usable
- PropertyCard renders correctly with mock property data
</success_criteria>

<output>
After completion, create `.planning/phases/02-property-catalog/02-01-SUMMARY.md`
</output>
