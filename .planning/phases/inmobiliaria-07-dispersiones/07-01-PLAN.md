---
phase: inmobiliaria-07-dispersiones
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/inmobiliaria/DispersionCard.tsx
  - src/components/inmobiliaria/DispersionTable.tsx
  - src/components/inmobiliaria/DispersionFilters.tsx
  - src/components/inmobiliaria/index.ts
autonomous: true

must_haves:
  truths:
    - "User can see dispersion card with propietario and amount info"
    - "User can see status badge with color coding"
    - "User can see table of dispersions with sortable columns"
    - "User can filter dispersions by propietario, status, and date"
    - "User can see net amount prominently displayed"
  artifacts:
    - path: "src/components/inmobiliaria/DispersionCard.tsx"
      provides: "Card showing individual dispersion info"
      min_lines: 150
    - path: "src/components/inmobiliaria/DispersionTable.tsx"
      provides: "Table view of dispersions with sorting"
      min_lines: 200
    - path: "src/components/inmobiliaria/DispersionFilters.tsx"
      provides: "Filters for dispersions list"
      min_lines: 150
  key_links:
    - from: "DispersionCard"
      to: "getDispersionStatusColor"
      via: "import from types/inmobiliaria"
      pattern: "getDispersionStatusColor"
    - from: "DispersionTable"
      to: "Dispersion"
      via: "import from types/inmobiliaria"
      pattern: "Dispersion"
---

<objective>
Create DispersionCard, DispersionTable, and DispersionFilters components for displaying owner disbursements.

Purpose: Display monthly owner payments with filtering and sorting capabilities.
Output: Core dispersion display components ready for page integration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@docs/ROADMAP-INMOBILIARIA.md

# Source files
@src/lib/types/inmobiliaria.ts (lines 306-353 for Dispersion types)
@src/lib/data/mock-inmobiliaria.ts (MOCK_DISPERSIONES)
@src/components/inmobiliaria/index.ts

# Similar patterns to follow
@src/components/inmobiliaria/CobroCard.tsx
@src/components/inmobiliaria/CobroTable.tsx
@src/components/inmobiliaria/CobroFilters.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Dispersion Helper Functions to Types</name>
  <files>src/lib/types/inmobiliaria.ts</files>
  <action>
Add helper functions for dispersion status colors (if not already present):

```typescript
export function getDispersionStatusColor(status: DispersionStatus): string {
  const colors: Record<DispersionStatus, string> = {
    pending: 'amber',
    processing: 'blue',
    completed: 'emerald',
    failed: 'red',
  };
  return colors[status];
}

export function getDispersionStatusLabel(status: DispersionStatus): string {
  const labels: Record<DispersionStatus, string> = {
    pending: 'Pendiente',
    processing: 'Procesando',
    completed: 'Completada',
    failed: 'Fallida',
  };
  return labels[status];
}
```

Add these at the end of the Dispersion section.
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>Helper functions added to types</done>
</task>

<task type="auto">
  <name>Task 2: Create DispersionCard Component</name>
  <files>src/components/inmobiliaria/DispersionCard.tsx</files>
  <action>
Create DispersionCard component following CobroCard pattern:

1. Card Structure:
   - Status-colored left border (amber/blue/emerald/red)
   - Propietario name as header
   - Month display (e.g., "Enero 2026")
   - Bank account info (masked)

2. Amounts Section:
   - Total collected (from all properties)
   - Commission deducted (with percentage)
   - Net to propietario (large, prominent, green)

3. Properties Summary:
   - Count of properties included
   - Expandable list or tooltip with property names

4. Status Indicator:
   - Status badge with color
   - Processing: show spinner or animation
   - Completed: show transfer reference and date
   - Failed: show error reason

5. Actions:
   - "Ver detalle" button
   - "Procesar" button (if pending)
   - Menu with additional actions

6. Compact Variant:
   - Just propietario, net amount, status
   - For use in summaries or dashboards

Props interface:
```typescript
interface DispersionCardProps {
  dispersion: Dispersion;
  onViewDetail?: (dispersion: Dispersion) => void;
  onProcess?: (dispersion: Dispersion) => void;
  compact?: boolean;
}
```

Use formatCurrency from lib/format.ts.
Use motion for subtle hover effects.
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>DispersionCard displays propietario disbursement info</done>
</task>

<task type="auto">
  <name>Task 3: Create DispersionTable Component</name>
  <files>src/components/inmobiliaria/DispersionTable.tsx</files>
  <action>
Create DispersionTable component following CobroTable pattern:

1. Columns:
   - Propietario (name with avatar/initials)
   - Mes (formatted month)
   - Propiedades (count badge)
   - Recaudado (total collected)
   - Comisión (commission amount)
   - Neto (net to propietario - emphasized)
   - Estado (status badge)
   - Fecha Pago (payment date if completed)
   - Acciones (menu)

2. Sortable Headers:
   - SortableHeader component for each sortable column
   - Default sort by month desc, then propietario

3. Row Styling:
   - Hover state for interaction
   - Status-based subtle row tinting
   - Completed rows slightly muted

4. Summary Footer:
   - Total to disburse
   - Total commissions
   - Count by status

5. Actions Column:
   - View detail
   - Process (if pending)
   - Download extracto
   - Retry (if failed)

6. Empty State:
   - "No hay dispersiones" with icon
   - Suggestion to generate dispersions

Props interface:
```typescript
interface DispersionTableProps {
  dispersiones: Dispersion[];
  onSort?: (column: string, direction: 'asc' | 'desc') => void;
  onViewDetail?: (dispersion: Dispersion) => void;
  onProcess?: (dispersion: Dispersion) => void;
  showSummary?: boolean;
}
```

Use Table components from shadcn/ui.
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>DispersionTable shows sortable list with actions</done>
</task>

<task type="auto">
  <name>Task 4: Create DispersionFilters Component</name>
  <files>src/components/inmobiliaria/DispersionFilters.tsx</files>
  <action>
Create DispersionFilters component following CobroFilters pattern:

1. Month Selector:
   - Prominent month/year picker
   - Navigation arrows for prev/next month
   - Current month highlighted

2. Status Tabs:
   - Todos, Pendientes, Procesando, Completadas, Fallidas
   - Count badges on each tab
   - Active tab styling

3. Propietario Filter:
   - Dropdown with all propietarios
   - Search within dropdown
   - "Todos los propietarios" option

4. Search Input:
   - Search by propietario name
   - Debounced search (300ms)

5. Quick Actions:
   - "Generar Dispersiones" button (prominent)
   - "Exportar" dropdown (Excel, PDF)

6. Active Filters Indicator:
   - Show count of active filters
   - Clear all button

Props interface:
```typescript
export interface DispersionFiltersState {
  month: string; // '2026-02'
  status: DispersionStatus | 'all';
  propietarioId: string | 'all';
  search: string;
}

interface DispersionFiltersProps {
  filters: DispersionFiltersState;
  onFiltersChange: (filters: DispersionFiltersState) => void;
  propietarios: { id: string; name: string }[];
  statusCounts: Record<DispersionStatus | 'all', number>;
  onGenerateDispersiones?: () => void;
}
```

Use Tabs component for status filter.
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>DispersionFilters provides filtering UI</done>
</task>

<task type="auto">
  <name>Task 5: Update Barrel Export</name>
  <files>src/components/inmobiliaria/index.ts</files>
  <action>
Add exports for new components (append Dispersion section):

```typescript
// Dispersiones (Disbursements) Components
export { DispersionCard, DispersionCardCompact } from './DispersionCard';
export { DispersionTable } from './DispersionTable';
export { DispersionFilters } from './DispersionFilters';
export type { DispersionFiltersState } from './DispersionFilters';
```

Note: Only export DispersionCardCompact if implemented as separate export.
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>Components exported from barrel</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm tsc --noEmit passes
- [ ] pnpm build succeeds
- [ ] DispersionCard renders with all dispersion info
- [ ] DispersionTable sorts correctly
- [ ] DispersionFilters changes filter state
- [ ] Status colors match across components
- [ ] formatCurrency used consistently
</verification>

<success_criteria>
- All tasks completed
- Card shows propietario, amounts, status
- Table has sortable columns and actions
- Filters include month, status, propietario
- Components follow existing inmobiliaria patterns
- All amounts formatted correctly
</success_criteria>

<output>
After completion, create `.planning/phases/inmobiliaria-07-dispersiones/07-01-SUMMARY.md`
</output>
