---
phase: inmobiliaria-10-configuracion-docs-analytics
plan: 07
type: execute
wave: 3
depends_on: ["10-06"]
files_modified:
  - src/lib/types/inmobiliaria.ts
  - src/lib/data/mock-inmobiliaria.ts
  - src/components/inmobiliaria/AnalyticsTrends.tsx
  - src/components/inmobiliaria/AnalyticsForecasting.tsx
  - src/components/inmobiliaria/index.ts
autonomous: true

must_haves:
  truths:
    - "User can see trend analysis with period comparison"
    - "User can view forecasted metrics for next periods"
    - "User can identify seasonal patterns"
    - "User can see alerts for anomalies"
    - "User can drill down into specific metrics"
  artifacts:
    - path: "src/components/inmobiliaria/AnalyticsTrends.tsx"
      provides: "Trend analysis with comparison and pattern detection"
      min_lines: 350
    - path: "src/components/inmobiliaria/AnalyticsForecasting.tsx"
      provides: "Forecasting charts with confidence intervals"
      min_lines: 300
  key_links:
    - from: "AnalyticsTrends"
      to: "TrendAnalysis"
      via: "import from types/inmobiliaria"
      pattern: "TrendAnalysis"
    - from: "AnalyticsForecasting"
      to: "ForecastData"
      via: "import from types/inmobiliaria"
      pattern: "ForecastData"
---

<objective>
Create trend analysis and forecasting components.

Purpose: Provide predictive insights and pattern recognition for agency planning.
Output: AnalyticsTrends and AnalyticsForecasting for analytics page.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@docs/ROADMAP-INMOBILIARIA.md

# Source files
@src/lib/types/inmobiliaria.ts
@src/lib/data/mock-inmobiliaria.ts

# Existing patterns
@src/components/inmobiliaria/AnalyticsDashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Trend & Forecast Types</name>
  <files>src/lib/types/inmobiliaria.ts</files>
  <action>
Add types for trends and forecasting:

```typescript
// ============================================================================
// Analytics - Trends & Forecasting
// ============================================================================

export type ComparisonPeriod = 'previous_period' | 'previous_year' | 'custom';

export interface PeriodComparison {
  current: {
    label: string;
    startDate: string;
    endDate: string;
    value: number;
  };
  previous: {
    label: string;
    startDate: string;
    endDate: string;
    value: number;
  };
  change: {
    absolute: number;
    percentage: number;
    direction: TrendDirection;
  };
}

export interface TrendDataPoint {
  date: string;
  value: number;
  label?: string;
  isProjected?: boolean;
}

export interface SeasonalPattern {
  month: number;
  monthName: string;
  averageValue: number;
  deviation: number;
  isHighSeason: boolean;
  notes?: string;
}

export interface TrendAnomaly {
  date: string;
  value: number;
  expectedValue: number;
  deviationPercent: number;
  severity: 'low' | 'medium' | 'high';
  description: string;
}

export interface TrendAnalysis {
  metricId: string;
  metricLabel: string;
  data: TrendDataPoint[];
  comparison: PeriodComparison;
  seasonalPatterns: SeasonalPattern[];
  anomalies: TrendAnomaly[];
  trendLine: {
    slope: number;
    direction: TrendDirection;
    confidence: number;
  };
  insights: string[];
}

export interface ForecastDataPoint {
  date: string;
  predicted: number;
  lowerBound: number;
  upperBound: number;
  confidence: number;
}

export interface ForecastScenario {
  id: string;
  name: string;
  description: string;
  assumptions: string[];
  data: ForecastDataPoint[];
  probability: number;
}

export interface ForecastData {
  metricId: string;
  metricLabel: string;
  unit: string;
  historical: TrendDataPoint[];
  baseline: ForecastDataPoint[];
  scenarios: ForecastScenario[];
  factors: {
    name: string;
    impact: 'positive' | 'negative' | 'neutral';
    weight: number;
  }[];
  lastUpdated: string;
}

// Helper functions
export function getAnomalySeverityColor(severity: TrendAnomaly['severity']): string {
  const colors = {
    low: 'bg-yellow-100 text-yellow-700',
    medium: 'bg-orange-100 text-orange-700',
    high: 'bg-red-100 text-red-700',
  };
  return colors[severity];
}

export function formatConfidence(confidence: number): string {
  return `${(confidence * 100).toFixed(0)}% confianza`;
}

export function getSeasonColor(isHighSeason: boolean): string {
  return isHighSeason
    ? 'bg-emerald-100 text-emerald-700'
    : 'bg-slate-100 text-slate-700';
}

export function getMonthName(month: number): string {
  const months = [
    'Enero', 'Febrero', 'Marzo', 'Abril', 'Mayo', 'Junio',
    'Julio', 'Agosto', 'Septiembre', 'Octubre', 'Noviembre', 'Diciembre'
  ];
  return months[month - 1] || '';
}
```
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>Trend and forecast types added</done>
</task>

<task type="auto">
  <name>Task 2: Add Mock Trend & Forecast Data</name>
  <files>src/lib/data/mock-inmobiliaria.ts</files>
  <action>
Add mock data for trends and forecasting:

```typescript
import type { TrendAnalysis, ForecastData, TrendDataPoint, ForecastDataPoint, SeasonalPattern, TrendAnomaly, ForecastScenario } from '@/lib/types/inmobiliaria';

function generateTrendData(months: number, baseValue: number, growthRate: number): TrendDataPoint[] {
  const data: TrendDataPoint[] = [];
  const today = new Date();

  for (let i = months - 1; i >= 0; i--) {
    const date = new Date(today);
    date.setMonth(date.getMonth() - i);
    const seasonalFactor = 1 + (Math.sin((date.getMonth() / 12) * 2 * Math.PI) * 0.1);
    const noise = (Math.random() - 0.5) * 0.1;
    const value = baseValue * (1 + growthRate) ** (months - i) * seasonalFactor * (1 + noise);

    data.push({
      date: date.toISOString().split('T')[0],
      value: Math.round(value),
      label: date.toLocaleDateString('es-CO', { month: 'short', year: '2-digit' }),
    });
  }

  return data;
}

function generateForecastData(months: number, lastValue: number, growthRate: number): ForecastDataPoint[] {
  const data: ForecastDataPoint[] = [];
  const today = new Date();

  for (let i = 1; i <= months; i++) {
    const date = new Date(today);
    date.setMonth(date.getMonth() + i);
    const predicted = lastValue * (1 + growthRate) ** i;
    const uncertainty = 0.05 * i; // Uncertainty grows with time

    data.push({
      date: date.toISOString().split('T')[0],
      predicted: Math.round(predicted),
      lowerBound: Math.round(predicted * (1 - uncertainty)),
      upperBound: Math.round(predicted * (1 + uncertainty)),
      confidence: Math.max(0.5, 0.95 - (i * 0.05)),
    });
  }

  return data;
}

export function generateTrendAnalysis(): TrendAnalysis[] {
  return [
    {
      metricId: 'revenue',
      metricLabel: 'Ingresos Mensuales',
      data: generateTrendData(12, 100000000, 0.02),
      comparison: {
        current: { label: 'Este mes', startDate: '2026-02-01', endDate: '2026-02-28', value: 125400000 },
        previous: { label: 'Mes anterior', startDate: '2026-01-01', endDate: '2026-01-31', value: 118200000 },
        change: { absolute: 7200000, percentage: 6.1, direction: 'up' },
      },
      seasonalPatterns: [
        { month: 1, monthName: 'Enero', averageValue: 95, deviation: -5, isHighSeason: false },
        { month: 2, monthName: 'Febrero', averageValue: 98, deviation: -2, isHighSeason: false },
        { month: 6, monthName: 'Junio', averageValue: 110, deviation: 10, isHighSeason: true, notes: 'Temporada alta por cambio de ano escolar' },
        { month: 7, monthName: 'Julio', averageValue: 115, deviation: 15, isHighSeason: true },
        { month: 12, monthName: 'Diciembre', averageValue: 88, deviation: -12, isHighSeason: false, notes: 'Baja por festivos' },
      ],
      anomalies: [
        { date: '2025-11-15', value: 85000000, expectedValue: 115000000, deviationPercent: -26, severity: 'high', description: 'Caida inusual por evento de mercado' },
      ],
      trendLine: { slope: 0.02, direction: 'up', confidence: 0.87 },
      insights: [
        'Crecimiento consistente de 2% mensual en el ultimo ano',
        'Junio-Julio son los meses mas fuertes (temporada escolar)',
        'Diciembre presenta caida estacional del 12%',
      ],
    },
    {
      metricId: 'occupancy',
      metricLabel: 'Tasa de Ocupacion',
      data: generateTrendData(12, 88, 0.003),
      comparison: {
        current: { label: 'Este mes', startDate: '2026-02-01', endDate: '2026-02-28', value: 92.3 },
        previous: { label: 'Mes anterior', startDate: '2026-01-01', endDate: '2026-01-31', value: 91.1 },
        change: { absolute: 1.2, percentage: 1.3, direction: 'up' },
      },
      seasonalPatterns: [
        { month: 1, monthName: 'Enero', averageValue: 90, deviation: 0, isHighSeason: false },
        { month: 6, monthName: 'Junio', averageValue: 95, deviation: 5, isHighSeason: true },
      ],
      anomalies: [],
      trendLine: { slope: 0.003, direction: 'up', confidence: 0.92 },
      insights: [
        'Ocupacion estable con tendencia ligeramente positiva',
        'Meta de 95% alcanzable para Q3 si se mantiene el ritmo',
      ],
    },
  ];
}

export function generateForecastData(): ForecastData[] {
  return [
    {
      metricId: 'revenue',
      metricLabel: 'Proyeccion de Ingresos',
      unit: 'COP',
      historical: generateTrendData(12, 100000000, 0.02),
      baseline: generateForecastData(6, 125400000, 0.015),
      scenarios: [
        {
          id: 'optimistic',
          name: 'Escenario Optimista',
          description: 'Expansion de portafolio + mejora en ocupacion',
          assumptions: ['10 propiedades nuevas', 'Ocupacion al 95%', 'Incremento promedio de canon 8%'],
          data: generateForecastData(6, 125400000, 0.03),
          probability: 0.25,
        },
        {
          id: 'conservative',
          name: 'Escenario Conservador',
          description: 'Mantener operacion actual sin expansion',
          assumptions: ['Sin nuevas propiedades', 'Ocupacion estable', 'Incremento canon igual a IPC'],
          data: generateForecastData(6, 125400000, 0.008),
          probability: 0.50,
        },
        {
          id: 'pessimistic',
          name: 'Escenario Pesimista',
          description: 'Contraccion del mercado',
          assumptions: ['Perdida de 5 propiedades', 'Ocupacion baja a 85%', 'Presion en precios'],
          data: generateForecastData(6, 125400000, -0.01),
          probability: 0.25,
        },
      ],
      factors: [
        { name: 'Crecimiento del mercado', impact: 'positive', weight: 0.3 },
        { name: 'Nuevas consignaciones', impact: 'positive', weight: 0.25 },
        { name: 'Competencia', impact: 'negative', weight: 0.2 },
        { name: 'Condiciones economicas', impact: 'neutral', weight: 0.25 },
      ],
      lastUpdated: new Date().toISOString(),
    },
    {
      metricId: 'occupancy',
      metricLabel: 'Proyeccion de Ocupacion',
      unit: '%',
      historical: generateTrendData(12, 88, 0.003).map(d => ({ ...d, value: d.value / 1000000 })),
      baseline: generateForecastData(6, 92.3, 0.005).map(d => ({
        ...d,
        predicted: d.predicted / 1000000,
        lowerBound: d.lowerBound / 1000000,
        upperBound: Math.min(100, d.upperBound / 1000000),
      })),
      scenarios: [],
      factors: [
        { name: 'Demanda del mercado', impact: 'positive', weight: 0.4 },
        { name: 'Calidad del portafolio', impact: 'positive', weight: 0.35 },
        { name: 'Tiempos de respuesta', impact: 'positive', weight: 0.25 },
      ],
      lastUpdated: new Date().toISOString(),
    },
  ];
}

export const MOCK_TREND_ANALYSIS = generateTrendAnalysis();
export const MOCK_FORECAST_DATA = generateForecastData();
```
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>Mock trend and forecast data created</done>
</task>

<task type="auto">
  <name>Task 3: Create AnalyticsTrends Component</name>
  <files>src/components/inmobiliaria/AnalyticsTrends.tsx</files>
  <action>
Create AnalyticsTrends component:

1. Header:
   - Title "Analisis de Tendencias"
   - Metric selector dropdown
   - Comparison period selector

2. Period Comparison Card:
   - Current period value (large)
   - Previous period value
   - Change percentage with arrow
   - Visual comparison bar

3. Trend Chart:
   - Line chart with historical data
   - Trend line overlay
   - Confidence band (shaded area)
   - Anomaly markers (red dots)
   - Hover tooltips

4. Seasonal Patterns Section:
   - 12-month mini bar chart
   - High season badges
   - Pattern description
   - Notes per month

5. Anomalies Table:
   - Date
   - Actual vs Expected
   - Deviation %
   - Severity badge
   - Description

6. Insights Panel:
   - Auto-generated insights list
   - Bullet points with icons
   - Actionable recommendations

Props interface:
```typescript
interface AnalyticsTrendsProps {
  data: TrendAnalysis[];
  selectedMetric?: string;
  onMetricChange?: (metricId: string) => void;
  onPeriodChange?: (period: ComparisonPeriod) => void;
}
```

Use SVG for charts.
Animate trend line drawing.
Highlight anomalies on hover.
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>AnalyticsTrends component created</done>
</task>

<task type="auto">
  <name>Task 4: Create AnalyticsForecasting Component</name>
  <files>src/components/inmobiliaria/AnalyticsForecasting.tsx</files>
  <action>
Create AnalyticsForecasting component:

1. Header:
   - Title "Proyecciones"
   - Metric selector
   - Forecast horizon selector (3/6/12 meses)

2. Main Forecast Chart:
   - Historical data (solid line)
   - Baseline forecast (dashed line)
   - Confidence interval (shaded band)
   - Scenario overlays (different colors)
   - Legend with scenario toggles

3. Scenario Cards:
   - Card per scenario (optimistic, conservative, pessimistic)
   - Probability badge
   - Key assumptions list
   - Projected end value
   - Select/highlight scenario

4. Forecast Details Table:
   - Month
   - Predicted value
   - Lower bound
   - Upper bound
   - Confidence %

5. Factors Panel:
   - Factor name
   - Impact indicator (positive/negative/neutral)
   - Weight bar
   - Description tooltip

6. Actions:
   - Export forecast
   - Adjust assumptions
   - Run what-if analysis

Props interface:
```typescript
interface AnalyticsForecastingProps {
  data: ForecastData[];
  selectedMetric?: string;
  horizon?: number;
  onMetricChange?: (metricId: string) => void;
  onHorizonChange?: (months: number) => void;
  onExport?: () => void;
}
```

Use SVG for charts.
Show confidence interval as gradient opacity.
Animate scenario switching.
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>AnalyticsForecasting component created</done>
</task>

<task type="auto">
  <name>Task 5: Update Barrel Export</name>
  <files>src/components/inmobiliaria/index.ts</files>
  <action>
Add exports for trend/forecast components:

```typescript
// Analytics - Trends & Forecasting (Phase 10)
export { AnalyticsTrends } from './AnalyticsTrends';
export { AnalyticsForecasting } from './AnalyticsForecasting';
```
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>Trend/forecast components exported</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm tsc --noEmit passes
- [ ] Trend and forecast types defined
- [ ] Mock data generators work
- [ ] AnalyticsTrends shows comparison and patterns
- [ ] Anomalies are highlighted
- [ ] AnalyticsForecasting shows scenarios
- [ ] Confidence intervals display correctly
- [ ] Components exported from barrel
</verification>

<success_criteria>
- All tasks completed
- Trend analysis compares periods correctly
- Seasonal patterns are visualized
- Anomalies table shows deviations
- Forecast shows multiple scenarios
- Confidence intervals are visible
</success_criteria>

<output>
After completion, create `.planning/phases/inmobiliaria-10-configuracion-docs-analytics/10-07-SUMMARY.md`
</output>
