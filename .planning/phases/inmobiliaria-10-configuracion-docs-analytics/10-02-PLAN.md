---
phase: inmobiliaria-10-configuracion-docs-analytics
plan: 02
type: execute
wave: 1
depends_on: ["10-01"]
files_modified:
  - src/lib/types/inmobiliaria.ts
  - src/lib/data/mock-inmobiliaria.ts
  - src/components/inmobiliaria/ConfigUsuarios.tsx
  - src/components/inmobiliaria/ConfigPermisos.tsx
  - src/components/inmobiliaria/index.ts
autonomous: true

must_haves:
  truths:
    - "User can view list of agency users with roles"
    - "User can invite new users by email"
    - "User can assign roles (admin, agente, contador, viewer)"
    - "User can configure permissions per role"
    - "User can deactivate users"
  artifacts:
    - path: "src/components/inmobiliaria/ConfigUsuarios.tsx"
      provides: "User management table with invite and role assignment"
      min_lines: 300
    - path: "src/components/inmobiliaria/ConfigPermisos.tsx"
      provides: "Permission matrix editor for roles"
      min_lines: 250
  key_links:
    - from: "ConfigUsuarios"
      to: "AgencyUser"
      via: "import from types/inmobiliaria"
      pattern: "AgencyUser"
    - from: "ConfigPermisos"
      to: "RolePermissions"
      via: "import from types/inmobiliaria"
      pattern: "RolePermissions"
---

<objective>
Create user management and permissions configuration components.

Purpose: Allow agency admins to manage team members and control access levels.
Output: ConfigUsuarios and ConfigPermisos components for team management.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@docs/ROADMAP-INMOBILIARIA.md

# Source files
@src/lib/types/inmobiliaria.ts
@src/lib/data/mock-inmobiliaria.ts

# Existing patterns
@src/components/inmobiliaria/AgentesTable.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add User & Permission Types</name>
  <files>src/lib/types/inmobiliaria.ts</files>
  <action>
Add types for users and permissions:

```typescript
// ============================================================================
// Configuracion - Users & Permissions
// ============================================================================

export type AgencyRole = 'admin' | 'agente' | 'contador' | 'viewer';

export type PermissionModule =
  | 'dashboard'
  | 'propietarios'
  | 'portafolio'
  | 'pipeline'
  | 'agentes'
  | 'cobros'
  | 'dispersiones'
  | 'operaciones'
  | 'reportes'
  | 'configuracion'
  | 'documentos'
  | 'analytics';

export type PermissionAction = 'view' | 'create' | 'edit' | 'delete' | 'export';

export interface RolePermission {
  module: PermissionModule;
  actions: PermissionAction[];
}

export interface RolePermissions {
  role: AgencyRole;
  permissions: RolePermission[];
}

export interface AgencyUser {
  id: string;
  email: string;
  name: string;
  role: AgencyRole;
  avatar?: string;
  phone?: string;
  status: 'active' | 'invited' | 'inactive';
  invitedAt?: string;
  lastLoginAt?: string;
  createdAt: string;
}

export interface UserInvite {
  email: string;
  name: string;
  role: AgencyRole;
  message?: string;
}

// Helper functions
export function getRoleLabel(role: AgencyRole): string {
  const labels: Record<AgencyRole, string> = {
    admin: 'Administrador',
    agente: 'Agente',
    contador: 'Contador',
    viewer: 'Solo Lectura',
  };
  return labels[role];
}

export function getRoleColor(role: AgencyRole): string {
  const colors: Record<AgencyRole, string> = {
    admin: 'bg-purple-100 text-purple-700 dark:bg-purple-900/30 dark:text-purple-400',
    agente: 'bg-blue-100 text-blue-700 dark:bg-blue-900/30 dark:text-blue-400',
    contador: 'bg-emerald-100 text-emerald-700 dark:bg-emerald-900/30 dark:text-emerald-400',
    viewer: 'bg-slate-100 text-slate-700 dark:bg-slate-900/30 dark:text-slate-400',
  };
  return colors[role];
}

export function getUserStatusColor(status: AgencyUser['status']): string {
  const colors = {
    active: 'bg-emerald-100 text-emerald-700',
    invited: 'bg-amber-100 text-amber-700',
    inactive: 'bg-slate-100 text-slate-700',
  };
  return colors[status];
}

export function getModuleLabel(module: PermissionModule): string {
  const labels: Record<PermissionModule, string> = {
    dashboard: 'Dashboard',
    propietarios: 'Propietarios',
    portafolio: 'Portafolio',
    pipeline: 'Pipeline',
    agentes: 'Agentes',
    cobros: 'Cobros',
    dispersiones: 'Dispersiones',
    operaciones: 'Operaciones',
    reportes: 'Reportes',
    configuracion: 'Configuracion',
    documentos: 'Documentos',
    analytics: 'Analitica',
  };
  return labels[module];
}

// Default permissions by role
export const DEFAULT_ROLE_PERMISSIONS: Record<AgencyRole, RolePermissions> = {
  admin: {
    role: 'admin',
    permissions: [
      { module: 'dashboard', actions: ['view'] },
      { module: 'propietarios', actions: ['view', 'create', 'edit', 'delete'] },
      { module: 'portafolio', actions: ['view', 'create', 'edit', 'delete'] },
      { module: 'pipeline', actions: ['view', 'create', 'edit', 'delete'] },
      { module: 'agentes', actions: ['view', 'create', 'edit', 'delete'] },
      { module: 'cobros', actions: ['view', 'create', 'edit', 'delete', 'export'] },
      { module: 'dispersiones', actions: ['view', 'create', 'edit', 'delete', 'export'] },
      { module: 'operaciones', actions: ['view', 'create', 'edit', 'delete'] },
      { module: 'reportes', actions: ['view', 'export'] },
      { module: 'configuracion', actions: ['view', 'edit'] },
      { module: 'documentos', actions: ['view', 'create', 'edit', 'delete'] },
      { module: 'analytics', actions: ['view', 'export'] },
    ],
  },
  agente: {
    role: 'agente',
    permissions: [
      { module: 'dashboard', actions: ['view'] },
      { module: 'propietarios', actions: ['view'] },
      { module: 'portafolio', actions: ['view', 'edit'] },
      { module: 'pipeline', actions: ['view', 'create', 'edit'] },
      { module: 'agentes', actions: ['view'] },
      { module: 'cobros', actions: ['view'] },
      { module: 'operaciones', actions: ['view', 'edit'] },
      { module: 'documentos', actions: ['view'] },
    ],
  },
  contador: {
    role: 'contador',
    permissions: [
      { module: 'dashboard', actions: ['view'] },
      { module: 'propietarios', actions: ['view'] },
      { module: 'cobros', actions: ['view', 'create', 'edit', 'export'] },
      { module: 'dispersiones', actions: ['view', 'create', 'edit', 'export'] },
      { module: 'reportes', actions: ['view', 'export'] },
      { module: 'analytics', actions: ['view', 'export'] },
    ],
  },
  viewer: {
    role: 'viewer',
    permissions: [
      { module: 'dashboard', actions: ['view'] },
      { module: 'propietarios', actions: ['view'] },
      { module: 'portafolio', actions: ['view'] },
      { module: 'cobros', actions: ['view'] },
      { module: 'reportes', actions: ['view'] },
    ],
  },
};
```
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>User and permission types added</done>
</task>

<task type="auto">
  <name>Task 2: Add Mock Users Data</name>
  <files>src/lib/data/mock-inmobiliaria.ts</files>
  <action>
Add mock agency users:

```typescript
import type { AgencyUser, AgencyRole } from '@/lib/types/inmobiliaria';

export const MOCK_AGENCY_USERS: AgencyUser[] = [
  {
    id: 'user-1',
    email: 'admin@premiumproperties.co',
    name: 'Juan Carlos Restrepo',
    role: 'admin',
    avatar: 'https://api.dicebear.com/7.x/avataaars/svg?seed=juan',
    phone: '+57 310 555 1234',
    status: 'active',
    lastLoginAt: new Date().toISOString(),
    createdAt: '2024-01-15T10:00:00Z',
  },
  {
    id: 'user-2',
    email: 'maria.agente@premiumproperties.co',
    name: 'Maria Fernanda Lopez',
    role: 'agente',
    avatar: 'https://api.dicebear.com/7.x/avataaars/svg?seed=maria',
    phone: '+57 311 555 2345',
    status: 'active',
    lastLoginAt: new Date(Date.now() - 2 * 24 * 60 * 60 * 1000).toISOString(),
    createdAt: '2024-02-01T10:00:00Z',
  },
  {
    id: 'user-3',
    email: 'carlos.contador@premiumproperties.co',
    name: 'Carlos Alberto Gomez',
    role: 'contador',
    avatar: 'https://api.dicebear.com/7.x/avataaars/svg?seed=carlos',
    phone: '+57 312 555 3456',
    status: 'active',
    lastLoginAt: new Date(Date.now() - 1 * 24 * 60 * 60 * 1000).toISOString(),
    createdAt: '2024-02-15T10:00:00Z',
  },
  {
    id: 'user-4',
    email: 'nuevo.agente@premiumproperties.co',
    name: 'Nuevo Agente',
    role: 'agente',
    status: 'invited',
    invitedAt: new Date(Date.now() - 3 * 24 * 60 * 60 * 1000).toISOString(),
    createdAt: new Date(Date.now() - 3 * 24 * 60 * 60 * 1000).toISOString(),
  },
  {
    id: 'user-5',
    email: 'antiguo@premiumproperties.co',
    name: 'Usuario Inactivo',
    role: 'viewer',
    status: 'inactive',
    createdAt: '2023-06-01T10:00:00Z',
  },
];
```
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>Mock agency users created</done>
</task>

<task type="auto">
  <name>Task 3: Create ConfigUsuarios Component</name>
  <files>src/components/inmobiliaria/ConfigUsuarios.tsx</files>
  <action>
Create ConfigUsuarios user management component:

1. Header Row:
   - Title "Equipo"
   - User count
   - "Invitar Usuario" button (opens modal)

2. Filter/Search:
   - Search by name/email
   - Filter by role (all, admin, agente, contador, viewer)
   - Filter by status (all, active, invited, inactive)

3. Users Table:
   - Avatar + Name + Email
   - Role badge (color coded)
   - Status badge
   - Last login (relative time)
   - Actions menu

4. Actions Menu per user:
   - Editar rol
   - Reenviar invitacion (if invited)
   - Desactivar / Reactivar
   - Eliminar (with confirmation)

5. Invite Modal:
   - Email input (required)
   - Name input (required)
   - Role select (required)
   - Custom message textarea (optional)
   - Send invite button

Props interface:
```typescript
interface ConfigUsuariosProps {
  users: AgencyUser[];
  onInvite?: (invite: UserInvite) => void;
  onUpdateRole?: (userId: string, role: AgencyRole) => void;
  onToggleStatus?: (userId: string) => void;
  onResendInvite?: (userId: string) => void;
  onDelete?: (userId: string) => void;
  isLoading?: boolean;
}
```

Use Table from shadcn.
Use Dialog for invite modal.
Show relative time for last login ("hace 2 horas").
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>ConfigUsuarios component created</done>
</task>

<task type="auto">
  <name>Task 4: Create ConfigPermisos Component</name>
  <files>src/components/inmobiliaria/ConfigPermisos.tsx</files>
  <action>
Create ConfigPermisos permission matrix component:

1. Header:
   - Title "Permisos por Rol"
   - Description text
   - Reset to defaults button

2. Role Tabs:
   - Tab for each role (Admin, Agente, Contador, Viewer)
   - Show permission count per role

3. Permission Matrix (for selected role):
   - Rows: Modules (Dashboard, Propietarios, etc.)
   - Columns: Actions (Ver, Crear, Editar, Eliminar, Exportar)
   - Checkbox at intersection
   - Row header with module icon + name
   - "Select All" checkbox per row
   - "Select All" checkbox per column

4. Visual Indicators:
   - Disabled checkboxes for inherited permissions
   - Warning icon for elevated permissions
   - Tooltip explaining each permission

5. Save button with loading state

Props interface:
```typescript
interface ConfigPermisosProps {
  permissions: Record<AgencyRole, RolePermissions>;
  onSave?: (permissions: Record<AgencyRole, RolePermissions>) => void;
  isLoading?: boolean;
}
```

Use Tabs from shadcn.
Use Checkbox from shadcn.
Show confirmation before saving permission changes.
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>ConfigPermisos component created</done>
</task>

<task type="auto">
  <name>Task 5: Update Barrel Export</name>
  <files>src/components/inmobiliaria/index.ts</files>
  <action>
Add exports for user/permission components:

```typescript
// Configuracion - Users & Permissions (Phase 10)
export { ConfigUsuarios } from './ConfigUsuarios';
export { ConfigPermisos } from './ConfigPermisos';
```
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>User/permission components exported</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm tsc --noEmit passes
- [ ] User and permission types defined
- [ ] Mock agency users exported
- [ ] ConfigUsuarios shows user table with actions
- [ ] ConfigUsuarios invite modal works
- [ ] ConfigPermisos shows permission matrix
- [ ] Role tabs switch correctly
- [ ] Components exported from barrel
</verification>

<success_criteria>
- All tasks completed
- User list displays with role badges
- Invite modal collects user info
- Permission matrix is interactive
- Role-based permission defaults work
- Actions (edit role, deactivate) trigger callbacks
</success_criteria>

<output>
After completion, create `.planning/phases/inmobiliaria-10-configuracion-docs-analytics/10-02-SUMMARY.md`
</output>
