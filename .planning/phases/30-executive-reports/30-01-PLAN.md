---
phase: 30-executive-reports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/inmobiliaria/reports/ExecutiveSummary.tsx
  - src/lib/data/mock-reports.ts
autonomous: true

must_haves:
  truths:
    - "ExecutiveSummary shows key metrics on a single page (occupancy, collections, maintenance)"
    - "Portfolio health score is a composite number 0-100 with color indicator"
    - "Month-over-month deltas show green up arrows or red down arrows"
    - "Component accepts data as props (not importing mock data directly)"
  artifacts:
    - path: "src/components/inmobiliaria/reports/ExecutiveSummary.tsx"
      provides: "C-level executive summary with health score and MoM comparison"
      exports: ["ExecutiveSummary"]
    - path: "src/lib/data/mock-reports.ts"
      provides: "Extended with executive mock data"
  key_links:
    - from: "ExecutiveSummary.tsx"
      to: "mock-reports.ts"
      via: "uses ExecutiveData type"
    - from: "ExecutiveSummary.tsx"
      to: "TrendChart.tsx"
      via: "reuses chart components for mini trends"
---

<objective>
Build the executive summary component with portfolio health score and month-over-month comparison.

Purpose: C-level view showing the most important metrics at a glance with trend indicators.
Output: ExecutiveSummary component + extended mock data.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/lib/data/mock-reports.ts
@src/components/inmobiliaria/reports/TrendChart.tsx
@src/components/inmobiliaria/reports/OccupancyReport.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add executive data to mock-reports.ts</name>
  <files>src/lib/data/mock-reports.ts</files>
  <action>
    Read the existing mock-reports.ts file. Add executive report types and mock data:

    Interface ExecutiveData:
    - healthScore: number (0-100, composite)
    - healthLevel: 'excellent' | 'good' | 'warning' | 'critical'
    - metrics: Array of {
        id: string,
        labelEs: string,
        labelEn: string,
        currentValue: number,
        previousValue: number,
        format: 'percent' | 'currency' | 'number' | 'days',
        higherIsBetter: boolean
      }
    - monthlySummary: Array of { month: string, revenue: number, expenses: number, netIncome: number } (last 6 months)

    Mock data should include these metrics:
    - Tasa de ocupación: 87% (prev: 84%) — higher is better
    - Recaudo mensual: $45.200.000 (prev: $42.800.000) — higher is better
    - Tasa de mora: 4.2% (prev: 5.1%) — lower is better
    - Días promedio vacancia: 12 (prev: 15) — lower is better
    - Comisiones generadas: $5.400.000 (prev: $4.900.000) — higher is better
    - Solicitudes mantenimiento: 8 (prev: 11) — lower is better

    Health score calculation: weighted average of occupancy (30%), collection rate (30%), mora inverse (20%), vacancy inverse (20%). Mock value: 82/100 = 'good'.

    Export mockExecutiveData and ExecutiveData type.
  </action>
  <verify>npx next build --no-lint | grep "Compiled"</verify>
  <done>ExecutiveData type and mockExecutiveData exported from mock-reports.ts</done>
</task>

<task type="auto">
  <name>Task 2: Build ExecutiveSummary component</name>
  <files>src/components/inmobiliaria/reports/ExecutiveSummary.tsx</files>
  <action>
    Create an executive summary component designed for C-level consumption. Clean, high-level, no clutter.

    Layout:
    1. **Health Score Hero** — Large circular or semi-circular indicator showing portfolio health 0-100
       - Color: emerald for excellent (90+), blue for good (70-89), amber for warning (50-69), red for critical (<50)
       - Label: "Salud del Portafolio" / "Portfolio Health"
       - Large number in the center
       - Subtitle: health level text

    2. **Key Metrics Grid** — 6 cards in 2x3 or 3x2 grid
       - Each card shows: label, current value (large), delta indicator
       - Delta: green TrendUp + percentage if improved, red TrendDown if worsened
       - Delta calculation: ((current - previous) / previous * 100).toFixed(1)
       - For metrics where lower is better (mora, vacancy, maintenance), reverse the color logic

    3. **Revenue Trend** — Small 6-month chart using BarChart component
       - Shows revenue vs expenses side by side
       - Net income line below

    Props: { data: ExecutiveData }
    Use useI18n() for locale. Use Phosphor icons (TrendUp, TrendDown, ChartLineUp, Heartbeat, Buildings).
    Use cn() from utils. Follow neutral color scheme (decision: neutral-agent-ui).
    Use formatCurrency from inmobiliaria types for COP.
  </action>
  <verify>npx next build --no-lint | grep "Compiled"</verify>
  <done>ExecutiveSummary renders health score, 6 metric cards with deltas, revenue trend</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx next build --no-lint` compiles successfully
- [ ] ExecutiveData type and mock data exported
- [ ] ExecutiveSummary renders health score with color indicator
- [ ] MoM deltas show correct up/down arrows and colors
</verification>

<success_criteria>

- Executive summary is a clean, C-level view
- Health score is a visible, prominent indicator
- Deltas correctly show improvement/decline
- Reuses BarChart from Phase 29
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/30-executive-reports/30-01-SUMMARY.md`
</output>
