---
phase: 26-plan-gating
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/hooks/useAgencyPlan.ts
  - src/lib/constants/feature-gates.ts
autonomous: true

must_haves:
  truths:
    - "useAgencyPlan().hasFeature('advanced-reports') returns false for starter plan"
    - "useAgencyPlan().hasFeature('advanced-reports') returns true for growth plan"
    - "useAgencyPlan().hasFeature('executive-reports') returns true only for business+ plans"
    - "useAgencyPlan().hasFeature('ai-agents') returns true for all flex plans"
    - "Plan tier can be changed via localStorage for testing"
  artifacts:
    - path: "src/lib/constants/feature-gates.ts"
      provides: "Feature-to-plan mapping configuration"
      exports: ["FEATURE_GATES", "FeatureName"]
    - path: "src/lib/hooks/useAgencyPlan.ts"
      provides: "Enhanced hook with hasFeature() method"
      exports: ["useAgencyPlan"]
  key_links:
    - from: "useAgencyPlan.ts"
      to: "feature-gates.ts"
      via: "imports FEATURE_GATES config"
      pattern: "FEATURE_GATES"
---

<objective>
Build the feature gating configuration and enhance useAgencyPlan hook with feature-level access checks.

Purpose: Foundation for all plan-gated features (reports, reminders, agents). Every gated feature will call `hasFeature('name')`.
Output: Enhanced hook + feature gates config file.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/lib/hooks/useAgencyPlan.ts
@src/lib/types/subscription.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create feature gates configuration</name>
  <files>src/lib/constants/feature-gates.ts</files>
  <action>
    Create a feature gates config that maps feature names to minimum required plan tiers.

    Define a FeatureName type with all gatable features:
    - 'advanced-reports' → growth (both subscription and flex)
    - 'executive-reports' → agency-business (both subscription and flex)
    - 'automatic-reminders' → growth
    - 'contract-reminders' → growth
    - 'ai-agents' → starter (only on flex plans — needs separate check)
    - 'multi-branch' → agency-business
    - 'pdf-export' → growth

    Structure: Record<FeatureName, { minTier: AgencyPlanId; flexOnly?: boolean; labelEs: string; labelEn: string }>

    The flexOnly flag means the feature is only available on flex plans (per-lease), not subscription plans. AI agents are the key differentiator for flex.

    Import AgencyPlanId from subscription types. Export FEATURE_GATES and FeatureName.
  </action>
  <verify>TypeScript compiles: npx tsc --noEmit src/lib/constants/feature-gates.ts (via build check)</verify>
  <done>FEATURE_GATES exported with all feature mappings, FeatureName type exported</done>
</task>

<task type="auto">
  <name>Task 2: Enhance useAgencyPlan hook with hasFeature()</name>
  <files>src/lib/hooks/useAgencyPlan.ts</files>
  <action>
    Extend the existing useAgencyPlan hook to add:

    1. `hasFeature(name: FeatureName): boolean` — checks if current plan tier meets the minimum for that feature. Also checks flexOnly flag against a new `isFlexPlan` state.

    2. `isFlexPlan: boolean` — read from localStorage key 'leasefy_agency_plan_type' ('flex' | 'subscription'). Default to 'flex' for demo purposes.

    3. `getUpgradeReason(name: FeatureName): { requiredPlan: string; labelEs: string; labelEn: string } | null` — returns info about what plan is needed if feature is locked, null if accessible.

    4. `setPlan(planId: AgencyPlanId, type: 'flex' | 'subscription')` — for testing, sets plan in localStorage.

    Keep existing hasMinPlan() for backward compatibility.

    Import FEATURE_GATES and FeatureName from feature-gates.ts. Do NOT change the existing PLAN_TIER or STORAGE_KEY constants.
  </action>
  <verify>npx next build --no-lint succeeds (compilation check)</verify>
  <done>useAgencyPlan returns hasFeature, isFlexPlan, getUpgradeReason, setPlan in addition to existing API</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx next build --no-lint` compiles successfully
- [ ] feature-gates.ts exports FEATURE_GATES and FeatureName
- [ ] useAgencyPlan.ts exports enhanced hook with hasFeature()
- [ ] No changes to existing hook API (backward compatible)
</verification>

<success_criteria>

- Feature gates config defines all gatable features with minimum tiers
- useAgencyPlan hook has hasFeature() that checks feature access
- flexOnly flag correctly differentiates flex vs subscription plans
- AI agents only available on flex plans
- Plan can be changed via localStorage for testing
</success_criteria>

<output>
After completion, create `.planning/phases/26-plan-gating/26-01-SUMMARY.md`
</output>
