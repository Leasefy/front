---
phase: v7-04-pagos-wompi
plan: 02
type: execute
wave: 2
depends_on: [v7-04-01]
files_modified:
  - src/components/tenant/PayRentModal.tsx
autonomous: true
requirements: [PAGO-02, PAGO-05]

must_haves:
  truths:
    - "The rent CTA no longer hits the /pse-mock gateway — PayRentModal POSTs to /api/inquilino/pagos/wompi-session and redirects to the Wompi hosted checkout"
    - "The client sends only { leaseId } (with a forwarded Authorization: Bearer) — it never sends an amount (the PayRentModal.tsx:173 client-amount anti-pattern is removed)"
    - "The confirm step shows only the real monthlyRent from payment-info before redirect (no fabricated cuota de manejo / recargo / surcharge line)"
    - "The period-blocked pre-flight (PENDING_VALIDATION/APPROVED) and the REJECTED reason are preserved; both entry points (pagos + arriendo) update via the shared modal"
  artifacts:
    - path: "src/components/tenant/PayRentModal.tsx"
      provides: "Wompi hosted-checkout seam replacing the PSE-mock in-page form"
      contains: "/api/inquilino/pagos/wompi-session"
  key_links:
    - from: "src/components/tenant/PayRentModal.tsx"
      to: "/api/inquilino/pagos/wompi-session"
      via: "POST { leaseId } with Authorization: Bearer, then redirect to hosted checkout"
      pattern: "wompi-session"
    - from: "src/components/tenant/PayRentModal.tsx"
      to: "src/lib/payments/wompi-rent-session.ts"
      via: "buildWompiCheckoutUrl (reuse the tested pure helper)"
      pattern: "buildWompiCheckoutUrl"
---

<objective>
Replace the `/pse-mock` seam (PAGO-02) inside the **shared** `PayRentModal` — the single component
rendered by BOTH `inquilino/pagos/page.tsx:464` and `inquilino/arriendo/[leaseId]/page.tsx`, so
swapping it once updates both entry points. Today the modal collects PSE bank/payer data and calls
`psePaymentsApi.processPayment` with a **client-sent `amount`** (`PayRentModal.tsx:173-183`) — the
exact anti-pattern PAGO-02 removes.

New flow: keep the `loading` → `period-blocked` / `confirm` pre-flight (it reads `payment-info`,
which stays the source of truth), but the confirm step's CTA now calls the v7-04-01 route
`POST /api/inquilino/pagos/wompi-session` with **only `{ leaseId }`** and a forwarded
`Authorization: Bearer` header (token from `getAccessToken()`), then builds the hosted-checkout URL
with the tested `buildWompiCheckoutUrl` helper and does a full-page redirect
(`window.location.href`). The redirect-url returns to `/inquilino/pagos`, where v7-04-03 handles the
Wompi return params. The PSE `form`/`processing`/`result` in-page steps and the PSE payer fields are
removed.

Depends on v7-04-01 (the route + `buildWompiCheckoutUrl` helper).

Purpose: Satisfy PAGO-02 at the seam and PAGO-05's "show only the real amount, no fabricated fee,
no high-fee rail pre-selected" (Wompi's hosted page presents PSE/tarjeta/Nequi — the in-app modal
discloses only the real `monthlyRent`).
Output: `src/components/tenant/PayRentModal.tsx` rewired to Wompi.

**Guardrails:** drop the client `amount`; send only `{ leaseId }`; show only real `monthlyRent`
(no invented cuota de manejo / recargo — `adminFee` is `@deprecated`, fabricating a fee is itself a
dark pattern); neutral copy on REJECTED (backend reason only). Zero new npm packages.
</objective>

<execution_context>
@/Users/nicolasgarcia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolasgarcia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/v7-04-pagos-wompi/v7-04-RESEARCH.md
@.planning/phases/v7-04-pagos-wompi/v7-04-PATTERNS.md
@.planning/phases/v7-04-pagos-wompi/v7-04-01-SUMMARY.md

# READ FIRST: docs/DESIGN.md §4 (Buttons — primary CTA UPPERCASE + tracking-wide),
# §8 + §17 (Lenis stop + data-lenis-prevent — the modal already conforms), §16 (formatCurrency).

# File to modify (rewire in place — do NOT delete the modal or its period-blocked/confirm steps):
@src/components/tenant/PayRentModal.tsx
# THE client-trigger analog — copy the fetch → parse → build URL → window.location.href idiom:
@src/components/avaluo/WompiPayButton.tsx

<interfaces>
<!-- From v7-04-01 (new, tested) -->
// src/lib/payments/wompi-rent-session.ts
interface WompiRentSession { reference: string; amountInCents: number; currency: string; integrity: string; publicKey: string }
buildWompiCheckoutUrl(p: { publicKey; currency; amountInCents; reference; integrity; redirectUrl }): string
// POST /api/inquilino/pagos/wompi-session  body { leaseId }  → WompiRentSession   (401 if no auth, 409 if period not payable)

<!-- Token store (client.ts) — forward the tenant JWT to the route -->
import { getAccessToken } from '@/lib/api/client'   // returns string | null (Supabase access token, in-memory)

<!-- payment-info the modal already loads (leases.types.ts) -->
BackendPaymentInfo { monthlyRent; currentPeriod {month,year}; currentPeriodStatus; currentPeriodRejectionReason }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap the PSE process step for the Wompi hosted-checkout redirect</name>
  <files>src/components/tenant/PayRentModal.tsx</files>
  <action>
    Rewire the modal to the Wompi route. Keep the `loading`, `period-blocked`, and `confirm` steps and
    all the `payment-info` loading logic (`useEffect` at lines ~105-135). Remove the PSE-specific pieces:
    the `form`/`processing`/`result` steps, the payer/bank form state (`personType`, `documentType`,
    `documentNumber`, `bankCode`, `banks`, `validateForm`, `handleProcess`, `result`, `DOCUMENT_TYPES`,
    the `getBanks()` call), the `psePaymentsApi` import, and the `PseBank/PseProcessDto/PseProcessResponse/
    PseDocumentType/PsePersonType` imports. (The `onPaid` prop becomes unused because the browser leaves the
    page on redirect — keep the prop in the signature for source-compat with the two call sites, but do not
    call it; add a one-line comment that success is confirmed server-side on return, not in-page.)

    Add the redirect handler, modeled on `WompiPayButton.tsx:24-69`:
      - `import { getAccessToken } from '@/lib/api/client'` and
        `import { buildWompiCheckoutUrl, type WompiRentSession } from '@/lib/payments/wompi-rent-session'`.
      - Add a `redirecting` step (spinner + "Te estamos llevando al pago seguro…") shown while the fetch runs.
      - `handlePayWithWompi`:
          setStep('redirecting');
          const token = getAccessToken();
          const res = await fetch('/api/inquilino/pagos/wompi-session', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json', ...(token ? { Authorization: `Bearer ${token}` } : {}) },
            body: JSON.stringify({ leaseId }),          // ONLY leaseId — never an amount
          });
          if (res.status === 409) → toast.error('Este período ya está pagado o en verificación.') + back to confirm;
          if (!res.ok) → throw;
          const s = (await res.json()) as WompiRentSession;
          const url = buildWompiCheckoutUrl({ ...s, redirectUrl: window.location.origin + '/inquilino/pagos' });
          window.location.href = url;
          catch → toast.error('No pudimos iniciar el pago. Intentá nuevamente.') + setStep('confirm').
      - Wire the confirm-step footer CTA to `handlePayWithWompi`. Label it **"PAGAR ARRIENDO"** (uppercase,
        DESIGN.md §4). For REJECTED, keep the "Reintentar pago" affordance but route it to the same handler.
      - Update the header subtitle from "Método: PSE" to "Método: Wompi (PSE, tarjeta o Nequi)".

    Confirm step content: keep the existing `MonoLabel` "Período" + "Monto a pagar" block showing ONLY
    `formatCurrency(paymentInfo.monthlyRent)` (lines ~303-311). Do NOT add a cuota-de-manejo / recargo /
    surcharge line — there is no such field (`adminFee` is `@deprecated`); replace the "El pago se procesa
    a través de PSE…" helper text with a neutral note that they'll complete the payment on Wompi's secure
    page (PSE, tarjeta o Nequi) and that the confirmation appears in their history once verified.

    Preserve the Lenis `useLenis().stop()` + `data-lenis-prevent` behavior already in the modal (DESIGN.md §8).
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && M=src/components/tenant/PayRentModal.tsx && grep -q "/api/inquilino/pagos/wompi-session" $M && grep -q "getAccessToken" $M && grep -q "buildWompiCheckoutUrl" $M && grep -q "window.location.href" $M && grep -q "PAGAR ARRIENDO" $M && test $(grep -c "amount: paymentInfo" $M) -eq 0 && test $(grep -c "psePaymentsApi" $M) -eq 0 && test $(grep -ciE "cuota de manejo|cuotaManejo|adminFee|recargo" $M) -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>PayRentModal POSTs { leaseId } (with Bearer) to the wompi-session route and redirects via buildWompiCheckoutUrl; PSE form/process/result steps + psePaymentsApi removed; no client amount sent; confirm shows only real monthlyRent with no fabricated fee; CTA is uppercase "PAGAR ARRIENDO"; period-blocked + REJECTED-reason preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant browser (modal) → Next route `/api/inquilino/pagos/wompi-session` | Client sends only `leaseId` + its own Bearer; the route owns the amount |
| tenant browser → Wompi hosted checkout (`checkout.wompi.co`) | Full-page redirect; card/PSE/Nequi data entered on Wompi, never in-app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v7-04-05 | Tampering | client-sent amount | mitigate | Request body is `{ leaseId }` only; grep-gated removal of `amount: paymentInfo` and `psePaymentsApi` |
| T-v7-04-06 | Spoofing / dark pattern | fabricated fee | mitigate | Confirm shows only real `monthlyRent`; grep-gated absence of cuota-de-manejo/adminFee/recargo |
| T-v7-04-07 | Info disclosure | integrity hash | accept (upstream-mitigated) | Hash comes from the v7-04-01 route; the modal never computes it (copies the WompiPayButton discipline) |
| T-v7-04-SC | Tampering (supply chain) | npm installs | accept | Zero new packages |
</threat_model>

<verification>
- `pnpm -C /Users/nicolasgarcia/rent/mvp-portal-inquilino build` succeeds.
- `pnpm test` — 0 NEW failures (≈7 pre-existing unrelated — see v7-01 deferred-items.md).
- Grep gates (Task 1 verify): `wompi-session` + `getAccessToken` + `buildWompiCheckoutUrl` +
  `window.location.href` + uppercase `PAGAR ARRIENDO` present; zero `amount: paymentInfo`, zero
  `psePaymentsApi`, zero fabricated-fee tokens.
- Manual smoke as `tenant`: open the modal from `/inquilino/pagos` (and from `/inquilino/arriendo/:id`)
  → confirm shows only the real monthly amount → "PAGAR ARRIENDO" redirects to `checkout.wompi.co`;
  a PENDING_VALIDATION/APPROVED period still shows the period-blocked panel (no double-pay path).
</verification>

<success_criteria>
- PAGO-02 (seam): the tenant rent flow no longer touches `/pse-mock`; it issues a Wompi hosted-checkout
  session via the server route with a server-side amount, from a single shared modal that updates both
  the pagos and arriendo entry points.
- PAGO-05 (cost disclosure): the confirm step shows only the real `monthlyRent` before the tenant leaves
  for the method chooser (Wompi's page), with no fabricated fee and no pre-selected high-fee rail.
</success_criteria>

<output>
After completion, create `.planning/phases/v7-04-pagos-wompi/v7-04-02-SUMMARY.md`.
</output>
</content>
