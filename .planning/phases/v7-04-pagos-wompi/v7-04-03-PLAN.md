---
phase: v7-04-pagos-wompi
plan: 03
type: execute
wave: 3
depends_on: [v7-04-02]
files_modified:
  - src/lib/api/tenant-payment-requests.types.ts
  - src/lib/api/tenant-payment-requests.service.ts
  - src/app/inquilino/pagos/page.tsx
autonomous: true
requirements: [PAGO-03, PAGO-05]

must_haves:
  truths:
    - "On Wompi redirect-return the pagos page shows a neutral 'estamos confirmando tu pago' state and refetches the real source — it NEVER prints 'pago exitoso' nor flips UI on the client-controlled ?status param"
    - "Payment history + saldo trace to tenant-payment-requests / payment-info as the single source (no second computed saldo, no fabricated fee line)"
    - "Each payment row offers a 'comprobante interno' download gated on hasReceipt; with no backend endpoint it degrades to an honest 'Próximamente' — never labeled 'factura', never a fake PDF"
    - "The date locale bug es-CL is fixed to es-CO; mora framing stays neutral (no guilt/countdown)"
  artifacts:
    - path: "src/lib/api/tenant-payment-requests.service.ts"
      provides: "getReceiptUrl contract (signed-URL shape), 403/404 → null so UI shows Próximamente"
      contains: "getReceiptUrl"
    - path: "src/app/inquilino/pagos/page.tsx"
      provides: "Wompi return handling (confirmando), single-source saldo, comprobante interno → Próximamente, es-CO"
      contains: "comprobante interno"
  key_links:
    - from: "src/app/inquilino/pagos/page.tsx"
      to: "useSearchParams (?id / ?status)"
      via: "neutral confirmando toast + refetchRequests/refetchPaymentInfo (no premature success)"
      pattern: "confirmando"
    - from: "src/app/inquilino/pagos/page.tsx"
      to: "tenantPaymentRequestsApi.getReceiptUrl"
      via: "comprobante interno download gated on hasReceipt → Próximamente"
      pattern: "getReceiptUrl"
---

<objective>
Finish the tenant payments surface for the Wompi return + PAGO-03/PAGO-05, on
`src/app/inquilino/pagos/page.tsx` (the page that renders the shared `PayRentModal` and the Wompi
`redirect-url` target from v7-04-02).

Three things:
1. **Wompi return (no premature success):** on mount, read the `?id`/`?status` query params Wompi
   appends (analog: `avaluo/estado/[submissionId]/page.tsx:28-42`) and show a **neutral** informational
   toast — "Estamos confirmando tu pago" — then `refetchRequests()` + `refetchPaymentInfo()`. **Never**
   print "pago exitoso" and **never** branch the UI on `?status === 'APPROVED'`: the redirect params are
   client-controlled; only the backend webhook + landlord validation flip status (RESEARCH Pitfall 1 /
   guardrail "no premature success"). The interim truth is the existing `PENDING_VALIDATION` card.
2. **Comprobante interno (PAGO-03):** add a per-row download gated on `request.hasReceipt`, wired to a new
   `tenantPaymentRequestsApi.getReceiptUrl(id)` contract (signed-URL shape, v7-02 discipline). No endpoint
   exists today, so `getReceiptUrl` resolves 403/404 → `null` and the UI degrades to an honest
   **"Próximamente"** — labeled **"comprobante interno"**, never "factura", and never a fabricated PDF.
3. **Single-source saldo + hygiene (PAGO-05):** keep saldo/next-payment tracing to `payment-info` /
   `tenant-payment-requests` (no second computed number, no fabricated cuota-de-manejo/recargo line);
   fix the `es-CL` → `es-CO` locale bug at line 95; keep mora framing neutral (no guilt/countdown — the
   existing amber `daysUntil <= 3` factual highlight stays as-is, no escalation).

Depends on v7-04-02 (the modal now redirects here). This plan does NOT touch the modal.

Purpose: PAGO-03 (history real + comprobante "Próximamente") and PAGO-05 (single source, cost
transparency, no dark patterns) with an honest, reconciliation-safe return experience.
Output: receipt contract + hardened pagos page.

**Guardrails:** no "pago exitoso" from redirect; no second saldo; no fabricated fee; "comprobante
interno" not "factura"; no fake receipt PDF; neutral mora. Zero new npm packages.
</objective>

<execution_context>
@/Users/nicolasgarcia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolasgarcia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/v7-04-pagos-wompi/v7-04-RESEARCH.md
@.planning/phases/v7-04-pagos-wompi/v7-04-PATTERNS.md
@.planning/phases/v7-04-pagos-wompi/v7-04-02-SUMMARY.md

# READ FIRST: docs/DESIGN.md §11 (EmptyState / honest "Próximamente"), §4 Buttons/cards,
# §16 (Money & date — formatCurrency + es-CO), §4 Toaster (Sonner).

# Files to modify / create:
@src/app/inquilino/pagos/page.tsx
@src/lib/api/tenant-payment-requests.service.ts
@src/lib/api/tenant-payment-requests.types.ts
# THE Wompi-return analog (neutral toast on ?id/?status, runs once):
@src/app/avaluo/estado/[submissionId]/page.tsx
# THE signed-URL contract shape + 403/404 → tolerant idiom to copy:
@src/lib/api/tenant-payment-requests.service.ts

<interfaces>
<!-- Existing (tenant-payment-requests.types.ts) — history is REAL, single source -->
BackendTenantPaymentRequest { id; leaseId; amount; periodMonth; periodYear; status; hasReceipt: boolean; validatedAt; createdAt; dueDate; bankName; rejectionReason; ... }
type TenantPaymentRequestStatus = 'PENDING_VALIDATION' | 'APPROVED' | 'REJECTED' | 'DISPUTED' | 'CANCELLED'

<!-- Existing service (tenant-payment-requests.service.ts): getMine() with 403/404 → [] idiom to mirror -->
tenantPaymentRequestsApi.getMine(): Promise<BackendTenantPaymentRequest[]>

<!-- Existing page hooks/handlers already present in pagos/page.tsx -->
useMyPaymentRequests() → { requests, refetch: refetchRequests }
useLeasePaymentInfo(leaseId) → { info, refetch: refetchPaymentInfo }
handlePaid() → refetchRequests() + refetchPaymentInfo()   // reuse for the return refetch
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: getReceiptUrl contract (comprobante interno, signed-URL shape, tolerant)</name>
  <files>src/lib/api/tenant-payment-requests.types.ts, src/lib/api/tenant-payment-requests.service.ts</files>
  <action>
    In `tenant-payment-requests.types.ts` add `export interface TenantReceiptUrl { url: string; expiresAt: string }`
    (signed/expiring shape, mirroring the v7-02 `ContractSignedPdf { url, expiresAt }` discipline).

    In `tenant-payment-requests.service.ts` add
    `async getReceiptUrl(requestId: string): Promise<TenantReceiptUrl | null>` →
    `apiClient.get<TenantReceiptUrl>(`/tenant-payments/requests/${requestId}/receipt-url`)`, wrapped so that
    `ApiError` with `status === 403 || 404` (endpoint not live yet) returns `null` — the same tolerant idiom
    as `getMine()`. JSDoc: this returns a signed, expiring URL to the **internal payment receipt
    ("comprobante interno", NOT a DIAN "factura")**; the backend endpoint does not exist yet, so `null`
    (→ UI "Próximamente") is the expected result today. Do NOT fabricate a URL.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && grep -q "getReceiptUrl" src/lib/api/tenant-payment-requests.service.ts && grep -q "TenantReceiptUrl" src/lib/api/tenant-payment-requests.types.ts && grep -qE "403 \|\| .*404|404" src/lib/api/tenant-payment-requests.service.ts && test $(grep -ci "factura" src/lib/api/tenant-payment-requests.service.ts) -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>getReceiptUrl exists returning TenantReceiptUrl | null; 403/404 → null (tolerant); JSDoc marks it "comprobante interno" (no "factura"); no fabricated URL.</done>
</task>

<task type="auto">
  <name>Task 2: Wompi return handling (neutral confirmando) + es-CO fix + single-source hygiene</name>
  <files>src/app/inquilino/pagos/page.tsx</files>
  <action>
    (a) **Return handling.** Import `useSearchParams` from `next/navigation` and `toast` from `sonner`.
    Add a run-once `useEffect` (empty deps + `// eslint-disable-line react-hooks/exhaustive-deps`, like the
    avalúo analog) that reads `searchParams.get('id')` and `searchParams.get('status')`; if BOTH present,
    call `toast.info('Estamos confirmando tu pago. Vas a ver la confirmación en tu historial cuando
    termine.', { duration: 6000 })` and then `refetchRequests()` + `refetchPaymentInfo()`. **Do NOT** map
    `status === 'APPROVED'` to any success copy or UI flip, and do NOT set any local "paid" flag from the
    param — the neutral copy is the same regardless of the returned status (only the backend confirms).

    (b) **es-CO fix.** Change `formatShortDate` locale `'es-CL'` → `'es-CO'` (line ~95) (DESIGN.md §16;
    same fix v7-02/v7-03 applied).

    (c) **Single-source hygiene (PAGO-05).** Do NOT introduce any second saldo or a fabricated
    cuota-de-manejo / recargo / surcharge line — the Next Payment / saldo already trace to
    `payment-info` (`nextAmount`, `PeriodStatusCard`). Keep the amber `daysUntil <= 3` highlight factual
    (no guilt copy, no countdown). Leave the neutral APPROVED/"al día" and PENDING_VALIDATION cards as-is.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && P=src/app/inquilino/pagos/page.tsx && grep -q "useSearchParams" $P && grep -qi "confirmando" $P && grep -q "es-CO" $P && test $(grep -c "es-CL" $P) -eq 0 && test $(grep -cE "get\('status'\) *=== *'APPROVED'" $P) -eq 0 && test $(grep -ciE "pago exitoso|cuota de manejo|cuotaManejo|adminFee|recargo" $P) -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>Return handler shows a neutral confirmando toast + refetches; no UI branch on ?status==='APPROVED'; es-CL→es-CO fixed; no "pago exitoso", no second saldo, no fabricated fee.</done>
</task>

<task type="auto">
  <name>Task 3: Comprobante interno download → honest "Próximamente" (gated on hasReceipt)</name>
  <files>src/app/inquilino/pagos/page.tsx</files>
  <action>
    Add a per-row "Descargar comprobante interno" affordance on each history row (near the `dateLabel`
    footer, ~lines 357-373). Import `tenantPaymentRequestsApi` from `@/lib/api/tenant-payment-requests.service`.
    Behavior: an async `handleDownloadReceipt(request)` that calls
    `tenantPaymentRequestsApi.getReceiptUrl(request.id)`; if it returns a `{ url }`, fetch it as a blob and
    trigger a download (v7-02 blob idiom — hides the storage URL); if it returns `null` (no backend), show
    `toast.info('El comprobante interno estará disponible próximamente.')` — an honest "Próximamente".
    Only render the affordance when `request.hasReceipt` (and for APPROVED/PENDING_VALIDATION rows); when
    `hasReceipt` is false, render nothing (or a muted "Comprobante próximamente" hint) — never a fake link.

    Labeling (guardrail, non-negotiable): the action/label text is **"comprobante interno"** — never
    "factura", never "Descargar factura". Add a small inline note once on the page: "Los comprobantes son
    de uso interno; la factura electrónica (DIAN) estará disponible más adelante." Do NOT generate a PDF
    client-side.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && P=src/app/inquilino/pagos/page.tsx && grep -q "getReceiptUrl" $P && grep -qi "comprobante interno" $P && grep -q "hasReceipt" $P && grep -qi "Próximamente" $P && test $(grep -ci "factura electrónica\|DIAN" $P) -ge 1 && test $(grep -ci "descargar factura" $P) -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>History rows offer a "comprobante interno" download gated on hasReceipt → real blob if available, honest "Próximamente" otherwise; DIAN/factura-electrónica disclosed as later; no "Descargar factura", no client-generated PDF.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wompi → tenant browser (redirect `?id`/`?status`) | Client-controlled return params; display-only, NEVER a source of "paid" |
| tenant browser → NestJS `/tenant-payments/requests/mine` + `/receipt-url` | Single source of history + signed comprobante; backend authorizes tenant on own requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v7-04-08 | Spoofing | forged "paid" via redirect params | mitigate | Neutral confirmando toast only; grep-gated absence of `get('status') === 'APPROVED'` and "pago exitoso"; refetch the real source |
| T-v7-04-09 | Tampering / integrity | second computed saldo | mitigate | Saldo traces to `payment-info`/`tenant-payment-requests`; grep-gated absence of fabricated fee lines |
| T-v7-04-10 | Repudiation / compliance (DIAN) | comprobante label | mitigate | Labeled "comprobante interno", never "factura"; real factura (FE-DIAN) disclosed as later; no fake PDF |
| T-v7-04-11 | Information disclosure (IDOR) | receipt URL | mitigate | `getReceiptUrl` returns a backend-signed/expiring URL; blob download hides it; 403/404 → null (no leak, no fabrication) |
| T-v7-04-SC | Tampering (supply chain) | npm installs | accept | Zero new packages |
</threat_model>

<verification>
- `pnpm -C /Users/nicolasgarcia/rent/mvp-portal-inquilino build` succeeds.
- `pnpm test` — 0 NEW failures (≈7 pre-existing unrelated — see v7-01 deferred-items.md).
- Grep gates (Task verifies): `getReceiptUrl` + `TenantReceiptUrl` present, tolerant 403/404, no "factura"
  in the service; page has `useSearchParams` + "confirmando" + `es-CO` (no `es-CL`), zero
  `get('status') === 'APPROVED'`, zero "pago exitoso"/fabricated-fee tokens; "comprobante interno" +
  `hasReceipt` + "Próximamente" present, DIAN disclosed, zero "descargar factura".
- Manual smoke as `tenant`: return to `/inquilino/pagos?id=X&status=APPROVED` → toast says "confirmando"
  (NOT "exitoso"), history/period card reflect the real source after refetch; a paid row shows
  "comprobante interno" → "Próximamente" (no factura, no fake PDF).
</verification>

<success_criteria>
- PAGO-03: payment history renders from the single source (`useMyPaymentRequests`) and each payment offers
  a "comprobante interno" download that degrades to an honest "Próximamente" (never "factura", never a fake PDF).
- PAGO-05: saldo/mora trace to the single source with no second computed number and no fabricated fee; the
  Wompi return is reconciliation-safe (neutral "confirmando", no premature success); es-CO locale fixed.
</success_criteria>

<output>
After completion, create `.planning/phases/v7-04-pagos-wompi/v7-04-03-SUMMARY.md`.
</output>
</content>
