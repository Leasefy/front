---
phase: v7-06-solicitudes-pqrs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/api/pqrs.types.ts
  - src/lib/date/business-days.ts
  - src/lib/date/business-days.test.ts
  - src/lib/api/pqrs.service.ts
  - src/lib/api/pqrs.service.test.ts
autonomous: true
requirements: [SOLI-01, SOLI-02, SOLI-03, SOLI-04]

must_haves:
  truths:
    - "The shared PQRS entity is REUSED, not forked: `pqrs.types.ts` gains ONLY optional additive fields (costoResponsable, cotizacionMonto, cotizacionAprobadaAt) — there is NO second declaration of PqrsEstado/SolicitudPqrs anywhere in src, and the agency page keeps rendering the same estado vocabulary unchanged"
    - "The interim SLA is a pure, deterministic, weekday-only computation: addBusinessDays(from, n) skips Sat/Sun (no holiday table), and resolveExpectedResponse prefers the authoritative slaVenceAt when present, else returns the createdAt+15-business-days estimate flagged estimated:true — never blank"
    - "pqrs.service.ts is a tolerant frontend-first CONTRACT: listMine degrades to [] on 404/403/0 (never fabricated rows), getMine(id) resolves from listMine (own-only, NO fetch-by-id → no IDOR), create + approveCotizacion throw a typed PqrsUnavailableError on not-live (never a fabricated radicado / never a fake 'aprobado')"
  artifacts:
    - path: "src/lib/api/pqrs.types.ts"
      provides: "additive optional tenant fields (CostoResponsable type + costoResponsable/cotizacionMonto/cotizacionAprobadaAt) on the SHARED entity; PqrsEstado/SolicitudPqrs declared exactly once"
      contains: "CostoResponsable"
    - path: "src/lib/date/business-days.ts"
      provides: "pure addBusinessDays (Mon-Fri, no holidays) + two-tier resolveExpectedResponse (authoritative slaVenceAt ?? estimate), reusing PQRS_SLA_BUSINESS_DAYS; zero deps"
      contains: "addBusinessDays"
    - path: "src/lib/api/pqrs.service.ts"
      provides: "tolerant contract: listMine → [], getMine(id) resolve-from-list, create/approveCotizacion → PqrsUnavailableError; isEndpointUnavailable(404/403/0)"
      contains: "PqrsUnavailableError"
    - path: "src/lib/date/business-days.test.ts"
      provides: "SOLI-03 unit proof: weekday skipping + two-tier resolver preference"
      contains: "addBusinessDays"
    - path: "src/lib/api/pqrs.service.test.ts"
      provides: "SOLI-01/02 unit proof: [] on 404/403/0, PqrsUnavailableError on create, resolve-from-list getMine"
      contains: "PqrsUnavailableError"
  key_links:
    - from: "src/lib/date/business-days.ts"
      to: "src/lib/constants/response-sla.ts (PQRS_SLA_BUSINESS_DAYS)"
      via: "resolveExpectedResponse reuses the shared 15-business-day constant"
      pattern: "PQRS_SLA_BUSINESS_DAYS"
    - from: "src/lib/api/pqrs.service.ts"
      to: "isEndpointUnavailable (404/403/0)"
      via: "listMine → [], create/approveCotizacion → PqrsUnavailableError (never fabricate)"
      pattern: "isEndpointUnavailable"
---

<objective>
Land the v7-06 FOUNDATION: extend the shared PQRS entity additively (no fork), build the pure business-day
SLA helper, and ship the tolerant `pqrs.service.ts` contract — all unit-tested. Nothing here is a real
backend call today; everything degrades honestly so the tenant surfaces (waves 2-3) build on stable seams.

Three honest deliverables:
1. **Reuse-not-fork the entity (SOLI-02 crux).** Add ONLY optional additive fields to the SHARED
   `pqrs.types.ts` (`CostoResponsable` type + `costoResponsable?`, `cotizacionMonto?`,
   `cotizacionAprobadaAt?`). `PqrsEstado`/`SolicitudPqrs` stay declared exactly ONCE; the agency page keeps
   the identical estado vocabulary. A grep gate forbids any second declaration (PITFALLS 1).
2. **Pure business-day SLA (SOLI-03).** `src/lib/date/business-days.ts`: `addBusinessDays(from, n)` skips
   Sat/Sun only (weekday-only, NO holiday table per ROADMAP:135), and `resolveExpectedResponse(createdAt,
   slaVenceAt?)` returns `{ date, estimated }` — authoritative `slaVenceAt` wins (`estimated:false`), else the
   `createdAt + PQRS_SLA_BUSINESS_DAYS` estimate (`estimated:true`). Never blank. Zero new npm packages.
3. **Tolerant contract (SOLI-01/02/04).** `pqrs.service.ts` modeled 1:1 on `lease-documents.service.ts` +
   `tenant-payment-requests.service.ts`: `listMine()` → `[]` on 404/403/0; `getMine(id)` resolves from
   `listMine()` (own-only, NO fetch-by-id → no IDOR); `create()` + `approveCotizacion()` throw a typed
   `PqrsUnavailableError` on not-live — NEVER a fabricated radicado, NEVER a fake "aprobado".

This is wave 1: it seeds the types/helper/service that waves 2-3 consume. Depends on nothing.

Purpose: SOLI-01..04 contract foundation, frontend-first, reusing the shared PQRS entity.
Output: additive types + pure SLA helper + tolerant service, all unit-tested.

**Guardrails:** NO fork of `pqrs.types.ts` (grep-gated); interim SLA weekday-only + never blank + labeled by
the `estimated` flag; own-requests-only (`getMine` resolves from `listMine`, never GET /pqrs/:id); no
fabricated radicado/status; `solicitanteTipo` is server-authoritative (client never claims a foreign tipo);
es-CO; zero new npm packages.
</objective>

<execution_context>
@/Users/nicolasgarcia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolasgarcia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/v7-06-solicitudes-pqrs/v7-06-RESEARCH.md
@.planning/phases/v7-06-solicitudes-pqrs/v7-06-PATTERNS.md

# READ FIRST: CLAUDE.md (additive only, es-CO, zero new npm packages, PR workflow).

# The SHARED entity to EXTEND additively (never fork):
@src/lib/api/pqrs.types.ts

# The tolerant "endpoint-not-live → honest degrade, never fake" idioms to COPY verbatim:
@src/lib/api/lease-documents.service.ts
@src/lib/api/tenant-payment-requests.service.ts

# The shared SLA constant to REUSE (do NOT introduce a new `15` literal):
@src/lib/constants/response-sla.ts

# Service-test analog (mocks globalThis.fetch; apiClient throws real ApiError from status):
@src/lib/api/funnel-applications.service.test.ts

<interfaces>
<!-- apiClient/ApiError (src/lib/api/client.ts) — a mocked fetch returning {ok:false,status:404,...}
     makes apiClient throw ApiError(404); status 0 = network/offline. -->
class ApiError extends Error { status: number }
apiClient.get<T>(path): Promise<T>       // GET  BACKEND_URL+path
apiClient.post<T>(path, body?): Promise<T> // POST BACKEND_URL+path

<!-- SHARED PQRS entity (pqrs.types.ts) — REUSE + extend additively. Existing fields already include:
     estado: PqrsEstado, solicitanteTipo: PqrsSolicitanteTipo ('inquilino'|'propietario'|'tercero'),
     cotizacionId?, slaVenceAt?, createdAt, updatedAt, resueltaAt?. -->
type PqrsEstado = 'recibida'|'asignada'|'en_proceso'|'en_cotizacion'|'resuelta'|'cerrada'
type PqrsTipo = 'peticion'|'queja'|'reclamo'|'sugerencia'|'solicitud'|'reparacion'
interface SolicitudPqrs { id; radicado; tipo; estado; solicitanteTipo; slaVenceAt?; cotizacionId?; createdAt; updatedAt; ... }

<!-- Shared SLA constant (response-sla.ts) — reuse, don't redeclare. -->
const PQRS_SLA_BUSINESS_DAYS = 15
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the SHARED pqrs.types.ts additively (reuse, NOT fork)</name>
  <files>src/lib/api/pqrs.types.ts</files>
  <action>
    Add, to the EXISTING file only (never a new parallel type file):
      - `export type CostoResponsable = 'dueno' | 'inquilino' | 'compartido';` (Ley 820 — `compartido` is the
        product convenience for the "split" case; value is BACKEND-sourced, never decided client-side).
      - Three OPTIONAL fields on the existing `SolicitudPqrs` interface (do NOT touch existing fields, do NOT
        reorder): `costoResponsable?: CostoResponsable;` `cotizacionMonto?: number;  // COP, server-provided`
        `cotizacionAprobadaAt?: string;  // ISO — set by backend when the tenant approves (SOLI-04)`.
    Add a one-line JSDoc above each new field noting it is additive/optional, set server-side, per SOLI-04.
    Do NOT add a new `PqrsEstado`/`SolicitudPqrs` declaration; do NOT narrow `solicitanteTipo`; do NOT touch
    the agency-consumed exports (`ResumenPqrs`, `RESUMEN_PQRS_VACIO`, `PqrsListResponse`). This keeps the
    single shared contract the agency `/panel/inmobiliaria/pqrs` already renders (PITFALLS 1).
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && T=src/lib/api/pqrs.types.ts && grep -q "export type CostoResponsable" $T && grep -q "costoResponsable?" $T && grep -q "cotizacionMonto?" $T && grep -q "cotizacionAprobadaAt?" $T && test -z "$(grep -rnE '^(export )?(type PqrsEstado|interface SolicitudPqrs)' src --include='*.ts' | grep -v 'api/pqrs.types.ts')" && echo GATE_OK</automated>
  </verify>
  <done>`CostoResponsable` + 3 optional fields added to the shared entity; no fork (PqrsEstado/SolicitudPqrs declared exactly once, only in pqrs.types.ts); agency exports untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure business-day SLA helper + unit tests (SOLI-03)</name>
  <files>src/lib/date/business-days.ts, src/lib/date/business-days.test.ts</files>
  <behavior>
    - addBusinessDays(new Date('2026-07-17'/*Fri*/), 1) → Mon 2026-07-20 (skips Sat/Sun).
    - addBusinessDays(from, 15) from a Monday lands on a weekday (never Sat/Sun); result getDay() ∉ {0,6}.
    - addBusinessDays does NOT mutate its `from` argument (returns a new Date).
    - addBusinessDays(from, 0) → same calendar day (no shift).
    - resolveExpectedResponse(createdAt, slaVenceAt) with slaVenceAt present → { date: new Date(slaVenceAt), estimated: false }.
    - resolveExpectedResponse(createdAt, undefined) → { date: addBusinessDays(createdAt, 15), estimated: true }.
    - No holiday table: a business day that is a Colombian holiday is still counted (weekday-only by design).
  </behavior>
  <action>
    Create `src/lib/date/business-days.ts` (pure, `'use client'` NOT needed — no React). Export:
      - `export function addBusinessDays(from: Date, days: number): Date` — copy `from` into a new Date, loop
        `days` times advancing one calendar day and counting only when `getDay()` ∉ {0 (Sun), 6 (Sat)}; return
        the new Date. NEVER mutate the input. NO holiday list (ROADMAP:135 "sin festivos").
      - `export interface ExpectedResponse { date: Date; estimated: boolean }`
      - `export function resolveExpectedResponse(createdAtIso: string, slaVenceAtIso?: string): ExpectedResponse`
        — if `slaVenceAtIso` is a non-empty string return `{ date: new Date(slaVenceAtIso), estimated: false }`;
        else return `{ date: addBusinessDays(new Date(createdAtIso), PQRS_SLA_BUSINESS_DAYS), estimated: true }`.
        Import `PQRS_SLA_BUSINESS_DAYS` from `@/lib/constants/response-sla` — do NOT introduce a new `15`.
    JSDoc the module: this is an INTERIM display estimate; the authoritative deadline is the server's
    `slaVenceAt` (M1). The estimate is intentionally optimistic (skips holidays) so callers must label it
    "estimado" and soft-frame it ("hacia el …"), never "vence el …".
    Write `business-days.test.ts` (vitest) covering every bullet in <behavior>. Use fixed ISO dates.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && grep -q "export function addBusinessDays" src/lib/date/business-days.ts && grep -q "resolveExpectedResponse" src/lib/date/business-days.ts && grep -q "PQRS_SLA_BUSINESS_DAYS" src/lib/date/business-days.ts && pnpm test -- src/lib/date/business-days.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>addBusinessDays (weekday-only, non-mutating) + resolveExpectedResponse (two-tier, reuses PQRS_SLA_BUSINESS_DAYS) exist; unit tests green; zero new packages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tolerant pqrs.service.ts contract + unit tests (SOLI-01/02/04)</name>
  <files>src/lib/api/pqrs.service.ts, src/lib/api/pqrs.service.test.ts</files>
  <behavior>
    - listMine(): fetch 200 → returns the array as-is; 404 → []; 403 → []; ApiError(0) → []; any other status (e.g. 500) → throws.
    - getMine(id): resolves from listMine() and returns the matching SolicitudPqrs or null; NEVER issues GET /pqrs/:id.
    - create(input): 200 → returns the server SolicitudPqrs (with server-assigned radicado); 404/403/0 → throws PqrsUnavailableError (no fabricated id/radicado); other status → throws the raw ApiError.
    - approveCotizacion(id): 404/403/0 → throws PqrsUnavailableError (no fake 'aprobado'); 200 → returns the updated SolicitudPqrs.
  </behavior>
  <action>
    Create `src/lib/api/pqrs.service.ts`. Import `apiClient, ApiError` from `./client` and
    `SolicitudPqrs, PqrsTipo` from `./pqrs.types`. Copy `isEndpointUnavailable(err)` (ApiError && status ∈
    {404,403,0}) VERBATIM from `lease-documents.service.ts:67`. Export:
      - `export class PqrsUnavailableError extends Error` (name `'PqrsUnavailableError'`, message `'pqrs_unavailable'`).
      - `export interface NuevaSolicitudInput { tipo: PqrsTipo; asunto: string; descripcion: string; contratoId?: string; propiedadId?: string; }`
        (photo bytes are NOT in this JSON body — they upload separately in v7-06-03; `solicitanteTipo` is set
        server-side from the JWT, the client never claims it).
      - `export const pqrsApi = { listMine, getMine, create, approveCotizacion }`:
        · `listMine(): Promise<SolicitudPqrs[]>` → `apiClient.get('/pqrs/mine')`, catch `isEndpointUnavailable` → `return []` (rethrow else). Mirror `tenant-payment-requests.service.ts:16-28` — honest empty, NEVER fabricated rows.
        · `getMine(id: string): Promise<SolicitudPqrs | null>` → `const all = await this.listMine(); return all.find(s => s.id === id) ?? null;` — own-only resolution, NO fetch-by-id (anti-IDOR, PITFALLS 4).
        · `create(input: NuevaSolicitudInput): Promise<SolicitudPqrs>` → `apiClient.post('/pqrs', input)`, catch `isEndpointUnavailable` → `throw new PqrsUnavailableError()` (rethrow else). Never fabricate a radicado.
        · `approveCotizacion(id: string): Promise<SolicitudPqrs>` → `apiClient.post(\`/pqrs/${id}/aprobar-cotizacion\`, {})`, catch `isEndpointUnavailable` → `throw new PqrsUnavailableError()` (rethrow else). Tenant APPROVES only; never assigns providers / fixes cost / self-closes.
    JSDoc each method: routes (`POST /pqrs`, `GET /pqrs/mine`, `POST /pqrs/:id/aprobar-cotizacion`) are a
    disclosed external dependency (M1) — `[]` / `PqrsUnavailableError` is the EXPECTED result today so the UI
    stays honest ("Próximamente"). Tag the exact paths as provisional (Assumptions A1/A4).
    Write `pqrs.service.test.ts` mirroring `funnel-applications.service.test.ts`: mock `globalThis.fetch`
    (restore in afterEach) so `apiClient` throws real `ApiError`s; cover every <behavior> bullet, incl. a 500
    that must rethrow (not swallow) and a getMine that never calls a `/pqrs/<id>` URL.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && S=src/lib/api/pqrs.service.ts && grep -q "PqrsUnavailableError" $S && grep -q "isEndpointUnavailable" $S && grep -q "listMine" $S && grep -q "approveCotizacion" $S && grep -qE "404|403" $S && test $(grep -cE "apiClient\.get\(\`?/pqrs/\\\$\{" $S) -eq 0 && pnpm test -- src/lib/api/pqrs.service.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>pqrsApi (listMine→[], getMine resolve-from-list, create/approveCotizacion→PqrsUnavailableError) exists; no GET /pqrs/:id; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant browser → api-client | Untrusted tenant input (tipo/asunto/descripcion, an id) crosses into `pqrsApi` here |
| api-client → NestJS/agent PQRS | JWT-scoped; server owns `solicitanteTipo`, `radicado`, `estado`, `slaVenceAt`, `costoResponsable` (all not live today) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v7-06-01 | Tampering | pqrsApi.create/approveCotizacion | mitigate | On 404/403/0 throw `PqrsUnavailableError`; never fabricate a radicado/id or a fake "aprobado". Status/radicado are backend-owned. |
| T-v7-06-02 | Information Disclosure | pqrsApi.getMine | mitigate | Resolve from `listMine()` (own-only, JWT-scoped); NO `GET /pqrs/:id` from a raw id (grep-gated). |
| T-v7-06-03 | Spoofing / Elevation of Privilege | NuevaSolicitudInput | mitigate | `solicitanteTipo` is NOT in the client input — server assigns `'inquilino'` from the JWT; client never claims a foreign tipo. |
| T-v7-06-04 | Tampering | pqrsApi.listMine | mitigate | 404/403/0 → `[]` (honest empty), never a fabricated row; other errors rethrow (not swallowed). |
| T-v7-06-SC | Tampering (supply chain) | npm installs | accept | N/A this phase — ZERO new dependencies (RESEARCH Package Legitimacy Audit: N/A); no install task exists. |
</threat_model>

<verification>
- `pnpm -C /Users/nicolasgarcia/rent/mvp-portal-inquilino build` succeeds (TS strict).
- `pnpm test -- src/lib/date/business-days.test.ts` and `pnpm test -- src/lib/api/pqrs.service.test.ts` green.
- `pnpm test` — 0 NEW failures (~7 pre-existing unrelated — see v7-01 deferred-items.md).
- Fork gate: `grep -rnE "^(export )?(type PqrsEstado|interface SolicitudPqrs)" src --include='*.ts' | grep -v "api/pqrs.types.ts"` returns EMPTY.
- Anti-IDOR gate: `pqrs.service.ts` has no `apiClient.get('/pqrs/${id}')`-shape fetch-by-id.
</verification>

<success_criteria>
- SOLI-02 foundation: the shared PQRS entity is reused + extended additively (no fork).
- SOLI-03 foundation: pure weekday-only `addBusinessDays` + two-tier `resolveExpectedResponse` (never blank).
- SOLI-01/04 foundation: tolerant `pqrs.service.ts` (honest degrade, no fabricated radicado, own-only getMine,
  approve-only affordance) — all unit-tested; zero new npm packages.
</success_criteria>

<output>
After completion, create `.planning/phases/v7-06-solicitudes-pqrs/v7-06-01-SUMMARY.md`.
</output>
