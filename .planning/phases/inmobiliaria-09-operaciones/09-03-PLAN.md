---
phase: inmobiliaria-09-operaciones
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/data/mock-inmobiliaria.ts
  - src/components/inmobiliaria/MantenimientoList.tsx
  - src/components/inmobiliaria/MantenimientoForm.tsx
  - src/components/inmobiliaria/index.ts
autonomous: true

must_haves:
  truths:
    - "User can see list of maintenance requests with status"
    - "User can filter by type, priority, and status"
    - "User can create new maintenance request"
    - "User can upload before photos for request"
  artifacts:
    - path: "src/components/inmobiliaria/MantenimientoList.tsx"
      provides: "List of maintenance requests with filters"
      min_lines: 400
    - path: "src/components/inmobiliaria/MantenimientoForm.tsx"
      provides: "Form to create new maintenance request"
      min_lines: 300
  key_links:
    - from: "MantenimientoList"
      to: "SolicitudMantenimiento"
      via: "import from types/inmobiliaria"
      pattern: "SolicitudMantenimiento"
    - from: "MantenimientoForm"
      to: "MantenimientoType"
      via: "import from types/inmobiliaria"
      pattern: "MantenimientoType"
---

<objective>
Create MantenimientoList and MantenimientoForm components for maintenance management.

Purpose: Display and manage maintenance requests with filtering and creation.
Output: Two components for listing and creating maintenance requests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@docs/ROADMAP-INMOBILIARIA.md

# Source files with existing types
@src/lib/types/inmobiliaria.ts - Already has SolicitudMantenimiento, MantenimientoType, etc.
@src/lib/data/mock-inmobiliaria.ts

# Existing patterns
@src/components/inmobiliaria/CobroTable.tsx
@src/components/inmobiliaria/PropietarioForm.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Mock Mantenimiento Data</name>
  <files>src/lib/data/mock-inmobiliaria.ts</files>
  <action>
Add mock maintenance requests:

```typescript
import type {
  SolicitudMantenimiento,
  MantenimientoType,
  MantenimientoPriority,
  MantenimientoStatus
} from '@/lib/types/inmobiliaria';

export function generateMockMantenimientos(): SolicitudMantenimiento[] {
  const types: MantenimientoType[] = ['plumbing', 'electrical', 'appliance', 'structural', 'painting', 'locks', 'other'];
  const priorities: MantenimientoPriority[] = ['low', 'medium', 'high', 'emergency'];
  const statuses: MantenimientoStatus[] = ['reported', 'quoted', 'approved', 'in_progress', 'completed', 'cancelled'];

  const titles: Record<MantenimientoType, string[]> = {
    plumbing: ['Fuga en baño', 'Lavamanos tapado', 'Presión baja de agua', 'Calentador no funciona'],
    electrical: ['Toma sin corriente', 'Cortocircuito cocina', 'Bombillos fundidos', 'Interruptor dañado'],
    appliance: ['Nevera no enfría', 'Lavadora no centrifuga', 'Estufa con falla', 'Aire acondicionado'],
    structural: ['Humedad en pared', 'Grieta en techo', 'Puerta descolgada', 'Ventana rota'],
    painting: ['Pintura descascarada', 'Manchas de humedad', 'Repintar apartamento', 'Pintura exterior'],
    locks: ['Chapa dañada', 'Llave perdida', 'Cerradura forzada', 'Cambio de chapas'],
    other: ['Limpieza general', 'Fumigación', 'Jardinería', 'Otros'],
  };

  const solicitudes: SolicitudMantenimiento[] = [];

  MOCK_CONSIGNACIONES.slice(0, 12).forEach((consignacion, index) => {
    const type = types[index % types.length];
    const priority = priorities[index % priorities.length];
    const status = statuses[index % statuses.length];
    const titleOptions = titles[type];
    const title = titleOptions[index % titleOptions.length];

    const createdDaysAgo = Math.floor(Math.random() * 30) + 1;
    const createdAt = new Date(Date.now() - createdDaysAgo * 24 * 60 * 60 * 1000).toISOString();

    solicitudes.push({
      id: `mant-${index + 1}`,
      consignacionId: consignacion.id,
      propertyId: consignacion.propertyId,
      propietarioId: consignacion.propietarioId,
      tenantId: `tenant-${index + 1}`,
      agenteId: consignacion.agenteId,
      propertyTitle: consignacion.propertyTitle,
      propertyAddress: consignacion.propertyAddress,
      tenantName: consignacion.currentTenantName || 'Inquilino Demo',
      propietarioName: MOCK_PROPIETARIOS.find(p => p.id === consignacion.propietarioId)?.name || 'Propietario',
      type,
      priority,
      title,
      description: `Descripción detallada del problema: ${title.toLowerCase()}. El inquilino reporta que necesita atención ${priority === 'emergency' ? 'inmediata' : 'pronto'}.`,
      photoUrls: index % 3 === 0 ? ['/placeholder-photo-1.jpg', '/placeholder-photo-2.jpg'] : undefined,
      status,
      quotes: status !== 'reported' ? generateMockQuotes(index) : [],
      selectedQuoteId: ['approved', 'in_progress', 'completed'].includes(status) ? `quote-${index}-1` : undefined,
      approvedAmount: ['approved', 'in_progress', 'completed'].includes(status) ? 150000 + index * 50000 : undefined,
      paidBy: 'owner',
      completedAt: status === 'completed' ? new Date(Date.now() - 2 * 24 * 60 * 60 * 1000).toISOString() : undefined,
      completionNotes: status === 'completed' ? 'Trabajo completado satisfactoriamente' : undefined,
      completionPhotoUrls: status === 'completed' ? ['/placeholder-after-1.jpg'] : undefined,
      createdAt,
      updatedAt: new Date().toISOString(),
    });
  });

  return solicitudes;
}

function generateMockQuotes(index: number): SolicitudMantenimiento['quotes'] {
  return [
    {
      id: `quote-${index}-1`,
      providerName: 'Servicios Técnicos ABC',
      providerPhone: '+57 310 555 1234',
      amount: 150000 + index * 30000,
      description: 'Mano de obra y materiales incluidos',
      estimatedDays: 2,
      createdAt: new Date(Date.now() - 5 * 24 * 60 * 60 * 1000).toISOString(),
    },
    {
      id: `quote-${index}-2`,
      providerName: 'Mantenimiento Express',
      providerPhone: '+57 311 666 5678',
      amount: 180000 + index * 25000,
      description: 'Servicio garantizado por 6 meses',
      estimatedDays: 1,
      createdAt: new Date(Date.now() - 4 * 24 * 60 * 60 * 1000).toISOString(),
    },
    {
      id: `quote-${index}-3`,
      providerName: 'Arreglos Rápidos',
      providerPhone: '+57 312 777 9012',
      amount: 120000 + index * 35000,
      description: 'Precio económico, materiales aparte',
      estimatedDays: 3,
      createdAt: new Date(Date.now() - 3 * 24 * 60 * 60 * 1000).toISOString(),
    },
  ];
}

export const MOCK_MANTENIMIENTOS = generateMockMantenimientos();
```
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>Mock maintenance data created</done>
</task>

<task type="auto">
  <name>Task 2: Create MantenimientoList Component</name>
  <files>src/components/inmobiliaria/MantenimientoList.tsx</files>
  <action>
Create MantenimientoList component:

1. Summary Cards Row:
   - Total solicitudes
   - Reportadas (gray)
   - En cotización (blue)
   - Aprobadas (lime)
   - En progreso (amber)
   - Completadas (emerald)

2. Filter Bar:
   - Type filter (dropdown with icons)
   - Priority filter (low/medium/high/emergency)
   - Status filter
   - Property/zone search
   - Date range

3. List View (cards, not table):
   - Card per solicitud with:
     - Type icon and title
     - Priority badge (colored)
     - Status badge
     - Property info (title, address)
     - Tenant name
     - Created date
     - Days since reported
     - Quotes count (if any)
     - Photo preview (if uploaded)
     - Actions (view, add quote, approve, etc.)

4. Sorting:
   - By priority (emergency first)
   - By date created
   - By status

5. Actions:
   - View details (opens drawer)
   - Add quote
   - Approve quote
   - Mark as completed
   - Cancel request

Props interface:
```typescript
interface MantenimientoListProps {
  data: SolicitudMantenimiento[];
  onViewDetails?: (solicitud: SolicitudMantenimiento) => void;
  onAddQuote?: (solicitud: SolicitudMantenimiento) => void;
  onApproveQuote?: (solicitud: SolicitudMantenimiento, quoteId: string) => void;
  onComplete?: (solicitud: SolicitudMantenimiento) => void;
  onCancel?: (solicitud: SolicitudMantenimiento) => void;
}
```

Use Card layout (not table) for better visual presentation.
Use priority colors: slate (low), blue (medium), amber (high), red (emergency).
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>MantenimientoList shows requests with filters</done>
</task>

<task type="auto">
  <name>Task 3: Create MantenimientoForm Component</name>
  <files>src/components/inmobiliaria/MantenimientoForm.tsx</files>
  <action>
Create MantenimientoForm component:

1. Property Selection:
   - Search/select property from consignaciones
   - Show property details when selected
   - Auto-fill tenant and owner info

2. Request Details:
   - Type selector (radio cards with icons)
   - Priority selector (radio with color indicators)
   - Title input
   - Description textarea
   - Urgency explanation for high/emergency

3. Photo Upload:
   - Drop zone for photos
   - Preview thumbnails
   - Remove photo button
   - Max 5 photos
   - Accept jpg, png, webp

4. Additional Info:
   - Preferred contact method
   - Availability notes
   - Access instructions

5. Responsibility:
   - Who pays (owner/tenant/split/agency)
   - Explanation of responsibility rules

6. Actions:
   - "Crear solicitud" button
   - "Cancelar" button
   - Validation feedback

Props interface:
```typescript
interface MantenimientoFormProps {
  consignaciones: Consignacion[];
  preselectedConsignacionId?: string;
  onSubmit: (data: MantenimientoFormData) => void;
  onCancel: () => void;
  isSubmitting?: boolean;
}

interface MantenimientoFormData {
  consignacionId: string;
  type: MantenimientoType;
  priority: MantenimientoPriority;
  title: string;
  description: string;
  photoUrls?: string[];
  paidBy: MantenimientoPaidBy;
  accessNotes?: string;
}
```

Use radio cards for type selection (similar to ConsignacionWizard).
Use Form from shadcn.
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>MantenimientoForm allows creating new requests</done>
</task>

<task type="auto">
  <name>Task 4: Update Barrel Export</name>
  <files>src/components/inmobiliaria/index.ts</files>
  <action>
Add exports for mantenimiento components:

```typescript
// Operaciones - Mantenimiento (Phase 9)
export { MantenimientoList } from './MantenimientoList';
export { MantenimientoForm } from './MantenimientoForm';
```
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>Mantenimiento components exported</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm tsc --noEmit passes
- [ ] MOCK_MANTENIMIENTOS exports from mock-inmobiliaria
- [ ] MantenimientoList shows requests with filters
- [ ] Priority and status filters work correctly
- [ ] MantenimientoForm validates and submits
- [ ] Type selection with icons works
- [ ] Components exported from barrel
</verification>

<success_criteria>
- All tasks completed
- MantenimientoList displays requests with proper filtering
- MantenimientoForm creates new maintenance requests
- Type icons and priority colors display correctly
- Photo upload zone present (mock functionality)
</success_criteria>

<output>
After completion, create `.planning/phases/inmobiliaria-09-operaciones/09-03-SUMMARY.md`
</output>
