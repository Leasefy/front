---
phase: 29-advanced-reports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/inmobiliaria/reports/OccupancyReport.tsx
  - src/components/inmobiliaria/reports/CollectionsReport.tsx
  - src/lib/data/mock-reports.ts
autonomous: true

must_haves:
  truths:
    - "OccupancyReport shows vacancy rate, avg days vacant, breakdown by property"
    - "CollectionsReport shows mora rate, avg days late, recovery rate by month"
    - "Both use mock data from mock-reports.ts"
    - "Both follow existing dashboard card/table patterns (neutral colors, rounded-xl)"
  artifacts:
    - path: "src/components/inmobiliaria/reports/OccupancyReport.tsx"
      provides: "Occupancy report with vacancy stats and property breakdown"
      exports: ["OccupancyReport"]
    - path: "src/components/inmobiliaria/reports/CollectionsReport.tsx"
      provides: "Collections report with mora stats and monthly breakdown"
      exports: ["CollectionsReport"]
    - path: "src/lib/data/mock-reports.ts"
      provides: "Mock data for all advanced reports"
      exports: ["mockOccupancyData", "mockCollectionsData", "mockAgentPerformanceData"]
  key_links:
    - from: "OccupancyReport.tsx"
      to: "mock-reports.ts"
      via: "imports mockOccupancyData"
    - from: "CollectionsReport.tsx"
      to: "mock-reports.ts"
      via: "imports mockCollectionsData"
---

<objective>
Build occupancy and collections report components with mock data.

Purpose: Two core report views that show property vacancy and payment collection performance.
Output: OccupancyReport, CollectionsReport components + mock data file.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/app/panel/inmobiliaria/reportes/page.tsx
@src/components/inmobiliaria/AnalyticsDashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mock report data</name>
  <files>src/lib/data/mock-reports.ts</files>
  <action>
    Create mock data file with TypeScript interfaces and mock data for all three report types:

    1. OccupancyData:
       - summary: { totalProperties, rented, vacant, vacancyRate, avgDaysVacant }
       - byProperty: Array of { id, title, zone, status: 'rented'|'vacant', daysVacant?, tenant?, rentAmount }
       - byZone: Array of { zone, total, rented, vacant, vacancyRate }
       - monthlyTrend: Array of { month: string, occupancyRate: number } (last 12 months)

    2. CollectionsData:
       - summary: { totalExpected, totalCollected, totalLate, moraRate, avgDaysLate, recoveryRate }
       - byMonth: Array of { month, expected, collected, late, moraRate } (last 12 months)
       - topDelinquents: Array of { tenantName, propertyTitle, daysLate, amount, attempts }

    3. AgentPerformanceData (for plan 02):
       - agents: Array of { id, name, closings, avgDaysToClose, conversionRate, totalRevenue, activeLeads }
       - teamSummary: { totalClosings, avgConversion, totalRevenue, avgDaysToClose }

    Use realistic Colombian property data (zones: Chapinero, Usaquén, Suba, Cedritos, Laureles). Amounts in COP.
    Export all interfaces and mock data.
  </action>
  <verify>npx next build --no-lint | grep "Compiled"</verify>
  <done>Mock data file exports all report data with TypeScript interfaces</done>
</task>

<task type="auto">
  <name>Task 2: Build OccupancyReport and CollectionsReport components</name>
  <files>src/components/inmobiliaria/reports/OccupancyReport.tsx, src/components/inmobiliaria/reports/CollectionsReport.tsx</files>
  <action>
    Create both report components following existing dashboard patterns (neutral colors, rounded-xl cards, Phosphor icons).

    OccupancyReport:
    - Summary row: 4 KPI cards (Total, Rented, Vacant, Vacancy Rate)
    - Bar chart section: Simple CSS bars showing occupancy rate by zone (no charting library — use div widths)
    - Property table: List of properties with status badge (rented/vacant), days vacant, zone
    - Monthly trend: Simple line visualization using CSS (dots connected by lines, or bars)
    - Use formatCurrency from inmobiliaria types for COP amounts
    - Use useI18n() for labels

    CollectionsReport:
    - Summary row: 4 KPI cards (Expected, Collected, Late, Mora Rate)
    - Monthly breakdown: CSS bar chart showing expected vs collected per month
    - Recovery rate trend: Simple percentage display by month
    - Top delinquents table: Tenant name, property, days late, amount, attempts
    - Use useI18n() for labels

    Both components should accept their data as props (not import mock data directly — the page will pass it).
    Create the reports/ directory under components/inmobiliaria/.

    Run build.
  </action>
  <verify>npx next build --no-lint | grep "Compiled"</verify>
  <done>Both report components render with proper styling, accept data props, build passes</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx next build --no-lint` compiles successfully
- [ ] mock-reports.ts exports all data with proper TypeScript types
- [ ] OccupancyReport renders vacancy stats and property table
- [ ] CollectionsReport renders mora stats and monthly breakdown
</verification>

<success_criteria>

- Mock data covers all three report types with realistic Colombian data
- Both components follow existing dashboard visual patterns
- No external charting library — pure CSS/Tailwind visualizations
- Components accept data as props for flexibility
</success_criteria>

<output>
After completion, create `.planning/phases/29-advanced-reports/29-01-SUMMARY.md`
</output>
