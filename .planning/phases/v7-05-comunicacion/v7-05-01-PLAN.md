---
phase: v7-05-comunicacion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/api/messages.types.ts
  - src/lib/api/messages.service.ts
  - src/components/messages/MessagesWidget.tsx
  - src/app/inquilino/casos/[caseId]/page.tsx
autonomous: true
requirements: [COMU-01]

must_haves:
  truths:
    - "The tenant chat visually reads as tied to the arriendo: a slim context header ('Sobre tu arriendo — {property}') renders for actor==='tenant' from the conversation's REAL property field — never a fabricated arriendo"
    - "messages.service.ts exposes a lease-scoped read/send CONTRACT (getMessagesByLease/sendMessageByLease) that degrades to null/honest-unavailable on 404/403/0 — never a faked lease thread, and the existing applicationId path stays intact"
    - "The v7-03 caso detail deep-links to the tenant chat (a real 'Escribir a la inmobiliaria' affordance → /inquilino/mensajes) without fabricating a thread selection"
    - "landlord/agency (actor!=='tenant') behavior is byte-identical — no new header, no new fetch; no fake online/typing/presence anywhere"
  artifacts:
    - path: "src/lib/api/messages.service.ts"
      provides: "lease-scoped read/send contract with isEndpointUnavailable (404/403/0) honest-unavailable idiom; applicationId path untouched"
      contains: "getMessagesByLease"
    - path: "src/lib/api/messages.types.ts"
      provides: "optional leaseId?/caseId? on BackendConversation + ChatConversation; mapper carries them only when present"
      contains: "leaseId"
    - path: "src/components/messages/MessagesWidget.tsx"
      provides: "actor-gated tenant arriendo context header from real property; additive, no landlord/agency change"
      contains: "Sobre tu arriendo"
    - path: "src/app/inquilino/casos/[caseId]/page.tsx"
      provides: "deep-link from caso detail to the tenant chat (no fabricated thread)"
      contains: "/inquilino/mensajes"
  key_links:
    - from: "src/components/messages/MessagesWidget.tsx"
      to: "selectedConversation.property"
      via: "actor==='tenant' context header (real property only)"
      pattern: "actor === 'tenant'"
    - from: "src/lib/api/messages.service.ts"
      to: "isEndpointUnavailable (404/403/0)"
      via: "lease-scoped contract → null/unavailable, never a fake thread"
      pattern: "getMessagesByLease"
---

<objective>
Compose the tenant chat INTO the arriendo/caso (COMU-01) and land the lease-scoped read/send as a
frontend-first CONTRACT + honest "Próximamente", on the shared `MessagesWidget.tsx` (actor-gated), the
shared `messages.service.ts`/`messages.types.ts`, and the v7-03 caso detail.

Three honest deliverables:
1. **Lease-scoped contract (COMU-01 core).** Extend `messages.service.ts` with lease-scoped read/send
   methods modeled 1:1 on the existing applicationId shape, but wrapped in the `isEndpointUnavailable`
   (404/403/0) idiom from `lease-documents.service.ts` so a not-live NestJS route degrades to `null` /
   honest-unavailable — NEVER a fabricated lease thread. Add optional `leaseId?`/`caseId?` to the
   conversation types; the mapper carries them ONLY when the backend returns them (undefined otherwise).
   The existing `/applications/:id/chat*` path is untouched (additive — v7.0 never breaks the shared widget).
2. **Arriendo context header (real today).** For `actor === 'tenant'` only, render a slim context banner
   above the messages area — "Sobre tu arriendo — {selectedConversation.property}" — using the REAL
   `property` field already on `ChatConversation`. This ties the chat to the arriendo visually NOW, before
   the server groups by lease. landlord/agency render nothing new (byte-identical). No fake presence.
3. **Deep-link from the caso detail.** On `casos/[caseId]/page.tsx` add a neutral secondary "Escribir a la
   inmobiliaria" affordance → `/inquilino/mensajes` (the inbox). `TenantCase` carries no applicationId/leaseId,
   so the honest link is to the inbox — it does NOT fabricate a preselected thread.

Depends on nothing. This is wave 1: it seeds the shared service/types/widget that v7-05-02 and v7-05-03
build on (all three touch `MessagesWidget.tsx` → strictly sequential).

Purpose: COMU-01 — chat atado al arriendo/caso, lease-scoped `messages.service.ts` as contract + honest
fallback to the app-scoped chat.
Output: lease-scoped contract + tenant context header + caso→chat deep-link.

**Guardrails:** no faked lease thread (404/403/0 → null); context header uses REAL property only; no
online/typing/presence; applicationId path intact; landlord/agency unchanged; buttons sentence case
(DESIGN.md §4). Zero new npm packages.
</objective>

<execution_context>
@/Users/nicolasgarcia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolasgarcia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/v7-05-comunicacion/v7-05-RESEARCH.md
@.planning/phases/v7-05-comunicacion/v7-05-PATTERNS.md

# READ FIRST: docs/DESIGN.md §4 (Buttons — SENTENCE CASE, not uppercase), §11 (EmptyState / honest
# "Próximamente"), §7 (A11y — pair icon+text). CLAUDE.md: additive only, es-CO, PR workflow.

# Files to modify:
@src/lib/api/messages.types.ts
@src/lib/api/messages.service.ts
@src/components/messages/MessagesWidget.tsx
@src/app/inquilino/casos/[caseId]/page.tsx

# THE honest "endpoint-not-live → null/unavailable, never fake" idiom to copy verbatim:
@src/lib/api/lease-documents.service.ts

<interfaces>
<!-- Existing messages.service.ts — applicationId-scoped, REAL for text. DO NOT break these. -->
messagesApi.getMessages(applicationId): Promise<BackendConversationWithMessages>   // GET /applications/:id/chat
messagesApi.sendMessage(applicationId, content): Promise<...>                       // POST /applications/:id/chat/messages
messagesApi.markAsRead(applicationId): Promise<...>                                 // PATCH /applications/:id/chat/read
messagesApi.getConversations(): Promise<BackendConversationsResponse>              // GET /messages/conversations

<!-- Existing messages.types.ts — ChatConversation ALREADY carries `property` (the arriendo title). -->
interface ChatConversation { id; applicationId; name; role; email; property; propertyId; lastMessage; lastMessageTime; unreadCount; updatedAt }
interface BackendConversation { id; applicationId; property:{id;title}; otherParticipant; lastMessage; unreadCount; updatedAt }
function mapToConversation(backend): ChatConversation   // add leaseId?/caseId? passthrough here

<!-- Honest-unavailable idiom (lease-documents.service.ts) — copy verbatim -->
import { apiClient, ApiError } from './client'
function isEndpointUnavailable(err): boolean  // ApiError && (status===404 || 403 || 0)

<!-- Widget: actor prop + selectedConversation already exist -->
MessagesWidget({ actor: 'tenant' | 'landlord' })   // isTenant = actor === 'tenant'
selectedConversation: ChatConversation | undefined   // .property is the arriendo title (REAL)

<!-- TenantCase (v7-03) — NOTE: carries NO applicationId/leaseId. Deep-link goes to the inbox only. -->
interface TenantCase { id; type; titulo; estadoLabel; tone; responsable; updatedAt; detailLink; sourceLink; events[] }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lease-scoped read/send CONTRACT + leaseId?/caseId? types (honest-unavailable)</name>
  <files>src/lib/api/messages.types.ts, src/lib/api/messages.service.ts</files>
  <action>
    In `messages.types.ts`: add an OPTIONAL `leaseId?: string` and `caseId?: string` to `BackendConversation`,
    to `BackendConversationWithMessages`, and to `ChatConversation`. In `mapToConversation`, pass them through
    ONLY when present on the backend object (`leaseId: backend.leaseId`, `caseId: backend.caseId`) — they stay
    `undefined` until the server returns them. Do NOT fabricate or derive them from the applicationId.

    In `messages.service.ts`: import `ApiError` from `./client` and add a local
    `isEndpointUnavailable(err)` (ApiError && status===404||403||0) copied verbatim from
    `lease-documents.service.ts:67`. Add two lease-scoped CONTRACT methods, modeled 1:1 on the existing
    applicationId shape, that degrade honestly:
      - `async getMessagesByLease(leaseId: string): Promise<BackendConversationWithMessages | null>` →
        `apiClient.get(`/leases/${leaseId}/chat`)`, wrapped so `isEndpointUnavailable` → `return null`.
      - `async sendMessageByLease(leaseId: string, content: string): Promise<{ id:string; content:string; senderId:string; createdAt:string } | null>` →
        `apiClient.post(`/leases/${leaseId}/chat/messages`, { content })`, wrapped so `isEndpointUnavailable` → `return null`.
    JSDoc each: the NestJS lease-scoped route does NOT exist yet (external dep — ROADMAP v7-05 external-deps);
    `null` is the expected result today so the UI keeps the app-scoped chat + honest "Próximamente" for true
    per-arriendo threading. NEVER fabricate a thread. Keep `getMessages`/`sendMessage`/`markAsRead`/
    `getConversations` exactly as-is (additive).
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && S=src/lib/api/messages.service.ts && T=src/lib/api/messages.types.ts && grep -q "getMessagesByLease" $S && grep -q "sendMessageByLease" $S && grep -q "isEndpointUnavailable" $S && grep -qE "404|403" $S && grep -q "getMessages(" $S && grep -q "sendMessage(" $S && grep -q "leaseId" $T && grep -q "caseId" $T && echo GATE_OK</automated>
  </verify>
  <done>Lease-scoped read/send exist returning `... | null`; 404/403/0 → null (honest-unavailable, no fabrication); leaseId?/caseId? added to conversation types + mapper passthrough; applicationId methods untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Actor-gated tenant arriendo context header (real property, no presence)</name>
  <files>src/components/messages/MessagesWidget.tsx</files>
  <action>
    Add a slim context banner rendered ONLY when `isTenant` (actor==='tenant') AND `selectedConversation` is
    set, placed at the TOP of the messages area (just inside the chat-content column, above the messages
    scroll region ~line 471-474). Copy (es/en, sentence case): es → "Sobre tu arriendo — {selectedConversation.property}"
    ; en → "About your rental — {selectedConversation.property}". Use a neutral tile + `House` icon (already
    imported) consistent with the info-panel property row (`:622-636`). Below the title, a subtle muted note:
    es → "Estamos conectando cada chat a su arriendo; el hilo por arriendo llega próximamente." / en → "We're
    tying each chat to its rental; per-rental threads are coming soon." — this is the honest COMU-01 disclosure
    that true lease-scoped threading is a backend dep.

    Render nothing when `selectedConversation.property` is empty (never a placeholder arriendo). For
    `actor !== 'tenant'` render NOTHING new — the landlord/agency layout stays byte-identical.

    Do NOT add any `online`/`en línea`/`typing`/`escribiendo`/presence indicator anywhere (PITFALLS UX — no
    instant-human illusion). This task is composition/labeling only.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && W=src/components/messages/MessagesWidget.tsx && grep -q "Sobre tu arriendo" $W && grep -q "actor === 'tenant'\|isTenant" $W && test $(grep -ciE "en línea|escribiendo|typing|is typing|\bonline\b" $W) -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>Tenant-only arriendo context header renders from the real `property` field with an honest per-arriendo "próximamente" note; landlord/agency unchanged; zero presence/typing indicators.</done>
</task>

<task type="auto">
  <name>Task 3: Deep-link from the caso detail to the tenant chat (no fabricated thread)</name>
  <files>src/app/inquilino/casos/[caseId]/page.tsx</files>
  <action>
    In the summary card of `CaseDetail` (next to the existing "Ver en {origen}" out-link button, ~lines
    196-205), add a second secondary `Button asChild` — es → "Escribir a la inmobiliaria" / en → "Message the
    agency" — linking with `Link href="/inquilino/mensajes"` and a `ChatCircle`/`Chat` Phosphor icon. Sentence
    case (DESIGN.md §4). This is a real "contactar" affordance for the case.

    Do NOT invent a preselected thread: `TenantCase` carries no applicationId/leaseId, so the link goes to the
    messages INBOX only — never `?applicationId=<fabricated>`. Keep the existing own-cases-only / no-IDOR
    resolution and all v7-03 guardrails intact (view-only; no new fetch, no new authz).
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && P="src/app/inquilino/casos/[caseId]/page.tsx" && grep -q "/inquilino/mensajes" "$P" && grep -qi "Escribir a la inmobiliaria" "$P" && test $(grep -c "applicationId=" "$P") -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>Caso detail offers a sentence-case "Escribir a la inmobiliaria" deep-link to the chat inbox; no fabricated thread param; v7-03 guardrails intact.</done>
</task>

</tasks>

<verification>
- `pnpm -C /Users/nicolasgarcia/rent/mvp-portal-inquilino build` succeeds.
- `pnpm test` — 0 NEW failures (~7 pre-existing unrelated — see v7-01 deferred-items.md).
- Grep gates (per task): lease-scoped read/send + `isEndpointUnavailable` present, applicationId path intact,
  `leaseId`/`caseId` in types; tenant context header from real property, zero presence/typing tokens; caso
  deep-link to `/inquilino/mensajes`, zero `applicationId=` fabrication.
- Manual smoke as `tenant`: open `/inquilino/mensajes`, select a conversation → header reads "Sobre tu
  arriendo — {property}" + the honest "próximamente" note; from a caso detail, "Escribir a la inmobiliaria"
  lands on the inbox. As `landlord`: the widget is unchanged (no new header).
</verification>

<success_criteria>
- COMU-01: `messages.service.ts` is extended to lease-scoped as a contract + honest fallback; the chat reads
  as tied to the arriendo (real context header) and is reachable from the caso detail — no fabricated lease
  thread, applicationId path intact, landlord/agency unaffected.
</success_criteria>

<output>
After completion, create `.planning/phases/v7-05-comunicacion/v7-05-01-SUMMARY.md`.
</output>
