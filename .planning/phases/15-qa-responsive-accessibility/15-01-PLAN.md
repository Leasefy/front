---
phase: 15-qa-responsive-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/ui/plan/PlanHeader.tsx, src/app/propiedades/page.tsx]
autonomous: true

must_haves:
  truths:
    - "Every page renders correctly on mobile (375px) without horizontal overflow"
    - "Every page renders correctly on tablet (768px) without layout breakage"
    - "Every page renders correctly on desktop (1280px+)"
    - "No horizontal overflow on any viewport"
    - "Touch targets meet 44px minimum on mobile"
  artifacts:
    - path: "src/components/ui/plan/PlanHeader.tsx"
      provides: "Responsive search/dropdown widths"
    - path: "src/app/propiedades/page.tsx"
      provides: "Responsive filter dropdowns"
  key_links: []
---

<objective>
Audit and fix responsive layout issues across all pages for mobile (375px), tablet (768px), and desktop (1280px+).

Purpose: Ensure every page renders correctly at all viewport sizes with no overflow and adequate touch targets.
Output: All responsive issues fixed, no horizontal overflow on any viewport.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix hardcoded widths causing overflow on small screens</name>
  <files>src/components/ui/plan/PlanHeader.tsx, src/app/propiedades/page.tsx</files>
  <action>
    Fix responsive issues in components with hardcoded pixel widths:

    1. PlanHeader.tsx: Replace w-[400px], w-[320px], w-[360px] with responsive variants (w-full md:w-[400px] or max-w-[400px])
    2. propiedades/page.tsx: Ensure filter dropdowns use min-w that works on mobile (min-w-0 on mobile, min-w-[140px] on md:+)

    Also scan all files for other hardcoded widths that could cause horizontal overflow:
    - Search for w-[XXXpx] where XXX > 200
    - Check each for whether they need responsive variants
    - Fix any that would overflow on 375px viewport
  </action>
  <verify>grep -r "w-\[" src/ | grep -E "w-\[[3-9][0-9]{2,}" to find remaining large fixed widths</verify>
  <done>No hardcoded widths cause horizontal overflow at 375px</done>
</task>

<task type="auto">
  <name>Task 2: Audit all 33 pages for responsive rendering and touch targets</name>
  <files>All page.tsx files in src/app/</files>
  <action>
    Systematic audit of all page files:

    1. Check each page for proper responsive grid/flex patterns (grid-cols-1 → md:grid-cols-2 → lg:grid-cols-3)
    2. Verify no fixed-width containers that would overflow on mobile
    3. Check all interactive elements (buttons, links, inputs) have minimum 44px touch targets on mobile
    4. For elements below 44px: add min-h-[44px] min-w-[44px] or increase padding
    5. Ensure all tables use responsive patterns (horizontal scroll wrapper or card view on mobile)

    Focus areas:
    - Pricing page tables
    - Dashboard data tables
    - Payment history tables
    - Settings toggle switches
    - Small icon buttons
  </action>
  <verify>npm run build passes</verify>
  <done>All pages render correctly across viewports, all touch targets >= 44px</done>
</task>

</tasks>

<verification>
- [ ] npm run build passes
- [ ] No hardcoded widths > 300px without responsive variants
- [ ] All interactive elements have minimum 44px touch target on mobile
</verification>

<success_criteria>
- All pages responsive across mobile/tablet/desktop
- No horizontal overflow at any viewport
- Touch targets meet 44px minimum
</success_criteria>

<output>
After completion, create `.planning/phases/15-qa-responsive-accessibility/15-01-SUMMARY.md`
</output>
