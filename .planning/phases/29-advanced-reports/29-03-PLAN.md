---
phase: 29-advanced-reports
plan: 03
type: execute
wave: 2
depends_on: ["29-01", "29-02"]
files_modified:
  - src/app/panel/inmobiliaria/reportes/page.tsx
  - src/components/inmobiliaria/reports/ReportPDFExport.tsx
autonomous: true

must_haves:
  truths:
    - "Reportes page has tabs for Ocupación, Cobros, Rendimiento Agentes"
    - "Each tab renders its respective report component with mock data"
    - "Reports page is wrapped with FeatureGate for 'advanced-reports' feature"
    - "Non-Growth users see upgrade prompt instead of reports"
    - "PDF export button exists and triggers basic PDF generation"
  artifacts:
    - path: "src/app/panel/inmobiliaria/reportes/page.tsx"
      provides: "Enhanced reports page with tabs and plan gating"
    - path: "src/components/inmobiliaria/reports/ReportPDFExport.tsx"
      provides: "Basic PDF export using window.print()"
      exports: ["ReportPDFExport"]
  key_links:
    - from: "reportes/page.tsx"
      to: "FeatureGate"
      via: "wraps report content with feature='advanced-reports'"
    - from: "reportes/page.tsx"
      to: "OccupancyReport + CollectionsReport + AgentPerformanceReport"
      via: "renders report in active tab"
---

<objective>
Wire all report components into the reportes page with tabs, plan gating, and PDF export.

Purpose: Integrate everything from plans 01 and 02 into the actual page, gate to Growth+ plans.
Output: Enhanced reportes page with 3 tabs + FeatureGate + PDF export.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-advanced-reports/29-01-SUMMARY.md
@.planning/phases/29-advanced-reports/29-02-SUMMARY.md

@src/app/panel/inmobiliaria/reportes/page.tsx
@src/components/inmobiliaria/UpgradePrompt.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance reportes page with tabs and report components</name>
  <files>src/app/panel/inmobiliaria/reportes/page.tsx</files>
  <action>
    Rewrite the reportes page to add tabbed navigation between 3 report types:

    1. Read the existing page first to understand current structure.

    2. Add tab state: 'ocupacion' | 'cobros' | 'agentes' (default: 'ocupacion')

    3. Tab bar: 3 tabs with icons (Buildings for Ocupación, CurrencyDollar for Cobros, Users for Agentes)
       - Style: similar to existing tabs in the app (inline-flex, rounded-lg, active has bg-white shadow)

    4. Wrap the tab content with FeatureGate:
       ```tsx
       <FeatureGate feature="advanced-reports">
         {activeTab === 'ocupacion' && <OccupancyReport data={mockOccupancyData} />}
         {activeTab === 'cobros' && <CollectionsReport data={mockCollectionsData} />}
         {activeTab === 'agentes' && <AgentPerformanceReport data={mockAgentPerformanceData} />}
       </FeatureGate>
       ```

    5. Import mock data from '@/lib/data/mock-reports'
    6. Import report components from '@/components/inmobiliaria/reports/'
    7. Import FeatureGate from '@/components/inmobiliaria/UpgradePrompt'

    8. Add PDF export button in the header area (next to title)

    Keep the existing page header/title, just replace the content area.
    Use useI18n() for tab labels.
  </action>
  <verify>npx next build --no-lint | grep "Compiled"</verify>
  <done>Reportes page has 3 tabs, renders report components, gated with FeatureGate</done>
</task>

<task type="auto">
  <name>Task 2: Create basic PDF export component</name>
  <files>src/components/inmobiliaria/reports/ReportPDFExport.tsx</files>
  <action>
    Create a simple PDF export component that uses window.print() with print-specific CSS.

    ReportPDFExport:
    - Props: { title: string }
    - Renders a button: "Exportar PDF" with FileText icon
    - On click: calls window.print()
    - The print CSS will hide navigation/sidebar and show only report content

    Add a @media print block (either inline style or via Tailwind's print: prefix) that:
    - Hides sidebar, header, tabs
    - Shows report content full-width
    - Removes shadows, borders for clean print
    - Uses className="print:hidden" on non-report elements in the page

    This is a basic approach — no PDF library needed. window.print() generates PDF via browser.

    Export the component.
  </action>
  <verify>npx next build --no-lint | grep "Compiled"</verify>
  <done>ReportPDFExport component exported, triggers window.print() on click</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx next build --no-lint` compiles successfully
- [ ] Reportes page shows 3 tabs
- [ ] Switching tabs renders correct report
- [ ] FeatureGate wraps report content
- [ ] PDF export button present and functional
</verification>

<success_criteria>

- Reportes page is a comprehensive report hub with 3 report types
- Reports are gated to Growth+ / Growth Flex+ plans
- PDF export works via browser print
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/29-advanced-reports/29-03-SUMMARY.md`
</output>
