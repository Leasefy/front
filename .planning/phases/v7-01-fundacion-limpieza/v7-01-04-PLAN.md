---
phase: v7-01-fundacion-limpieza
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/inquilino/layout.tsx
  - src/components/tenant/TenantDashboardSidebar.tsx
autonomous: true
requirements: [BASE-04]

must_haves:
  truths:
    - "The tenant sidebar nav exposes Notificaciones, Perfil and Configuración items"
    - "The Mensajes nav badge shows the real unread count (not a hardcoded 2)"
    - "The dead file TenantDashboardSidebar.tsx no longer exists and nothing imports it"
  artifacts:
    - path: "src/app/inquilino/layout.tsx"
      provides: "Sidebar nav including notificaciones/perfil/configuracion + real unread badge"
      contains: "/inquilino/configuracion"
  key_links:
    - from: "src/app/inquilino/layout.tsx"
      to: "useUnreadMessages"
      via: "real Mensajes badge"
      pattern: "useUnreadMessages"
---

<objective>
Expose Notificaciones / Perfil / Configuración in the tenant sidebar nav (they are currently
only reachable via the header dropdown), replace the hardcoded Mensajes `badge: 2` with the
real unread count, and delete the confirmed-dead `TenantDashboardSidebar.tsx`.

Purpose: Satisfy BASE-04 (nav exposes Notificaciones/Perfil/Configuración; dead code removed).
Output: `src/app/inquilino/layout.tsx` nav includes the three items + real badge;
`src/components/tenant/TenantDashboardSidebar.tsx` is deleted.
</objective>

<execution_context>
@/Users/nicolasgarcia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolasgarcia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/v7-01-fundacion-limpieza/v7-01-RESEARCH.md
@.planning/phases/v7-01-fundacion-limpieza/v7-01-PATTERNS.md

# File to modify:
@src/app/inquilino/layout.tsx
# File to delete (mine its labels/icons/hrefs first, then remove):
@src/components/tenant/TenantDashboardSidebar.tsx

# IMPORTANT — read docs/DESIGN.md §4 "Sidebar / Layout". PlanSidebar is canonical; only
# extend the nav array — do not restyle the shell.

<interfaces>
<!-- From src/components/ui/plan/PlanSidebar.tsx — NavItem the layout builds -->
interface NavItem { label: string; href: string; icon: PhosphorIcon; exact?: boolean; badge?: number }
// PlanSidebar renders badge via `count={item.badge > 0 ? item.badge : undefined}` (:261)

// From src/lib/hooks/useMessages.ts (already used by the dead sidebar):
useUnreadMessages(): { unreadCount: number }

// Labels/icons/hrefs to mine from TenantDashboardSidebar.tsx NAV_ITEMS (:42-53):
//   Notificaciones → /inquilino/notificaciones  (Bell)
//   Perfil        → /inquilino/perfil           (UserCircle)
//   Configuración → /inquilino/configuracion    (Gear)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the three nav items + wire the real Mensajes badge</name>
  <files>src/app/inquilino/layout.tsx</files>
  <action>
    In `useTenantNavItems()` (lines 25-38):
    - Add icon imports `Bell, UserCircle, Gear` to the existing `@phosphor-icons/react` import.
    - Import `useUnreadMessages` from `@/lib/hooks/useMessages` and call
      `const { unreadCount } = useUnreadMessages();` inside `useTenantNavItems`.
    - Replace the hardcoded Mensajes `badge: 2` (line 36) with
      `badge: unreadCount > 0 ? unreadCount : undefined` (R: no fabricated count).
    - Append three items to the returned array (after Documentos/Mensajes), matching the
      labels the header dropdown + dead sidebar already use:
        { label: t('nav.notifications') ?? 'Notificaciones', href: '/inquilino/notificaciones', icon: Bell }
        { label: t('nav.profile') ?? 'Perfil', href: '/inquilino/perfil', icon: UserCircle }
        { label: t('nav.settings') ?? 'Configuración', href: '/inquilino/configuracion', icon: Gear }
      Use the existing i18n keys if present (grep the i18n dict for `nav.notifications`,
      `nav.profile`, `nav.settings`); otherwise use the `locale === 'es' ? … : …` literal
      form already used elsewhere in this file. Keep item shape `{ label, href, icon, exact? }`.
    - Keep the header dropdown links intact (they stay — R3: sidebar + header both).
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && grep -q '/inquilino/notificaciones' src/app/inquilino/layout.tsx && grep -q '/inquilino/perfil' src/app/inquilino/layout.tsx && grep -q '/inquilino/configuracion' src/app/inquilino/layout.tsx && grep -q 'useUnreadMessages' src/app/inquilino/layout.tsx && test $(grep -c 'badge: 2' src/app/inquilino/layout.tsx) -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>Sidebar nav includes Notificaciones/Perfil/Configuración; Mensajes badge is the real unreadCount; no hardcoded `badge: 2`.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the dead TenantDashboardSidebar.tsx</name>
  <files>src/components/tenant/TenantDashboardSidebar.tsx</files>
  <action>
    Confirm zero importers first:
    `grep -rn "TenantDashboardSidebar" src/` must return ONLY its own definition line.
    Confirm the barrel `src/components/tenant/index.ts` does not export it (it exports only
    Application* components — verified in recon). Then delete the file
    `src/components/tenant/TenantDashboardSidebar.tsx` (this is dead code — the layout uses
    `PlanSidebar`, not this component). No barrel edit is expected; if a stale export exists,
    remove it.
    Commit this deletion with a `chore(v7-01): …` prefix (per repo convention for dead-code removal).
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && test ! -f src/components/tenant/TenantDashboardSidebar.tsx && test $(grep -rc "TenantDashboardSidebar" src/ | grep -v ':0' | wc -l | tr -d ' ') -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>TenantDashboardSidebar.tsx is deleted; `grep -rn TenantDashboardSidebar src/` returns nothing; no broken import.</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds (run locally — CI skips `next build`; a dangling import to the
  deleted file would fail the build).
- `pnpm test` green.
- Grep gates: three nav hrefs present + `useUnreadMessages` in `layout.tsx`, no `badge: 2`;
  `grep -rn TenantDashboardSidebar src/` returns zero results.
- Manual smoke as `tenant`: sidebar shows Notificaciones/Perfil/Configuración and they route;
  Mensajes badge reflects real unread (or none).
</verification>

<success_criteria>
- BASE-04: the layout sidebar exposes Notificaciones/Perfil/Configuración with a real
  Mensajes unread badge, and the dead `TenantDashboardSidebar.tsx` is removed with no
  dangling references.
</success_criteria>

<output>
After completion, create `.planning/phases/v7-01-fundacion-limpieza/v7-01-04-SUMMARY.md`.
</output>
