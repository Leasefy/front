---
phase: v7-07-acuerdos-pago
plan: 04
type: execute
wave: 3
depends_on: [v7-07-01, v7-07-03]
files_modified:
  - src/components/tenant/CuotaPlanTable.tsx
  - src/app/inquilino/acuerdos/page.tsx
  - src/app/inquilino/layout.tsx
  - src/app/inquilino/casos/page.tsx
autonomous: true
requirements: [ACUE-01]

must_haves:
  truths:
    - "The tenant sees a real /inquilino/acuerdos page listing their OWN agency-approved acuerdos (useTenantAcuerdos) with a per-plan cuota summary; every peso shown (totalDueCop, cuota amountCop) is read VERBATIM from the agent record — CuotaPlanTable renders installments[] with NO saldo math (no reduce/sum/'restante')"
    - "At ship time listMine() degrades to [] → the page shows an HONEST 'Próximamente'/all-clear EmptyState (never fabricated acuerdo/cuota rows); each real row deep-links to /inquilino/acuerdos/[id]; there is NO approve button and NO terms editor on this surface"
    - "An 'Acuerdos' nav entry (distinct Phosphor icon, not Handshake/Lifebuoy/ClipboardText) routes to /inquilino/acuerdos, and the caso hub's Acuerdos 'Próximamente' ProximamenteSection is replaced by a real Link to /inquilino/acuerdos — exactly as v7-06 replaced the PQRS placeholder with the Solicitudes link"
  artifacts:
    - path: "src/components/tenant/CuotaPlanTable.tsx"
      provides: "renders AcuerdoInstallment[] verbatim (Cuota N · fecha · monto · estado) with formatCurrency + es-CO dates + tabular-nums; NO saldo/total arithmetic; reused by list + detail"
      contains: "installments"
    - path: "src/app/inquilino/acuerdos/page.tsx"
      provides: "own-acuerdos list (useTenantAcuerdos) with totalDueCop + cuota summary per row + tone badge; honest empty/'Próximamente'; rows link to /inquilino/acuerdos/[id]; no approve/terms affordance"
      contains: "useTenantAcuerdos"
    - path: "src/app/inquilino/layout.tsx"
      provides: "'Acuerdos' nav entry → /inquilino/acuerdos (distinct icon)"
      contains: "/inquilino/acuerdos"
    - path: "src/app/inquilino/casos/page.tsx"
      provides: "Acuerdos ProximamenteSection replaced by a real Link to /inquilino/acuerdos"
      contains: "/inquilino/acuerdos"
  key_links:
    - from: "src/app/inquilino/acuerdos/page.tsx"
      to: "useTenantAcuerdos() → CuotaPlanTable(installments)"
      via: "list own acuerdos; render cuota plan verbatim from the record; [] → honest empty"
      pattern: "CuotaPlanTable"
    - from: "src/app/inquilino/casos/page.tsx"
      to: "/inquilino/acuerdos"
      via: "real entry-point Link replaces the Acuerdos 'Próximamente' placeholder"
      pattern: "/inquilino/acuerdos"
---

<objective>
Ship the ACUE-01 READ surface: a dedicated `/inquilino/acuerdos` list page (own-scoped, honest-degrading) with a
reusable `CuotaPlanTable` that renders the cuota plan VERBATIM from the agent record — plus the nav entry and the
caso-hub swap that make it the one honest entry point. Mirrors the agency acuerdos page's disabled/"Próximamente"
posture, tenant-scoped.

Three honest deliverables:
1. **CuotaPlanTable (ACUE-01, PITFALLS 9).** A pure presentational component: `{ installments:
   AcuerdoInstallment[]; locale: string }` → a table/list of `Cuota N · {dueDate es-CO} · {formatCurrency
   amountCop} · {estado}` rows, `font-mono tabular-nums`, neutral cuota-status chips. It renders the record's
   `installments[]` verbatim — NO `.reduce`, NO sum, NO "saldo/restante" arithmetic (the agent is the sole saldo
   authority). Reused by the list (summary) + detail (v7-07-05).
2. **/inquilino/acuerdos list (ACUE-01).** Tenant page shell + gates copied from `casos/page.tsx` (Spinner →
   `useOnboardingStatus` + `CompleteProfileFirst` → error `EmptyState`; `min-h-screen bg-[#f8f8f8]
   dark:bg-[#0e0e10]` + `max-w-7xl` + framer `motion.header`). List via `useTenantAcuerdos`; each row shows the
   plan `totalDueCop` (`formatCurrency`), a neutral tone badge (`acuerdoStatusToTone/Label`), and a short cuota
   summary (e.g. "N cuotas"), linking to `/inquilino/acuerdos/${encodeURIComponent(planId)}`. Empty / not-live
   `[]` → an HONEST EmptyState ("Aún no tienes acuerdos" + a factual "Próximamente" note that agency-approved
   acuerdos will appear here). NO approve button, NO terms editor, NO fabricated rows. Neutral tone only (no
   red/countdown). Inline `locale === 'es'` copy (tenant convention).
3. **Nav + hub swap (ACUE-01).** In `layout.tsx` add an "Acuerdos" nav item with a DISTINCT Phosphor icon
   (`Handshake` is taken by Contratos, `Lifebuoy` by Solicitudes, `ClipboardText` by Mis casos — use `Scroll`),
   `{ label: locale === 'es' ? 'Acuerdos' : 'Agreements', href: '/inquilino/acuerdos', icon: Scroll }`, placed
   near "Pagos". In `casos/page.tsx` REPLACE the Acuerdos `ProximamenteSection` (:360-370) with a real `Link
   href="/inquilino/acuerdos"` entry-point card (reuse the Solicitudes real-link card styling at :335-358) —
   exactly the v7-06 PQRS-placeholder swap. Real acuerdo rows already fold into the hub list via v7-07-03.

Depends on v7-07-01 (types) and v7-07-03 (useTenantAcuerdos + mappers). Wave 3.

Purpose: ACUE-01 — the tenant sees + navigates their approved acuerdos with the cuota plan, honestly.
Output: CuotaPlanTable + acuerdos list + nav/hub wiring.

**Guardrails:** no saldo math anywhere (render installments/totalDueCop verbatim — PITFALLS 9); no approve
button / no terms editor (A5, T-323 — the tenant only VIEWS here); own-acuerdos-only (`useTenantAcuerdos`/
`listMine`, no fetch-by-id); `[]` → honest empty (no fabricated acuerdo/cuota rows); neutral tone (no red
countdown, no credit-bureau copy); es-CO copy + `formatCurrency` (§16); buttons sentence case (§4); zero new
npm packages.
</objective>

<execution_context>
@/Users/nicolasgarcia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolasgarcia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/v7-07-acuerdos-pago/v7-07-RESEARCH.md
@.planning/phases/v7-07-acuerdos-pago/v7-07-PATTERNS.md

# READ FIRST: docs/DESIGN.md §11 (EmptyState / honest "Próximamente"), §16 (money/date formatCurrency + es-CO),
# §4 (Buttons — SENTENCE CASE), §7 (A11y — pair icon+text). CLAUDE.md: additive, es-CO, zero new npm packages.

# Tenant page shell + gates + CaseRow/TONE_BADGE + the v7-06 real-link swap to COPY:
@src/app/inquilino/casos/page.tsx
# Solicitudes list is the exact list-page analog (shell + useTenantPqrs + honest empty):
@src/app/inquilino/solicitudes/page.tsx
# The agency acuerdos posture to MIRROR (disabled "Próximamente", cuota desglose, T-323) — read once, tenant-scope it:
@src/app/panel/inmobiliaria/ai/cobranza/acuerdos/page.tsx
# File to modify (nav):
@src/app/inquilino/layout.tsx

<interfaces>
<!-- v7-07-01 / v7-07-03 outputs consumed here -->
useTenantAcuerdos(): { items: AcuerdoDetail[]; isLoading; error; refetch }   // from '@/lib/hooks/use-tenant-acuerdos'
type AcuerdoDetail = { planId; status: string; totalDueCop; paymentUrl: string|null; offeredAt; acceptedAt: string|null; installments: AcuerdoInstallment[] }
type AcuerdoInstallment = { number; dueDate; amountCop; status; paidAt: string|null }
acuerdoStatusToTone(status): CaseTone   // from '@/lib/types/tenant-case'
acuerdoStatusToLabel(status): string
useI18n(): { formatCurrency(n): string; locale: 'es'|'en' }   // formatCurrency for COP

<!-- layout.tsx nav array (:30-46): each item { label, href, icon }. Insert Acuerdos near "Pagos" (:36).
     Distinct icon (Scroll) — Handshake/Lifebuoy/ClipboardText already taken. -->
<!-- casos/page.tsx: the Acuerdos ProximamenteSection is at :360-370; the Solicitudes real-link card at :335-358. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CuotaPlanTable — render installments[] verbatim (no saldo math)</name>
  <files>src/components/tenant/CuotaPlanTable.tsx</files>
  <action>
    Create `src/components/tenant/CuotaPlanTable.tsx` (`'use client'`). Props: `{ installments:
    AcuerdoInstallment[]; locale?: string; className?: string }`. Render each installment as a row: `Cuota
    {number}` · due date formatted `new Intl.DateTimeFormat(locale==='es'?'es-CO':'en-US', { day:'numeric',
    month:'long', year:'numeric' }).format(new Date(dueDate))` · amount via `useI18n().formatCurrency(amountCop)`
    in `font-mono tabular-nums` · a neutral status chip for the cuota `status` (factual label; paid cuota shows
    its `paidAt` date). Import `AcuerdoInstallment` from `@/lib/api/tenant-acuerdos.types`.
    HARD RULE: render values VERBATIM from the record — do NOT compute a total, a "saldo", or a "restante"; NO
    `.reduce`, NO `+` accumulation over `amountCop`. If the caller wants the plan total it passes `totalDueCop`
    from the record (a separate prop or a sibling line) — this component never derives it. Neutral styling only
    (no red/countdown). Empty `installments` → a small "Sin cuotas" line (honest). es-CO labels.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && T=src/components/tenant/CuotaPlanTable.tsx && grep -q "installments" $T && grep -q "amountCop" $T && grep -q "formatCurrency" $T && grep -qE "es-CO" $T && test $(grep -vE '^\s*(\*|//|/\*)' $T | grep -cE "\.reduce\(|restante|saldo") -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>CuotaPlanTable renders installments[] verbatim (Cuota N · fecha · monto · estado) with formatCurrency + es-CO dates; zero saldo/total arithmetic; reusable by list + detail.</done>
</task>

<task type="auto">
  <name>Task 2: /inquilino/acuerdos list page (ACUE-01)</name>
  <files>src/app/inquilino/acuerdos/page.tsx</files>
  <action>
    Create the page (`'use client'`) reusing the tenant page shell + gates from `casos/page.tsx` (Spinner
    loading gate → `useOnboardingStatus` + `CompleteProfileFirst` → error `EmptyState`; `min-h-screen
    bg-[#f8f8f8] dark:bg-[#0e0e10]` + `max-w-7xl mx-auto` + framer `motion.header`). Header: es "Acuerdos de
    pago" / en "Payment agreements" + a factual subtitle.
    List from `useTenantAcuerdos()`. For each `AcuerdoDetail` render a row (reuse the `CaseRow`/`TONE_BADGE`
    idiom): leading icon (`Scroll`), title "Acuerdo de pago", a neutral badge from `acuerdoStatusToTone/Label`,
    the plan total `formatCurrency(p.totalDueCop)` (`font-mono tabular-nums`), and a short "· {N} cuotas" summary
    (N = `p.installments.length`, a length, not a sum). Each row is a `Link
    href={`/inquilino/acuerdos/${encodeURIComponent(p.planId)}`}`.
    Empty list (incl. not-live `[]`) → an HONEST `EmptyState` (`Scroll` icon, es "Aún no tienes acuerdos de
    pago" + a factual note: "Cuando tu inmobiliaria te apruebe un acuerdo, aparecerá aquí." — mirror the agency
    "Próximamente" posture, tenant-scoped). NO fabricated rows, NO approve button, NO terms editor, NO "solicitar
    plan" CTA yet (that arrives in v7-07-07). Neutral tone only. Inline `locale === 'es'` copy.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && P=src/app/inquilino/acuerdos/page.tsx && grep -q "useTenantAcuerdos" $P && grep -q "acuerdoStatusToTone" $P && grep -q "formatCurrency" $P && grep -q "/inquilino/acuerdos/" $P && grep -q "EmptyState" $P && test $(grep -vE '^\s*(\*|//|/\*)' $P | grep -ciE "aprobar acuerdo|editar t[eé]rmino|\.reduce\(|restante|por qu[eé]|central") -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>Acuerdos list renders own acuerdos with totalDueCop + cuota count + neutral tone; honest empty/"Próximamente" when []; rows link to the detail; no approve/terms/saldo/"por qué"/bureau affordance.</done>
</task>

<task type="auto">
  <name>Task 3: Nav entry + caso-hub Acuerdos real-link swap</name>
  <files>src/app/inquilino/layout.tsx, src/app/inquilino/casos/page.tsx</files>
  <action>
    In `layout.tsx`: import `Scroll` from `@phosphor-icons/react` and insert a nav item `{ label: locale === 'es'
    ? 'Acuerdos' : 'Agreements', href: '/inquilino/acuerdos', icon: Scroll }` into the `useTenantNavItems` array
    near the "Pagos" entry (:36) — acuerdos are a payment concern. Do NOT reuse `Handshake` (Contratos),
    `Lifebuoy` (Solicitudes), or `ClipboardText` (Mis casos). Follow the exact object shape of the neighbors.
    In `casos/page.tsx`: REPLACE the Acuerdos `ProximamenteSection` block (:360-370) with a real entry-point
    `Link href="/inquilino/acuerdos"` card, reusing the Solicitudes real-link card styling (:335-358): a
    `Handshake` icon tile, es "Acuerdos de pago" title, copy "Consulta y gestiona tus acuerdos de pago." and a
    sentence-case "Ver acuerdos"/"Solicitar plan" affordance with `ArrowUpRight`. Keep the grid two-up (the
    Solicitudes card stays as-is). `TYPE_ICON` already maps `acuerdo: Handshake`. If `ProximamenteSection` is no
    longer referenced anywhere after this swap, remove the now-unused import/definition to keep the file clean
    (only if truly unused).
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && L=src/app/inquilino/layout.tsx && C=src/app/inquilino/casos/page.tsx && grep -q "/inquilino/acuerdos" $L && grep -q "Scroll" $L && grep -q "/inquilino/acuerdos" $C && echo GATE_OK</automated>
  </verify>
  <done>"Acuerdos" nav entry added (distinct Scroll icon); caso hub Acuerdos placeholder replaced by a real Link to /inquilino/acuerdos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| useTenantAcuerdos rows → acuerdos list UI | JWT-scoped own records enter the read projection; only whitelisted fields render |
| tenant → /inquilino/acuerdos navigation | A read-only surface; no mutation crosses here (accept/pay/request are later plans) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v7-07-10 | Tampering / PITFALLS 9 | CuotaPlanTable / list | mitigate | Every peso rendered verbatim from the record (`totalDueCop`, `installments[].amountCop`); NO `.reduce`/sum/"restante" (grep-gated, comment-stripped). The agent is the sole saldo authority. |
| T-v7-07-11 | Elevation of Privilege (A5, T-323) | acuerdos list | mitigate | Read-only: NO "aprobar acuerdo" button, NO terms editor (grep-gated). The tenant only VIEWS here; approval/policy stay agent-side. |
| T-v7-07-12 | Tampering | acuerdos list empty state | mitigate | `listMine()` → `[]` → honest "Próximamente" EmptyState; NO fabricated acuerdo/cuota rows on a real-tenant path (PITFALLS 5). |
| T-v7-07-13 | Compliance (Ley 2300 / 1266, PITFALLS 4/5) | list copy | mitigate | Neutral tone; no "por qué la mora", no centrales-de-riesgo copy (grep-gated). |
| T-v7-07-SC | Tampering (supply chain) | npm installs | accept | N/A — zero new dependencies this phase. |
</threat_model>

<verification>
- `pnpm -C /Users/nicolasgarcia/rent/mvp-portal-inquilino build` succeeds (new `/inquilino/acuerdos` route registers).
- `pnpm test` — 0 NEW failures (~7 pre-existing unrelated).
- No-saldo gate: CuotaPlanTable + list (comment-stripped) have 0 `.reduce(` / `restante` / `saldo`.
- A5 gate: acuerdos list (comment-stripped) has 0 `aprobar acuerdo` / `editar término` / `por qué` / `central`.
- Manual smoke as `tenant`: `/inquilino/acuerdos` shows an honest empty/"Próximamente" state today (listMine → []);
  the "Acuerdos" nav item + the caso-hub card both route here.
</verification>

<success_criteria>
- ACUE-01: the tenant sees a real `/inquilino/acuerdos` surface listing their own approved acuerdos with the
  cuota plan rendered verbatim (no saldo math), honest-degrading to "Próximamente" when empty; the nav + caso
  hub route to it; no approve/terms affordance.
</success_criteria>

<output>
After completion, create `.planning/phases/v7-07-acuerdos-pago/v7-07-04-SUMMARY.md`.
</output>
