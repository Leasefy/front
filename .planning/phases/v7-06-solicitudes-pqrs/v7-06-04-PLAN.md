---
phase: v7-06-solicitudes-pqrs
plan: 04
type: execute
wave: 3
depends_on: [v7-06-02]
files_modified:
  - src/components/tenant/CostoResponsabilidadCard.tsx
  - src/app/inquilino/casos/[caseId]/page.tsx
autonomous: true
requirements: [SOLI-03, SOLI-04]

must_haves:
  truths:
    - "The unified caso detail shows a 'Respuesta esperada' SLA for PQRS/mantenimiento cases — authoritative slaVenceAt when present, else the createdAt+15-business-days estimate labeled 'estimado' — never blank, neutral tone, alongside the existing source-timestamp timeline"
    - "For a PQRS case the detail renders a Ley 820 cost-responsibility card with the BACKEND-sourced costoResponsable (dueño / inquilino / compartido) as a factual label — the frontend renders it, never decides it"
    - "When estado === 'en_cotizacion' AND cost is the tenant's, an 'Aprobar cotización' affordance appears (contract-first via pqrsApi.approveCotizacion); if the endpoint is not live it degrades to an honest 'Próximamente' / disabled — never a fake 'aprobado', and the tenant never assigns providers or sets the cost"
    - "SLA + cost sections render ONLY for cases carrying solicitud metadata (pqrs/mantenimiento); pago/aplicacion cases are byte-identical to before (no regression), and own-cases-only resolution is untouched"
  artifacts:
    - path: "src/components/tenant/CostoResponsabilidadCard.tsx"
      provides: "Ley 820 transparent cost label (dueno/inquilino/compartido) + contract-first 'Aprobar cotización' affordance (approve-only, honest 'Próximamente' on unavailable)"
      contains: "costoResponsable"
    - path: "src/app/inquilino/casos/[caseId]/page.tsx"
      provides: "SLA 'Respuesta esperada' row (resolveExpectedResponse, 'estimado', never blank) + CostoResponsabilidadCard, both gated on caso.solicitud"
      contains: "resolveExpectedResponse"
  key_links:
    - from: "src/app/inquilino/casos/[caseId]/page.tsx"
      to: "resolveExpectedResponse (business-days) via caso.solicitud"
      via: "SLA 'Respuesta esperada' — authoritative ?? estimate, never blank"
      pattern: "resolveExpectedResponse"
    - from: "src/components/tenant/CostoResponsabilidadCard.tsx"
      to: "pqrsApi.approveCotizacion"
      via: "approve-only affordance; PqrsUnavailableError → honest 'Próximamente'"
      pattern: "approveCotizacion"
---

<objective>
Complete the phase on the UNIFIED caso detail (SOLI-03 timeline/SLA + SOLI-04 cost transparency): extend
`/inquilino/casos/[caseId]` — the single detail surface (NO parallel `/solicitudes/[id]`) — with the SLA row
and the Ley 820 cost-responsibility card + quote-approval affordance, both driven by the `caso.solicitud`
metadata that v7-06-02 attaches. Additive and gated: pago/aplicación cases are unchanged.

Two honest deliverables:
1. **SLA "Respuesta esperada" (SOLI-03).** For cases carrying `caso.solicitud`, add a neutral "Respuesta
   esperada" field computed with `resolveExpectedResponse(caso.solicitud.createdAt, caso.solicitud.slaVenceAt)`:
   authoritative → es "Respuesta a más tardar el {es-CO date}"; estimate → es "Respuesta estimada hacia el
   {date}" + a small `· estimado` chip + tooltip "Fecha estimada; tu inmobiliaria confirma la definitiva."
   NEVER blank, NEVER "vence el {estimate}", NEVER a red countdown (neutral styling only). Rendered next to the
   existing timeline (which stays source-timestamp-only).
2. **Ley 820 cost card + quote approval (SOLI-04).** New `CostoResponsabilidadCard` renders the BACKEND-sourced
   `caso.solicitud.costoResponsable` (dueño / inquilino / compartido) with factual es-CO copy — the frontend
   RENDERS it, never decides it. When `caso.solicitud.estado === 'en_cotizacion'` AND `costoResponsable ===
   'inquilino'`, show the quote amount (`cotizacionMonto`, server-provided, formatted COP) + an "Aprobar
   cotización" button that calls `pqrsApi.approveCotizacion(caso.id)`. On `PqrsUnavailableError` → honest
   "Próximamente"/disabled (never a fake "aprobado"); if `cotizacionAprobadaAt` is already set, show an
   "Aprobada el {date}" read-only state. The tenant APPROVES only — no provider assignment, no cost input, no
   self-close. Wire the card into the PQRS branch of the detail.

Depends on v7-06-02 (`TenantCase.solicitud` metadata + `pqrsToCase`); consumes v7-06-01's `resolveExpectedResponse`
+ `pqrsApi.approveCotizacion`. Wave 3 (parallel with v7-06-03 — no shared files).

Purpose: SOLI-03 (visible computed SLA on the detail) + SOLI-04 (Ley 820 cost transparency + approve-before-execute).
Output: SLA row + cost/quote card on the unified caso detail.

**Guardrails:** SLA never blank + "estimado" label + neutral tone (no red countdown, no "vence el" on an
estimate); `costoResponsable`/`cotizacionMonto` are backend-sourced (frontend renders, never decides);
approve-only (no provider assignment / no cost input / no self-close); honest "Próximamente" on unavailable
(no fake "aprobado"); own-cases-only resolution untouched (no IDOR); es-CO; buttons sentence case (DESIGN §4);
zero new npm packages.
</objective>

<execution_context>
@/Users/nicolasgarcia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolasgarcia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/v7-06-solicitudes-pqrs/v7-06-RESEARCH.md
@.planning/phases/v7-06-solicitudes-pqrs/v7-06-PATTERNS.md

# READ FIRST: docs/DESIGN.md §4 (Buttons — SENTENCE CASE), §11 (EmptyState / honest "Próximamente"),
# §7 (A11y — pair icon+text). CLAUDE.md: additive, es-CO, PR workflow, zero new npm packages.

# The detail surface to EXTEND (own-only resolution + PlanActivityTimeline already in place):
@src/app/inquilino/casos/[caseId]/page.tsx
# Honest info-card idiom (primary-soft banner) to model the cost card on:
@src/app/panel/inmobiliaria/pqrs/page.tsx

<interfaces>
<!-- v7-06-01 / v7-06-02 outputs consumed here -->
resolveExpectedResponse(createdAtIso, slaVenceAtIso?): { date: Date; estimated: boolean }   // '@/lib/date/business-days'
pqrsApi.approveCotizacion(id: string): Promise<SolicitudPqrs>   // throws PqrsUnavailableError on 404/403/0
class PqrsUnavailableError extends Error
type CostoResponsable = 'dueno'|'inquilino'|'compartido'   // '@/lib/api/pqrs.types'

<!-- TenantCase.solicitud metadata (v7-06-02) — present ONLY for pqrs/mantenimiento cases -->
caso.solicitud?: { estado: PqrsEstado; createdAt: string; slaVenceAt?: string; costoResponsable?: CostoResponsable;
  cotizacionMonto?: number; cotizacionId?: string; cotizacionAprobadaAt?: string }

<!-- existing detail helpers to reuse: CaseDetail, formatRelative, useI18n().formatCurrency, Button, EmptyState -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SLA "Respuesta esperada" row on the caso detail (SOLI-03)</name>
  <files>src/app/inquilino/casos/[caseId]/page.tsx</files>
  <action>
    In `CaseDetail`, when `caso.solicitud` is present, add a "Respuesta esperada" field to the summary card's
    `dl` grid (next to Responsable / Actualizado), using a `Clock`-style neutral icon. Compute
    `const sla = resolveExpectedResponse(caso.solicitud.createdAt, caso.solicitud.slaVenceAt);` and format the
    date with `new Intl.DateTimeFormat(locale === 'es' ? 'es-CO' : 'en-US', { day:'numeric', month:'long',
    year:'numeric' }).format(sla.date)`. Render:
      · `sla.estimated` → es "Respuesta estimada hacia el {date}" + a subtle `· estimado` chip and a
        `title`/tooltip "Fecha estimada; tu inmobiliaria confirma la definitiva." (en analog).
      · authoritative → es "Respuesta a más tardar el {date}".
    NEVER blank, NEVER "vence" next to an estimate, neutral styling only (reuse the existing muted `dt/dd`
    classes — no red/destructive/countdown). Import `resolveExpectedResponse` from `@/lib/date/business-days`.
    Do NOT alter the timeline, the own-only `.find()` resolution, or the pago/aplicación render path.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && P="src/app/inquilino/casos/[caseId]/page.tsx" && grep -q "resolveExpectedResponse" "$P" && grep -q "caso.solicitud" "$P" && grep -q "estimado" "$P" && grep -q "es-CO" "$P" && test $(grep -ciE "vence el|destructive|countdown|red-" "$P") -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>PQRS/mantenimiento detail shows a never-blank "Respuesta esperada" (authoritative vs "estimado", neutral); pago/aplicación unchanged; timeline + own-only resolution intact.</done>
</task>

<task type="auto">
  <name>Task 2: Ley 820 CostoResponsabilidadCard + quote approval, wired into the detail (SOLI-04)</name>
  <files>src/components/tenant/CostoResponsabilidadCard.tsx, src/app/inquilino/casos/[caseId]/page.tsx</files>
  <action>
    Create `CostoResponsabilidadCard.tsx` (`'use client'`). Props: `{ caseId: string; solicitud: NonNullable<
    TenantCase['solicitud']> }`. Model the container on the honest primary-soft info-card idiom
    (`pqrs/page.tsx:51-57`). Render:
      - A factual cost-responsibility label from `solicitud.costoResponsable` (render the card only when it is
        set — it is backend-sourced): map es-CO copy —
        `dueno` → "A cargo del propietario (reparación necesaria — Ley 820)."
        `inquilino` → "A cargo tuyo (reparación locativa por uso — Ley 820)."
        `compartido` → "Costo compartido. Tu inmobiliaria confirma la distribución."
        (en analogs). The frontend RENDERS this; it never computes/decides the responsibility.
      - Quote block: when `solicitud.estado === 'en_cotizacion'` AND `solicitud.costoResponsable === 'inquilino'`,
        show the amount `solicitud.cotizacionMonto` formatted COP (`useI18n().formatCurrency`) and an "Aprobar
        cotización" `Button` (sentence case). If `solicitud.cotizacionAprobadaAt` is set, instead show a
        read-only "Aprobada el {es-CO date}" state (no button).
      - `onApprove`: `try { await pqrsApi.approveCotizacion(caseId); toast.success(...); } catch (e) { if (e
        instanceof PqrsUnavailableError) toast.info(<honest "Próximamente" — "La aprobación de cotizaciones se
        habilitará pronto.">); else toast.error(...); }`. Use an `isApproving` state for `Button isLoading`.
        Never optimistically flip to "aprobado" — the state only reflects a server `cotizacionAprobadaAt`.
      - NO provider-assignment control, NO cost input, NO estado mutation / self-close. The tenant approves only.
    In `casos/[caseId]/page.tsx`: import + render `<CostoResponsabilidadCard caseId={caso.id} solicitud={caso.
    solicitud} />` in `CaseDetail`, gated on `caso.solicitud && caso.solicitud.costoResponsable` (so it appears
    only for PQRS cases that have a cost determination). Place it between the summary card and the timeline.
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && C=src/components/tenant/CostoResponsabilidadCard.tsx && P="src/app/inquilino/casos/[caseId]/page.tsx" && grep -q "costoResponsable" $C && grep -q "approveCotizacion" $C && grep -q "PqrsUnavailableError" $C && grep -qi "Ley 820" $C && grep -q "CostoResponsabilidadCard" "$P" && test $(grep -ciE "asignar proveedor|assign provider|setEstado|cerrar caso|input.*costo|costo.*input" $C) -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>Cost card shows the backend-sourced Ley 820 responsibility + an approve-only affordance (honest "Próximamente" on unavailable, no fake "aprobado"); no provider/cost/close controls; wired into the PQRS detail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caso.solicitud metadata → detail UI | Backend-sourced cost/SLA/estado render here; the frontend must not decide any of them |
| tenant "Aprobar" action → pqrsApi.approveCotizacion | The only tenant-initiated state change; server persists + authorizes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v7-06-10 | Elevation of Privilege | CostoResponsabilidadCard | mitigate | Tenant APPROVES only; no provider-assignment, no cost input, no estado mutation / self-close (grep-gated). `costoResponsable`/`cotizacionMonto` are backend-sourced. |
| T-v7-06-11 | Tampering | quote approval state | mitigate | `approveCotizacion` → `PqrsUnavailableError` → honest "Próximamente"; never optimistically flip to "aprobado" — the approved state reflects only a server `cotizacionAprobadaAt`. |
| T-v7-06-12 | Information Disclosure / Compliance | SLA + own-only detail | mitigate | Two-tier resolver never blank; estimate labeled "estimado" + soft "hacia el" (no false "late", PITFALLS 6); detail resolved own-only from `useTenantCases` (no fetch-by-id, no IDOR). Neutral tone (Ley 1480). |
| T-v7-06-SC | Tampering (supply chain) | npm installs | accept | N/A — zero new dependencies this phase. |
</threat_model>

<verification>
- `pnpm -C /Users/nicolasgarcia/rent/mvp-portal-inquilino build` succeeds.
- `pnpm test` — 0 NEW failures (~7 pre-existing unrelated).
- Grep gates (per task): detail uses `resolveExpectedResponse` + `caso.solicitud` + `es-CO`, no "vence el"/
  countdown; cost card uses `costoResponsable` + `approveCotizacion` + `PqrsUnavailableError` + "Ley 820", no
  provider/cost/self-close controls; card wired into the detail.
- Manual smoke as `tenant`: a `pago`/`aplicación` caso detail is unchanged (no SLA/cost sections); a PQRS caso
  (when real data lands) shows a never-blank "Respuesta esperada" ("estimado" until the engine sets slaVenceAt)
  and, for an `en_cotizacion` tenant-cost case, an "Aprobar cotización" affordance that degrades to an honest
  "Próximamente" today.
</verification>

<success_criteria>
- SOLI-03: the unified caso detail shows a computed, visible, never-blank SLA (authoritative vs "estimado")
  for PQRS/mantenimiento cases, alongside the source-timestamp timeline.
- SOLI-04: the detail shows the Ley 820 cost responsibility (backend-sourced) and, when the cost is the
  tenant's and the case is `en_cotizacion`, an approve-before-execute affordance — approve-only, honest on
  unavailable, never fabricating an "aprobado".
</success_criteria>

<output>
After completion, create `.planning/phases/v7-06-solicitudes-pqrs/v7-06-04-SUMMARY.md`.
</output>
