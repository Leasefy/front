---
phase: 32-integration-qa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/hooks/useAgencyPlan.ts
  - src/lib/constants/feature-gates.ts
  - src/app/panel/inmobiliaria/reportes/page.tsx
  - src/app/panel/inmobiliaria/operaciones/page.tsx
  - src/app/panel/inmobiliaria/page.tsx
autonomous: true

must_haves:
  truths:
    - "Starter plan: sees upgrade prompts on reports tab, reminders tab, executive tab"
    - "Growth plan: can access reports and reminders but NOT executive dashboard"
    - "Business plan: can access everything including executive"
    - "Flex plans: see AI agents on dashboard; subscription plans see upgrade prompt"
    - "Build compiles with no new errors"
  artifacts:
    - path: "src/lib/constants/feature-gates.ts"
      provides: "Complete feature gate mapping for all gated features"
    - path: "src/lib/hooks/useAgencyPlan.ts"
      provides: "Hook with hasFeature working for all defined features"
  key_links:
    - from: "reportes/page.tsx"
      to: "FeatureGate"
      via: "advanced-reports gates occupancy/cobros/agentes tabs"
    - from: "reportes/page.tsx"
      to: "FeatureGate"
      via: "executive-reports gates ejecutivo tab"
    - from: "operaciones/page.tsx"
      to: "FeatureGate"
      via: "automatic-reminders gates recordatorios tab"
    - from: "page.tsx (dashboard)"
      to: "FeatureGate"
      via: "ai-agents gates agent section"
---

<objective>
Verify all plan gating works end-to-end across all tiers and fix any issues.

Purpose: Ensure the entire gating system works correctly — different plan tiers see different features.
Output: Verified gating across all pages, any fixes applied.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/lib/constants/feature-gates.ts
@src/lib/hooks/useAgencyPlan.ts
@src/components/inmobiliaria/UpgradePrompt.tsx
@src/app/panel/inmobiliaria/page.tsx
@src/app/panel/inmobiliaria/reportes/page.tsx
@src/app/panel/inmobiliaria/operaciones/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify feature gates config covers all features</name>
  <files>src/lib/constants/feature-gates.ts, src/lib/hooks/useAgencyPlan.ts</files>
  <action>
    Read feature-gates.ts and verify ALL required features are mapped:

    Required mappings:
    - 'advanced-reports' → minTier: 'growth' (Growth+ on both sub and flex)
    - 'executive-reports' → minTier: 'agency-business' (Business+ on both)
    - 'automatic-reminders' → minTier: 'growth'
    - 'contract-reminders' → minTier: 'growth'
    - 'ai-agents' → minTier: 'starter', flexOnly: true (only Flex plans)
    - 'pdf-export' → minTier: 'growth'

    Verify useAgencyPlan.ts:
    - PLAN_TIER ordering: starter(0) < growth(1) < agency-business(2) < enterprise(3)
    - hasFeature checks minTier AND flexOnly flag
    - isFlexPlan reads from localStorage 'leasefy_agency_plan_type'
    - Default plan type is 'flex' (per decision flex-default)

    If any feature is missing or misconfigured, fix it.

    Then verify the UpgradePrompt.tsx:
    - FeatureGate component exists and works (checks hasFeature, renders children or UpgradePrompt)
    - UpgradePrompt shows correct messaging for the feature

    Run build.
  </action>
  <verify>npx next build --no-lint | grep "Compiled"</verify>
  <done>All features mapped, tier ordering correct, hasFeature logic verified, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Verify gating is wired on all gated pages</name>
  <files>src/app/panel/inmobiliaria/reportes/page.tsx, src/app/panel/inmobiliaria/operaciones/page.tsx, src/app/panel/inmobiliaria/page.tsx</files>
  <action>
    Read each page and verify FeatureGate is correctly applied:

    1. Dashboard (page.tsx):
       - AgentSection wrapped with `<FeatureGate feature="ai-agents">`
       - This means: Flex users see agents, Subscription users see upgrade prompt

    2. Reportes (reportes/page.tsx):
       - Advanced reports tabs (ocupacion, cobros, agentes) wrapped with `<FeatureGate feature="advanced-reports">`
       - Ejecutivo tab wrapped with `<FeatureGate feature="executive-reports">`
       - This means: Growth can see 3 tabs but not ejecutivo. Business sees all 4.

    3. Operaciones (operaciones/page.tsx):
       - Recordatorios tab wrapped with `<FeatureGate feature="automatic-reminders">`
       - This means: Growth+ can configure reminders, Starter cannot.

    Verify:
    - No double-wrapping (FeatureGate inside FeatureGate for same feature)
    - No missing imports
    - Tab navigation still works when gated (user can click tab, sees upgrade prompt, not broken)

    Fix any issues found. Run build.
  </action>
  <verify>npx next build --no-lint | grep "Compiled"</verify>
  <done>All gated pages verified, gating correctly applied, no double-wrapping, build passes</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx next build --no-lint` compiles successfully
- [ ] feature-gates.ts has all 6+ features mapped
- [ ] All 3 pages have correct FeatureGate wrapping
- [ ] No circular imports or missing dependencies
</verification>

<success_criteria>

- Every gated feature has correct FeatureGate wrapping
- Tier logic is correct (starter < growth < business < enterprise)
- flexOnly flag works for AI agents
- Build passes with no new errors
</success_criteria>

<output>
After completion, create `.planning/phases/32-integration-qa/32-01-SUMMARY.md`
</output>
