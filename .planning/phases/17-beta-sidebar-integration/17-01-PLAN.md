---
phase: 17-beta-sidebar-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/panel/(landlord)/layout.tsx
  - src/app/panel/inmobiliaria/layout.tsx
  - src/app/panel/beta/page.tsx
  - src/app/panel/inmobiliaria/beta/page.tsx
  - src/components/beta/AppSwitcher.tsx
autonomous: true
must_haves:
  truths:
    - "Sidebar de propietarios muestra item 'AI Beta' con icono sparkle"
    - "Sidebar de inmobiliarias muestra item 'AI Beta' con icono sparkle"
    - "Ruta /panel/beta carga una pagina sin errores"
    - "Ruta /panel/inmobiliaria/beta carga una pagina sin errores"
    - "Todas las paginas existentes del dashboard siguen funcionando"
    - "App switcher visible en sidebar permite toggle Dashboard <-> AI Beta"
  artifacts:
    - path: "src/app/panel/beta/page.tsx"
      provides: "Beta entry page for propietarios"
      min_lines: 10
    - path: "src/app/panel/inmobiliaria/beta/page.tsx"
      provides: "Beta entry page for inmobiliarias"
      min_lines: 10
    - path: "src/components/beta/AppSwitcher.tsx"
      provides: "Dashboard <-> AI Beta workspace switcher"
      min_lines: 30
  key_links:
    - from: "src/app/panel/(landlord)/layout.tsx"
      to: "/panel/beta"
      via: "Nav item in LANDLORD_NAV_ITEMS array"
      pattern: "beta"
    - from: "src/app/panel/inmobiliaria/layout.tsx"
      to: "/panel/inmobiliaria/beta"
      via: "Nav item in INMOBILIARIA_NAV_ITEMS array"
      pattern: "beta"
---

<objective>
Add "AI Beta" navigation to both dashboards and create the route structure for the Beta section.

Purpose: Establish the entry points (sidebar nav items, routes, app switcher) that connect existing dashboards to the new AI Beta universe. This is the "bridge" — users click to enter Beta.
Output: Nav items in both sidebars, working routes at /panel/beta and /panel/inmobiliaria/beta, and an AppSwitcher component.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-beta-sidebar-integration/17-CONTEXT.md

# Existing layouts to modify:
@src/app/panel/(landlord)/layout.tsx
@src/app/panel/inmobiliaria/layout.tsx

# Shared sidebar component (read to understand NavItem interface and patterns):
@src/components/ui/plan/PlanSidebar.tsx

# Design tokens for consistent styling:
@src/lib/design-tokens.ts

# i18n patterns (inmobiliaria uses translations):
@src/lib/i18n/translations.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "AI Beta" nav item to both dashboard sidebars + create route placeholder pages</name>
  <files>src/app/panel/(landlord)/layout.tsx, src/app/panel/inmobiliaria/layout.tsx, src/app/panel/beta/page.tsx, src/app/panel/inmobiliaria/beta/page.tsx</files>
  <action>
    **1. Add nav item to propietarios sidebar** (`src/app/panel/(landlord)/layout.tsx`):
    - Add a new NavItem to `LANDLORD_NAV_ITEMS` array
    - Use `Sparkle` icon from `@phosphor-icons/react` (matches AI/sparkle requirement)
    - Label: `'AI Beta'`
    - href: `'/panel/beta'`
    - Add visual distinction: use the NavItem `badge` property with text like `'Beta'` or add a special style
    - Position: Add as the LAST item before the settings/bottom section, visually separated (add a divider comment or separator in the nav items)

    **2. Add nav item to inmobiliarias sidebar** (`src/app/panel/inmobiliaria/layout.tsx`):
    - Same pattern: add NavItem with `Sparkle` icon
    - Label: Use i18n key if translations exist, fallback to `'AI Beta'`
    - href: `'/panel/inmobiliaria/beta'`
    - Same badge/distinction as propietarios

    **3. Create route placeholder pages**:
    - `src/app/panel/beta/page.tsx`: Simple page component that renders a centered message "AI Beta - Cargando..." or similar placeholder. This page will be replaced by the full layout in Plan 02. Mark as `'use client'` if needed. Export default.
    - `src/app/panel/inmobiliaria/beta/page.tsx`: Same pattern for inmobiliarias route.

    **Important**: Do NOT modify PlanSidebar.tsx — the nav items are defined in the layout files and passed as props. The sidebar component is generic and already handles any NavItem array.

    **Important**: The badge on the nav item should indicate "Beta" status. Check how the existing `badge` property works in PlanSidebar — it renders a number badge. If it only supports numbers, add the "Beta" text as part of the label (e.g., `'AI Beta'`) or extend the badge to support strings. Prefer the simplest approach that communicates experimental status.
  </action>
  <verify>
    - `npm run build` succeeds
    - Navigate to /panel — sidebar shows "AI Beta" item with sparkle icon
    - Navigate to /panel/inmobiliaria — sidebar shows "AI Beta" item
    - Click "AI Beta" in propietarios → navigates to /panel/beta without errors
    - Click "AI Beta" in inmobiliarias → navigates to /panel/inmobiliaria/beta without errors
    - All existing dashboard pages still work (spot-check /panel, /panel/propiedades, /panel/inmobiliaria, /panel/inmobiliaria/cobros)
  </verify>
  <done>
    - Both sidebars show "AI Beta" nav item with Sparkle icon
    - /panel/beta and /panel/inmobiliaria/beta routes load without errors
    - Existing pages unaffected (BETA-06)
    - Requirements BETA-01, BETA-02, BETA-03 partially satisfied (routes exist, nav items visible)
  </done>
</task>

<task type="auto">
  <name>Task 2: Build AppSwitcher component for Dashboard ↔ AI Beta workspace toggle</name>
  <files>src/components/beta/AppSwitcher.tsx</files>
  <action>
    Create `src/components/beta/AppSwitcher.tsx` — a workspace switcher component inspired by Slack's workspace selector.

    **Component design**:
    - Small, clean dropdown at the top of the sidebar area
    - Shows current workspace: "Dashboard" or "AI Beta" with appropriate icon
    - Click toggles between the two workspaces (navigates to the other)
    - Use `useRouter` from `next/navigation` for navigation
    - Accept props: `currentWorkspace: 'dashboard' | 'beta'` and `basePath: string` (to handle `/panel` vs `/panel/inmobiliaria` prefix)

    **Visual design**:
    - Follow existing design tokens (bg-card, text-foreground, etc.)
    - Use Phosphor icons: `SquaresFour` for Dashboard, `Sparkle` for AI Beta
    - Compact: fits in a small area, similar height to a nav item
    - Subtle hover effect matching existing sidebar hover styles
    - Show a small "Beta" badge/pill next to the AI Beta option using `bg-indigo-500 text-white text-[11px] rounded-full px-1.5`

    **Behavior**:
    - Dashboard mode: clicking switches to `/panel/beta` (or `/panel/inmobiliaria/beta`)
    - Beta mode: clicking switches to `/panel` (or `/panel/inmobiliaria`)
    - Use `usePathname()` to detect current state if `currentWorkspace` not explicitly passed

    **Integration note**: This component will be integrated into the Beta layout in Plan 02. For now, just create the component file — it doesn't need to be mounted anywhere yet.

    **Do NOT** create a complex dropdown with animations. Keep it simple: a clickable element that shows current workspace and navigates on click. Can be enhanced later.
  </action>
  <verify>
    - `npm run build` succeeds (component compiles without TypeScript errors)
    - Component file exists at `src/components/beta/AppSwitcher.tsx`
    - Exports named `AppSwitcher` component
  </verify>
  <done>
    - AppSwitcher component created with workspace toggle logic
    - Handles both propietarios and inmobiliarias base paths
    - Ready for integration in Plan 02's Beta layout
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Both sidebars show "AI Beta" nav item with Sparkle icon
- [ ] /panel/beta loads without errors
- [ ] /panel/inmobiliaria/beta loads without errors
- [ ] Existing dashboard pages unaffected (spot-check 4+ pages)
- [ ] AppSwitcher component exists and compiles
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- BETA-01 satisfied (propietarios sidebar shows Beta)
- BETA-02 satisfied (inmobiliarias sidebar shows Beta)
- BETA-03 partially satisfied (routes exist, layout comes in Plan 02)
- BETA-06 satisfied (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/17-beta-sidebar-integration/17-01-SUMMARY.md`
</output>
