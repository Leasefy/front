---
phase: inmobiliaria-05-pipeline
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - src/components/inmobiliaria/PipelineBoard.tsx
  - src/components/inmobiliaria/index.ts
  - src/app/panel/inmobiliaria/pipeline/page.tsx
autonomous: true

must_haves:
  truths:
    - "User can see full Kanban board with all pipeline stages"
    - "User can drag cards between columns to change stage"
    - "User can filter the board by agente, property, or date"
    - "User can click card to see full details"
    - "Board updates immediately after drag-and-drop"
  artifacts:
    - path: "src/components/inmobiliaria/PipelineBoard.tsx"
      provides: "Full Kanban board with drag-and-drop"
      min_lines: 250
    - path: "src/app/panel/inmobiliaria/pipeline/page.tsx"
      provides: "Pipeline Kanban page"
      exports: ["default"]
  key_links:
    - from: "PipelineBoard"
      to: "@dnd-kit/core"
      via: "npm package"
      pattern: "DndContext|useDraggable|useDroppable"
    - from: "pipeline/page.tsx"
      to: "MOCK_PIPELINE_ITEMS"
      via: "import from mock-inmobiliaria"
      pattern: "MOCK_PIPELINE_ITEMS"
---

<objective>
Create the PipelineBoard with drag-and-drop and the pipeline page for the Inmobiliaria module.

Purpose: Provide a complete Kanban board for managing the rental pipeline with drag-and-drop stage transitions.
Output: PipelineBoard component and /panel/inmobiliaria/pipeline page with full Kanban functionality.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@docs/ROADMAP-INMOBILIARIA.md

# Prior plan context
@.planning/phases/inmobiliaria-05-pipeline/05-01-PLAN.md
@.planning/phases/inmobiliaria-05-pipeline/05-02-PLAN.md

# Source files
@src/lib/types/inmobiliaria.ts
@src/lib/data/mock-inmobiliaria.ts
@src/components/inmobiliaria/index.ts
@src/components/inmobiliaria/PipelineCard.tsx
@src/components/inmobiliaria/PipelineColumn.tsx
@src/components/inmobiliaria/PipelineFilters.tsx
@src/components/inmobiliaria/PipelineDetail.tsx

# Existing page patterns
@src/app/panel/inmobiliaria/agentes/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and Configure @dnd-kit</name>
  <files>package.json</files>
  <action>
Check if @dnd-kit/core is already installed. If not, install it:

```bash
pnpm add @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities
```

@dnd-kit is the recommended drag-and-drop library for React:
- @dnd-kit/core: Core drag-and-drop functionality
- @dnd-kit/sortable: For sortable lists (optional but useful)
- @dnd-kit/utilities: CSS utilities for transforms

Verify installation with pnpm tsc --noEmit.
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>@dnd-kit packages installed and verified</done>
</task>

<task type="auto">
  <name>Task 2: Create PipelineBoard Component</name>
  <files>src/components/inmobiliaria/PipelineBoard.tsx</files>
  <action>
Create PipelineBoard component with full Kanban functionality:

1. Board Layout:
   - Horizontal scroll container for columns
   - All 10 pipeline stages as columns (exclude 'lost' from main view or show at end)
   - Fixed column width (260px), flexible height
   - Smooth horizontal scroll with mouse wheel

2. Drag-and-Drop Setup:
   - Use DndContext from @dnd-kit/core
   - Make PipelineCard draggable with useDraggable
   - Make PipelineColumn droppable with useDroppable
   - DragOverlay for smooth drag preview

3. Column Implementation:
   - Use PipelineColumn component for each stage
   - Pass filtered items to each column
   - Show drop indicator when dragging over
   - Animate card movement

4. State Management:
   - Local state for items (copy of prop data)
   - onDragEnd handler to update stage
   - Optimistic update on drop
   - Show toast on successful move

5. Stage Transition Rules (optional enhancement):
   - Optionally validate allowed transitions
   - For MVP: allow any stage transition
   - Show toast with old → new stage

6. Integration Props:
```typescript
interface PipelineBoardProps {
  items: PipelineItem[];
  onItemClick: (item: PipelineItem) => void;
  onStageChange: (itemId: string, newStage: PipelineStage) => void;
}
```

Use motion from framer-motion for smooth animations.
Follow accessibility best practices with ARIA labels.
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>PipelineBoard renders with drag-and-drop between columns</done>
</task>

<task type="auto">
  <name>Task 3: Create Pipeline Page</name>
  <files>src/app/panel/inmobiliaria/pipeline/page.tsx, src/components/inmobiliaria/index.ts</files>
  <action>
1. Create pipeline/page.tsx:

   Header Section:
   - Title: "Pipeline de Arriendos"
   - Breadcrumb: Inmobiliaria > Pipeline
   - Stats row: Total leads, En proceso, Cerrados este mes, Tasa de conversión

   Filter Section:
   - PipelineFilters component
   - Apply filters to items before passing to board

   Board Section:
   - PipelineBoard component with filtered items
   - Full width, horizontal scroll if needed

   Detail Section:
   - PipelineDetail modal (controlled by selectedItem state)
   - Opens when card is clicked

   State Management:
   - items: filtered MOCK_PIPELINE_ITEMS
   - selectedItem: for detail modal
   - filters: for PipelineFilters

   Handlers:
   - handleCardClick: open detail modal
   - handleStageChange: update item stage (toast success)
   - handleFilterChange: update filter state

2. Update barrel export with PipelineBoard:
```typescript
export { PipelineBoard } from './PipelineBoard';
```

3. Add sidebar link (if not present):
   - Check if /panel/inmobiliaria/pipeline is in navigation
   - Add if missing with icon Kanban or ListChecks

Use MOCK_PIPELINE_ITEMS, MOCK_AGENTES, MOCK_CONSIGNACIONES from mock-inmobiliaria.
Follow agentes/page.tsx pattern for layout and animations.
  </action>
  <verify>pnpm build</verify>
  <done>Pipeline page loads with Kanban board, filters, and detail modal</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm tsc --noEmit passes
- [ ] pnpm build succeeds
- [ ] Navigate to /panel/inmobiliaria/pipeline shows Kanban
- [ ] All 10 stages display as columns
- [ ] Drag card from one column to another works
- [ ] Drop updates card stage
- [ ] Toast shows on stage change
- [ ] Click card opens detail modal
- [ ] Filters reduce visible items
</verification>

<success_criteria>
- All tasks completed
- @dnd-kit installed and working
- Kanban board shows all stages
- Drag-and-drop transitions stages
- Filters apply correctly
- Detail modal opens on card click
- Stage changes show feedback
</success_criteria>

<output>
After completion, create `.planning/phases/inmobiliaria-05-pipeline/05-03-SUMMARY.md`
</output>
