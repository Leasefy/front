---
phase: inmobiliaria-10-configuracion-docs-analytics
plan: 03
type: execute
wave: 1
depends_on: ["10-01"]
files_modified:
  - src/lib/types/inmobiliaria.ts
  - src/lib/data/mock-inmobiliaria.ts
  - src/components/inmobiliaria/ConfigIntegraciones.tsx
  - src/components/inmobiliaria/ConfigFacturacion.tsx
  - src/components/inmobiliaria/index.ts
autonomous: true

must_haves:
  truths:
    - "User can view available integrations (PSE, payment gateways, accounting)"
    - "User can toggle integrations on/off"
    - "User can view billing plan and usage"
    - "User can see invoice history"
    - "User can update payment method (UI only)"
  artifacts:
    - path: "src/components/inmobiliaria/ConfigIntegraciones.tsx"
      provides: "Integration cards with toggle switches and status"
      min_lines: 250
    - path: "src/components/inmobiliaria/ConfigFacturacion.tsx"
      provides: "Billing overview with plan, usage, and invoices"
      min_lines: 300
  key_links:
    - from: "ConfigIntegraciones"
      to: "AgencyIntegration"
      via: "import from types/inmobiliaria"
      pattern: "AgencyIntegration"
    - from: "ConfigFacturacion"
      to: "AgencyBilling"
      via: "import from types/inmobiliaria"
      pattern: "AgencyBilling"
---

<objective>
Create integrations and billing configuration components.

Purpose: Allow agency to manage third-party integrations and view billing information.
Output: ConfigIntegraciones and ConfigFacturacion components for settings page.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@docs/ROADMAP-INMOBILIARIA.md

# Source files
@src/lib/types/inmobiliaria.ts
@src/lib/data/mock-inmobiliaria.ts

# Existing patterns
@src/components/pricing/PricingCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Integration & Billing Types</name>
  <files>src/lib/types/inmobiliaria.ts</files>
  <action>
Add types for integrations and billing:

```typescript
// ============================================================================
// Configuracion - Integrations
// ============================================================================

export type IntegrationCategory = 'payments' | 'accounting' | 'communications' | 'storage';

export type IntegrationStatus = 'active' | 'inactive' | 'pending' | 'error';

export interface AgencyIntegration {
  id: string;
  name: string;
  description: string;
  category: IntegrationCategory;
  icon: string;          // Phosphor icon name
  status: IntegrationStatus;
  isEnabled: boolean;
  configUrl?: string;
  apiKeyConfigured?: boolean;
  lastSyncAt?: string;
  errorMessage?: string;
}

// ============================================================================
// Configuracion - Billing
// ============================================================================

export type BillingPlan = 'starter' | 'professional' | 'enterprise';

export type BillingCycle = 'monthly' | 'annual';

export interface PlanLimits {
  maxProperties: number;
  maxUsers: number;
  maxAgents: number;
  includesReports: boolean;
  includesAnalytics: boolean;
  includesIntegrations: boolean;
  includesApi: boolean;
  supportLevel: 'email' | 'priority' | 'dedicated';
}

export interface AgencyBilling {
  plan: BillingPlan;
  cycle: BillingCycle;
  pricePerMonth: number;
  nextBillingDate: string;
  paymentMethod?: {
    type: 'card' | 'pse' | 'transfer';
    last4?: string;
    brand?: string;
    bankName?: string;
  };
  usage: {
    properties: number;
    users: number;
    agents: number;
  };
  limits: PlanLimits;
}

export interface BillingInvoice {
  id: string;
  date: string;
  amount: number;
  status: 'paid' | 'pending' | 'failed';
  pdfUrl?: string;
}

// Helper functions
export function getPlanLabel(plan: BillingPlan): string {
  const labels: Record<BillingPlan, string> = {
    starter: 'Starter',
    professional: 'Profesional',
    enterprise: 'Enterprise',
  };
  return labels[plan];
}

export function getPlanColor(plan: BillingPlan): string {
  const colors: Record<BillingPlan, string> = {
    starter: 'bg-slate-100 text-slate-700',
    professional: 'bg-indigo-100 text-indigo-700',
    enterprise: 'bg-purple-100 text-purple-700',
  };
  return colors[plan];
}

export function getIntegrationCategoryLabel(category: IntegrationCategory): string {
  const labels: Record<IntegrationCategory, string> = {
    payments: 'Pagos',
    accounting: 'Contabilidad',
    communications: 'Comunicaciones',
    storage: 'Almacenamiento',
  };
  return labels[category];
}

export function getIntegrationStatusColor(status: IntegrationStatus): string {
  const colors: Record<IntegrationStatus, string> = {
    active: 'bg-emerald-100 text-emerald-700',
    inactive: 'bg-slate-100 text-slate-700',
    pending: 'bg-amber-100 text-amber-700',
    error: 'bg-red-100 text-red-700',
  };
  return colors[status];
}

// Plan limits configuration
export const PLAN_LIMITS: Record<BillingPlan, PlanLimits> = {
  starter: {
    maxProperties: 20,
    maxUsers: 3,
    maxAgents: 2,
    includesReports: true,
    includesAnalytics: false,
    includesIntegrations: false,
    includesApi: false,
    supportLevel: 'email',
  },
  professional: {
    maxProperties: 100,
    maxUsers: 10,
    maxAgents: 5,
    includesReports: true,
    includesAnalytics: true,
    includesIntegrations: true,
    includesApi: false,
    supportLevel: 'priority',
  },
  enterprise: {
    maxProperties: -1, // unlimited
    maxUsers: -1,
    maxAgents: -1,
    includesReports: true,
    includesAnalytics: true,
    includesIntegrations: true,
    includesApi: true,
    supportLevel: 'dedicated',
  },
};
```
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>Integration and billing types added</done>
</task>

<task type="auto">
  <name>Task 2: Add Mock Integrations & Billing Data</name>
  <files>src/lib/data/mock-inmobiliaria.ts</files>
  <action>
Add mock data for integrations and billing:

```typescript
import type { AgencyIntegration, AgencyBilling, BillingInvoice } from '@/lib/types/inmobiliaria';

export const MOCK_INTEGRATIONS: AgencyIntegration[] = [
  {
    id: 'int-pse',
    name: 'PSE',
    description: 'Pagos en linea via debito bancario',
    category: 'payments',
    icon: 'Bank',
    status: 'active',
    isEnabled: true,
    lastSyncAt: new Date().toISOString(),
  },
  {
    id: 'int-wompi',
    name: 'Wompi',
    description: 'Pagos con tarjeta de credito y debito',
    category: 'payments',
    icon: 'CreditCard',
    status: 'active',
    isEnabled: true,
    apiKeyConfigured: true,
    lastSyncAt: new Date(Date.now() - 60 * 60 * 1000).toISOString(),
  },
  {
    id: 'int-nequi',
    name: 'Nequi',
    description: 'Pagos via billetera digital Nequi',
    category: 'payments',
    icon: 'Wallet',
    status: 'inactive',
    isEnabled: false,
  },
  {
    id: 'int-siigo',
    name: 'Siigo',
    description: 'Integracion contable con Siigo',
    category: 'accounting',
    icon: 'Calculator',
    status: 'pending',
    isEnabled: true,
    apiKeyConfigured: false,
  },
  {
    id: 'int-alegra',
    name: 'Alegra',
    description: 'Facturacion electronica con Alegra',
    category: 'accounting',
    icon: 'Receipt',
    status: 'inactive',
    isEnabled: false,
  },
  {
    id: 'int-whatsapp',
    name: 'WhatsApp Business',
    description: 'Notificaciones via WhatsApp',
    category: 'communications',
    icon: 'WhatsappLogo',
    status: 'active',
    isEnabled: true,
    lastSyncAt: new Date().toISOString(),
  },
  {
    id: 'int-email',
    name: 'SendGrid',
    description: 'Envio de emails transaccionales',
    category: 'communications',
    icon: 'EnvelopeSimple',
    status: 'error',
    isEnabled: true,
    errorMessage: 'API key expirada',
  },
  {
    id: 'int-s3',
    name: 'AWS S3',
    description: 'Almacenamiento de documentos',
    category: 'storage',
    icon: 'Cloud',
    status: 'active',
    isEnabled: true,
    lastSyncAt: new Date(Date.now() - 30 * 60 * 1000).toISOString(),
  },
];

export const MOCK_BILLING: AgencyBilling = {
  plan: 'professional',
  cycle: 'monthly',
  pricePerMonth: 299000,
  nextBillingDate: new Date(Date.now() + 15 * 24 * 60 * 60 * 1000).toISOString().split('T')[0],
  paymentMethod: {
    type: 'card',
    last4: '4242',
    brand: 'Visa',
  },
  usage: {
    properties: 45,
    users: 5,
    agents: 3,
  },
  limits: {
    maxProperties: 100,
    maxUsers: 10,
    maxAgents: 5,
    includesReports: true,
    includesAnalytics: true,
    includesIntegrations: true,
    includesApi: false,
    supportLevel: 'priority',
  },
};

export const MOCK_INVOICES: BillingInvoice[] = [
  {
    id: 'inv-001',
    date: '2026-02-01',
    amount: 299000,
    status: 'paid',
    pdfUrl: '#',
  },
  {
    id: 'inv-002',
    date: '2026-01-01',
    amount: 299000,
    status: 'paid',
    pdfUrl: '#',
  },
  {
    id: 'inv-003',
    date: '2025-12-01',
    amount: 299000,
    status: 'paid',
    pdfUrl: '#',
  },
  {
    id: 'inv-004',
    date: '2025-11-01',
    amount: 199000,
    status: 'paid',
    pdfUrl: '#',
  },
];
```
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>Mock integrations and billing data created</done>
</task>

<task type="auto">
  <name>Task 3: Create ConfigIntegraciones Component</name>
  <files>src/components/inmobiliaria/ConfigIntegraciones.tsx</files>
  <action>
Create ConfigIntegraciones component:

1. Header:
   - Title "Integraciones"
   - Description "Conecta servicios externos"
   - Active integrations count badge

2. Category Tabs:
   - Todos
   - Pagos (with icon)
   - Contabilidad (with icon)
   - Comunicaciones (with icon)
   - Almacenamiento (with icon)

3. Integration Cards Grid:
   - Icon + Name + Description
   - Status badge (active, inactive, pending, error)
   - Toggle switch to enable/disable
   - "Configurar" button (if enabled)
   - Last sync time (if active)
   - Error message (if error status)

4. Integration Detail Dialog:
   - Full description
   - Setup instructions
   - API key input (if applicable)
   - Test connection button
   - Save button

Props interface:
```typescript
interface ConfigIntegracionesProps {
  integrations: AgencyIntegration[];
  onToggle?: (integrationId: string, enabled: boolean) => void;
  onConfigure?: (integrationId: string, config: Record<string, string>) => void;
  isLoading?: boolean;
}
```

Use Switch from shadcn.
Use Card from shadcn.
Show toast on toggle/save.
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>ConfigIntegraciones component created</done>
</task>

<task type="auto">
  <name>Task 4: Create ConfigFacturacion Component</name>
  <files>src/components/inmobiliaria/ConfigFacturacion.tsx</files>
  <action>
Create ConfigFacturacion billing component:

1. Current Plan Card:
   - Plan name with badge (Professional)
   - Price per month
   - Billing cycle (monthly/annual)
   - Next billing date
   - "Cambiar Plan" button (opens upgrade modal)

2. Usage Meters:
   - Properties: X of Y (progress bar)
   - Users: X of Y (progress bar)
   - Agents: X of Y (progress bar)
   - Color: green if <70%, amber if <90%, red if >90%

3. Payment Method Card:
   - Card icon + last 4 digits + brand
   - Or bank name for PSE
   - "Actualizar" button (opens modal)

4. Plan Features List:
   - Checkmarks for included features
   - X marks for excluded features
   - Highlight what's not in current plan

5. Invoice History Table:
   - Date
   - Amount
   - Status badge (paid, pending, failed)
   - Download PDF link

6. Upgrade Modal (triggered by "Cambiar Plan"):
   - Show all 3 plans with pricing
   - Highlight current plan
   - Comparison table
   - Select button per plan

Props interface:
```typescript
interface ConfigFacturacionProps {
  billing: AgencyBilling;
  invoices: BillingInvoice[];
  onUpgrade?: (plan: BillingPlan) => void;
  onUpdatePaymentMethod?: () => void;
  isLoading?: boolean;
}
```

Use Progress from shadcn.
Use Dialog for modals.
Format currency as COP.
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>ConfigFacturacion component created</done>
</task>

<task type="auto">
  <name>Task 5: Update Barrel Export</name>
  <files>src/components/inmobiliaria/index.ts</files>
  <action>
Add exports for integration/billing components:

```typescript
// Configuracion - Integrations & Billing (Phase 10)
export { ConfigIntegraciones } from './ConfigIntegraciones';
export { ConfigFacturacion } from './ConfigFacturacion';
```
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>Integration/billing components exported</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm tsc --noEmit passes
- [ ] Integration and billing types defined
- [ ] Mock data exports work
- [ ] ConfigIntegraciones shows integration cards
- [ ] Toggle switches work
- [ ] ConfigFacturacion shows plan and usage
- [ ] Invoice table displays correctly
- [ ] Components exported from barrel
</verification>

<success_criteria>
- All tasks completed
- Integration cards display with status
- Toggle enables/disables integrations
- Billing shows current plan and usage
- Usage meters show progress bars
- Invoice history is viewable
</success_criteria>

<output>
After completion, create `.planning/phases/inmobiliaria-10-configuracion-docs-analytics/10-03-SUMMARY.md`
</output>
