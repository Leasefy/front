---
phase: 34-avaluos-ui
plan: 05
type: execute
wave: 3
depends_on: ["34-03", "34-04"]
files_modified:
  - src/app/panel/inmobiliaria/avaluos/page.tsx
  - src/app/panel/inmobiliaria/avaluos/nuevo/page.tsx
  - src/app/panel/inmobiliaria/avaluos/[id]/page.tsx
autonomous: true

must_haves:
  truths:
    - "An agency user can open /panel/inmobiliaria/avaluos and see a list of avalúos with status badges"
    - "An agency user can start a new avalúo at /panel/inmobiliaria/avaluos/nuevo using the SAME wizard, with their email pre-filled"
    - "An agency user can open an avalúo detail and, when status is 'entregado', download the certificate"
    - "The panel pages reuse AvaluoProvider/AvaluoWizardShell and the status hook — no duplicate wizard"
  artifacts:
    - path: "src/app/panel/inmobiliaria/avaluos/page.tsx"
      provides: "Agency avalúos list with status badges"
      contains: "STATUS_BADGE"
      min_lines: 40
    - path: "src/app/panel/inmobiliaria/avaluos/nuevo/page.tsx"
      provides: "Authenticated wizard (same components) with email pre-filled"
      contains: "AvaluoProvider"
    - path: "src/app/panel/inmobiliaria/avaluos/[id]/page.tsx"
      provides: "Avalúo detail + download when entregado"
      contains: "useAvaluoStatus"
  key_links:
    - from: "src/app/panel/inmobiliaria/avaluos/nuevo/page.tsx"
      to: "AvaluoProvider initialEmail"
      via: "useContext(AuthContext)?.user?.email passed to provider"
      pattern: "initialEmail"
    - from: "src/app/panel/inmobiliaria/avaluos/[id]/page.tsx"
      to: "useAvaluoStatus"
      via: "polling hook reuse"
      pattern: "useAvaluoStatus"
    - from: "src/app/panel/inmobiliaria/avaluos/page.tsx"
      to: "/panel/inmobiliaria/avaluos/nuevo"
      via: "Link to start new request"
      pattern: "avaluos/nuevo"
---

<objective>
Integrate avalúos into the inmobiliaria panel: a list page with status badges, a "nuevo"
page that reuses the exact same wizard (email pre-filled from the authenticated user), and a
detail page that reuses the status hook and exposes the download when entregado.

Purpose: Agencies manage their avalúos from inside the panel without leaving the authenticated
shell. Reusing the wizard + status hook proves the "same component, two contexts" decision
and avoids duplication.
Output: 3 panel pages under `src/app/panel/inmobiliaria/avaluos/`.
</objective>

<execution_context>
@/home/victo/.claude/get-shit-done/workflows/execute-plan.md
@/home/victo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/34-avaluos-ui/34-RESEARCH.md
@.planning/phases/34-avaluos-ui/34-03-SUMMARY.md
@.planning/phases/34-avaluos-ui/34-04-SUMMARY.md

# Panel page pattern (read before writing):
@src/app/panel/inmobiliaria/ai/page.tsx

# Reused building blocks:
@src/lib/types/avaluo.ts
@src/lib/api/avaluo.service.ts
@src/lib/auth/use-auth.ts

# IMPORTANT — read docs/DESIGN.md before writing any UI.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Avalúos list page (panel)</name>
  <files>src/app/panel/inmobiliaria/avaluos/page.tsx</files>
  <action>
    READ docs/DESIGN.md first. `'use client'` page under the existing panel layout (the
    parent layout already provides PlanSidebar/header + AuthProvider — do NOT add ForceLightMode
    here; the panel is NOT a public light-only route).

    - Page header: title "Avalúos" + a primary `<Link href="/panel/inmobiliaria/avaluos/nuevo">`
      button "SOLICITAR AVALÚO".
    - Render a list/table of the agency's avalúos. The list-fetching endpoint is not yet
      defined by the backend — define a local typed shape
      `AvaluoListItem = { id: string; address: string; status: AvaluoStatus; createdAt: string }`
      and a small `useAgencyAvaluos()` data source that returns `[]` for now with a clearly
      labelled `// TODO: wire backend list endpoint when available`. Render an HONEST empty
      state ("Aún no tenés avalúos") when the list is empty — match the project's empty-state
      pattern (e.g. NoDataYetBadge if appropriate per ai/page.tsx) rather than fabricating rows.
    - For each row: address, a `<Badge>` using `STATUS_BADGE[item.status]` (import from
      `@/lib/types/avaluo`), createdAt, and a Link to `/panel/inmobiliaria/avaluos/{id}`.
    - Do not fabricate fake avalúos — empty state until the backend list endpoint exists.
  </action>
  <verify>npx tsc --noEmit passes. Visit /panel/inmobiliaria/avaluos → header + "SOLICITAR AVALÚO" link + honest empty state. rg confirms STATUS_BADGE usage.</verify>
  <done>List page renders with status badges (when data exists) and an honest empty state; CTA links to the panel nuevo wizard.</done>
</task>

<task type="auto">
  <name>Task 2: Panel "nuevo" wizard (reuse, email pre-filled) + detail page</name>
  <files>src/app/panel/inmobiliaria/avaluos/nuevo/page.tsx, src/app/panel/inmobiliaria/avaluos/[id]/page.tsx</files>
  <action>
    READ docs/DESIGN.md first.

    `src/app/panel/inmobiliaria/avaluos/nuevo/page.tsx` (`'use client'`):
    - REUSE the same wizard from plan 34-03 — do NOT create a second wizard. Wrap
      `<AvaluoProvider initialEmail={user?.email}>` around `<AvaluoWizardShell>` exactly like
      the public /avaluo/nuevo page, switching step components on currentStep.
    - Use `useContext(AuthContext)` (import from `@/lib/auth/auth-context`) to get the user
      email: `const authCtx = useContext(AuthContext); const email = authCtx?.user?.email ?? ''`.
      Pass it as `initialEmail` to AvaluoProvider. (StepContacto already pre-fills + disables
      the email field when an authenticated user is present — verify it picks up the
      authenticated email via AuthContext.)
    - Factor the step-switcher into a tiny shared inner component if it helps avoid copy-paste,
      but the simplest path is to replicate the small inner "render active step" component.
    - On submit, the context already routes to `/avaluo/estado/[id]`; that public status page
      works for authenticated users too (acceptable for v1).

    `src/app/panel/inmobiliaria/avaluos/[id]/page.tsx` (`'use client'`):
    - Read `id` from params. Call `useAvaluoStatus(id)` (reuse the hook from 34-04).
    - Render a detail card: the avalúo id/reference, the current `<Badge>` via STATUS_BADGE,
      and the same status-driven CTA logic as the public estado card, EXCEPT framed for the
      agency. Specifically: when `statusData.status === 'entregado'` and
      `statusData.downloadUrl` exists, render a primary "DESCARGAR CERTIFICADO" link; when
      'firmado', you MAY reuse `<WompiPayButton submissionId={id} />` (payment still allowed
      from the panel). For other states show the processing/refresh hint.
    - You can reuse `<AvaluoEstadoCard>` directly here if it fits — prefer reuse over a new
      component.
  </action>
  <verify>
    npx tsc --noEmit passes. Visit /panel/inmobiliaria/avaluos/nuevo → same wizard, email
    field pre-filled with the logged-in user's email and disabled. Visit
    /panel/inmobiliaria/avaluos/test-1 → detail card with status badge; download link appears
    only on 'entregado'. rg confirms AvaluoProvider + useAvaluoStatus reuse (no duplicate wizard).
  </verify>
  <done>
    Panel nuevo reuses AvaluoProvider/AvaluoWizardShell with email pre-filled; detail page
    reuses useAvaluoStatus and shows download on 'entregado'. No wizard duplication.
  </done>
</task>

</tasks>

<verification>
- npx tsc --noEmit passes.
- rg "AvaluoProvider" src/app/panel/inmobiliaria/avaluos/nuevo/page.tsx → reuses the wizard (not a new one).
- rg "useAvaluoStatus" src/app/panel/inmobiliaria/avaluos/[id]/page.tsx → reuses the status hook.
- List page shows status badges via STATUS_BADGE + honest empty state (no fabricated rows).
- Panel pages do NOT add ForceLightMode.
</verification>

<success_criteria>
- Agency users list avalúos with status badges, start a new one with their email pre-filled
  using the SAME wizard, and download the certificate from the detail page when entregado.
</success_criteria>

<output>
After completion, create `.planning/phases/34-avaluos-ui/34-05-SUMMARY.md`.
</output>
