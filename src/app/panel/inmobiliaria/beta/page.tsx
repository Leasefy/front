'use client';

import { BetaWelcome } from '@/components/beta/BetaWelcome';

/**
 * Beta entry page for inmobiliarias.
 * Shows the welcome state with Leasefy AI branding and suggested prompts.
 */
export default function InmobiliariaBetaPage() {
  return <BetaWelcome />;
}
