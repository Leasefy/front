---
phase: v7-07-acuerdos-pago
plan: 06
type: execute
wave: 5
depends_on: [v7-07-01, v7-07-05]
files_modified:
  - src/app/api/inquilino/acuerdos/wompi-session/route.ts
  - src/app/api/inquilino/acuerdos/wompi-session/route.test.ts
  - src/app/inquilino/acuerdos/[id]/page.tsx
autonomous: true
requirements: [ACUE-03]

must_haves:
  truths:
    - "The cuota payment reuses the v7-04 Wompi rail invariants EXACTLY: a nodejs-runtime server route that resolves the cuota amount + reference SERVER-SIDE from the agent's payment-plan record (never reads body.amount), keeps the integrity secret server-only (never returned to the client), and forwards the tenant JWT for ownership — the client only sends { planId, cuotaNumber }"
    - "The 'Pagar cuota' affordance on the acuerdo detail is GATED until the tenant-scoped agent route serves a paymentUrl/amount: acuerdosApi.getCuotaPaymentUrl returns null → the button is disabled 'Próximamente'; when a paymentUrl exists the client redirects to Wompi hosted checkout, and the return shows 'confirmando' (never a premature 'pagado' — status flips only via the backend webhook)"
    - "No client-built cuota amount, no fabricated checkout URL, no premature success: the amount originates in the agent record (installments[].amountCop / plan.paymentUrl); the DIAN carry-over holds (any receipt is a 'comprobante interno', not a 'factura')"
  artifacts:
    - path: "src/app/api/inquilino/acuerdos/wompi-session/route.ts"
      provides: "v7-04 clone: runtime=nodejs, server-only WOMPI secret, amount+reference resolved server-side from the agent cuota record (never body.amount), JWT forwarded; returns { reference, amountInCents, currency, integrity, publicKey }"
      contains: "runtime = 'nodejs'"
    - path: "src/app/api/inquilino/acuerdos/wompi-session/route.test.ts"
      provides: "ACUE-03 unit proof: body.amount ignored (server resolves), secret never in the response, 401 without auth, agent-record amount used"
      contains: "amount"
    - path: "src/app/inquilino/acuerdos/[id]/page.tsx"
      provides: "'Pagar cuota' affordance: getCuotaPaymentUrl → redirect or disabled 'Próximamente'; 'confirmando' on return; no premature 'pagado'"
      contains: "getCuotaPaymentUrl"
  key_links:
    - from: "src/app/api/inquilino/acuerdos/wompi-session/route.ts"
      to: "agent cartera/payment-plans cuota record (server-resolved amount)"
      via: "resolve amountInCents + reference from the record under the forwarded JWT — never body.amount"
      pattern: "runtime = 'nodejs'"
    - from: "src/app/inquilino/acuerdos/[id]/page.tsx"
      to: "acuerdosApi.getCuotaPaymentUrl → buildWompiCheckoutUrl / server route"
      via: "null → disabled 'Próximamente'; paymentUrl → redirect; 'confirmando' on return"
      pattern: "getCuotaPaymentUrl"
---

<objective>
Ship ACUE-03: pay a cuota on the SAME v7-04 Wompi rail, with the amount + link ORIGINATING in the agent's
payment-plan record — never the client. Until the tenant-scoped agent route serves the cuota's paymentUrl/amount,
the "Pagar cuota" affordance is disabled "Próximamente" (`getCuotaPaymentUrl` → `null`) — never a fabricated
checkout URL or a client-built amount.

Two honest deliverables:
1. **Cuota Wompi server route (ACUE-03).** Clone `app/api/inquilino/pagos/wompi-session/route.ts` →
   `app/api/inquilino/acuerdos/wompi-session/route.ts`, preserving ALL v7-04 invariants: `export const runtime =
   'nodejs'`; server-only `WOMPI_INTEGRITY_SECRET`/`WOMPI_PUBLIC_KEY` (no `NEXT_PUBLIC_WOMPI`, secret NEVER in
   the response); the body accepts ONLY `{ planId, cuotaNumber }` (NEVER an `amount`); the tenant JWT is
   forwarded (401 without it) for ownership. The ONE change: resolve the cuota `amountInCents` + a namespaced
   `reference` SERVER-SIDE from the agent's payment-plan/installment record (fetch the plan under the forwarded
   JWT; read `installments[cuotaNumber].amountCop`) — swapping the v7-04 `/leases/:id/payment-info` lookup for a
   cuota-amount lookup. Reuse `computeWompiIntegrity` + a `buildAcuerdoReference(planId, cuotaNumber)`
   (acuerdo-namespaced so it never collides with rent/avalúo references). Validate `amountInCents > 0` (502 on
   invalid). Return `{ reference, amountInCents, currency, integrity, publicKey }`. `getCuotaPaymentUrl` (A4)
   remains the read-side contract; this route is the Leasefy-hosted fallback shape when only an amount (not a
   ready paymentUrl) is available.
   Write `route.test.ts` mirroring the v7-04 route test: a POST with a `body.amount` is IGNORED (the server
   resolves from the agent record); the response NEVER contains the integrity secret; 401 without an
   `authorization` header; `wompi_not_configured` (500) when the secret env is unset.
2. **Pay-cuota affordance on the detail (ACUE-03).** In `acuerdos/[id]/page.tsx`, add a "Pagar cuota" button
   per payable cuota (or a single primary CTA for the next due cuota). Call `acuerdosApi.getCuotaPaymentUrl(planId,
   cuotaNumber)`: `null` → the button is DISABLED with a "Próximamente" title (the honest default today); a
   `paymentUrl` string → redirect to it (agent-provided hosted checkout), OR POST `{ planId, cuotaNumber }` to
   the new server route and `buildWompiCheckoutUrl(session, { redirectUrl: origin + '/inquilino/acuerdos/' +
   planId })`. On return, the detail shows "confirmando" for that cuota — NEVER an optimistic "pagado" (status
   flips only via the backend webhook, v7-04 no-premature-success). DIAN carry-over: any receipt copy says
   "comprobante interno", never "factura". Sentence-case buttons.
Depends on v7-07-01 (getCuotaPaymentUrl) and v7-07-05 (the detail page exists). Wave 5.

Purpose: ACUE-03 — pay a cuota on the shipped Wompi rail with a server-resolved, agent-sourced amount.
Output: cuota Wompi server route + gated pay-cuota affordance.

**Guardrails (v7-04 invariants carried):** `runtime='nodejs'`; server-only secret (never in the response);
amount resolved SERVER-SIDE from the agent record (route never reads `body.amount`); JWT forwarded for ownership;
no client cuota amount; no fabricated checkout URL (`getCuotaPaymentUrl` null → disabled "Próximamente"); no
premature "pagado" ("confirmando" on return, webhook-only settlement); DIAN "comprobante interno"; es-CO;
sentence-case buttons; zero new npm packages.
</objective>

<execution_context>
@/Users/nicolasgarcia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolasgarcia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/v7-07-acuerdos-pago/v7-07-RESEARCH.md
@.planning/phases/v7-07-acuerdos-pago/v7-07-PATTERNS.md

# READ FIRST: CLAUDE.md (additive, es-CO, zero new npm packages). docs/DESIGN.md §4 (Buttons sentence case).

# The v7-04 rail to CLONE — preserve EVERY invariant (runtime=nodejs, server-only secret, server-resolved amount):
@src/app/api/inquilino/pagos/wompi-session/route.ts
@src/lib/payments/wompi-rent-session.ts
# The client redirect + "no premature success" lifecycle to MIRROR (server resolves amount, browser only redirects):
@src/components/tenant/PayRentModal.tsx
# The detail page to extend (from v7-07-05):
@src/app/inquilino/acuerdos/[id]/page.tsx

<interfaces>
<!-- v7-04 helpers to REUSE (do NOT rebuild the hash/url) -->
computeWompiIntegrity(reference, amountInCents, currency, secret): string   // '@/lib/payments/wompi-integrity' (server-only)
buildWompiCheckoutUrl({ publicKey, currency, amountInCents, reference, integrity, redirectUrl }): string   // client-safe
// server route response shape:
interface WompiRentSession { reference; amountInCents; currency; integrity; publicKey }

<!-- v7-07-01 read-side contract -->
acuerdosApi.getCuotaPaymentUrl(planId: string, cuotaNumber?: number): Promise<string | null>   // null on not-live → disabled "Próximamente"

<!-- v7-04 route invariants to preserve verbatim: export const runtime = 'nodejs'; process.env.WOMPI_INTEGRITY_SECRET
     + WOMPI_PUBLIC_KEY (server-only); body = { planId, cuotaNumber } ONLY (never amount); Authorization forwarded. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cuota Wompi server route (v7-04 clone, agent-sourced amount) (ACUE-03)</name>
  <files>src/app/api/inquilino/acuerdos/wompi-session/route.ts, src/app/api/inquilino/acuerdos/wompi-session/route.test.ts</files>
  <behavior>
    - POST { planId, cuotaNumber }: resolves amountInCents + reference SERVER-SIDE from the agent cuota record; returns { reference, amountInCents, currency, integrity, publicKey }.
    - A body that ALSO includes `amount` is IGNORED — the resolved amount comes only from the agent record (anti-tamper parity with v7-04).
    - The response NEVER contains WOMPI_INTEGRITY_SECRET (secret stays server-only).
    - Missing `authorization` header → 401; missing WOMPI_* env → 500 `wompi_not_configured`; agent lookup non-ok → the upstream status; amountInCents ≤ 0 or non-finite → 502.
  </behavior>
  <action>
    Create `src/app/api/inquilino/acuerdos/wompi-session/route.ts` by cloning
    `src/app/api/inquilino/pagos/wompi-session/route.ts` (keep the header-comment invariants). Changes:
      - Body parse accepts `{ planId?: string; cuotaNumber?: number }` (400 if `planId` missing). NEVER read an
        `amount` from the body.
      - Keep `export const runtime = 'nodejs'` and the server-only `WOMPI_INTEGRITY_SECRET`/`WOMPI_PUBLIC_KEY`
        guard (500 `wompi_not_configured` if unset). Keep the `authorization` forward + 401.
      - Resolve the amount SERVER-SIDE: `fetch(`${BACKEND_URL}/cartera/payment-plans/${planId}`, { headers: {
        Authorization } })` (the BFF forwards to the agent; ownership enforced there). Non-ok → return the
        upstream status. Read the cuota: `installments.find(i => i.number === cuotaNumber)?.amountCop` (or the
        plan-level amount when no cuotaNumber). Validate `Number.isFinite(amountCop) && amountCop > 0` (502
        otherwise). `amountInCents = Math.round(amountCop * 100)`.
      - Add `buildAcuerdoReference(planId, cuotaNumber)` (acuerdo-namespaced, e.g. `acuerdo-${planId}-c${n}`) in
        `wompi-rent-session.ts` OR inline — it must not collide with `rent-`/`avaluo-` references.
      - `integrity = computeWompiIntegrity(reference, amountInCents, 'COP', integritySecret)`. Return `{
        reference, amountInCents, currency: 'COP', integrity, publicKey }`.
    Write `route.test.ts` mirroring the v7-04 route test: mock `fetch` for the agent lookup; assert a `body.amount`
    is ignored (resolved amount = agent record), the response has no `integrity` secret leak, 401 without auth,
    and 500 when the env is unset. (If a Wave-0 route test harness for the rent route exists, copy its structure.)
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && R=src/app/api/inquilino/acuerdos/wompi-session/route.ts && grep -q "runtime = 'nodejs'" $R && grep -q "WOMPI_INTEGRITY_SECRET" $R && grep -q "cartera/payment-plans" $R && test $(grep -vE '^\s*(\*|//|/\*)' $R | grep -cE "body\.amount|NEXT_PUBLIC_WOMPI") -eq 0 && pnpm test -- src/app/api/inquilino/acuerdos/wompi-session/route.test.ts 2>&1 | tail -6</automated>
  </verify>
  <done>Cuota Wompi route: runtime=nodejs, server-only secret (never in response), amount+reference resolved server-side from the agent record (never body.amount), JWT forwarded; unit-tested for anti-tamper + no-secret-leak + 401/500.</done>
</task>

<task type="auto">
  <name>Task 2: Pay-cuota affordance on the acuerdo detail (ACUE-03)</name>
  <files>src/app/inquilino/acuerdos/[id]/page.tsx</files>
  <action>
    In `acuerdos/[id]/page.tsx`, add a "Pagar cuota" affordance (a per-cuota button in/near the CuotaPlanTable, or
    a single primary CTA for the next payable cuota). Wire it via `acuerdosApi.getCuotaPaymentUrl(plan.planId,
    cuotaNumber)`:
      - resolves to `null` (the default today, not-live) → the button is DISABLED with a `title` "Próximamente"
        (mirror the agency disabled-"Próximamente" posture); no redirect, no fabricated URL.
      - resolves to a `paymentUrl` string → either `window.location.href = paymentUrl` (agent-provided hosted
        checkout) OR POST `{ planId, cuotaNumber }` to `/api/inquilino/acuerdos/wompi-session` (Bearer token from
        `getAccessToken()`, mirroring `PayRentModal.handlePayWithWompi`) and redirect via `buildWompiCheckoutUrl(
        session, { redirectUrl: window.location.origin + '/inquilino/acuerdos/' + plan.planId })`.
    On the Wompi return, the detail shows a "confirmando" state for that cuota (read from the record's cuota
    `status`) — NEVER an optimistic "pagado" (v7-04 no-premature-success; settlement is webhook-only). Any receipt
    copy = "comprobante interno" (never "factura"), es-CO. Sentence-case button. Do NOT compute a cuota amount
    client-side; do NOT read `body.amount` anywhere; do NOT touch the accept panel wiring (v7-07-05).
  </action>
  <verify>
    <automated>cd /Users/nicolasgarcia/rent/mvp-portal-inquilino && P="src/app/inquilino/acuerdos/[id]/page.tsx" && grep -q "getCuotaPaymentUrl" "$P" && grep -qE "Próximamente|confirmando" "$P" && test $(grep -vE '^\s*(\*|//|/\*)' "$P" | grep -ciE "pagado|factura|body\.amount|amountInCents \*|\* 100") -eq 0 && echo GATE_OK</automated>
  </verify>
  <done>"Pagar cuota" affordance gated on getCuotaPaymentUrl (null → disabled "Próximamente"; paymentUrl → redirect); "confirmando" on return, no premature "pagado"; no client amount; DIAN "comprobante interno".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant browser → cuota Wompi server route | Untrusted `{ planId, cuotaNumber }` crosses; the route NEVER trusts a client amount |
| server route → agent (cartera/payment-plans) | JWT-forwarded; the agent owns the cuota amount + ownership (RLS); the integrity secret stays in the server process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v7-07-18 | Tampering | cuota Wompi route | mitigate | Amount resolved SERVER-SIDE from the agent record; the route NEVER reads `body.amount` (grep-gated). Integrity hash binds reference+amount+currency (v7-04). |
| T-v7-07-19 | Information Disclosure | cuota Wompi route response | mitigate | `WOMPI_INTEGRITY_SECRET` is server-only (no `NEXT_PUBLIC_` prefix) and never returned; only `{ reference, amountInCents, currency, integrity, publicKey }` leaves. Unit-tested. |
| T-v7-07-20 | Spoofing / Access Control | cuota Wompi route | mitigate | 401 without `authorization`; the JWT is forwarded to the agent, which enforces ownership (a plan not owned by the caller → upstream non-ok → propagated). |
| T-v7-07-21 | Repudiation / Integrity | pay-cuota return | mitigate | No premature "pagado" — "confirmando" on return; the cuota `status` flips only via the backend webhook. `getCuotaPaymentUrl` null → disabled, never a fabricated URL. |
| T-v7-07-22 | Compliance (DIAN) | receipt copy | mitigate | Any receipt is a "comprobante interno", never "factura" (grep-gated), carried from v7-04. |
| T-v7-07-SC | Tampering (supply chain) | npm installs | accept | N/A — zero new dependencies this phase. |
</threat_model>

<verification>
- `pnpm -C /Users/nicolasgarcia/rent/mvp-portal-inquilino build` succeeds (new API route registers, nodejs runtime).
- `pnpm test -- src/app/api/inquilino/acuerdos/wompi-session/route.test.ts` green.
- `pnpm test` — 0 NEW failures (~7 pre-existing unrelated).
- Anti-tamper gate: the route (comment-stripped) has 0 `body.amount` / 0 `NEXT_PUBLIC_WOMPI`; the detail (comment-stripped) has 0 `pagado`/`factura`/`body.amount`/client `* 100`.
- Manual smoke as `tenant`: the "Pagar cuota" button is disabled "Próximamente" today (getCuotaPaymentUrl → null); no fabricated checkout.
</verification>

<success_criteria>
- ACUE-03: a cuota pays on the same v7-04 Wompi rail with the amount + reference resolved server-side from the
  agent record (never the client), the integrity secret server-only, gated to disabled "Próximamente" until the
  tenant-scoped agent route serves a paymentUrl/amount, and no premature "pagado" — route unit-tested.
</success_criteria>

<output>
After completion, create `.planning/phases/v7-07-acuerdos-pago/v7-07-06-SUMMARY.md`.
</output>
