---
phase: 30-executive-reports
plan: 02
type: execute
wave: 1
depends_on: ["30-01"]
files_modified:
  - src/app/panel/inmobiliaria/reportes/page.tsx
autonomous: true

must_haves:
  truths:
    - "Reportes page has an 'Ejecutivo' tab in addition to existing 3 tabs"
    - "Ejecutivo tab renders ExecutiveSummary component with mock data"
    - "Ejecutivo tab is wrapped with FeatureGate for 'executive-reports' feature"
    - "Non-Business users see upgrade prompt when clicking Ejecutivo tab"
  artifacts:
    - path: "src/app/panel/inmobiliaria/reportes/page.tsx"
      provides: "Enhanced reportes page with 4 tabs including executive"
  key_links:
    - from: "reportes/page.tsx"
      to: "ExecutiveSummary.tsx"
      via: "renders in ejecutivo tab"
    - from: "reportes/page.tsx"
      to: "FeatureGate"
      via: "wraps executive content with feature='executive-reports'"
---

<objective>
Add the Executive tab to the reportes page and gate it to Business+ plans.

Purpose: Wire the ExecutiveSummary into the existing tabbed reports page with proper plan gating.
Output: Enhanced reportes page with 4th tab gated to Business+.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-executive-reports/30-01-SUMMARY.md

@src/app/panel/inmobiliaria/reportes/page.tsx
@src/components/inmobiliaria/UpgradePrompt.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Ejecutivo tab to reportes page</name>
  <files>src/app/panel/inmobiliaria/reportes/page.tsx</files>
  <action>
    Read the existing reportes page. It should already have 3 tabs (ocupacion, cobros, agentes) from Phase 29.

    Add a 4th tab:
    - id: 'ejecutivo'
    - Label: 'Ejecutivo' (use t() if i18n key exists, otherwise hardcode)
    - Icon: ChartLineUp from Phosphor
    - Position: last tab (after agentes)

    Update the tab type to include 'ejecutivo'.

    For the ejecutivo tab content, wrap with a SEPARATE FeatureGate for 'executive-reports':
    ```tsx
    {activeTab === 'ejecutivo' && (
      <FeatureGate feature="executive-reports">
        <ExecutiveSummary data={mockExecutiveData} />
      </FeatureGate>
    )}
    ```

    Note: The existing 3 tabs are already wrapped with FeatureGate for 'advanced-reports' (Growth+).
    The ejecutivo tab needs its OWN FeatureGate for 'executive-reports' (Business+).
    This means Growth users can see occupancy/cobros/agentes but NOT ejecutivo.

    Import ExecutiveSummary from '@/components/inmobiliaria/reports/ExecutiveSummary'.
    Import mockExecutiveData from '@/lib/data/mock-reports'.

    Run build.
  </action>
  <verify>npx next build --no-lint | grep "Compiled"</verify>
  <done>Reportes page has 4 tabs, ejecutivo tab gated to Business+ with FeatureGate</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx next build --no-lint` compiles successfully
- [ ] Reportes page shows 4 tabs
- [ ] Ejecutivo tab renders ExecutiveSummary
- [ ] Ejecutivo tab has its own FeatureGate for 'executive-reports'
</verification>

<success_criteria>

- Executive report accessible only to Business+ / Business Flex+ users
- Growth users see upgrade prompt on Ejecutivo tab
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/30-executive-reports/30-02-SUMMARY.md`
</output>
